---
phase: 20-cut-over-external-catalog-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MIG-05]
must_haves:
  truths:
    - "maccat catalogs correctly against an external (non-personal/office) catalog dir resolved via --catalog-dir"
    - "Verification runs entirely inside a disposable mktemp -d git dir and never touches the real personal/ or office/ trees"
    - "The generated catalog contains the expected section structure (Installed Mac Software List header + Homebrew Packages + multiple sections)"
    - "The disposable dir is cleaned up after verification"
  artifacts:
    - path: "/tmp/<mktemp-dir>/<computer>/mac-software-list-*.txt"
      provides: "Generated external catalog used only for assertion (ephemeral, removed at end)"
      contains: "Installed Mac Software List"
  key_links:
    - from: "maccat .pyz / python3 -m maccat"
      to: "external mktemp catalog dir"
      via: "--catalog-dir <tmpdir> --computer test --no-commit"
      pattern: "--catalog-dir"
---

<objective>
Prove maccat (the now-public tool at github.com/scottkw/maccat) correctly catalogs against a
genuinely EXTERNAL catalog repo resolved through its config precedence — satisfying MIG-05 —
WITHOUT ever pointing it at the real `personal/`/`office/` catalog trees in this repo.

Purpose: This is the last gate before the irreversible cut-over (20-02). maccat and the legacy
`update-list.sh` are destructive against a live catalog dir (retention moves/prunes catalogs,
git commit/push). MIG-05 verification therefore runs against a disposable `mktemp -d` git dir
with `--no-commit`, asserts the produced catalog has the expected sections, then cleans up.

Output: A passing verification run (no committed artifacts). NON-destructive against this repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cut-over-external-catalog-verification/20-CONTEXT.md

<interfaces>
<!-- maccat config precedence (verified Phase 18/19): --catalog-dir > MACCAT_CATALOG_DIR > ~/.config/maccat/config.toml -->
<!-- Build script (this repo, src/ is source of truth): -->
From scripts/build-pyz.sh:
  python3 -m zipapp src --output dist/maccat.pyz --python "/usr/bin/env python3" --main "maccat.__main__:main" --compress
<!-- Entry point: src/maccat/__main__.py exposes main(); `python3 -m maccat` works from this repo's src on PYTHONPATH. -->
<!-- Catalog output filename pattern: mac-software-list-[<computer>]-YYYYMMDDHHMMSS.txt written under <catalog-dir>/<computer>/ -->
<!-- Section structure: a "Installed Mac Software List" header section, then per-collector sections
     ("Homebrew Packages", "App Store Applications", editor/AI-CLI/browser extension sections...).
     Optional sources degrade gracefully, so assert on the always-present header + Homebrew + a section-count floor, NOT an exact title list. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Obtain a runnable maccat in a disposable workspace</name>
  <files>(none committed — all work in a mktemp dir)</files>
  <read_first>
    - scripts/build-pyz.sh (build invocation and src/ requirement)
    - src/maccat/__main__.py (entry point: main)
    - .planning/phases/20-cut-over-external-catalog-verification/20-CONTEXT.md (Safety + Integration sections)
  </read_first>
  <action>
    Create a disposable work dir: `WORK="$(mktemp -d)"`. Obtain a runnable maccat by EITHER
    (preferred) building the `.pyz` from THIS repo's `src/` via the project venv —
    `./venv/bin/python -m zipapp src --output "$WORK/maccat.pyz" --python "/usr/bin/env python3" --main "maccat.__main__:main" --compress`
    (matches scripts/build-pyz.sh; uses src/ as source so `import maccat` resolves) — OR fall back to
    running `PYTHONPATH=src ./venv/bin/python -m maccat ...` directly from this repo if zipapp is unavailable.
    Do NOT clone the GitHub maccat repo for this task; the local src/ is byte-identical to the public repo
    at this point and avoids network/auth dependencies. Record which method was used in the SUMMARY.
    NEVER run maccat against `personal/` or `office/` in this task.
  </action>
  <verify>
    <automated>test -s "$WORK/maccat.pyz" && /usr/bin/env python3 "$WORK/maccat.pyz" --help >/dev/null 2>&1 && echo OK || PYTHONPATH=src ./venv/bin/python -m maccat --help >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `$WORK` is a fresh `mktemp -d` path under a temp location (not inside this repo).
    - Either `$WORK/maccat.pyz` exists and `--help` exits 0, OR `PYTHONPATH=src python -m maccat --help` exits 0.
    - No file under `personal/` or `office/` was read or written.
  </acceptance_criteria>
  <done>A runnable maccat (pyz or module) is available and responds to --help; nothing in personal/office touched.</done>
</task>

<task type="auto">
  <name>Task 2: Run maccat against an external mktemp catalog dir and assert section structure</name>
  <files>(none committed — assertions only)</files>
  <read_first>
    - .planning/phases/20-cut-over-external-catalog-verification/20-CONTEXT.md (decision 1: MIG-05 disposable-dir run)
    - src/maccat/cli.py (catalog-dir resolution + "Installed Mac Software List" header at write time)
  </read_first>
  <action>
    Create a separate disposable catalog dir `CAT="$(mktemp -d)"` and `git init "$CAT"` (so maccat's
    git-aware paths resolve, with NO remote — push is impossible). Run maccat NON-destructively against it:
    `/usr/bin/env python3 "$WORK/maccat.pyz" --catalog-dir "$CAT" --computer test --no-commit`
    (or the PYTHONPATH=src module fallback with the same flags). The `--catalog-dir` flag is the
    highest-precedence config source, proving external-dir resolution. After the run, locate the produced
    catalog file under `"$CAT"/test/` and assert: (a) exactly one new mac-software-list-*.txt exists,
    (b) it contains the "Installed Mac Software List" header line, (c) it contains the "Homebrew Packages"
    section, (d) it has at least 5 section-separator lines (the 36-dash rule) confirming multi-section
    output. Then clean up BOTH temp dirs: `rm -rf "$WORK" "$CAT"`. Assert neither temp path is inside
    this repo before removal. Confirm `personal/` and `office/` are byte-unchanged (no new files, no git
    status changes attributable to this run).
  </action>
  <verify>
    <automated>CATFILE=$(ls "$CAT"/test/mac-software-list-*.txt 2>/dev/null | head -1); test -n "$CATFILE" && grep -q '^Installed Mac Software List$' "$CATFILE" && grep -q '^Homebrew Packages$' "$CATFILE" && [ "$(grep -c '^------------------------------------$' "$CATFILE")" -ge 5 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - maccat was invoked with `--catalog-dir "$CAT" --computer test --no-commit` where `$CAT` is a mktemp -d git dir with no remote.
    - Generated catalog under `$CAT/test/` contains `Installed Mac Software List` and `Homebrew Packages` and >= 5 separator lines.
    - `git -C "$CAT" log --oneline 2>/dev/null | wc -l` is 0 (no commit was made — --no-commit honored).
    - `git status --porcelain personal office` shows no new/modified files caused by this run (pre-existing `D personal/...txt` churn is unrelated and untouched).
    - Both `$WORK` and `$CAT` are removed at the end; assert with `test ! -e "$WORK" && test ! -e "$CAT"`.
  </acceptance_criteria>
  <done>maccat produced a complete, multi-section catalog in an external mktemp dir via --catalog-dir, made no commit, left personal/office untouched, and the temp dirs are cleaned up. MIG-05 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maccat process → catalog dir | maccat mutates/prunes/commits whatever dir it is pointed at — destructive if that dir is real catalog data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering/Destruction | maccat run against wrong catalog dir | mitigate | Only ever pass a fresh `mktemp -d` to `--catalog-dir`; `--no-commit`; mktemp git dir has no remote so push is impossible; assert temp paths are outside this repo before any rm; assert personal/office unchanged after run |
| T-20-02 | Denial | Leftover temp dirs accumulate | accept | `rm -rf` both temp dirs at end; low impact if a single run leaks under /tmp |
</threat_model>

<verification>
- maccat resolves an external catalog dir via `--catalog-dir` and writes a complete multi-section catalog there.
- The run is non-destructive to this repo: no commit in the temp dir, no changes under personal/office, temp dirs removed.
</verification>

<success_criteria>
- MIG-05 satisfied: maccat correctly catalogs against an isolated external dir with config precedence honored, never touching real catalog trees.
</success_criteria>

<output>
Create `.planning/phases/20-cut-over-external-catalog-verification/20-01-SUMMARY.md` when done.
Record: which maccat acquisition method was used (pyz build vs module), the asserted section markers, and confirmation that personal/office were untouched and temp dirs cleaned up.
</output>
