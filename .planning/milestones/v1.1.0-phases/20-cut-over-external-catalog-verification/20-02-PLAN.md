---
phase: 20-cut-over-external-catalog-verification
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/
  - tests/
  - scripts/
  - docs/
  - pyproject.toml
  - update-list.sh
  - .github/
  - .python-version
  - config.example.toml
  - CLAUDE.md
  - README.md
  - test-parse-arguments-11-02.sh
  - test-rename-back-12-02.sh
  - test-rename-front-12-01.sh
autonomous: false
requirements: [MIG-04]
must_haves:
  truths:
    - "Before any destructive git rm, a blocking human checkpoint surfaces the exact keep/remove file list and confirms lifecycle + .planning push + v1.1.0 release already happened"
    - "All code/tooling (src, tests, scripts, docs, pyproject.toml, update-list.sh, .github, .python-version, config.example.toml, CLAUDE.md, throwaway root test scripts) is removed from this repo"
    - "Catalog data (personal/, office/, any computer folders), machine-labels.tsv, and each folder's archive/ remain intact"
    - "README is replaced with a short pointer to https://github.com/scottkw/maccat"
    - "The strip commit is pushed to this repo's a private Git host origin (NOT GitHub)"
    - "ORCHESTRATOR-OWNED: .planning/ removal happens AFTER this executor strip commit, the milestone lifecycle, and the maccat .planning push — it is NOT an executor git rm task in this plan"
  artifacts:
    - path: "README.md"
      provides: "Pointer README — this repo is catalog data; tool lives at scottkw/maccat"
      contains: "https://github.com/scottkw/maccat"
    - path: "machine-labels.tsv"
      provides: "Preserved hostname→computer-folder map (must still exist after strip)"
      contains: "hostname"
  key_links:
    - from: "this repo"
      to: "a private Git host origin"
      via: "git push origin after strip commit"
      pattern: "private-remote"
---

<objective>
Final cut-over (MIG-04): reduce THIS repo (origin = private remote) to catalog-data-only. Remove the
maccat code, tests, build tooling, docs, the zsh reference, CI config, and root throwaway scripts;
replace the code README with a one-line pointer to the public maccat repo; keep all catalog data
(`personal/`, `office/`, any computer folders), `machine-labels.tsv`, and the `archive/` subfolders.
Commit and push to the a private Git host origin.

Purpose: This separates the now-public code (already on github.com/scottkw/maccat) from the private
catalog data that stays here. It is treated as irreversible, so a BLOCKING human-verify checkpoint
runs immediately before the destructive `git rm`, surfacing the exact keep/remove list and confirming
the orchestrator-level prerequisites have already happened.

CRITICAL ORDERING — `.planning/` is removed LAST and ONLY by the orchestrator, never by this executor.
The milestone lifecycle (audit → complete-milestone → cleanup), the final `.planning/` push to the
maccat repo, and the `v1.1.0` release tag all happen BETWEEN 20-01 and this strip — they are
ORCHESTRATOR-level steps, not plan tasks. This plan strips CODE/TOOLING only; `.planning/` removal is
the orchestrator's final manual act AFTER this strip commit + lifecycle + maccat push, to avoid
deleting the GSD home mid-execution.

Output: A catalog-data-only working tree on a private remote origin (minus `.planning/`, which the orchestrator
removes afterward). DESTRUCTIVE — gated behind a blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cut-over-external-catalog-verification/20-CONTEXT.md

<interfaces>
<!-- This repo's origin (MUST stay a private Git host — assert before push): -->
  git remote get-url origin → a private catalog remote
<!-- Tracked top-level code/tooling to REMOVE (from `git ls-files`): -->
  src/  tests/  scripts/  docs/  .github/  pyproject.toml  update-list.sh
  .python-version  CLAUDE.md  README.md(rewrite, not rm)
  test-parse-arguments-11-02.sh  test-rename-back-12-02.sh  test-rename-front-12-01.sh
<!-- KEEP (catalog data — never git rm these): -->
  personal/  office/  (any other computer folders)  machine-labels.tsv  personal/archive/  office/archive/  .git
<!-- NOTE: config.example.toml and venv/ are NOT tracked here (no git ls-files entry); remove from the
     working tree with plain `rm -rf` only if present, and never via git rm. dist/, .mypy_cache, .pytest_cache,
     .ruff_cache are gitignored build/cache dirs — remove from working tree if present, untracked. -->
<!-- DO NOT TOUCH .planning/ — orchestrator removes it last. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING checkpoint — confirm prerequisites and approve the keep/remove list</name>
  <what-built>
    Nothing destructive yet. This checkpoint surfaces the exact cut-over plan for human approval
    before any irreversible `git rm`.
  </what-built>
  <how-to-verify>
    The executor MUST print, for the human to confirm:

    1. ORCHESTRATOR PREREQUISITES (confirm all THREE already happened — if any is "no", STOP):
       - Milestone v1.1.0 lifecycle ran HERE (audit → complete-milestone → cleanup) while `.planning/` still exists.
       - The final `.planning/` was pushed to the maccat repo (fresh clone of github.com/scottkw/maccat).
       - The real `v1.1.0` tag was pushed to maccat and the GitHub Release published WITH `maccat.pyz` attached.

    2. ORIGIN ASSERTION (print the actual value): `git remote get-url origin`
       — MUST be the a private Git host host `a private catalog remote.git`. If it is GitHub, STOP.

    3. KEEP list (must remain after strip):
       - `personal/`, `office/`, and any other computer folders (catalog data)
       - each folder's `archive/`
       - `machine-labels.tsv`
       - `.git`
       - `.planning/` stays for now (orchestrator removes it LAST, not this plan)

    4. REMOVE list (this executor strip): `src/`, `tests/`, `scripts/`, `docs/`, `pyproject.toml`,
       `update-list.sh`, `.github/`, `.python-version`, `CLAUDE.md`, and the three root throwaway
       scripts (`test-parse-arguments-11-02.sh`, `test-rename-back-12-02.sh`, `test-rename-front-12-01.sh`).
       Untracked-if-present: `config.example.toml`, `venv/`, `dist/`, cache dirs — removed from working tree only.
       README.md is REWRITTEN (pointer), not deleted.

    5. EXPLICIT NON-ACTION: `.planning/` is NOT removed by this plan. State this aloud.

    Print the live `git ls-files` count and the exact paths that the next task will `git rm` so the
    human sees the real targets, not just a static list.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the strip, or describe corrections / which prerequisite is missing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Strip code/tooling, write pointer README, commit and push to a private Git host</name>
  <files>src/, tests/, scripts/, docs/, .github/, pyproject.toml, update-list.sh, .python-version, CLAUDE.md, test-parse-arguments-11-02.sh, test-rename-back-12-02.sh, test-rename-front-12-01.sh, README.md</files>
  <read_first>
    - .planning/phases/20-cut-over-external-catalog-verification/20-CONTEXT.md ("This repo after the strip" + Integration safety)
    - machine-labels.tsv (confirm it exists and is a KEEP item before stripping)
  </read_first>
  <action>
    Only run after Task 1 approval. First ASSERT origin is a private Git host — `git remote get-url origin` MUST
    contain `a private git host`; if not, abort with a hard error (do not push). Then remove the
    tracked code/tooling with a single staged `git rm -r`:
    `git rm -r src tests scripts docs .github pyproject.toml update-list.sh .python-version CLAUDE.md test-parse-arguments-11-02.sh test-rename-back-12-02.sh test-rename-front-12-01.sh`.
    Do NOT pass `personal`, `office`, `machine-labels.tsv`, any computer folder, `archive/`, or
    `.planning` to git rm. Remove untracked-if-present cruft from the working tree only (never git rm):
    `rm -rf config.example.toml venv dist .mypy_cache .pytest_cache .ruff_cache` (ignore missing).
    Then OVERWRITE README.md with a short pointer: a title line and the sentence
    "This repo holds my machine software catalogs (data). The maccat tool lives at
    https://github.com/scottkw/maccat." and `git add README.md`.
    Do NOT stage or touch the pre-existing uncommitted `D personal/...txt` deletions — they are
    unrelated catalog churn; scope `git add`/`git rm` strictly to the keep/remove targets above.
    Commit: `git commit -m "chore(MIG-04): reduce repo to catalog-data-only — remove maccat code/tooling, point README at scottkw/maccat"`.
    Push to a private Git host: `git push origin HEAD`. DO NOT remove `.planning/` — that is the orchestrator's
    final step after this commit + lifecycle + maccat push.
  </action>
  <verify>
    <automated>git remote get-url origin | grep -q private-remote && test ! -e src && test ! -e tests && test ! -e scripts && test ! -e update-list.sh && test ! -e CLAUDE.md && test -d personal && test -d office && test -f machine-labels.tsv && test -d personal/archive && grep -q 'github.com/scottkw/maccat' README.md && test -d .planning && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` contains `private-remote` (asserted BEFORE the commit/push; GitHub would abort).
    - `test -e src && echo BAD || echo OK` → OK (and likewise for tests, scripts, docs, .github, pyproject.toml, update-list.sh, .python-version, CLAUDE.md, and the three throwaway test scripts).
    - `test -d personal && test -d office && test -f machine-labels.tsv && test -d personal/archive && test -d office/archive` all pass (KEEP items intact).
    - `README.md` contains `https://github.com/scottkw/maccat`.
    - `test -d .planning && echo OK` → OK (`.planning/` MUST still exist — this plan does not remove it).
    - `git log -1 --pretty=%s` shows the MIG-04 strip commit; `git status` shows the push succeeded to the a private Git host origin.
    - The pre-existing `D personal/...txt` working-tree deletions were neither staged nor restored by this plan.
  </acceptance_criteria>
  <done>This repo's working tree is code/tooling-free (except .planning/, left for the orchestrator), README points at scottkw/maccat, catalog data + machine-labels.tsv + archives remain, and the strip commit is pushed to a private Git host. MIG-04 strip complete; orchestrator removes .planning/ last.</done>
</task>

</tasks>

<orchestrator_notes>
**`.planning/` removal is ORCHESTRATOR-OWNED, not an executor task.** After this plan's strip commit
is pushed to a private Git host, AND after the milestone lifecycle (audit → complete-milestone → cleanup) and the
final `.planning/` push to the maccat repo have completed, the orchestrator performs the last
irreversible act manually:
`git rm -r .planning && git commit -m "chore(MIG-04): remove .planning — GSD home now lives in scottkw/maccat" && git push origin HEAD`.
This is deliberately excluded from the executor tasks above to avoid deleting the GSD home
mid-execution. The must_have "ORCHESTRATOR-OWNED: .planning/ removal..." is verified at the
orchestrator level, not by `20-02-SUMMARY.md`.
</orchestrator_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → git working tree | A wrong `git rm` path could delete irreplaceable catalog data |
| executor → git remote | Pushing to the wrong remote could leak data to GitHub instead of private remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Destruction | accidental git rm of catalog data | mitigate | Explicit enumerated remove-list (never includes personal/office/machine-labels.tsv/archive); blocking human checkpoint prints live `git rm` targets before execution; KEEP-item assertions in verify |
| T-20-04 | Information Disclosure | pushing private catalog data to GitHub | mitigate | Assert `git remote get-url origin` contains `private-remote` before push; abort if GitHub |
| T-20-05 | Destruction | deleting .planning (GSD home) mid-execution | mitigate | `.planning/` excluded from executor git rm; verify asserts `.planning/` still exists; removal deferred to orchestrator after lifecycle + maccat push |
</threat_model>

<verification>
- Code/tooling gone, catalog data + machine-labels.tsv + archives intact, README points at scottkw/maccat.
- Origin is still a private Git host and the strip commit was pushed there.
- `.planning/` still present (orchestrator removes it last).
</verification>

<success_criteria>
- MIG-04 strip satisfied: this repo is reduced to catalog-data-only (modulo .planning/, removed by the orchestrator afterward), committed and pushed to the a private Git host origin, behind a blocking human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/20-cut-over-external-catalog-verification/20-02-SUMMARY.md` when done.
Record: the confirmed origin URL, the exact paths removed, confirmation that personal/office/machine-labels.tsv/archives survived, the pointer README content, the commit hash, and an explicit note that `.planning/` removal remains pending for the orchestrator.
</output>
