---
phase: 19-ci-build-release-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /tmp/maccat-staging.AxZxJN/.github/workflows/ci.yml
autonomous: true
requirements: [CI-01, CI-02]
must_haves:
  truths:
    - "On push/PR to main, the test matrix runs (ruff + mypy --strict + pytest + zsh -n) on macos-latest"
    - "On push/PR to main, CI builds maccat.pyz via scripts/build-pyz.sh and uploads it as a downloadable workflow artifact"
    - "The most recent push run on the maccat repo reports conclusion=success and the run has an artifact named maccat.pyz"
    - "maccat.pyz is NOT committed to the repo (dist/ stays gitignored)"
  artifacts:
    - path: "/tmp/maccat-staging.AxZxJN/.github/workflows/ci.yml"
      provides: "Test matrix job + build/upload-artifact step"
      contains: "actions/upload-artifact"
  key_links:
    - from: ".github/workflows/ci.yml build step"
      to: "scripts/build-pyz.sh"
      via: "run: ./scripts/build-pyz.sh"
      pattern: "build-pyz\\.sh"
    - from: ".github/workflows/ci.yml upload step"
      to: "dist/maccat.pyz"
      via: "actions/upload-artifact with path dist/maccat.pyz"
      pattern: "upload-artifact"
---

<objective>
Confirm the migrated test workflow (CI-01) is green on the maccat repo, then extend `.github/workflows/ci.yml` to build the `.pyz` via `scripts/build-pyz.sh` and upload it as a workflow artifact on every push/PR to `main` (CI-02). Verify by observing a REAL green Actions run that carries a `maccat.pyz` artifact.

Purpose: Make CI prove the distributable artifact builds on every change, not just that tests pass.
Output: Updated `ci.yml` in `github.com/scottkw/maccat` `main`, plus an observed green run with the `maccat.pyz` artifact attached.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ci-build-release-pipeline/19-CONTEXT.md

# IMPORTANT: All deliverables live in the maccat GitHub repo, NOT this repo.
# Work in the staging tree /tmp/maccat-staging.AxZxJN (origin = github.com/scottkw/maccat, tracks origin/main).
# If that tree is missing or dirty, instead: git clone https://github.com/scottkw/maccat into a fresh mktemp -d and work there.
# NEVER push to this repo's a private Git host origin.

<interfaces>
<!-- Existing ci.yml in the maccat repo (do NOT rewrite the test job; ADD a build job/step). -->

.github/workflows/ci.yml (current):
- name: CI
- on: push (branches: [main]) + pull_request
- jobs.test: runs-on macos-latest; strategy.matrix.pythonhashseed: [0, 42, "random"];
  env.PYTHONHASHSEED from matrix; steps: checkout@v4, setup-python@v5 (3.11),
  "python -m venv venv && ./venv/bin/pip install -e .[dev]", ruff check, mypy --strict src/maccat,
  pytest -x -q, "zsh -n update-list.sh".

scripts/build-pyz.sh (executable, OS-agnostic — uses stdlib python3 -m zipapp src/ ...):
- Produces dist/maccat.pyz. Run from repo root as ./scripts/build-pyz.sh. Echoes "Built: .../dist/maccat.pyz".

.gitignore already contains `dist/` — the built artifact must NOT be committed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm CI-01 green run and add build+upload-artifact step to ci.yml</name>
  <read_first>
    - /tmp/maccat-staging.AxZxJN/.github/workflows/ci.yml (the file being edited)
    - /tmp/maccat-staging.AxZxJN/scripts/build-pyz.sh (the script the build step invokes)
    - /tmp/maccat-staging.AxZxJN/.gitignore (confirm dist/ excluded)
  </read_first>
  <action>
    Work in /tmp/maccat-staging.AxZxJN (verify `git -C /tmp/maccat-staging.AxZxJN remote get-url origin` == https://github.com/scottkw/maccat.git; if the tree is missing/dirty, `git clone https://github.com/scottkw/maccat` into `$(mktemp -d)` and use that path everywhere below).

    First, confirm CI-01 is already satisfied — observe the existing green run:
    `gh run list --repo scottkw/maccat --workflow ci.yml --limit 1` should show conclusion `success`. Record the run id for the SUMMARY. Do NOT recreate the test job.

    Then edit `.github/workflows/ci.yml` to add the build+artifact capability for CI-02. Add a SEPARATE job named `build` (parallel to `test`, single run — NOT inside the macOS matrix, to avoid 3 redundant artifact uploads with the same name):
    - `runs-on: macos-latest` (matches the project's existing runner; zipapp is OS-agnostic but keep CI on macOS for consistency)
    - steps: `actions/checkout@v4`; `actions/setup-python@v5` with `python-version: "3.11"`; a step that runs `chmod +x scripts/build-pyz.sh && ./scripts/build-pyz.sh` (the script needs `python3` on PATH — setup-python provides it; build-pyz.sh uses stdlib zipapp, no venv/deps needed); then `actions/upload-artifact@v4` with `name: maccat.pyz` and `path: dist/maccat.pyz` and `if-no-files-found: error`.
    - Keep `on:` unchanged (push branches [main] + pull_request) so both test and build fire on push/PR to main.

    Do NOT commit dist/maccat.pyz — confirm `git status` shows only the workflow change staged. If build-pyz.sh produced a local dist/ during any test, leave it ignored.
  </action>
  <verify>
    <automated>cd /tmp/maccat-staging.AxZxJN && grep -q 'actions/upload-artifact' .github/workflows/ci.yml && grep -q 'build-pyz.sh' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && git status --porcelain dist/ | grep -q . && echo "DIST TRACKED - FAIL" || echo "yaml-valid artifact-step-present dist-untracked OK"</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --repo scottkw/maccat --workflow ci.yml --limit 1` shows conclusion `success` (CI-01 confirmed; run id recorded)
    - `ci.yml` parses as valid YAML
    - `ci.yml` contains a `build` job with `runs-on: macos-latest`, a step running `scripts/build-pyz.sh`, and an `actions/upload-artifact@v4` step with `name: maccat.pyz` / `path: dist/maccat.pyz` / `if-no-files-found: error`
    - `git status --porcelain dist/` is empty (artifact not tracked)
  </acceptance_criteria>
  <done>ci.yml has a build job invoking build-pyz.sh and uploading maccat.pyz; YAML is valid; dist/ untracked; existing CI run confirmed green.</done>
</task>

<task type="auto">
  <name>Task 2: Push to maccat main and observe a real green run carrying the maccat.pyz artifact</name>
  <read_first>
    - /tmp/maccat-staging.AxZxJN/.github/workflows/ci.yml (just-edited workflow)
  </read_first>
  <action>
    Commit and push the ci.yml change to the maccat repo's `main`. Use `git -C /tmp/maccat-staging.AxZxJN` for every git op so the push targets github.com/scottkw/maccat (NEVER this repo's a private Git host origin):
    - `git -C <tree> add .github/workflows/ci.yml`
    - `git -C <tree> commit -m "ci: build maccat.pyz and upload as workflow artifact (CI-02)"`
    - `git -C <tree> push origin main`

    Then observe the REAL Actions run triggered by this push:
    - `gh run list --repo scottkw/maccat --workflow ci.yml --limit 1 --json databaseId -q '.[0].databaseId'` to get the new run id.
    - `gh run watch <run-id> --repo scottkw/maccat --exit-status` to block until it finishes and fail if it failed.
    - Confirm the run produced the artifact: `gh run view <run-id> --repo scottkw/maccat --json` (or `gh api repos/scottkw/maccat/actions/runs/<run-id>/artifacts -q '.artifacts[].name'`) includes `maccat.pyz`.

    If the run fails, read the failing job logs (`gh run view <run-id> --repo scottkw/maccat --log-failed`), fix ci.yml in the tree, re-push, and re-observe. Do NOT mark done until a push run is green AND has the maccat.pyz artifact.
  </action>
  <verify>
    <automated>RID=$(gh run list --repo scottkw/maccat --workflow ci.yml --limit 1 --json databaseId -q '.[0].databaseId') && gh run watch "$RID" --repo scottkw/maccat --exit-status && gh api "repos/scottkw/maccat/actions/runs/$RID/artifacts" -q '.artifacts[].name' | grep -qx 'maccat.pyz' && echo "RUN GREEN + ARTIFACT PRESENT OK"</automated>
  </verify>
  <acceptance_criteria>
    - Latest ci.yml push run on scottkw/maccat reports conclusion `success`
    - That run's artifact list (`gh api repos/scottkw/maccat/actions/runs/<id>/artifacts -q '.artifacts[].name'`) includes exactly `maccat.pyz`
    - `git -C <tree> log -1 --format=%s` shows the build commit on `origin/main`
    - This repo's origin (`git -C /Users/ken/dev/mac-software-list remote get-url origin`) is unchanged (private-remote)
  </acceptance_criteria>
  <done>A real push to scottkw/maccat main triggered a green CI run that uploaded the maccat.pyz artifact; CI-01 and CI-02 observed end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree → maccat GitHub repo | git push publishes workflow changes to the public repo |
| GitHub Actions runner → repo | runner executes build-pyz.sh and uploads artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | wrong git remote (a private Git host vs maccat) | mitigate | Every git op uses `git -C <maccat-tree>`; verify origin == github.com/scottkw/maccat before push; assert this repo's origin unchanged in acceptance criteria |
| T-19-02 | Information disclosure | committed build artifact | mitigate | dist/ stays gitignored; acceptance asserts `git status --porcelain dist/` empty |
| T-19-03 | Tampering | malicious 3rd-party action | accept | Only first-party `actions/*` pinned-major actions used; no third-party actions |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs; build-pyz.sh uses stdlib zipapp only |
</threat_model>

<verification>
- `gh run list --repo scottkw/maccat --workflow ci.yml --limit 1` → conclusion success
- New push run uploaded artifact named `maccat.pyz`
- dist/ not tracked in git
- This repo's a private Git host origin untouched
</verification>

<success_criteria>
ci.yml on scottkw/maccat builds maccat.pyz via scripts/build-pyz.sh and uploads it as a workflow artifact on push/PR to main, proven by a real green Actions run carrying the maccat.pyz artifact (CI-01 confirmed, CI-02 delivered).
</success_criteria>

<output>
Create `.planning/phases/19-ci-build-release-pipeline/19-01-SUMMARY.md` when done. Record the confirmed CI-01 run id and the new CI-02 run id + artifact name.
</output>
