---
phase: 19-ci-build-release-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - /tmp/maccat-staging.AxZxJN/.github/workflows/release.yml
autonomous: true
requirements: [CI-03]
must_haves:
  truths:
    - "Pushing a tag matching v*.*.* triggers a release workflow that builds maccat.pyz on ubuntu-latest"
    - "The release workflow creates a GitHub Release for the tag with maccat.pyz attached as an asset using gh release create in a run step"
    - "The release job grants only contents: write permission (no broader scope)"
    - "The throwaway tag v0.0.1-ci-test and its Release are deleted (remote + local) after validation; the real v1.1.0 is NOT cut"
  artifacts:
    - path: "/tmp/maccat-staging.AxZxJN/.github/workflows/release.yml"
      provides: "Tag-triggered release job that builds .pyz and publishes a Release"
      contains: "gh release create"
  key_links:
    - from: ".github/workflows/release.yml trigger"
      to: "tag push v*.*.*"
      via: "on.push.tags"
      pattern: "v\\*\\.\\*\\.\\*"
    - from: ".github/workflows/release.yml publish step"
      to: "GitHub Release asset"
      via: "gh release create with maccat.pyz"
      pattern: "gh release create"
---

<objective>
Add a SEPARATE `.github/workflows/release.yml` to the maccat repo that, on a `v*.*.*` tag push, builds `maccat.pyz` on `ubuntu-latest` and publishes a GitHub Release with the `.pyz` attached via `gh release create` (built-in gh + GITHUB_TOKEN, `permissions: contents: write`, no third-party action) (CI-03). Prove it end-to-end with a throwaway tag `v0.0.1-ci-test`, confirm the Release + asset via a REAL Actions run, then delete the test release and tag.

Purpose: Make a version tag the single trigger that ships a downloadable `.pyz` Release.
Output: `release.yml` on `github.com/scottkw/maccat` `main`, an observed release run for the test tag, and a clean repo (test tag/release removed; real v1.1.0 NOT cut).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ci-build-release-pipeline/19-CONTEXT.md

# IMPORTANT: All deliverables live in the maccat GitHub repo, NOT this repo.
# Work in the staging tree /tmp/maccat-staging.AxZxJN (origin = github.com/scottkw/maccat, tracks origin/main).
# If that tree is missing or dirty, instead: git clone https://github.com/scottkw/maccat into a fresh mktemp -d and work there.
# NEVER push to this repo's a private Git host origin.
# Depends on 19-01: ci.yml build step already landed on main. release.yml is a NEW separate file (no overlap).

<interfaces>
<!-- This plan CREATES .github/workflows/release.yml. -->

scripts/build-pyz.sh (executable, OS-agnostic — stdlib python3 -m zipapp src/ ...):
- Run from repo root as ./scripts/build-pyz.sh. Produces dist/maccat.pyz. zipapp is OS-agnostic, safe on ubuntu-latest.

Locked decisions (19-CONTEXT.md):
- Separate release.yml (not in ci.yml). Trigger: on.push.tags v*.*.*.
- Build on ubuntu-latest (release job needs NO macOS test matrix).
- Publish with `gh release create` in a run step (GITHUB_TOKEN, permissions: contents: write). NO softprops / no third-party action.
- Validate with throwaway tag v0.0.1-ci-test, then delete release + tag (remote + local). Do NOT cut real v1.1.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release.yml (tag-triggered build + gh release create) and push to maccat main</name>
  <read_first>
    - /tmp/maccat-staging.AxZxJN/scripts/build-pyz.sh (the script the release job invokes)
    - /tmp/maccat-staging.AxZxJN/.github/workflows/ci.yml (reference structure/style; do NOT modify it here)
  </read_first>
  <action>
    Work in /tmp/maccat-staging.AxZxJN (verify origin == https://github.com/scottkw/maccat.git; pull latest so 19-01's ci.yml change is present: `git -C <tree> pull origin main`. If the tree is missing/dirty, clone fresh into `$(mktemp -d)`).

    Create `.github/workflows/release.yml`:
    - `name: Release`
    - `on: push: tags: ['v*.*.*']` (matches v1.1.0 and v0.0.1-ci-test)
    - top-level or job-level `permissions: contents: write` (needed for gh release create; nothing broader)
    - single job `release` with `runs-on: ubuntu-latest`
    - steps:
      1. `actions/checkout@v4`
      2. `actions/setup-python@v5` with `python-version: "3.11"`
      3. build: `chmod +x scripts/build-pyz.sh && ./scripts/build-pyz.sh` (stdlib zipapp, no deps)
      4. publish: a `run:` step with `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` executing `gh release create "${GITHUB_REF_NAME}" dist/maccat.pyz --title "${GITHUB_REF_NAME}" --generate-notes` (built-in gh; uses the pushed tag name). Do NOT echo the token anywhere.

    Confirm dist/maccat.pyz is NOT committed (only release.yml is the new tracked file). Then commit and push to main:
    - `git -C <tree> add .github/workflows/release.yml`
    - `git -C <tree> commit -m "ci: add tag-triggered release workflow publishing maccat.pyz (CI-03)"`
    - `git -C <tree> push origin main`

    The release workflow must exist on `main` BEFORE the test tag is pushed (Task 2), otherwise the tag won't trigger it.
  </action>
  <verify>
    <automated>cd /tmp/maccat-staging.AxZxJN && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); assert d['on']['push']['tags']==['v*.*.*'], d['on']; assert d['permissions']['contents']=='write'" && grep -q 'gh release create' .github/workflows/release.yml && grep -q 'ubuntu-latest' .github/workflows/release.yml && ! grep -q 'softprops' .github/workflows/release.yml && echo "release.yml valid OK"</automated>
  </verify>
  <acceptance_criteria>
    - `release.yml` parses as valid YAML; `on.push.tags == ['v*.*.*']`
    - `permissions.contents == write` (and no broader permission grants)
    - Contains a `release` job on `runs-on: ubuntu-latest`, a build step running `scripts/build-pyz.sh`, and a `gh release create ... dist/maccat.pyz` publish step using `secrets.GITHUB_TOKEN`
    - No third-party action (no `softprops`, no marketplace release action)
    - `git -C <tree> log -1 --format=%s` shows the release-workflow commit on origin/main; `git status --porcelain dist/` empty
  </acceptance_criteria>
  <done>release.yml exists on scottkw/maccat main: triggers on v*.*.* tags, builds .pyz on ubuntu-latest, publishes a Release via gh release create with contents:write only, no third-party action.</done>
</task>

<task type="auto">
  <name>Task 2: Validate CI-03 with throwaway tag v0.0.1-ci-test, confirm Release+asset, then clean up</name>
  <read_first>
    - /tmp/maccat-staging.AxZxJN/.github/workflows/release.yml (the workflow being validated)
  </read_first>
  <action>
    Push a THROWAWAY pre-release tag to trigger release.yml (all git ops via `git -C <tree>` targeting github.com/scottkw/maccat):
    - `git -C <tree> tag v0.0.1-ci-test` (on the current main tip that contains release.yml)
    - `git -C <tree> push origin v0.0.1-ci-test`

    Observe the REAL release run:
    - `gh run list --repo scottkw/maccat --workflow release.yml --limit 1 --json databaseId -q '.[0].databaseId'` to get the run id
    - `gh run watch <run-id> --repo scottkw/maccat --exit-status` (block + fail on failure)

    Confirm the Release exists with the asset attached:
    - `gh release view v0.0.1-ci-test --repo scottkw/maccat --json assets -q '.assets[].name'` includes `maccat.pyz`.

    If the run fails, read `gh run view <run-id> --repo scottkw/maccat --log-failed`, fix release.yml in the tree, push the fix to main, DELETE the failed tag/release (see cleanup below), re-tag, and re-observe.

    Once the Release + maccat.pyz asset are CONFIRMED, CLEAN UP so the throwaway tag is never mistaken for a real release:
    - `gh release delete v0.0.1-ci-test --repo scottkw/maccat --yes --cleanup-tag` (deletes the release and the remote tag). If `--cleanup-tag` leaves the tag, also run `git -C <tree> push origin :refs/tags/v0.0.1-ci-test`.
    - Delete the local tag: `git -C <tree> tag -d v0.0.1-ci-test`.

    Do NOT create the real `v1.1.0` tag — that is deferred to Phase 20 cut-over.
  </action>
  <verify>
    <automated>gh release view v0.0.1-ci-test --repo scottkw/maccat >/dev/null 2>&1 && echo "TEST RELEASE STILL EXISTS - FAIL" || (gh api repos/scottkw/maccat/git/refs/tags/v0.0.1-ci-test >/dev/null 2>&1 && echo "TEST TAG STILL EXISTS - FAIL" || echo "test release+tag removed OK")</automated>
  </verify>
  <acceptance_criteria>
    - During validation: the release.yml run for tag v0.0.1-ci-test reported conclusion `success` (recorded in SUMMARY)
    - During validation: `gh release view v0.0.1-ci-test --repo scottkw/maccat --json assets -q '.assets[].name'` included `maccat.pyz` (recorded in SUMMARY)
    - After cleanup: `gh release view v0.0.1-ci-test --repo scottkw/maccat` fails (release gone) AND `gh api repos/scottkw/maccat/git/refs/tags/v0.0.1-ci-test` fails (remote tag gone) AND `git -C <tree> tag -l v0.0.1-ci-test` is empty (local tag gone)
    - No `v1.1.0` tag was created on the repo (`gh api repos/scottkw/maccat/git/refs/tags/v1.1.0` returns not-found)
    - This repo's origin (`git -C /Users/ken/dev/mac-software-list remote get-url origin`) unchanged (private-remote)
  </acceptance_criteria>
  <done>The throwaway tag triggered a green release run that published a Release with maccat.pyz attached (observed), then the release and tag were fully deleted; real v1.1.0 untouched. CI-03 proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree → maccat GitHub repo | git push (branch + tag) publishes to the public repo and triggers the release |
| GitHub Actions runner → GitHub Releases API | release job uses GITHUB_TOKEN to create a Release and upload an asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Elevation of privilege | GITHUB_TOKEN scope in release.yml | mitigate | `permissions: contents: write` only — no broader scope; acceptance asserts contents==write and nothing more |
| T-19-05 | Information disclosure | token leaked in logs | mitigate | Token passed via `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`; never echoed; gh reads it from env |
| T-19-06 | Repudiation | throwaway test release mistaken for real | mitigate | Delete release + remote tag + local tag after validation; acceptance asserts all three gone; real v1.1.0 not created |
| T-19-07 | Tampering | malicious 3rd-party release action | mitigate | Only first-party `actions/*` + built-in `gh`; no softprops/marketplace action (acceptance asserts no softprops) |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs; build-pyz.sh uses stdlib zipapp only |
</threat_model>

<verification>
- release.yml triggers only on v*.*.* tags, builds on ubuntu-latest, publishes via gh release create with contents:write
- Real release run for v0.0.1-ci-test was green and produced a Release with maccat.pyz asset
- Test release AND tag (remote + local) deleted afterward
- No real v1.1.0 cut; this repo's a private Git host origin untouched
</verification>

<success_criteria>
A v*.*.* tag push to scottkw/maccat builds maccat.pyz on ubuntu-latest and publishes a GitHub Release with the .pyz attached via gh release create (contents:write only, no third-party action), proven by a real release run for v0.0.1-ci-test (Release + asset observed) that is then fully cleaned up — CI-03 delivered.
</success_criteria>

<output>
Create `.planning/phases/19-ci-build-release-pipeline/19-02-SUMMARY.md` when done. Record the release run id, the observed asset name, and confirmation that the test release + tag were deleted.
</output>
