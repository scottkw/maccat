---
phase: 28-chromium-refactor-edge-brave
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/maccat/collectors/edge.py
  - src/maccat/collectors/brave.py
  - src/maccat/collectors/__init__.py
  - tests/collectors/test_edge.py
  - tests/collectors/test_brave.py
  - tests/collectors/test_section_titles.py
autonomous: true
requirements:
  - BRW-01
  - BRW-02

must_haves:
  truths:
    - "A generated catalog includes a 'Microsoft Edge Extensions' section"
    - "A generated catalog includes a 'Brave Browser Extensions' section"
    - "Edge component extensions excluded via COMPONENT_DENYLIST | EDGE_COMPONENT_DENYLIST"
    - "All 20 Brave component IDs excluded via COMPONENT_DENYLIST | BRAVE_COMPONENT_DENYLIST"
    - "Base dir with only NativeMessagingHosts returns items=[] with no NOTE (both browsers)"
    - "Section-title uniqueness test passes for all 21 titles"
    - "Registry order: Chrome then Edge then Brave then Firefox"
  artifacts:
    - path: "src/maccat/collectors/edge.py"
      provides: "EdgeCollector thin subclass, EDGE_COMPONENT_DENYLIST, _TITLE='Microsoft Edge Extensions'"
      exports: ["EdgeCollector", "EDGE_COMPONENT_DENYLIST"]
    - path: "src/maccat/collectors/brave.py"
      provides: "BraveCollector thin subclass, BRAVE_COMPONENT_DENYLIST (20 IDs), _TITLE='Brave Browser Extensions'"
      exports: ["BraveCollector", "BRAVE_COMPONENT_DENYLIST"]
    - path: "tests/collectors/test_edge.py"
      provides: "Edge tests: absent, present, multi-profile, NativeMessagingHosts, denylist"
    - path: "tests/collectors/test_brave.py"
      provides: "Brave tests: absent, present, multi-profile, NativeMessagingHosts, denylist"
  key_links:
    - from: "src/maccat/collectors/__init__.py"
      to: "EdgeCollector, BraveCollector"
      via: "get_registry() list at Chrome→Edge→Brave→Firefox positions"
      pattern: "EdgeCollector|BraveCollector"
    - from: "tests/collectors/test_section_titles.py"
      to: "edge_mod._TITLE, brave_mod._TITLE"
      via: "module-level _TITLE constant access"
      pattern: "edge_mod._TITLE|brave_mod._TITLE"
---

<objective>
Add EdgeCollector and BraveCollector as thin subclasses of ChromiumBaseCollector (from Plan 01).
Register them in get_registry() between Chrome and Firefox. Update the section-title uniqueness
test from 19 to 21 titles. Add fixture tests for the NativeMessagingHosts-only presence case.

Purpose: Delivers BRW-01 (Edge extensions cataloged) and BRW-02 (Brave extensions cataloged).
The presence-detection test documents that profile enumeration, not base-dir existence, governs
whether a section emits items or silently returns empty.

Output: edge.py, brave.py, updated __init__.py, test_edge.py, test_brave.py, updated test_section_titles.py.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/PROJECT.md
@/Users/ken/dev/maccat/.planning/ROADMAP.md
@/Users/ken/dev/maccat/.planning/phases/28-chromium-refactor-edge-brave/28-CONTEXT.md
@/Users/ken/dev/maccat/.planning/phases/28-chromium-refactor-edge-brave/28-PATTERNS.md
@/Users/ken/dev/maccat/.planning/phases/28-chromium-refactor-edge-brave/28-01-SUMMARY.md
@/Users/ken/dev/maccat/src/maccat/collectors/__init__.py
@/Users/ken/dev/maccat/tests/collectors/test_chrome.py
@/Users/ken/dev/maccat/tests/collectors/test_section_titles.py

<interfaces>
<!-- Contracts from Plan 01 and existing code that this plan builds against. -->

From src/maccat/collectors/chromium.py (created in Plan 01):
  class ChromiumBaseCollector(Collector):
      _base: Path = Path()
      _title: str = ""
      _denylist: frozenset[str] = frozenset()
      _browser_name: str = ""
      def _collect_profile(self, extensions_dir: Path) -> list[str]: ...
      def collect(self) -> CollectorResult: ...
  COMPONENT_DENYLIST: frozenset[str]  # 10 Chromium component IDs

Current __init__.py Chrome/Firefox block (from source):
  from maccat.collectors.chrome import ChromeCollector
  from maccat.collectors.firefox import FirefoxCollector
  ...
      ChromeCollector(),
      FirefoxCollector(),

BRAVE_COMPONENT_DENYLIST — all 20 IDs (from STACK.md, Brave Components wiki):
  "eeigpngbgcognadeebkilcpcaedhellh"  # Autofill States Data
  "iodkpdagapdfkphljnddpjlldadblomo"  # Brave Ad Block Updater
  "gkboaolpopklhgplhaaiboijnklogmbc"  # Brave Ad Block List Catalog
  "mfddibmblmbccpadfndgakiopmmhebop"  # Brave Ad Block Resources Library
  "afalakplffnnnlkncjhbmahjfjhmlkal"  # Brave Local Data Updater
  "cldoidikboihgcjfkhdeidbpclkineef"  # Brave Tor Client Updater (x86)
  "cpoalefficncklhjfpglfiplenlpccdb"  # Brave Tor Client Updater (arm64)
  "biahpgbdmdkfgndcmfiipgcebobojjkp"  # Brave Tor Client Updater (arm)
  "kkjipiepeooghlclkedllogndmohhnhi"  # Brave User Model Installer
  "giekcmmlnklenlaomppkphknjmnnpneh"  # Certificate Error Assistant
  "hfnkpimlhhgieaddgfemjhofmfblmnib"  # CRLSet
  "ggkkehgbnfjpeggfpleeakpidbkibbmn"  # Crowd Deny
  "khaoiebndkojlmppeemjhbpbandiljpe"  # File Type Policies
  "jamhcnnkihinmdlkakkaopbjbbcngflc"  # Hyphenation
  "laoigpblnllgcgjnjnllmfolckpjlhki"  # MEI Preload
  "gccbbckogglekeggclmmekihdgdpdgoe"  # NTP Sponsored Images
  "aoojcmojmmcbpfgoecoadbdpnagfchel"  # NTP Background Images
  "jflookgnkcckhobaglndicnbbgbonegd"  # Safety Tips
  "oimompecagnajdejgnnjijobebaeigek"  # Widevine
  "ojhpjlocmbogdgmfpkhlaaeamibhnphh"  # Zxcvbn Data Dictionaries
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create edge.py and brave.py thin subclasses with denylists</name>
  <files>src/maccat/collectors/edge.py, src/maccat/collectors/brave.py</files>
  <behavior>
    - EdgeCollector._base == Path.home() / "Library/Application Support/Microsoft Edge"
    - EdgeCollector._title == "Microsoft Edge Extensions"
    - EdgeCollector._denylist == COMPONENT_DENYLIST | EDGE_COMPONENT_DENYLIST
    - EdgeCollector._browser_name == "Microsoft Edge"
    - edge_mod._TITLE == "Microsoft Edge Extensions" (module-level constant)
    - BraveCollector._base == Path.home() / "Library/Application Support/BraveSoftware/Brave-Browser"
    - BraveCollector._title == "Brave Browser Extensions"
    - BraveCollector._denylist == COMPONENT_DENYLIST | BRAVE_COMPONENT_DENYLIST
    - BraveCollector._browser_name == "Brave Browser"
    - brave_mod._TITLE == "Brave Browser Extensions" (module-level constant)
    - len(BRAVE_COMPONENT_DENYLIST) == 20
  </behavior>
  <action>
    Create src/maccat/collectors/edge.py:

    Line 1: from __future__ import annotations. Imports: from pathlib import Path,
    from maccat.collectors.chromium import COMPONENT_DENYLIST, ChromiumBaseCollector.
    __all__ = ["EdgeCollector", "EDGE_COMPONENT_DENYLIST"].

    EDGE_COMPONENT_DENYLIST: frozenset[str] = frozenset() — empty, with a multiline comment above it:
    "Edge-specific component extension IDs. NOTE: Microsoft publishes no canonical component-ID list.
    This constant starts empty; COMPONENT_DENYLIST (the Chrome baseline) is applied via union in
    _denylist. Expand by installing Edge, opening edge://extensions, and cross-referencing IDs present
    on disk in Default/Extensions/ that are invisible in the UI (those are component IDs).
    Over-listing user extensions is the failure mode — do not block on completeness.
    Known gap: see STATE.md Deferred Items 'Edge denylist'."

    Module constants: _BASE = Path.home() / "Library/Application Support/Microsoft Edge"
    _TITLE = "Microsoft Edge Extensions"  (module-level; test_section_titles.py reads edge_mod._TITLE)

    class EdgeCollector(ChromiumBaseCollector):
      Docstring: "Thin subclass — Edge-specific paths and denylist."
      _base = _BASE
      _title = _TITLE
      _denylist = COMPONENT_DENYLIST | EDGE_COMPONENT_DENYLIST
      _browser_name = "Microsoft Edge"

    ---

    Create src/maccat/collectors/brave.py:

    Line 1: from __future__ import annotations. Imports: from pathlib import Path,
    from maccat.collectors.chromium import COMPONENT_DENYLIST, ChromiumBaseCollector.
    __all__ = ["BraveCollector", "BRAVE_COMPONENT_DENYLIST"].

    Two comment lines: "# Source: https://github.com/brave/brave-browser/wiki/Brave-Components"
    "# 20 confirmed component extension IDs — all 32-char lowercase alpha strings."
    BRAVE_COMPONENT_DENYLIST: frozenset[str] = frozenset({ all 20 IDs from interfaces block above,
    each on its own line with its inline comment }).

    Module constants: _BASE = Path.home() / "Library/Application Support/BraveSoftware/Brave-Browser"
    _TITLE = "Brave Browser Extensions"  (module-level; test_section_titles.py reads brave_mod._TITLE)

    class BraveCollector(ChromiumBaseCollector):
      Docstring: "Thin subclass — Brave-specific paths and denylist."
      _base = _BASE
      _title = _TITLE
      _denylist = COMPONENT_DENYLIST | BRAVE_COMPONENT_DENYLIST
      _browser_name = "Brave Browser"

    CRITICAL: Both files expose _TITLE as a module-level string constant AND set _title as a class
    attribute equal to that constant. test_section_titles.py accesses `edge_mod._TITLE` and
    `brave_mod._TITLE` — not the class attribute. ChromiumBaseCollector.collect() uses `self._title`
    — not the module constant. Both must exist and must be the same string.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/python -m pytest tests/collectors/test_edge.py tests/collectors/test_brave.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - edge.py and brave.py exist and are importable
    - edge_mod._TITLE == "Microsoft Edge Extensions"
    - brave_mod._TITLE == "Brave Browser Extensions"
    - len(BRAVE_COMPONENT_DENYLIST) == 20
    - ruff and mypy --strict exit 0 on both files
    - test_edge.py and test_brave.py pass (created in Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test_edge.py and test_brave.py; update __init__.py and test_section_titles.py</name>
  <files>
    tests/collectors/test_edge.py,
    tests/collectors/test_brave.py,
    src/maccat/collectors/__init__.py,
    tests/collectors/test_section_titles.py
  </files>
  <behavior>
    - test_edge.py: EdgeCollector absent returns items=[] + NOTE with "Microsoft Edge" in stderr
    - test_edge.py: EdgeCollector with Default profile extension collects it
    - test_edge.py: EdgeCollector multi-profile accumulates items from Default + Profile 1
    - test_edge.py: EdgeCollector section title == "Microsoft Edge Extensions"
    - test_edge.py: EdgeCollector section raw == False
    - test_edge.py: EDGE_COMPONENT_DENYLIST ID is excluded; real extension is not excluded
    - test_edge.py: base dir with only NativeMessagingHosts/ returns items=[], no NOTE in stderr
    - test_brave.py: same structure, same behaviors, for BraveCollector
    - test_brave.py: BRAVE_COMPONENT_DENYLIST ID is excluded; real extension is not excluded
    - test_brave.py: NativeMessagingHosts-only base dir test for Brave
    - __init__.py: get_registry() returns [... ChromeCollector(), EdgeCollector(), BraveCollector(), FirefoxCollector() ...]
    - test_section_titles.py: passes with 21 titles (assert len == 21)
  </behavior>
  <action>
    PART A — Create tests/collectors/test_edge.py:

    Mirror test_chrome.py structure exactly. Module docstring: "Tests for maccat.collectors.edge."
    from __future__ import annotations. Imports: json, Path, patch, pytest, capsys.
    import maccat.collectors.edge as edge_mod
    from maccat.collectors.edge import EDGE_COMPONENT_DENYLIST, EdgeCollector
    from maccat.collectors.chromium import COMPONENT_DENYLIST

    Copy _make_ext helper verbatim from test_chrome.py (lines 21-28; no changes needed).

    Test classes and methods (patch target: patch.object(EdgeCollector, "_base", new=base)):

    TestEdgeCollect:
      test_collects_default_profile — base/Default/Extensions/ has one ext; assert it is in items
      test_collects_multiple_profiles — base/Default/Extensions and base/Profile 1/Extensions; both appear
      test_edge_section_title — assert result.sections[0].title == "Microsoft Edge Extensions"
      test_edge_raw_is_false — assert result.sections[0].raw is False

    TestEdgeExclusions:
      test_skips_component_extension — use next(iter(COMPONENT_DENYLIST)) as the denied ID; add a real ext; assert denied is absent, real is present
      test_skips_temp_directory — ext_id "Temp" is excluded
      test_skips_underscore_directory — ext_id "_metadata" is excluded
      test_version_sort_tail_used — two version dirs (1.0.0_0 / 2.0.0_0); assert 2.0.0_0 manifest name is returned

    TestEdgeDegradation:
      test_edge_not_installed — missing_base = tmp_path / "NoEdge"; assert items==[], "NOTE" in err, "Microsoft Edge" in err
      test_profile_iterdir_oserror_degrades — call _collect_profile(ext_root) with iterdir side_effect=OSError; assert returns []
      test_ext_dir_iterdir_oserror_skips_that_ext — flaky_iterdir skips bad_dir; assert Good Ext still in items

    TestEdgeNativeMessagingOnly:
      test_base_dir_without_profiles_returns_empty_silently — create base dir with only NativeMessagingHosts/ subdir (no Extensions/); assert items==[], "NOTE" NOT in stderr.
      This test documents the profile-enumeration presence-detection rule: the NOTE fires only when
      base dir does not exist at all, not when it exists but has no profile-level Extensions dirs.

    ---

    PART B — Create tests/collectors/test_brave.py:

    Identical structure to test_edge.py. Substitute: BraveCollector for EdgeCollector,
    brave_mod for edge_mod, BRAVE_COMPONENT_DENYLIST for EDGE_COMPONENT_DENYLIST.
    Title assertion: "Brave Browser Extensions".
    Not-installed NOTE test: assert "NOTE" in err, "Brave Browser" in err.
    NativeMessagingHosts test: same fixture pattern (base dir exists, NativeMessagingHosts/ only).
    For test_skips_component_extension: use next(iter(BRAVE_COMPONENT_DENYLIST)) as the denied ID.

    ---

    PART C — Update src/maccat/collectors/__init__.py:

    In get_registry() function body (deferred imports section), add two imports alphabetically:
      from maccat.collectors.brave import BraveCollector
      from maccat.collectors.edge import EdgeCollector

    In the return list, insert after ChromeCollector() and before FirefoxCollector():
      ChromeCollector(),
      EdgeCollector(),   # NEW — BRW-01
      BraveCollector(),  # NEW — BRW-02
      FirefoxCollector(),

    Update the docstring section count line from "19 sections from 13 collectors" to
    "21 sections from 15 collectors". Add two lines to the section list in the docstring:
      19. Microsoft Edge Extensions
      20. Brave Browser Extensions
    and renumber Firefox from 19 to 21.

    ---

    PART D — Update tests/collectors/test_section_titles.py:

    Add two import lines after the existing zed_mod import:
      import maccat.collectors.brave as brave_mod
      import maccat.collectors.edge as edge_mod

    In test_all_section_titles_are_unique(), in the titles list, add two entries after
    chrome_mod._TITLE and before ff_mod._TITLE:
      edge_mod._TITLE,   # "Microsoft Edge Extensions"
      brave_mod._TITLE,  # "Brave Browser Extensions"

    Update the count assertion from 19 to 21:
      assert len(titles) == 21, f"Expected 21 titles, got {len(titles)}"

    Update the docstring count reference from "19" to "21" and add two lines to the
    "Title constant locations:" block:
      edge_mod._TITLE                — "Microsoft Edge Extensions"
      brave_mod._TITLE               — "Brave Browser Extensions"

    Also update the test function docstring first line from "All 19 collector section title
    constants must be unique." to "All 21 collector section title constants must be unique."

    Do NOT alter test_new_titles_fall_to_manual_checklist() — that test covers Codex Plugins and
    Zed Extensions from Phase 27 and requires no changes.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/python -m pytest tests/collectors/test_edge.py tests/collectors/test_brave.py tests/collectors/test_chrome.py tests/collectors/test_section_titles.py -q --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_edge.py passes (all test classes: TestEdgeCollect, TestEdgeExclusions, TestEdgeDegradation, TestEdgeNativeMessagingOnly)
    - test_brave.py passes (same structure, same classes for Brave)
    - test_section_titles.py passes with assert len(titles) == 21
    - test_chrome.py still passes (regression guard: no changes to chrome.py in this plan)
    - get_registry() list contains ChromeCollector(), EdgeCollector(), BraveCollector(), FirefoxCollector() in that order
    - ruff and mypy --strict exit 0 on all new/modified files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python | Reads manifest.json files from Edge/Brave profile dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28B-01 | Information Disclosure | edge.py / brave.py read manifest.json | accept | Same trust level as Chrome collector; own profile dir; no credentials, no network |
| T-28B-02 | Tampering | Malformed manifest.json | accept | json_get returns empty string on missing/invalid fields; emit_item skips; inherited from ChromiumBaseCollector |
| T-28B-03 | Denial of Service | Brave/Edge NativeMessagingHosts-only dir | accept | Profile enumeration returns items=[] silently; no crash, no spurious output |
| T-28B-SC | Tampering | No new package installs in this plan | accept | stdlib-only; no pip install commands |
</threat_model>

<verification>
Full phase completion check (all plans):

```
PYTHONPATH=src ./venv/bin/python -m pytest \
  tests/collectors/test_chrome.py \
  tests/collectors/test_edge.py \
  tests/collectors/test_brave.py \
  tests/collectors/test_section_titles.py \
  -v --tb=short

MYPYPATH=src ./venv/bin/mypy --strict \
  src/maccat/collectors/chromium.py \
  src/maccat/collectors/chrome.py \
  src/maccat/collectors/edge.py \
  src/maccat/collectors/brave.py \
  src/maccat/collectors/__init__.py

PYTHONPATH=src ./venv/bin/ruff check \
  src/maccat/collectors/chromium.py \
  src/maccat/collectors/chrome.py \
  src/maccat/collectors/edge.py \
  src/maccat/collectors/brave.py \
  src/maccat/collectors/__init__.py \
  tests/collectors/test_edge.py \
  tests/collectors/test_brave.py \
  tests/collectors/test_section_titles.py
```

Phase 28 success criteria cross-check:
1. Chrome byte-identical: test_chrome.py passes with new patch targets (Plan 01 gate)
2. Edge section + documented denylist gap: test_edge.py passes; EDGE_COMPONENT_DENYLIST comment present
3. Brave section + 20 IDs: test_brave.py passes; len(BRAVE_COMPONENT_DENYLIST) == 20
4. Profile presence detection: NativeMessagingHosts test in both test_edge.py and test_brave.py passes
5. COMPONENT_DENYLIST re-export + 21-title uniqueness: from maccat.collectors.chrome import COMPONENT_DENYLIST works; test_section_titles.py passes
</verification>

<success_criteria>
- edge.py and brave.py exist; both are importable; _TITLE module constants are correct strings
- BRAVE_COMPONENT_DENYLIST has exactly 20 IDs; EDGE_COMPONENT_DENYLIST has documented gap comment
- test_edge.py and test_brave.py both pass including the NativeMessagingHosts-only fixture test
- get_registry() order: [..., ChromeCollector(), EdgeCollector(), BraveCollector(), FirefoxCollector()]
- test_section_titles.py passes with 21 titles (assert len == 21)
- test_chrome.py still passes (no regression from __init__.py change)
- ruff and mypy --strict exit 0 on all 6 modified/created source files
</success_criteria>

<output>
Create `.planning/phases/28-chromium-refactor-edge-brave/28-02-SUMMARY.md` when done
</output>
