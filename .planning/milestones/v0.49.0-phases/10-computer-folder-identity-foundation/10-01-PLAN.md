---
phase: 10-computer-folder-identity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - update-list.sh
  - machine-labels.tsv
autonomous: true
requirements:
  - CID-01
  - CID-02
  - CID-03

must_haves:
  truths:
    - "A catalog saved into folder 'Example Computer' is named mac-software-list-[Example Computer]-YYYYMMDDHHMMSS.txt — no raw hostname in the name"
    - "A name containing /, [, ], tab, newline, or leading/trailing whitespace is rejected; names with spaces, apostrophes, letters, digits, -, _, . are accepted"
    - "The validation rule is a single shared implementation — one function body, two call-mode wrappers (fatal and re-prompt)"
    - "After the computer (folder) is chosen, machine-labels.tsv contains hostname TAB folder-name, and the next run can read it to know the remembered folder"
  artifacts:
    - path: "update-list.sh"
      provides: "validate_computer_name (fatal) and validate_computer_name_quiet (non-fatal) — same regex/rule, two exit behaviors"
      contains: "validate_computer_name"
    - path: "update-list.sh"
      provides: "CURRENT_MACHINE set from TARGET_LOCATION so OUTPUT_FILENAME uses the folder name as the [label]"
      contains: "CURRENT_MACHINE=\"$TARGET_LOCATION\""
    - path: "update-list.sh"
      provides: "upsert_machine_label records TARGET_LOCATION (the folder name) as the value, not a separate MACHINE_LABEL"
      contains: "hostname->computer-folder"
    - path: "machine-labels.tsv"
      provides: "Updated header comment describing hostname->computer-folder semantics"
      contains: "computer-folder"
  key_links:
    - from: "parse_arguments (--machine flag path)"
      to: "validate_computer_name"
      via: "direct call; replaces validate_machine_label"
    - from: "resolve_machine_label (create-new path)"
      to: "validate_computer_name_quiet"
      via: "direct call in re-prompt loop; replaces validate_machine_label_quiet"
    - from: "main block"
      to: "OUTPUT_FILENAME"
      via: "CURRENT_MACHINE=\"$TARGET_LOCATION\" feeds mac-software-list-[$CURRENT_MACHINE]-$CURRENT_DATE.txt"
    - from: "resolve_machine_label (or inline in main)"
      to: "upsert_machine_label"
      via: "upsert_machine_label changed to read TARGET_LOCATION directly (not MACHINE_LABEL) — records hostname→folder name in TSV"
---

<objective>
Establish the computer-folder identity model foundation in update-list.sh:

1. Add a Zsh source-guard immediately before the main block so the script is safely sourceable for isolated function testing.
2. Consolidate the two validation functions (validate_machine_label / validate_machine_label_quiet) into a renamed pair (validate_computer_name / validate_computer_name_quiet) so the rule exists in exactly one place and both the fatal flag-path and the re-prompt interactive path share it. Update all three call sites.
3. Wire CURRENT_MACHINE from TARGET_LOCATION (the chosen folder name) so the catalog filename's [label] always equals the folder name, not a separately-resolved MACHINE_LABEL.
4. Update upsert_machine_label to record TARGET_LOCATION as the map value (hostname→computer-folder), update its header-comment template, and update the live machine-labels.tsv header comment to describe the new semantics.
5. Update display_usage to reflect the folder-as-identity model.

Purpose: Phase 11 builds the always-shown select_computer menu and --computer flag on top of these wiring primitives. If CURRENT_MACHINE is still driven by a separate MACHINE_LABEL by the time Phase 11 lands, the filename identity will be wrong.

Output: update-list.sh with source-guard, renamed helpers, and corrected wiring; machine-labels.tsv with updated header.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/STATE.md
@/Users/ken/dev/mac-software-list/.planning/phases/10-computer-folder-identity-foundation/10-CONTEXT.md
@/Users/ken/dev/mac-software-list/docs/superpowers/specs/2026-06-14-computer-folder-model-design.md

## DESTRUCTIVE-RUN WARNING (mandatory)

update-list.sh is destructive to run: a normal run generates catalogs and archives;
--rename moves real files and commits. ALL acceptance criteria in this plan MUST use:
  - zsh -n update-list.sh  (syntax check)
  - source-and-call isolated function tests in mktemp -d fixtures
  - grep/source assertions against the script text
NEVER run ./update-list.sh live against the real personal/office trees.

<interfaces>
<!-- Key identifiers the executor needs — extracted from update-list.sh. No codebase exploration needed. -->

Source-guard insertion point (line 2354–2355):
  # ==============================================================================
  # MAIN SCRIPT EXECUTION
  # ==============================================================================
  Insert one line immediately before this block:
    [[ "$ZSH_EVAL_CONTEXT" =~ :file ]] && return 0
  This is the sole source-guard; no other change is needed for it to work.

Existing validation functions (lines ~118 and ~157) to rename:
  validate_machine_label()        → rename to validate_computer_name()        # fatal: exit 1
  validate_machine_label_quiet()  → rename to validate_computer_name_quiet()  # non-fatal: return 1

Current call sites of these two functions (all three must be updated):
  parse_arguments      line ~226  – validate_machine_label "$val"
  resolve_machine_label line ~556 – reason=$(validate_machine_label_quiet "$new_label")
  rename_machine       line ~684  – reason=$(validate_machine_label_quiet "$new_label")
  (no other callers confirmed by grep)

Current MACHINE_LABEL global (top of script, line ~51):
  MACHINE_LABEL=""   # set by --machine flag or resolve_machine_label

Current main-block wiring (lines ~2387-2389):
  CURRENT_DATE=$(date "+%Y%m%d%H%M%S")
  CURRENT_MACHINE="$MACHINE_LABEL"
  OUTPUT_FILENAME="mac-software-list-[${CURRENT_MACHINE}]-${CURRENT_DATE}.txt"

Current upsert_machine_label signature (line ~372):
  upsert_machine_label()   # reads MACHINE_LABEL global; writes hostname<TAB>MACHINE_LABEL to machine-labels.tsv

Current machine-labels.tsv header lines (must update):
  # Mac Software List — Machine Labels
  # Format: hostname<TAB>label
  # One entry per line. Lines beginning with # and blank lines are ignored.

upsert_machine_label creates these header lines when the file does not exist (lines ~379-383):
  printf '# Mac Software List — Machine Labels\n' > "$map_file"
  printf '# Format: hostname\tlabel\n' >> "$map_file"
  printf '# One entry per line. Lines beginning with # and blank lines are ignored.\n' >> "$map_file"

git_commit_and_push commit message (line ~2331) — references CURRENT_MACHINE:
  local commit_message="Added ${TARGET_LOCATION} catalog for [${CURRENT_MACHINE}] at ${CURRENT_DATE}"

Validation regex (must remain identical in both functions):
  reject: '[][/]'  (bracket expression: ] first makes it literal, then [, then /)
  reject: leading/trailing [[:space:]]
  reject: interior $'\t' or $'\n'
  non-empty check

display_usage option lines to update (lines ~84-93):
  --machine "Label"    Set the machine label for this run's catalog filename
  --rename             Rename a machine label across all catalog files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Zsh source-guard before the main block</name>
  <files>update-list.sh</files>

  <read_first>
    Read update-list.sh lines 2352-2360 (the separator comment and MAIN SCRIPT EXECUTION header) to confirm exact placement.
  </read_first>

  <action>
Insert exactly one line immediately before the `# MAIN SCRIPT EXECUTION` section (currently line 2355). The insertion point is the blank line at ~2353, just before the `# ====…====` separator.

Add:
  [[ "$ZSH_EVAL_CONTEXT" =~ :file ]] && return 0

Place it on its own line, followed by a blank line separating it from the `# ====…====` block. No other edits in this task.

How it works: when Zsh sources the script (e.g. `source update-list.sh` inside a subshell for testing), `ZSH_EVAL_CONTEXT` contains `:file`; the guard returns 0 and the main block never runs. When the script is executed directly (`./update-list.sh`), `ZSH_EVAL_CONTEXT` does not contain `:file`; the guard is skipped and the main block runs normally. This is what makes all the source-based isolated function tests in Tasks 2 and 3 valid.
  </action>

  <verify>
    <automated>
      # 1. Syntax check — guard must not break the script
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh && echo "PASS: syntax OK"

      # 2. Source-guard line exists
      grep -c 'ZSH_EVAL_CONTEXT.*:file.*return 0' /Users/ken/dev/mac-software-list/update-list.sh
      # Expected: 1

      # 3. Sourcing the script prints "sourced-ok" and does NOT run the main block
      # (no menu prompt, no ERROR about missing TTY, no catalog generation output)
      zsh -c "source /Users/ken/dev/mac-software-list/update-list.sh; echo sourced-ok" 2>&1
      # Expected output: exactly "sourced-ok" — no other lines from the main block
    </automated>
  </verify>

  <done>
    - `[[ "$ZSH_EVAL_CONTEXT" =~ :file ]] && return 0` exists on a single line immediately before the `# MAIN SCRIPT EXECUTION` section.
    - `zsh -n` passes.
    - `zsh -c "source update-list.sh; echo sourced-ok"` prints only `sourced-ok` with no main-block output.
    - Direct execution behavior is unchanged (guard does not trigger when run as `./update-list.sh`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename validation helpers and update all call sites</name>
  <files>update-list.sh</files>

  <read_first>
    Read update-list.sh lines 104-176 (validate_machine_label and validate_machine_label_quiet function bodies).
    Read update-list.sh lines 220-230 (parse_arguments --machine case, call to validate_machine_label).
    Read update-list.sh lines 549-570 (resolve_machine_label create-new path, call to validate_machine_label_quiet).
    Read update-list.sh lines 680-690 (rename_machine new-label prompt, call to validate_machine_label_quiet).
  </read_first>

  <action>
Rename validate_machine_label to validate_computer_name and validate_machine_label_quiet to validate_computer_name_quiet. The validation logic (four checks: non-empty, no leading/trailing [[:space:]], no '[][/]', no interior TAB or newline) must remain byte-for-byte identical — only the function name and the error message prefix changes.

Update error messages from "--machine label" to "computer name" in both functions:
  - "ERROR: --machine label must not be empty" → "ERROR: computer name must not be empty"
  - "ERROR: --machine label must not have leading or trailing whitespace (got '${val}')" → "ERROR: computer name must not have leading or trailing whitespace (got '${val}')"
  - "ERROR: --machine label must not contain /, [, or ] (got '${val}')" → "ERROR: computer name must not contain /, [, or ] (got '${val}')"
  - "ERROR: --machine label must not contain tab or newline characters" → "ERROR: computer name must not contain tab or newline characters"
  - The quiet variant uses "ERROR: Label must not …" — change "Label" to "computer name" throughout.

Update the function-header doc comments for both functions: replace "machine label" with "computer name"; update the Arguments description to say "computer/folder name to validate".

Update all three call sites:
  1. parse_arguments (~line 226): validate_machine_label "$val" → validate_computer_name "$val"
  2. resolve_machine_label create-new path (~line 556): reason=$(validate_machine_label_quiet "$new_label") → reason=$(validate_computer_name_quiet "$new_label")
  3. rename_machine new-label prompt (~line 684): reason=$(validate_machine_label_quiet "$new_label") → reason=$(validate_computer_name_quiet "$new_label")

No other code changes in this task. Do not touch main-block wiring, upsert_machine_label, or display_usage yet.
  </action>

  <verify>
    <automated>
      # 1. Syntax check — catches typos in the function body
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh

      # 2. Old names must be gone (grep should return exit 1 = no matches)
      grep -v '^#' /Users/ken/dev/mac-software-list/update-list.sh | grep -c 'validate_machine_label'
      # Expected output: 0  (any non-zero means a call site or function definition was missed)

      # 3. New names present for both functions and all three call sites
      grep -c 'validate_computer_name' /Users/ken/dev/mac-software-list/update-list.sh
      # Expected: 5 minimum (2 function defs + 3 call sites; comments add more)

      # 4. Fatal variant exits 1 on empty string — isolated function test (source-guard makes this safe)
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        zsh -c "source '$tmp/s.sh'; validate_computer_name ''" 2>&1
        echo "exit=$?"
      )
      # Expected: prints "ERROR: computer name must not be empty", exit=1

      # 5. Fatal variant exits 1 on bracket in name
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        zsh -c "source '$tmp/s.sh'; validate_computer_name 'bad[name'" 2>&1
        echo "exit=$?"
      )
      # Expected: prints "ERROR: computer name must not contain /, [, or ]", exit=1

      # 6. Quiet variant returns 0 on valid name — does not exit
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        zsh -c "source '$tmp/s.sh'; validate_computer_name_quiet \"Example Computer\"; echo \$?"
      )
      # Expected: 0

      # 7. Quiet variant returns 1 on slash — does not exit the shell
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        zsh -c "source '$tmp/s.sh'; out=\$(validate_computer_name_quiet 'bad/name'); rc=\$?; echo \"rc=\$rc msg=\$out\""
      )
      # Expected: rc=1 msg=ERROR: computer name must not contain /, [, or ]
    </automated>
  </verify>

  <done>
    - validate_computer_name (fatal) and validate_computer_name_quiet (non-fatal) exist as the only implementations of the name validation rule.
    - No occurrence of validate_machine_label (excluding comment history if any) remains.
    - All three call sites updated.
    - zsh -n passes.
    - Isolated function tests confirm fatal exits with exit 1, quiet returns 1 without exiting the shell, and valid names pass.
    - Source-based tests work cleanly because the Task 1 source-guard prevents the main block from running on source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CURRENT_MACHINE from TARGET_LOCATION; update upsert, map header, and display_usage</name>
  <files>update-list.sh, machine-labels.tsv</files>

  <read_first>
    Read update-list.sh lines 2382-2395 (main block: resolve_machine_label call, CURRENT_MACHINE/OUTPUT_FILENAME assignment, OUTPUT_FILE assignment).
    Read update-list.sh lines 372-418 (upsert_machine_label full body — the map write and the header-creation block).
    Read update-list.sh lines 67-102 (display_usage).
    Read update-list.sh lines 2325-2335 (git_commit_and_push commit_message line).
    Read machine-labels.tsv (current header comment lines).
  </read_first>

  <action>
Four edits to update-list.sh and one edit to machine-labels.tsv:

EDIT 1 — main block CURRENT_MACHINE wiring.
  Current: CURRENT_MACHINE="$MACHINE_LABEL"
  Change to: CURRENT_MACHINE="$TARGET_LOCATION"
  This makes the [label] segment in OUTPUT_FILENAME equal the chosen folder name (per CID-01).
  The three lines around it (CURRENT_DATE, OUTPUT_FILENAME, OUTPUT_FILE) are unchanged.

EDIT 2 — upsert_machine_label: record TARGET_LOCATION (folder) not MACHINE_LABEL.
  The function body uses MACHINE_LABEL as the value written to the TSV:
    printf '%s\t%s\n' "$current_host" "$MACHINE_LABEL"    (two places: replace and append)
  Change both occurrences to: printf '%s\t%s\n' "$current_host" "$TARGET_LOCATION"
  Also update the echo at the end:
    "Saved machine label mapping: ${current_host} -> ${MACHINE_LABEL}" → "Saved computer folder mapping: ${current_host} -> ${TARGET_LOCATION}"

  Update the function-header doc comment: replace "label mapping" / "MACHINE_LABEL global" with "computer-folder mapping" / "TARGET_LOCATION global".

  Update the header-comment creation block (lines ~379-383) that runs when machine-labels.tsv does not exist:
    printf '# Mac Software List — Machine Labels\n'     → '# Mac Software List — hostname to computer-folder map\n'
    printf '# Format: hostname\tlabel\n'                 → '# Format: hostname\tcomputer-folder\n'
    printf '# One entry per line. Lines beginning with # and blank lines are ignored.\n'   (keep as-is)

EDIT 3 — display_usage: update the --machine option description and the output filename comment.
  Line ~84: --machine "Label"    Set the machine label for this run's catalog filename
    Update to reflect that the folder name IS the identity (Phase 10 still keeps --machine for back-compat; Phase 11 adds --computer):
    --machine "Label"    Set the computer folder for this run (back-compat alias; prefer --computer in Phase 11)
  Line ~30 area: "Creates a file named: mac-software-list-[machine-label]-YYYYMMDDHHMMSS.txt"
    Update to: "Creates a file named: mac-software-list-[computer-folder]-YYYYMMDDHHMMSS.txt"
  Comment at line ~50: "Machine label for this run's catalog filename (set by --machine flag or resolve_machine_label)"
    Update to: "Computer folder for this run's catalog filename (set by TARGET_LOCATION)"

EDIT 4 — git_commit_and_push commit message.
  Current: "Added ${TARGET_LOCATION} catalog for [${CURRENT_MACHINE}] at ${CURRENT_DATE}"
  After the wiring change, CURRENT_MACHINE equals TARGET_LOCATION, making "Added personal catalog for [personal]" redundant.
  Update to: "Added [${CURRENT_MACHINE}] catalog at ${CURRENT_DATE}"
  (CURRENT_MACHINE now carries the full folder name, which may be "Example Computer" in Phase 11 onward.)

EDIT 5 — machine-labels.tsv: update the live header comment to match the new template.
  The file currently has three header lines. Replace them:
    # Mac Software List — Machine Labels       → # Mac Software List — hostname to computer-folder map
    # Format: hostname\tlabel                   → # Format: hostname\tcomputer-folder
    # One entry per line. Lines beginning...    (keep as-is)
  Write the three updated header lines to the file (preserving any data lines that follow).
  Important: the file currently has no data lines (only the three header lines), so the edit is straightforward.
  Use the same atomic-write pattern as upsert_machine_label: write to machine-labels.tsv.tmp, then mv to machine-labels.tsv.
  Actually for this one-time header update, a direct Write of the file is acceptable since there are no data lines to preserve.
  </action>

  <verify>
    <automated>
      # 1. Syntax check
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh

      # 2. CURRENT_MACHINE is set from TARGET_LOCATION in the main block
      grep -v '^#' /Users/ken/dev/mac-software-list/update-list.sh | grep 'CURRENT_MACHINE='
      # Expected: exactly one match: CURRENT_MACHINE="$TARGET_LOCATION"
      # Must NOT contain CURRENT_MACHINE="$MACHINE_LABEL"

      # 3. upsert_machine_label writes TARGET_LOCATION as the map value
      grep 'TARGET_LOCATION' /Users/ken/dev/mac-software-list/update-list.sh | grep 'printf.*current_host'
      # Expected: two matching lines (replace and append paths both use TARGET_LOCATION)

      # 4. Old MACHINE_LABEL references inside upsert_machine_label are gone
      grep -n 'MACHINE_LABEL' /Users/ken/dev/mac-software-list/update-list.sh
      # Expected: only the global declaration line (MACHINE_LABEL="") and the --machine
      # case in parse_arguments (which still sets MACHINE_LABEL for back-compat; Phase 11
      # will remove it). No occurrence inside upsert_machine_label body.

      # 5. machine-labels.tsv header comment contains "computer-folder"
      grep 'computer-folder' /Users/ken/dev/mac-software-list/machine-labels.tsv
      # Expected: two matching lines (the map name line and the Format line)

      # 6. Isolated upsert function test — verify it writes TARGET_LOCATION to the map
      # (source-guard from Task 1 ensures the main block does not run on source)
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        # Override SCRIPT_DIR so the map file goes to tmp/
        zsh -c "
          SCRIPT_DIR='$tmp'
          TARGET_LOCATION='personal'
          source '$tmp/s.sh'
          upsert_machine_label
          cat '$tmp/machine-labels.tsv'
        " 2>&1
      )
      # Expected: map file contains a data line: <hostname><TAB>personal
      # (header lines will also be present)

      # 7. Isolated upsert test with a folder name containing spaces/apostrophe
      (
        tmp=$(mktemp -d)
        cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
        zsh -c "
          SCRIPT_DIR='$tmp'
          TARGET_LOCATION=\"Example Computer\"
          source '$tmp/s.sh'
          upsert_machine_label
          grep -c \"Example Computer\" '$tmp/machine-labels.tsv'
        " 2>&1
      )
      # Expected: 1  (the folder name is preserved in the TSV value column)

      # 8. display_usage no longer says [machine-label] in the filename description
      grep 'machine-label' /Users/ken/dev/mac-software-list/update-list.sh
      # Expected: 0 occurrences (old language removed)

      # 9. OUTPUT_FILENAME still contains the correct interpolation pattern
      grep 'OUTPUT_FILENAME=' /Users/ken/dev/mac-software-list/update-list.sh
      # Expected: mac-software-list-[${CURRENT_MACHINE}]-${CURRENT_DATE}.txt  (unchanged pattern)
    </automated>
  </verify>

  <done>
    - CURRENT_MACHINE="$TARGET_LOCATION" is the sole assignment in the main block; CURRENT_MACHINE="$MACHINE_LABEL" is gone.
    - upsert_machine_label writes TARGET_LOCATION (not MACHINE_LABEL) as the TSV value.
    - upsert_machine_label's creation template uses "computer-folder" header language.
    - machine-labels.tsv live header comment says "computer-folder" in both the title and Format lines.
    - display_usage uses "computer-folder" terminology; [machine-label] reference is gone from the output filename description.
    - zsh -n passes.
    - Isolated upsert test confirms a name with spaces and an apostrophe is round-tripped correctly into the TSV.
    - Source-based tests work cleanly because the Task 1 source-guard prevents the main block from running on source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → parse_arguments | --machine value is untrusted user input; validated before use |
| interactive TTY → read -r | User-typed computer name is untrusted; validated before use |
| machine-labels.tsv → resolve_machine_label | File content is local/trusted; read defensively (skip blank/comment lines) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | OUTPUT_FILENAME via CURRENT_MACHINE | mitigate | validate_computer_name rejects /, [, ] and whitespace before TARGET_LOCATION is ever used in a filename |
| T-10-02 | Tampering | machine-labels.tsv header overwrite | accept | File is local git-tracked repo; no untrusted write path in this phase |
| T-10-03 | Information Disclosure | hostname in machine-labels.tsv | accept | File is committed to a personal git repo; hostname exposure is intentional and existing |
| T-10-04 | Denial of Service | malformed TSV line corrupting upsert | mitigate | upsert_machine_label skips blank/comment lines and splits on TAB only; a hand-edited bad line is preserved verbatim (harmless) |
</threat_model>

<verification>
After all three tasks complete, run the full suite:

```zsh
# 1. Final syntax check
zsh -n /Users/ken/dev/mac-software-list/update-list.sh && echo "PASS: syntax OK"

# 2. Source-guard works — no main-block output on source
out=$(zsh -c "source /Users/ken/dev/mac-software-list/update-list.sh; echo sourced-ok" 2>&1)
[[ "$out" == "sourced-ok" ]] && echo "PASS: source-guard works" || echo "FAIL: unexpected output: $out"

# 3. No old validation function names remain (outside comments)
count=$(grep -v '^[[:space:]]*#' /Users/ken/dev/mac-software-list/update-list.sh | grep -c 'validate_machine_label' || true)
[[ "$count" -eq 0 ]] && echo "PASS: old names gone" || echo "FAIL: $count old name(s) remain"

# 4. Both new functions defined
grep -c 'validate_computer_name[^_]' /Users/ken/dev/mac-software-list/update-list.sh
grep -c 'validate_computer_name_quiet' /Users/ken/dev/mac-software-list/update-list.sh

# 5. CURRENT_MACHINE wired from TARGET_LOCATION
grep 'CURRENT_MACHINE=' /Users/ken/dev/mac-software-list/update-list.sh | grep -v '^[[:space:]]*#'
# Must show: CURRENT_MACHINE="$TARGET_LOCATION"

# 6. machine-labels.tsv has new header
grep 'computer-folder' /Users/ken/dev/mac-software-list/machine-labels.tsv

# 7. Success criterion 1: filename pattern uses [folder] identity
# Simulate: with TARGET_LOCATION="Example Computer", CURRENT_MACHINE gets that value
# and OUTPUT_FILENAME would be mac-software-list-[Example Computer]-YYYYMMDDHHMMSS.txt
# Verified by: CURRENT_MACHINE assignment + OUTPUT_FILENAME pattern grep (both in script)

# 8. Success criterion 2: validation accepts apostrophes/spaces, rejects brackets/slash
(tmp=$(mktemp -d); cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
 zsh -c "source '$tmp/s.sh'; validate_computer_name_quiet \"Example Computer\" && echo PASS || echo FAIL")
(tmp=$(mktemp -d); cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
 zsh -c "source '$tmp/s.sh'; validate_computer_name_quiet 'bad[name' && echo FAIL || echo PASS")
(tmp=$(mktemp -d); cp /Users/ken/dev/mac-software-list/update-list.sh "$tmp/s.sh"
 zsh -c "source '$tmp/s.sh'; validate_computer_name_quiet ' leading' && echo FAIL || echo PASS")
```
</verification>

<success_criteria>
1. Script is safely sourceable: `zsh -c "source update-list.sh; echo sourced-ok"` prints only `sourced-ok` — the main block (including the non-TTY exit guard in resolve_machine_label) never runs on source.
2. A catalog saved into folder "Example Computer" is named mac-software-list-[Example Computer]-YYYYMMDDHHMMSS.txt: confirmed by CURRENT_MACHINE="$TARGET_LOCATION" assignment and OUTPUT_FILENAME pattern.
3. Invalid names rejected, valid names accepted: confirmed by isolated validate_computer_name / validate_computer_name_quiet function tests.
4. Single shared validation rule: one function body (validate_computer_name) with fatal behavior; one wrapper (validate_computer_name_quiet) with non-fatal behavior; both call sites in resolve_machine_label and rename_machine use the quiet variant.
5. machine-labels.tsv records hostname→folder: confirmed by isolated upsert_machine_label test writing TARGET_LOCATION as the TSV value.
</success_criteria>

<output>
Create `.planning/phases/10-computer-folder-identity-foundation/10-01-SUMMARY.md` when done.
</output>
