---
phase: 11-computer-selection-cli
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - QUIT-01

must_haves:
  truths:
    - "--computer \"Name\" runs non-interactively, selecting or creating that folder without showing the menu"
    - "--personal, --office, and --machine \"X\" work as equivalent aliases routing to the same select-or-create path"
    - "Conflicting selecting-flags fail fast with an error and exit 1"
    - "An invalid --computer/alias name fails fast (exit 1) via the fatal validator"
    - "The main block calls select_computer once (replacing get_target_location + resolve_machine_label)"
    - "Quit inside select_computer exits 0 before generate_catalog and git_commit_and_push run"
    - "display_usage documents --computer and the back-compat aliases"
  artifacts:
    - path: "update-list.sh"
      provides: "parse_arguments --computer arm + alias routing + conflict guard; main block calling select_computer; legacy functions removed"
      contains: "--computer)"
  key_links:
    - from: "parse_arguments --computer arm"
      to: "validate_computer_name"
      via: "fatal validation of flag value"
      pattern: "validate_computer_name "
    - from: "main block"
      to: "select_computer"
      via: "single selection call before generate_catalog"
      pattern: "select_computer"
---

<objective>
Wire `select_computer` (created in Plan 01) into `update-list.sh`:

1. `parse_arguments`: add a `--computer "Name"` arm (fatal-validated, sets
   TARGET_LOCATION), keep `--personal`/`--office` as TARGET_LOCATION aliases, repoint
   `--machine "X"` to set TARGET_LOCATION (silent alias), add a multi-selecting-flag
   conflict guard, update the `--rename` conflict guard and the invalid-option message.
2. Main block: replace the `get_target_location` + `resolve_machine_label` call pair
   with a single `select_computer` call, placed so a Quit exits 0 BEFORE
   `generate_catalog` / retention / `git_commit_and_push`.
3. Remove the now-dead `get_target_location` and `resolve_machine_label` functions.
4. Update `display_usage` to document `--computer` and the aliases.

Depends on Plan 01: `select_computer` must already exist. Touches the same file as
Plan 01, so this is Wave 2 (sequential — file overlap).

Purpose: Implements CLI-01, CLI-02, and the main-block-wiring half of QUIT-01.
Output: A fully rewired script whose sole selection entry point is select_computer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-computer-selection-cli/11-CONTEXT.md
@.planning/phases/11-computer-selection-cli/11-PATTERNS.md
@docs/superpowers/specs/2026-06-14-computer-folder-model-design.md
@update-list.sh

<interfaces>
<!-- Existing helpers/globals the executor relies on. -->

validate_computer_name "$val"
  # Fatal validator (line ~118): echoes ERROR and `exit 1` on invalid; returns on valid.
  # Use it in the --computer arm and keep it in --machine.

select_computer
  # NEW in Plan 01 (defined above the source-guard). Reads TARGET_LOCATION (flag path),
  # else shows the menu; sets TARGET_LOCATION; may `exit 0` on Quit; calls upsert.

TARGET_LOCATION         # global the flags set and select_computer/main block read
MACHINE_LABEL           # legacy global — Phase 11 stops using it for --machine routing
RENAME_MODE             # global set by --rename
</interfaces>

Existing `--machine` arm (lines 220-229) and `--personal`/`--office` arms (lines 194-201)
are the analogs to copy/repoint. The post-loop `--rename`+`--machine` guard is at
lines 245-248. The invalid-option message is at line 236. Main-block selection sequence
is lines 2374-2391; source-guard is line ~2354.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse_arguments — add --computer, repoint --machine alias, add conflict guard, update messages</name>
  <files>update-list.sh</files>
  <read_first>
    - update-list.sh (focus: lines 190-249 parse_arguments — the case arms and post-loop guard; line 118 validate_computer_name signature)
    - .planning/phases/11-computer-selection-cli/11-PATTERNS.md (the "--computer flag + aliases" section — exact arm bodies, counter-based conflict guard, guard update)
    - .planning/phases/11-computer-selection-cli/11-CONTEXT.md (Area 4 CLI flag semantics)
  </read_first>
  <behavior>
    - `--computer "X"` sets TARGET_LOCATION=X after fatal `validate_computer_name "X"`;
      a missing/dash value errors (`--computer requires a value`, exit 1); an invalid
      name (e.g. `a/b`) exits 1 via the validator.
    - `--machine "X"` sets TARGET_LOCATION=X (same body as --computer), no deprecation
      warning, validated; MACHINE_LABEL is no longer the routing global for this.
    - `--personal` sets TARGET_LOCATION=personal; `--office` sets TARGET_LOCATION=office
      (unchanged bodies).
    - Passing two selecting-flags (any two of --personal/--office/--computer/--machine,
      e.g. `--personal --computer X`) errors (`mutually exclusive`) and exits 1.
    - `--rename` combined with any selecting-flag errors and exits 1.
    - Invalid option message lists `--computer` among valid options.
    - A single selecting-flag, or none, parses without error (exit 0 from parse_arguments).
  </behavior>
  <action>
    In `parse_arguments` (lines 190-249):

    1. Add a `--computer)` case arm modeled on the `--machine` arm (lines 220-229):
       require a value (`if [[ -z "$2" || "$2" == --* ]]; then echo "ERROR: --computer
       requires a value"; exit 1; fi`), `local val="$2"`, call fatal
       `validate_computer_name "$val"`, set `TARGET_LOCATION="$val"`, `shift 2`. Also
       increment the selecting-flag counter (see step 4).

    2. Repoint the `--machine)` arm: keep value-required + `validate_computer_name "$val"`,
       but set `TARGET_LOCATION="$val"` instead of `MACHINE_LABEL="$val"` (silent alias,
       no warning). Increment the counter.

    3. Keep `--personal)` / `--office)` arms setting TARGET_LOCATION; increment the
       counter in each.

    4. Add a selecting-flag conflict guard: declare `local selecting_flags_seen=0` at the
       top of parse_arguments; increment it inside each of the four selecting arms
       (--personal, --office, --computer, --machine). After the while loop, add:
       `if (( selecting_flags_seen > 1 )); then echo "ERROR: --personal, --office,
       --computer, and --machine are mutually exclusive."; exit 1; fi`.

    5. Update the existing `--rename`+selecting-flag guard (lines 245-248): since
       --machine now writes TARGET_LOCATION (not MACHINE_LABEL), change the condition to
       `if [[ "$RENAME_MODE" == "true" && -n "$TARGET_LOCATION" ]]; then` and update the
       message to `ERROR: --rename cannot be combined with a computer-selecting flag
       (--personal/--office/--computer/--machine).`, exit 1.

    6. Update the invalid-option message (line 236) to include `--computer` in the valid
       list.

    Do not remove the MACHINE_LABEL global declaration (line 51) — Phase 12 may still
    reference it; just stop routing --machine through it.
  </action>
  <verify>
    Source/syntax assertions only (script is destructive — NEVER run it live; use the
    source-guard for isolated parse tests):
    - `zsh -n update-list.sh` exits 0.
    - `grep -c '\-\-computer)' update-list.sh` >= 1; `grep -c 'selecting_flags_seen' update-list.sh` >= 5;
      `grep -c 'mutually exclusive' update-list.sh` >= 1; `grep -c 'TARGET_LOCATION="$val"' update-list.sh` >= 2.
    - Isolated, in its own `zsh -c` subshell: `source update-list.sh; parse_arguments --computer "Example Computer"; echo "$TARGET_LOCATION"`
      prints `Example Computer`; `parse_arguments --personal --computer X` exits non-zero with `mutually exclusive`.
  </verify>
  <acceptance_criteria>
    - `zsh -n update-list.sh` exits 0.
    - Source assertions (non-comment lines):
      `grep -c '\-\-computer)' update-list.sh` >= 1,
      `grep -c 'selecting_flags_seen' update-list.sh` >= 5 (declare + 4 increments + guard),
      `grep -c 'mutually exclusive' update-list.sh` >= 1,
      in the --machine arm region the routing target is TARGET_LOCATION:
      `grep -c 'TARGET_LOCATION="$val"' update-list.sh` >= 2 (computer + machine arms),
      the invalid-option line lists --computer:
      `grep -c 'Valid options.*--computer' update-list.sh` >= 1.
    - Isolated parse tests (script is destructive — never run live; use the source-guard).
      In each, `source update-list.sh` in a subshell, prefer the real
      `validate_computer_name`, call `parse_arguments` with args, then echo
      `$TARGET_LOCATION` / capture exit status:
        a) `parse_arguments --computer "Example Computer"` → TARGET_LOCATION equals
           `Example Computer`, no exit.
        b) `parse_arguments --machine Foo` → TARGET_LOCATION equals `Foo` (alias routes
           to the same global).
        c) `parse_arguments --personal` → TARGET_LOCATION equals `personal`.
        d) `parse_arguments --computer 'a/b'` in a `zsh -c` subshell → exits non-zero
           (fatal validator), output contains the validation ERROR.
        e) `parse_arguments --personal --computer X` in a subshell → exits non-zero,
           output contains `mutually exclusive`.
        f) `parse_arguments --rename --computer X` in a subshell → exits non-zero,
           output contains `--rename cannot be combined`.
      All a–f must hold. (Each fatal case runs in its own `zsh -c` so the `exit 1` does
      not kill the test harness.)
  </acceptance_criteria>
  <done>
    parse_arguments accepts --computer (fatal-validated → TARGET_LOCATION), routes
    --machine/--personal/--office to TARGET_LOCATION, fails fast on >1 selecting-flag
    and on --rename + selecting-flag, and lists --computer in its invalid-option message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire main block to call select_computer once; remove dead functions; update display_usage</name>
  <files>update-list.sh</files>
  <read_first>
    - update-list.sh (focus: lines 2374-2391 main-block selection sequence; lines 264-305 get_target_location; lines 439-570 resolve_machine_label; lines 67-102 display_usage; line ~2354 source-guard)
    - .planning/phases/11-computer-selection-cli/11-PATTERNS.md ("Main-block rewire" section — exact replacement sequence and removal note)
    - .planning/phases/11-computer-selection-cli/11-CONTEXT.md (Quit must exit before catalog/commit)
    - docs/superpowers/specs/2026-06-14-computer-folder-model-design.md (lines 105-131: CLI surface + Quit ordering)
  </read_first>
  <action>
    1. Main block (lines 2374-2391): delete the two calls `get_target_location` (line
       2375) and `resolve_machine_label` (line 2381) and replace with a single
       `select_computer` call, placed BEFORE `resolve_archive_retention` and `git_pull`
       so that a Quit (which does `exit 0` inside select_computer) returns before any
       catalog generation, retention sweep, prune, or commit. Keep
       `resolve_archive_retention`, `git_pull`, the `CURRENT_DATE=...`,
       `CURRENT_MACHINE="$TARGET_LOCATION"`, and `OUTPUT_FILENAME=...` lines (the
       filename construction is unchanged — folder name IS the label). Add a brief
       comment noting Quit exits before catalog/commit work.

    2. Remove the now-dead functions `get_target_location` (lines 264-305) and
       `resolve_machine_label` (lines 439-570) entirely, including their banner comment
       blocks. (Their `_label_in_list` helper lived inside resolve_machine_label and is
       removed with it; select_computer has its own local `_name_in_list`.) Confirm no
       other call sites remain.

    3. `display_usage` (lines 67-102): add a `--computer "Name"` option line as the
       primary selection flag; update the `--machine` line to describe it as a silent
       back-compat alias for `--computer`; ensure `--personal`/`--office` are described
       as aliases. Update the USAGE synopsis line (line 84) to include
       `[--computer "Name"]`. Keep the `(no option) → interactive menu` line.
  </action>
  <verify>
    Source/syntax assertions only (script is destructive — NEVER run it live):
    - `zsh -n update-list.sh` exits 0.
    - `zsh -c "source update-list.sh; echo sourced-ok"` prints only `sourced-ok` (source-guard fires).
    - `grep -c 'get_target_location()' update-list.sh` == 0; `grep -c 'resolve_machine_label()' update-list.sh` == 0;
      `grep -v '^[[:space:]]*#' update-list.sh | grep -c '_label_in_list'` == 0.
    - select_computer precedes generate_catalog in the main block:
      `awk '/ZSH_EVAL_CONTEXT.*:file/{m=1} m&&/^select_computer$/{s=NR} m&&/^generate_catalog$/{g=NR} END{exit !(s>0 && g>0 && s<g)}' update-list.sh`
  </verify>
  <acceptance_criteria>
    - `zsh -n update-list.sh` exits 0.
    - `zsh -c "source update-list.sh; echo sourced-ok"` prints only `sourced-ok` (the
      source-guard still fires; no main-block execution on source).
    - Dead-function removal (non-comment lines):
      `grep -c 'get_target_location()' update-list.sh` == 0,
      `grep -c 'resolve_machine_label()' update-list.sh` == 0,
      `grep -v '^[[:space:]]*#' update-list.sh | grep -c 'get_target_location'` == 0,
      `grep -v '^[[:space:]]*#' update-list.sh | grep -c 'resolve_machine_label'` == 0,
      `grep -v '^[[:space:]]*#' update-list.sh | grep -c '_label_in_list'` == 0.
    - Main-block wiring: `grep -v '^[[:space:]]*#' update-list.sh | grep -c '^select_computer$'`
      >= 1, and select_computer appears BEFORE generate_catalog in the main block:
      `awk '/ZSH_EVAL_CONTEXT.*:file/{m=1} m&&/^select_computer$/{s=NR} m&&/^generate_catalog$/{g=NR} END{exit !(s>0 && g>0 && s<g)}' update-list.sh` exits 0.
    - display_usage documents the flag: `grep -c '\-\-computer' update-list.sh` >= 2
      (the parse arm from Task 1 + the usage line) and the usage synopsis includes
      `--computer`: `grep -c 'computer "Name"' update-list.sh` >= 1.
  </acceptance_criteria>
  <done>
    The main block calls select_computer exactly once (before generate_catalog/commit),
    get_target_location and resolve_machine_label (and the orphaned _label_in_list) are
    fully removed, the script still sources cleanly, and display_usage documents
    --computer plus the aliases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → TARGET_LOCATION | `--computer`/`--machine` values become a directory name and the catalog filename `[segment]`. Untrusted argv crosses here. |
| main block → catalog/commit | A Quit must short-circuit before any filesystem write or git commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | --computer / --machine arms in parse_arguments | mitigate | Both arms call the fatal `validate_computer_name "$val"` (rejects `/`, `[`, `]`, TAB, newline, leading/trailing whitespace) BEFORE setting TARGET_LOCATION — same shared rule as the interactive path (Plan 01). |
| T-11-05 | Elevation of Privilege / Spoofing | conflicting selecting-flags | mitigate | Post-loop `selecting_flags_seen > 1` guard + the `--rename` + selecting-flag guard fail fast (exit 1), preventing ambiguous folder targeting. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; pure Zsh edits to a single existing file, no new dependencies. |
</threat_model>

<verification>
- `zsh -n update-list.sh` passes after each task.
- `zsh -c "source update-list.sh; echo sourced-ok"` prints only `sourced-ok`.
- `get_target_location` and `resolve_machine_label` are fully removed (0 non-comment refs).
- The main block invokes `select_computer` exactly once, before `generate_catalog`.
- `--computer`/`--machine` route through the fatal validator; conflict guards fire.
- display_usage documents `--computer` and the aliases.
</verification>

<success_criteria>
- CLI-01: `--computer "Name"` selects-or-creates non-interactively (skips the menu).
- CLI-02: `--personal`, `--office`, and `--machine "X"` work as equivalent aliases.
- QUIT-01 (wiring half): a Quit in select_computer exits 0 before any catalog is
  written or commit is made, because select_computer runs before generate_catalog and
  git_commit_and_push in the main block.
</success_criteria>

<output>
Create `.planning/phases/11-computer-selection-cli/11-02-SUMMARY.md` when done.
</output>
