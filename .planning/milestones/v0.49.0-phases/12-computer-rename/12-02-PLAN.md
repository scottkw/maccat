---
phase: 12-computer-rename
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - update-list.sh
autonomous: true
requirements: [RNM-02, RNM-03]
must_haves:
  truths:
    - "After a rename, the user is asked 'Rewrite all existing catalogs in 'X' to '[NewName]'? [Y/n]:' defaulting to Y"
    - "On Y (or empty/yes), every catalog in the new folder + its archive whose [label]==oldname is renamed to [newname], preserving the 14-digit timestamp"
    - "Filename rewrite skips (never overwrites) any destination collision and leaves non-matching labels untouched"
    - "On n (opt-out), the folder keeps the new name but existing filenames retain the old [label]"
    - "In BOTH rewrite and opt-out modes, every hostname->oldname map entry is updated to hostname->newname atomically"
    - "A folder rename with zero file rewrites (opt-out, or all-collision) still updates the map and commits (no renamed_count==0 abort)"
    - "The folder move, filename rewrites, and map update are staged and committed in a single git commit"
    - "With --no-commit, manual git instructions are printed instead of committing"
  artifacts:
    - path: "update-list.sh"
      provides: "Reworked rename_machine back-half: opt-out-gated in-folder rewrite, unconditional map update, single commit / --no-commit path, renamed_count==0 abort removed"
      contains: "Rewrite all existing catalogs in"
  key_links:
    - from: "rename_machine map rewrite"
      to: "machine-labels.tsv"
      via: "atomic .tmp + mv, matching TAB-bearing value==old_name, run in BOTH modes"
      pattern: "machine-labels.tsv.tmp|tmp_file"
    - from: "rename_machine git block"
      to: "git add -A old_name/ + new_name/ + machine-labels.tsv"
      via: "single commit staging old (deletions) + new (adds) + map"
      pattern: "git add -A"
---

<objective>
Rework the BACK HALF of `rename_machine` (the region after the folder move from Plan 01:
old file-rename loop ~737-773, the renamed_count==0 abort gate ~775-786, map rewrite ~788-817,
git block ~819-877) into the folder-centric rewrite/map/commit:

1. Gate the in-folder filename rewrite behind a `[Y/n]` prompt (default Y):
   `Rewrite all existing catalogs in '${new_name}' to '[${new_name}]'? [Y/n]: `.
2. Rewrite ONLY files whose `[label]`==`old_name` inside the MOVED folder + its `archive/`
   (`new_dir` and `new_dir/archive`), preserving the 14-digit timestamp, skipping collisions.
3. CRITICAL DIVERGENCE 1: DROP the `renamed_count==0` abort gate. The folder move already
   happened (and the map must update) in opt-out mode and in all-collision runs — gate the
   commit on "the folder was moved", not "files were rewritten".
4. Update EVERY `hostname -> old_name` map entry to `-> new_name` in BOTH rewrite and opt-out
   modes, atomically (.tmp + mv). Place the map update UNCONDITIONALLY after the opt-out branch.
5. Single commit: stage `old_name/` (deletions) + `new_name/` (adds) + `machine-labels.tsv`;
   honor `--no-commit` by printing manual instructions with the new paths.
6. Remove the Plan-01 temporary `old_label`/`new_label` shims (if any) and the legacy
   4-hardcoded-dir `dirs` array references — this plan owns the entire back-half.

Purpose: RNM-02 (rewrite contained catalogs by default, opt-out leaves filenames) and RNM-03
(update the map + single commit honoring --no-commit; no-op/no-match already guarded in Plan 01).

Output: A complete folder-centric `rename_machine`. CRITICAL DIVERGENCE 2 (hard refuse-clobber)
was implemented in Plan 01; this plan must NOT reintroduce a soft collision warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-computer-rename/12-CONTEXT.md
@.planning/phases/12-computer-rename/12-PATTERNS.md
@.planning/phases/12-computer-rename/12-01-SUMMARY.md

<interfaces>
<!-- Locals set by Plan 01's front half (already in scope when this back-half runs): -->
<!--   old_name, new_name  (validated computer names; new != old, both pass validation) -->
<!--   old_dir = "${SCRIPT_DIR}/${old_name}"   (already moved away; may no longer exist) -->
<!--   new_dir = "${SCRIPT_DIR}/${new_name}"   (the moved folder; archive/ rode along) -->
<!--   map_file = "${SCRIPT_DIR}/machine-labels.tsv" -->
<!-- Globals: SCRIPT_DIR, AUTO_COMMIT. Source-guard at ~line 2385 — keep rename_machine above it. -->

Reuse verbatim from 12-PATTERNS.md:
- Unit 4 file-rename loop body (analog update-list.sh lines 737-773): pure-zsh timestamp parse
  `${base##*-}` + `=~ ^[0-9]{14}$`, label-match-skip (`[[ "$file_label2" != "$old_name" ]]`),
  collision-skip (`[[ -e "$dest" ]]` -> warn + skip). Re-scope `rewrite_dirs=("$new_dir"
  "${new_dir}/archive")`.
- Unit 5 map rewrite (analog lines 788-817): atomic .tmp + mv, preserve `^#`/blank lines,
  TAB-required guard `[[ "$line" == *$'\t'* && "${line#*$'\t'}" == "$old_name" ]]`.
- Unit 6 git block (analog lines 819-872): cd guard, `git rev-parse` guard,
  `git diff --cached --quiet` no-op guard, push + recovery message, --no-commit manual path.
  Stage `git add -A "${old_name}/"` + `git add -A "${new_name}/"` + `git add machine-labels.tsv`.

Opt-out prompt idiom (analog resolve_archive_retention lines ~522-538): `printf ...; read -r
ans; lc="${ans:l}"; [[ -z "$lc" || "$lc" == "y" || "$lc" == "yes" ]]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opt-out-gated in-folder rewrite; drop renamed_count==0 abort; unconditional atomic map update in both modes</name>
  <files>update-list.sh</files>
  <read_first>
    - update-list.sh lines 737-817 (legacy file-rename loop, renamed_count==0 abort gate 775-786,
      map rewrite 788-817 — the region this task reworks)
    - update-list.sh lines 509-539 (resolve_archive_retention — the [Y/n] read + empty-default idiom)
    - .planning/phases/12-computer-rename/12-PATTERNS.md Unit 4 (rewrite loop + opt-out prompt +
      the CRITICAL "do NOT reuse the renamed_count==0 abort" divergence) and Unit 5 (map rewrite)
    - .planning/phases/12-computer-rename/12-CONTEXT.md Areas 3, 4
    - .planning/phases/12-computer-rename/12-01-SUMMARY.md (confirms old_name/new_name/new_dir locals + any shims to remove)
  </read_first>
  <action>
    Edit `rename_machine` back-half in update-list.sh (after Plan 01's folder move). Remove any
    temporary `old_label`/`new_label` shims and all references to the legacy 4-hardcoded-dir
    `dirs` array left from the analog.

    Add the opt-out prompt (12-PATTERNS Unit 4), default Y:
    `printf "Rewrite all existing catalogs in '${new_name}' to '[${new_name}]'? [Y/n]: "`,
    `read -r rewrite_ans`, `local lc_ans="${rewrite_ans:l}"`. Initialize
    `local renamed_count=0` and `local skipped_count=0` BEFORE the branch (so they exist in the
    summary regardless of mode).

    On `[[ -z "$lc_ans" || "$lc_ans" == "y" || "$lc_ans" == "yes" ]]`, run the rewrite loop
    re-scoped to `local rewrite_dirs=("$new_dir" "${new_dir}/archive")`:
    - `for dir2 in "${rewrite_dirs[@]}"; do [[ ! -d "$dir2" ]] && continue; setopt
      local_options null_glob; for file2 in "${dir2}"/mac-software-list-*.txt; do ... done; done`
    - Keep null-glob guard `[[ -e "$file2" ]] || continue` and `[[ -d "$file2" ]] && continue`.
    - Parse label: `local tmp2="${filename2#*\[}"; local file_label2="${tmp2%\]-*}"`; skip when
      `[[ "$file_label2" != "$old_name" ]]` (non-matching labels untouched in both modes).
    - Pure-zsh timestamp: `local base="${filename2%.txt}"; local ts="${base##*-}"`; if
      `[[ -z "$ts" || ! "$ts" =~ ^[0-9]{14}$ ]]` warn + continue.
    - `local new_filename="mac-software-list-[${new_name}]-${ts}.txt"`;
      `local dest="${dir2}/${new_filename}"`; if `[[ -e "$dest" ]]` warn
      `Destination already exists, skipping` + `((skipped_count++))` + continue;
      else `mv "$file2" "$dest"`, echo `Renamed: ...`, `((renamed_count++))`.

    CRITICAL DIVERGENCE 1 — DELETE the `renamed_count==0` abort gate (analog lines 775-786).
    Do NOT add any `(( renamed_count == 0 )) ... exit` branch. A move with zero rewrites is a
    valid, commit-worthy change. `renamed_count`/`skipped_count` are for the closing summary only.

    Add the map update UNCONDITIONALLY (NOT inside the Y branch), per 12-PATTERNS Unit 5: atomic
    `${map_file}.tmp` write preserving `^#`/blank lines, rewriting only TAB-bearing data lines
    whose value equals `old_name` (`[[ "$line" == *$'\t'* && "${line#*$'\t'}" == "$old_name" ]]`
    -> `printf '%s\t%s\n' "${line%%$'\t'*}" "$new_name"`), then `mv "$tmp_file" "$map_file"` and
    echo `Updated machine-labels.tsv: '${old_name}' -> '${new_name}'`. This MUST run in both
    rewrite (Y) and opt-out (n) modes.
  </action>
  <acceptance_criteria>
    All checks are source/grep or isolated mktemp-fixture tests — NEVER a live run, NEVER real
    personal/ office/ trees, NEVER real git.
    - `zsh -n update-list.sh` exits 0; `zsh -c "source update-list.sh; echo ok"` prints only `ok`.
    - Opt-out prompt present: `grep -Fc "Rewrite all existing catalogs in '"` >= 1 and the prompt
      text contains `[Y/n]`.
    - Rewrite re-scoped to the moved folder: `awk '/^rename_machine\(\)/,/^}/' update-list.sh |
      grep -Fc 'rewrite_dirs=("$new_dir" "${new_dir}/archive")'` == 1; the legacy
      `dirs=("personal" "personal/archive" "office" "office/archive")` no longer appears in
      rename_machine (`awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -c
      'personal/archive'` == 0).
    - DIVERGENCE 1 enforced: within rename_machine, NO abort gate on renamed_count —
      `awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -Ec '\(\( *renamed_count *== *0 *\)\)'` == 0
      AND no `Map not updated. Nothing committed.` text remains (`grep -Fc 'Map not updated.' update-list.sh` == 0).
    - No leftover label shims: `awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -Ec 'old_label|new_label'` == 0.
    - Map update unconditional + both modes: in an isolated mktemp fixture (source the script,
      set SCRIPT_DIR to fixture, AUTO_COMMIT=false), driving `rename_machine` with `n` (opt-out)
      STILL rewrites the map value old->new (assert the fixture's machine-labels.tsv now maps the
      host to new_name) AND leaves the old-labelled filenames intact.
    - Collision-skip + label-match: in the fixture, a file whose label != old_name is left
      untouched; a pre-existing destination collision is skipped (file not overwritten,
      `skipped_count` reflected in the summary line).
    - rename_machine still ABOVE the source-guard (line of `rename_machine()` < line of `ZSH_EVAL_CONTEXT`).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && zsh -n update-list.sh && grep -Fq "Rewrite all existing catalogs in '" update-list.sh && [ "$(awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -Ec '\(\( *renamed_count *== *0 *\)\)')" -eq 0 ] && [ "$(grep -Fc 'Map not updated.' update-list.sh)" -eq 0 ] && [ "$(awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -Ec 'old_label|new_label')" -eq 0 ] && echo PASS</automated>
  </verify>
  <done>
    The in-folder rewrite is opt-out-gated and re-scoped to the moved folder + its archive; the
    renamed_count==0 abort gate is gone; the map is rewritten atomically in BOTH Y and n modes;
    no legacy label shims or 4-dir references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Single commit staging old+new folder paths + map; --no-commit manual path; closing summary; full fixture test</name>
  <files>update-list.sh, test-rename-back-12-02.sh</files>
  <read_first>
    - update-list.sh lines 819-877 (legacy git block + --no-commit path + closing summary — reworked here)
    - update-list.sh lines 2399-2403 (main-block --rename short-circuit: git_pull; rename_machine; exit 0 — KEEP)
    - .planning/phases/12-computer-rename/12-PATTERNS.md Unit 6 (git block rework: staged paths + recovery message)
    - .planning/phases/12-computer-rename/12-CONTEXT.md Area 4 (single commit, honor --no-commit)
    - .planning/phases/11-computer-selection-cli/11-02-SUMMARY.md (mktemp harness pattern + git()-stub approach + grep-flavor caveats)
  </read_first>
  <action>
    Rework the git block (12-PATTERNS Unit 6). Keep the `AUTO_COMMIT=="true"` branch with: `cd
    "$SCRIPT_DIR"` guard, `git rev-parse --git-dir` guard. REPLACE the fixed personal/office
    staging loop with:
    `git add -A "${old_name}/" 2>/dev/null || true`,
    `git add -A "${new_name}/" 2>/dev/null || true`,
    `git add machine-labels.tsv 2>/dev/null || true`.
    Keep `if git diff --cached --quiet; then echo "  No changes staged."; return; fi`.
    Commit message: `local commit_message="Rename computer: '${old_name}' -> '${new_name}'"`.
    Keep the commit/push structure and rework the push-failure recovery message to say the
    FOLDER has already moved (`'${old_name}/' -> '${new_name}/'`) and to NOT re-run --rename;
    resolve with `cd $SCRIPT_DIR && git pull --rebase && git push`.

    REPLACE the `--no-commit` (else) manual path to print the new staged paths:
    `cd $SCRIPT_DIR && git add -A '${old_name}/' && git add -A '${new_name}/' && git add
    machine-labels.tsv`, then `git commit -m 'Rename computer: ${old_name} -> ${new_name}'`,
    then `git push`.

    Update the closing summary line to a folder-centric message reporting `renamed_count`
    rewritten and `skipped_count` skipped (collision), and noting the folder rename.

    Then extend/author `test-rename-back-12-02.sh` (a throwaway mktemp-fixture harness, NOT a
    live tool run). It MUST: build a fixture with a computer folder (main + archive) holding
    matching-label, non-matching-label, and collision stub files, plus a `machine-labels.tsv`
    mapping a host to old_name; `source update-list.sh`; set `SCRIPT_DIR` to the fixture and
    `AUTO_COMMIT=false`; define a local `git()` no-op stub so NO real git runs; and drive
    `rename_machine` via scripted stdin. NEVER touch the repo's real personal/ office/ trees or
    real git history.

    Cover:
    - Y (rewrite): matching-label files in main+archive get `[new_name]` (timestamp preserved),
      non-matching-label files untouched, collision skipped (not overwritten), folder is at
      new_name, map maps host->new_name.
    - n (opt-out): folder at new_name, old-labelled filenames unchanged, map STILL host->new_name.
    - --no-commit branch (AUTO_COMMIT=false): manual instructions printed (assert the output
      contains `git add -A '${new_name}/'` and `Rename computer:`), no commit attempted.
  </action>
  <acceptance_criteria>
    - `zsh -n update-list.sh` exits 0; `zsh -c "source update-list.sh; echo ok"` prints only `ok`.
    - Single-commit staging reworked: `awk '/^rename_machine\(\)/,/^}/' update-list.sh |
      grep -Fc 'git add -A "${old_name}/"'` == 1 AND `... grep -Fc 'git add -A "${new_name}/"'` == 1
      AND `... grep -Fc 'git add machine-labels.tsv'` >= 1; the legacy
      `for loc in personal office` staging loop no longer appears in rename_machine
      (`awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -c 'for loc in personal office'` == 0).
    - No-op-staged guard kept: `awk '/^rename_machine\(\)/,/^}/' update-list.sh |
      grep -Fc 'git diff --cached --quiet'` >= 1.
    - Commit message folder-centric: `grep -Fc 'Rename computer:' update-list.sh` >= 1.
    - --no-commit path updated: `awk '/^rename_machine\(\)/,/^}/' update-list.sh |
      grep -Fc "git add -A '\${new_name}/'"` >= 1 (manual instructions reference the new folder path).
    - Main-block short-circuit untouched: `grep -Fc 'rename_machine' update-list.sh` (in the main
      block region after the source-guard) still shows the `if RENAME_MODE ... rename_machine` call.
    - Full fixture harness passes: `zsh test-rename-back-12-02.sh` exits 0, all cases PASS, runs
      ONLY in a mktemp fixture with a stubbed `git()`; `grep -Ec '/personal/|/office/'
      test-rename-back-12-02.sh` == 0 and no `git push`/`git commit` against the real repo
      (`grep -Ec 'git (push|commit)' test-rename-back-12-02.sh` only inside the local stub).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && zsh -n update-list.sh && [ "$(awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -Fc 'git add -A "${new_name}/"')" -eq 1 ] && [ "$(awk '/^rename_machine\(\)/,/^}/' update-list.sh | grep -c 'for loc in personal office')" -eq 0 ] && grep -Fq 'Rename computer:' update-list.sh && zsh test-rename-back-12-02.sh && [ "$(grep -Ec '/personal/|/office/' test-rename-back-12-02.sh)" -eq 0 ] && echo PASS</automated>
  </verify>
  <done>
    rename_machine commits the folder move + filename rewrites + map update in a single commit
    staging old+new folder paths + the map (and prints folder-centric manual instructions under
    --no-commit); a full mktemp-fixture harness with a stubbed git proves Y, opt-out, and
    --no-commit behaviors without touching real trees or real git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rename_machine -> filesystem (batch rename) | Multiple catalog files inside the moved folder are renamed (destructive) |
| rename_machine -> machine-labels.tsv | The hostname->folder map is rewritten in place (config integrity) |
| rename_machine -> git | A single commit + push records the move/rewrites/map (history integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering/Destruction | in-folder filename rewrite | mitigate | collision-skip never overwrites (`[[ -e "$dest" ]]` -> warn + skip); only `[label]`==old_name files touched; non-matching labels untouched; 14-digit timestamp validated before any mv |
| T-12-05 | Tampering | atomic map rewrite | mitigate | write to `${map_file}.tmp` then `mv` (atomic); TAB-required guard so bare-hostname lines are never rewritten; `^#`/blank lines preserved verbatim |
| T-12-06 | Repudiation/Integrity | single git commit | mitigate | stage old+new folder paths + map in ONE commit; `git diff --cached --quiet` no-op guard avoids empty commits; push-failure recovery message warns NOT to re-run --rename (folder already moved); --no-commit prints manual instructions instead |
| T-12-07 | Denial of Service | opt-out [Y/n] prompt | accept | empty input defaults to Y (safe rewrite); any other input treated as opt-out; no infinite loop |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs; pure Zsh edits to one existing file. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `zsh -n update-list.sh` passes after each task; rename_machine stays above the source-guard.
- The renamed_count==0 abort gate is removed; map updates and commits occur in BOTH rewrite and opt-out modes.
- Full mktemp-fixture harnesses prove rewrite/opt-out/collision/map/commit behavior with a stubbed git and zero real-tree access.
- Main-block --rename short-circuit (`git_pull; rename_machine; exit 0`) is untouched.
</verification>

<success_criteria>
- RNM-02: default Y rewrites every matching catalog (main + archive) to `[new_name]`
  (timestamp preserved, collisions skipped, non-matching labels untouched); opt-out leaves
  existing filenames while the folder takes the new name.
- RNM-03: every hostname->old_name map entry becomes hostname->new_name (atomic, both modes);
  the folder move + rewrites + map update are staged in a single commit (or printed manually
  under --no-commit). No-op/no-match guards (from Plan 01) prevent empty renames from committing.
- Both critical divergences hold: renamed_count==0 abort removed (this plan); hard
  refuse-clobber (Plan 01) — no soft collision warning reintroduced.
- All verification via `zsh -n`, source/grep, and isolated mktemp fixtures with a stubbed git.
</success_criteria>

<output>
Create `.planning/phases/12-computer-rename/12-02-SUMMARY.md` when done.
</output>
