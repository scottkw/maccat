---
phase: 21-cli-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - tests/test_identity.py
  - tests/test_cli.py
  - tests/test_naming.py
autonomous: true
requirements:
  - CLI-03
  - CLI-04
  - CLI-06

must_haves:
  truths:
    - "All TestResolveComputerSelection tests call resolve_computer_selection(computer=...) only"
    - "maccat --personal, maccat --office, maccat --machine each exit nonzero (argparse error)"
    - "maccat --computer MyMac selects the MyMac folder"
    - "Existing non-regression tests for --rename, --no-commit, --archive-days, --catalog-dir still pass"
    - "Full pytest suite passes with zero failures"
    - "ruff check and mypy --strict pass on test files"
  artifacts:
    - path: "tests/test_identity.py"
      provides: "Tests using new resolve_computer_selection(*, computer) signature"
      contains: "def test_computer_resolves"
    - path: "tests/test_cli.py"
      provides: "Regression tests: removed flags yield SystemExit; --computer still works"
      contains: "def test_personal_flag_is_unrecognized"
  key_links:
    - from: "tests/test_identity.py"
      to: "src/maccat/identity.py"
      via: "resolve_computer_selection(computer=...)"
      pattern: "resolve_computer_selection\\(computer="
    - from: "tests/test_cli.py"
      to: "src/maccat/cli.py"
      via: "_build_parser().parse_args(['--personal'])"
      pattern: "parse_args.*--personal"
---

<objective>
Migrate test_identity.py to the new resolve_computer_selection signature. Update
test_cli.py to drop stale --personal/--office/--machine references and add three
new regression tests proving the removed flags now produce argparse errors. Verify
test_naming.py needs no changes (its "personal"/"office" strings are catalog-content
fixtures, not flag references). Confirm the full test suite passes clean.

Purpose: Prove the Phase 21 source changes are correct and non-regressions are
covered. This is the CLI-06 gate: interactive select_computer, --rename, --no-commit,
--archive-days, --catalog-dir all behave exactly as before.

Output:
- tests/test_identity.py: 11 call sites updated to new signature; obsolete tests
  replaced with new ones matching the simplified function
- tests/test_cli.py: stale flag tests removed; three new "removed flag errors" tests
  added; --rename x --computer test retained; --no-commit / --archive-days /
  --catalog-dir / config dispatch tests untouched
- tests/test_naming.py: confirmed unchanged (no flag references)
</objective>

<execution_context>
@/Users/ken/dev/maccat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/dev/maccat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/ROADMAP.md
@/Users/ken/dev/maccat/.planning/phases/21-cli-cleanup/21-CONTEXT.md
@/Users/ken/dev/maccat/.planning/phases/21-cli-cleanup/21-01-SUMMARY.md

<interfaces>
<!-- New signature from Plan 01 — use exactly this. -->

From src/maccat/identity.py (after Plan 01):
  def resolve_computer_selection(
      *,
      computer: str | None,
  ) -> str | None:
      """Map --computer flag to a folder name, or None for interactive fallback.
      If computer is falsy (None or ""), returns None.
      Otherwise calls validate_computer_name(computer) and returns computer.
      """

From src/maccat/cli.py (after Plan 01):
  - Namespace has: computer (str | None), rename (bool), no_commit (bool),
    archive_days (int | None), catalog_dir (str | None), subcommand (str | None)
  - Namespace does NOT have: personal, office, machine
  - Passing --personal/--office/--machine yields SystemExit(code=2) from argparse
    "unrecognized arguments: --personal" (standard argparse error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate TestResolveComputerSelection in test_identity.py</name>
  <files>tests/test_identity.py</files>

  <read_first>
  tests/test_identity.py lines ~90–167 — the full TestResolveComputerSelection class,
  all 11 test methods, their call sites.
  </read_first>

  <action>
  Rewrite the TestResolveComputerSelection class to match the new single-parameter
  signature. The class currently has 11 tests; after the rewrite it should have 7,
  covering all meaningful behaviors of the simplified function:

  Tests to REMOVE (behaviors that no longer exist):
  - test_personal_resolves: --personal flag is gone
  - test_office_resolves: --office flag is gone
  - test_machine_alias_resolves: --machine flag is gone
  - test_mutual_exclusion_errors_on_two_flags_personal_office: no multi-flag logic
  - test_mutual_exclusion_errors_on_computer_and_machine: no multi-flag logic
  - test_invalid_machine_name_raises: machine param is gone
  - test_empty_string_machine_treated_as_none: machine param is gone

  Tests to KEEP (update call sites only):
  - test_no_flag_returns_none_for_interactive → rename to test_none_returns_none;
    call: resolve_computer_selection(computer=None)
  - test_computer_alias_resolves → rename to test_computer_resolves;
    call: resolve_computer_selection(computer="Laptop"); assert result == "Laptop"
  - test_invalid_computer_name_raises → keep, update call:
    resolve_computer_selection(computer="bad/name")
  - test_empty_string_computer_treated_as_none → keep, update call:
    resolve_computer_selection(computer="")

  Tests to ADD (new behaviors worth testing):
  - test_valid_name_returned_unchanged: resolve_computer_selection(computer="WorkLaptop")
    assert result == "WorkLaptop"
  - test_name_with_spaces_is_valid: resolve_computer_selection(computer="My Mac")
    assert result == "My Mac" (spaces are allowed per validate_computer_name)
  - test_none_returns_none_for_interactive_fallback: resolve_computer_selection(computer=None)
    assert result is None

  All calls must use the keyword-only form: resolve_computer_selection(computer=...).
  mypy --strict must accept all call sites (no Any, no positional calls).
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/test_identity.py::TestResolveComputerSelection -v 2>&1 | tail -20</automated>
  </verify>

  <done>
  TestResolveComputerSelection passes all tests; no call site uses the old
  personal=/office=/machine= keyword arguments; mypy --strict on test_identity.py
  exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update test_cli.py — remove stale flag tests, add removed-flag regression tests</name>
  <files>tests/test_cli.py</files>

  <read_first>
  tests/test_cli.py — read the full file (498 lines). Key locations to update:
  - TestArgparse class (lines ~35–128): test_mutual_exclusion_personal_office,
    test_machine_has_separate_dest, test_personal_and_computer_are_mutually_exclusive,
    test_machine_and_office_are_mutually_exclusive — all reference removed flags
  - TestRenameFlag class (lines ~136–166): test_rename_with_personal_exits,
    test_rename_with_office_exits, test_rename_with_machine_exits — stale;
    test_rename_with_computer_exits is valid and must be kept
  - _patch_run_dependencies helper (line ~178): computer_name default "personal"
  - TestNoCommit (lines ~219–284): three tests use argv ["maccat", "--personal", ...]
  - TestGenerateThenSweep (lines ~292–360): two tests use argv ["maccat", "--personal", ...]
  </read_first>

  <action>
  Make the following changes to test_cli.py:

  1. TestArgparse — REMOVE these tests (the flags no longer exist):
     - test_mutual_exclusion_personal_office (--personal --office → no such flags)
     - test_machine_has_separate_dest (--machine → removed)
     - test_personal_and_computer_are_mutually_exclusive (--personal → removed)
     - test_machine_and_office_are_mutually_exclusive (--machine → removed)

  2. TestArgparse — ADD three new tests asserting removed flags now produce argparse errors:
     - test_personal_flag_is_unrecognized: _build_parser().parse_args(["--personal"])
       must raise SystemExit with code 2 (argparse "unrecognized arguments" exit code).
     - test_office_flag_is_unrecognized: _build_parser().parse_args(["--office"])
       must raise SystemExit with code 2.
     - test_machine_flag_is_unrecognized: _build_parser().parse_args(["--machine", "box"])
       must raise SystemExit with code 2.

  3. TestArgparse — KEEP and do not alter:
     - test_version_exits_zero, test_help_exits_zero, test_computer_has_own_dest,
       test_no_commit_flag_parsed, test_no_commit_default_is_false,
       test_archive_days_parsed, test_archive_days_default_is_none,
       test_catalog_dir_parsed, test_bare_invocation_subcommand_is_none,
       test_rename_flag_parsed

  4. TestRenameFlag — REMOVE:
     - test_rename_with_personal_exits (--personal no longer a valid flag to combine)
     - test_rename_with_office_exits (--office no longer a valid flag to combine)
     - test_rename_with_machine_exits (--machine no longer a valid flag to combine)

  5. TestRenameFlag — KEEP:
     - test_rename_with_computer_exits (--rename --computer box must still exit nonzero)

  6. _patch_run_dependencies helper: change the default computer_name from "personal"
     to "MyMac" so the helper no longer implies the removed folder names.

  7. TestNoCommit (three tests use argv ["maccat", "--personal", ...]):
     Replace "--personal" with "--computer", "MyMac" in each argv list. The tests'
     assertions (git_commit_and_push not called, git_pull called, catalog file written
     to computer/ dir) remain the same logic — only update the folder name from
     "personal" to "MyMac" in directory path assertions.

  8. TestGenerateThenSweep (two tests use argv ["maccat", "--personal", ...] and
     monkeypatch select_computer return_value "personal"):
     - Replace "--personal" with "--computer", "MyMac" in argv.
     - Replace select_computer return_value "personal" with "MyMac".
     - Replace "personal" folder path references with "MyMac".
     Also update test_config_show_rename_flag_errors and any other test that passes
     "--rename" alongside now-removed flags in argv.

  9. TestConfigDispatch / TestSelectComputerQuit: these tests do not use the removed
     flags directly — verify no edits needed (grep for "personal\|office\|machine"
     in those test methods before touching them).

  Do NOT alter any import, fixture, or helper not listed above.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/test_cli.py -v 2>&1 | tail -30</automated>
  </verify>

  <done>
  All tests in test_cli.py pass; three new tests assert --personal/--office/--machine
  each yield SystemExit(2); test_rename_with_personal_exits/office_exits/machine_exits
  are gone; ruff check and mypy --strict on test_cli.py exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full suite gate + static analysis</name>
  <files>tests/test_identity.py, tests/test_cli.py, tests/test_naming.py</files>

  <read_first>
  No file reads needed — this task runs verification commands only and fixes any
  remaining issues surfaced by them.
  </read_first>

  <action>
  Run the full test suite and static analysis tools. Fix any failures before
  declaring the plan complete.

  Required command sequence (run each in order; stop and fix before continuing if
  a command fails):

  1. ./venv/bin/python -m pytest tests/ -x -q
     All tests must pass; zero failures, zero errors. If a test fails, identify
     whether it is a stale reference to a removed flag (personal/office/machine) or a
     genuine regression, and fix it. Do NOT skip or xfail any test.

  2. ./venv/bin/python -m ruff check src/maccat/ tests/
     Must exit 0. Fix any lint errors surfaced by the Phase 21 changes.

  3. ./venv/bin/python -m mypy --strict src/maccat/identity.py src/maccat/cli.py
     Must exit 0. Fix any type errors introduced by the signature change.

  Additionally, confirm dead-code elimination via grep:
  4. grep -rn 'args\.personal\|args\.office\|args\.machine\|personal=\|office=\|"--personal"\|"--office"\|"--machine"' src/maccat/ tests/test_identity.py tests/test_cli.py
     Expected: zero matches (all removed-flag references eliminated from source and
     from the two updated test files). Note: test_naming.py uses "personal" only as
     a filename fixture string (not a flag reference) — confirm it does NOT appear
     in the grep output above since the pattern targets flag-style strings.
     If any match appears in src/maccat/, fix it. If any match appears in the updated
     test files, fix it.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/ -x -q && ./venv/bin/python -m ruff check src/maccat/ tests/ && ./venv/bin/python -m mypy --strict src/maccat/identity.py src/maccat/cli.py</automated>
  </verify>

  <done>
  pytest exits 0 with zero failures; ruff check exits 0; mypy --strict exits 0;
  grep for removed-flag patterns in src/maccat/ and the two updated test files
  returns no output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test argv injection → parser | monkeypatch.setattr(sys, "argv", [...]) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Information Disclosure | test_cli.py fixtures using real folder names | accept | Tests use disposable_catalog_repo fixture — never real personal/office dirs |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/ -q` — exits 0, all tests pass
2. `cd /Users/ken/dev/maccat && ./venv/bin/python -m ruff check src/maccat/ tests/` — exits 0
3. `cd /Users/ken/dev/maccat && ./venv/bin/python -m mypy --strict src/maccat/identity.py src/maccat/cli.py` — exits 0
4. `cd /Users/ken/dev/maccat && grep -rn '"--personal"\|"--office"\|"--machine"' tests/test_cli.py tests/test_identity.py` — returns no matches (all removed-flag references gone from test files)
5. `cd /Users/ken/dev/maccat && maccat --personal 2>&1; echo "exit: $?"` — prints "unrecognized arguments: --personal" and exits nonzero (argparse error)
6. `cd /Users/ken/dev/maccat && maccat --help 2>&1 | grep -E "personal|office|machine"` — returns no output (--help shows no removed flags)
</verification>

<success_criteria>
- pytest passes on all tests in tests/ with zero failures
- Three new tests in TestArgparse confirm --personal, --office, --machine each raise SystemExit(2)
- All TestRenameFlag tests for removed flags are gone; test_rename_with_computer_exits remains
- ruff check exits 0 on src/maccat/ and tests/
- mypy --strict exits 0 on identity.py and cli.py
- No "personal=", "office=", "machine=", "args.personal", "args.office", "args.machine" strings remain in src/maccat/
</success_criteria>

<output>
Create `.planning/phases/21-cli-cleanup/21-02-SUMMARY.md` when done.
</output>
