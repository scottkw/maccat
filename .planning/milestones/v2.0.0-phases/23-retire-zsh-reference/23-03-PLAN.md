---
phase: 23-retire-zsh-reference
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - README.md
autonomous: true
requirements:
  - ZSH-04
must_haves:
  truths:
    - "README no longer describes update-list.sh as a live reference script"
    - "README no longer documents --personal, --office, --machine flags as current CLI"
    - "README contains one history note explaining the zsh origin without operational framing"
    - "No remaining operational zsh or byte-parity references in README or docs"
    - "Full pytest + ruff + mypy gate still passes after the doc edit"
  artifacts:
    - path: "README.md"
      provides: "Updated README describing maccat as the standalone Python tool"
  key_links:
    - from: "README.md"
      to: "maccat CLI"
      via: "Usage section documents current --computer flag, not removed --personal/--office"
      pattern: "--computer"
---

<objective>
Scrub operational zsh references from README and update CLI docs to match current flags (ZSH-04).

Purpose: After the zsh script and parity suite are gone, the README still has a
"## Zsh Reference Script" section that presents update-list.sh as a live reference.
The Usage section also documents --personal, --office, and --machine flags that were
removed in Phase 21. Both sections need updating so README matches the actual tool.

Output: README.md with: (1) "Zsh Reference Script" section removed, replaced with a
single-sentence history note; (2) Usage section updated to show --computer flag and
remove the stale --personal/--office/--machine entries; (3) options table updated to
current CLI surface.

Note on ZSH-04 scope boundary: ZSH-04 covers removing operational zsh/parity
references. Updating the CLI docs (--personal → --computer) is in-scope because those
flags were removed in Phase 21 and the README currently shows them as live flags —
leaving them is an operational mistruth in the same category as the zsh reference
section. The code comments ("Behavioral spec: update-list.sh lines N-M") in
tests/collectors/ are NON-BREAKING docstring comments — scrubbing them is optional
polish at Claude's discretion (default: leave them; they describe origin, not current
behavior, and the effort/noise ratio is high).
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-retire-zsh-reference/23-CONTEXT.md
@.planning/phases/23-retire-zsh-reference/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — remove zsh reference section; fix CLI docs</name>
  <files>README.md</files>
  <read_first>
    README.md — read the full file before editing. Identify all of:
      1. The "## Zsh Reference Script" section (~line 321-323) — remove entirely,
         replace with a single sentence in a natural location (e.g. at the end of
         "## Overview" or before "## Troubleshooting"):
         "maccat was originally implemented as a Zsh script and ported to Python in v1.0.0."
      2. The Usage section showing --personal / --office / --machine flags and
         the interactive menu example that shows "personal/" and "work/" with
         numbered prompts — update to show --computer NAME. The three stale
         usage examples ("Save to the 'home' computer-folder", etc.) need
         replacement with a single example showing --computer.
      3. The options table listing --personal, --office, --machine — replace with
         current CLI surface: --computer NAME, --catalog-dir, --no-commit,
         --archive-days N, --rename.
      4. Any inline text that says "the script" referring to update-list.sh
         — replace with "maccat".
      5. The Commit Message Format example that says "Added personal catalog for"
         — update if the format changed in Phase 21 (check the actual git.py
         source if uncertain; otherwise leave as is).
      6. The "## Machine Identity" section that says "--machine "Label" flag" and
         the label-resolution numbered list that mentions "--machine" — update to
         "--computer NAME" per Phase 21 decisions.

    KEEP: The interactive machine-label menu (that's still the current behavior
    when no --computer flag is given). KEEP: all other sections intact.
    KEEP: The "## Example Output" section — output format is unchanged.
  </read_first>
  <action>
    Edit README.md with targeted surgical replacements:

    REMOVE the "## Zsh Reference Script" section entirely (the heading + the
    paragraph that says "update-list.sh is a Zsh implementation kept in this repo
    as a parity reference..."). In its place add a brief history note — one sentence
    in the "## Overview" intro paragraph or just before "## Troubleshooting":
      "maccat was originally implemented as a Zsh script and ported to Python in v1.0.0."

    REMOVE the three stale usage examples in the "### Basic run" subsection that
    show --personal / --office / --machine. Replace with a single example showing
    --computer:
      # Skip the interactive folder-selection prompt
      python3 maccat.pyz --computer MyMac

    UPDATE the options table to the current CLI surface. Remove --personal, --office,
    --machine rows. The current flags are:
      --computer NAME     Select (or create) the named computer-folder non-interactively
      --catalog-dir PATH  Path to the catalog git repository
      --no-commit         Skip automatic git commit and push
      --archive-days N    Archive-retention period in days for this run (default: 30)
      --rename            Rename a machine label across all catalog files

    UPDATE the "## Machine Identity" section: replace mentions of "--machine "Label""
    with "--computer NAME" throughout that section and its numbered resolution list.

    Do NOT rewrite prose that doesn't reference zsh, --personal, --office, or --machine.
    Do NOT change the catalog filename format, output sections, or git integration prose.
  </action>
  <verify>
    <automated>
      grep -c "update-list\.sh\|Zsh Reference\|zsh reference\|parity reference\|--personal\|--office\|--machine" /Users/ken/dev/maccat/README.md | grep -q "^0$" &&
      grep -c "v1\.0\.0\|originally" /Users/ken/dev/maccat/README.md | grep -q "^[1-9]" &&
      grep -c "\-\-computer" /Users/ken/dev/maccat/README.md | grep -q "^[1-9]" &&
      echo "README CLEAN"
    </automated>
  </verify>
  <done>
    README.md contains no operational references to update-list.sh, zsh, parity,
    --personal, --office, or --machine. Contains one history note about v1.0.0 port.
    Contains --computer in the usage and options table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final full suite gate</name>
  <files></files>
  <read_first>
    No file reads needed — validation only.
  </read_first>
  <action>
    Run the complete quality gate:
      1. PYTHONPATH=src ./venv/bin/ruff check src tests
      2. PYTHONPATH=src ./venv/bin/mypy --strict src/maccat
      3. PYTHONPATH=src ./venv/bin/pytest -x -q

    If ruff reports issues in any file, fix them. If pytest fails on any test,
    diagnose and fix. Markdown edits do not affect ruff/mypy/pytest — this task
    should pass cleanly.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/maccat &&
      PYTHONPATH=src ./venv/bin/ruff check src tests &&
      PYTHONPATH=src ./venv/bin/mypy --strict src/maccat &&
      PYTHONPATH=src ./venv/bin/pytest -x -q
    </automated>
  </verify>
  <done>
    All three gates exit 0. Phase 23 is fully complete:
    ZSH-01: update-list.sh gone.
    ZSH-02: parity suite + CI gate gone.
    ZSH-03: chrome_name + vsc_name helpers have direct tests.
    ZSH-04: README describes maccat as standalone Python tool with history note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → user | Documentation only; no code execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-04 | Information Disclosure | README.md | accept | Removing zsh reference reduces attack surface (no longer advertising an unexecuted script path) |
</threat_model>

<verification>
After both tasks:
- grep -c "update-list\.sh\|parity reference\|--personal\|--office\|--machine" README.md == 0
- grep -c "v1\.0\.0\|originally" README.md >= 1
- grep -c "\-\-computer" README.md >= 1
- PYTHONPATH=src ./venv/bin/pytest -x -q exits 0
- PYTHONPATH=src ./venv/bin/ruff check src tests exits 0
- PYTHONPATH=src ./venv/bin/mypy --strict src/maccat exits 0
</verification>

<success_criteria>
1. ZSH-04: README contains no operational zsh/parity references.
2. ZSH-04: README CLI docs match Phase 21 output (--computer, no --personal/--office/--machine).
3. README contains the one-sentence history note about v1.0.0 port.
4. All three quality gates (pytest, ruff, mypy) pass clean.
5. Phase 23 complete: all four ZSH-* requirements satisfied.
</success_criteria>

<output>
Create .planning/phases/23-retire-zsh-reference/23-03-SUMMARY.md when done.
</output>
