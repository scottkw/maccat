---
phase: 23-retire-zsh-reference
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - update-list.sh
  - tests/test_golden_parity.py
  - tests/test_update_list_integrity.py
  - tests/golden/
  - tests/conftest.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - ZSH-01
  - ZSH-02
must_haves:
  truths:
    - "update-list.sh does not exist in the working tree"
    - "tests/test_golden_parity.py does not exist"
    - "tests/test_update_list_integrity.py does not exist"
    - "tests/golden/ directory does not exist"
    - "CI workflow no longer contains the zsh -n update-list.sh step"
    - "conftest.py no longer defines --update-golden option or update_golden fixture"
    - "No remaining runtime import of tests.golden in any test file"
    - "pytest passes with zero parity-related skips; ruff and mypy --strict clean"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow without TEST-04 zsh step; PYTHONHASHSEED matrix preserved"
      contains: "pythonhashseed"
    - path: "tests/conftest.py"
      provides: "Cleaned conftest — no --update-golden, no update_golden fixture"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_golden_parity.py"
      via: "update_golden fixture — severed by deleting both file and fixture"
      pattern: "update_golden"
---

<objective>
Delete the entire zsh reference and its parity scaffold (ZSH-01 + ZSH-02).

Purpose: Now that ZSH-03 backfill is complete, the parity suite is redundant. This
plan removes update-list.sh, tests/test_golden_parity.py,
tests/test_update_list_integrity.py, the tests/golden/ directory, the CI
zsh -n gate, and the conftest.py --update-golden option/fixture that only
served the parity suite.

Output: None of the removed artifacts exist in the working tree. pytest passes
clean with no parity-skip messages. CI workflow still runs pytest/ruff/mypy and
the PYTHONHASHSEED matrix (legitimate determinism coverage).
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-retire-zsh-reference/23-CONTEXT.md
@.planning/phases/23-retire-zsh-reference/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no runtime golden imports remain, then delete the scaffold</name>
  <files>
    update-list.sh
    tests/test_golden_parity.py
    tests/test_update_list_integrity.py
    tests/golden/
  </files>
  <read_first>
    Run this grep to confirm safety before deleting:
      grep -rn "from tests\.golden\|import tests\.golden\|from tests\.golden" tests/ .github/
    Expected: only hits in tests/test_golden_parity.py itself (the file about to be deleted).
    If any OTHER file imports tests.golden, sever that import first.

    Coupling check from planning: test_writer.py, test_gitops.py, test_format.py,
    test_identity.py, test_config.py, test_naming.py, and all collectors/test_*.py
    reference update-list.sh only in DOCSTRING COMMENTS — these are non-breaking and
    require no changes in this plan.
  </read_first>
  <action>
    After confirming no live imports outside parity files, delete all four targets:

    1. git rm update-list.sh
    2. git rm tests/test_golden_parity.py
    3. git rm tests/test_update_list_integrity.py
    4. git rm -r tests/golden/

    Use git rm (not plain rm) so the deletions are staged. Do not commit yet —
    Tasks 2 and 3 must also be staged before the single commit at plan end.

    IMPORTANT: do NOT remove tests/conftest.py itself — it still serves all
    other tests. Only the --update-golden option and update_golden fixture are
    removed (Task 2).
  </action>
  <verify>
    <automated>
      test ! -f /Users/ken/dev/maccat/update-list.sh &&
      test ! -f /Users/ken/dev/maccat/tests/test_golden_parity.py &&
      test ! -f /Users/ken/dev/maccat/tests/test_update_list_integrity.py &&
      test ! -d /Users/ken/dev/maccat/tests/golden &&
      echo "ALL DELETED"
    </automated>
  </verify>
  <done>
    All four scaffold targets are absent from the working tree.
    grep -rn "from tests\.golden" tests/ returns zero matches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean conftest.py and remove CI zsh -n step</name>
  <files>
    tests/conftest.py
    .github/workflows/ci.yml
  </files>
  <read_first>
    tests/conftest.py — read the full file. Remove only:
      - The pytest_addoption() function that registers --update-golden
      - The update_golden() fixture function
      Keep: tmp_json, git_repo, catalog_repo fixtures — those serve many other tests.

    .github/workflows/ci.yml — read the full file. Remove only:
      - The "Check update-list.sh syntax (TEST-04)" step (name: line + run: line,
        approximately lines 39-40 per CONTEXT.md).
      Keep: the strategy/matrix block with pythonhashseed: [0, 42, "random"],
      the PYTHONHASHSEED env var, all other steps (checkout, setup-python, install,
      ruff, mypy, pytest), and the entire build job.
  </read_first>
  <action>
    Edit tests/conftest.py to remove the two golden-related items:

    From pytest_addoption(): remove the entire addoption() call that registers
    "--update-golden". If pytest_addoption() only registered that one option,
    remove the entire function. If it registered other options too, remove only
    the --update-golden block.

    Remove the entire update_golden() fixture function (lines defining
    `def update_golden(request: ...)` through its return statement).

    Edit .github/workflows/ci.yml to remove exactly the step:
      - name: Check update-list.sh syntax (TEST-04)
        run: zsh -n update-list.sh
    Remove both the `- name:` line and the `run:` line for that step only.
    Do not touch the PYTHONHASHSEED matrix, the build job, or any other step.
  </action>
  <verify>
    <automated>
      grep -c "update-list.sh" /Users/ken/dev/maccat/.github/workflows/ci.yml | grep -q "^0$" &&
      grep -c "update_golden\|update-golden" /Users/ken/dev/maccat/tests/conftest.py | grep -q "^0$" &&
      grep -c "pythonhashseed" /Users/ken/dev/maccat/.github/workflows/ci.yml | grep -q "^[1-9]" &&
      echo "CI + CONFTEST CLEAN"
    </automated>
  </verify>
  <done>
    conftest.py no longer defines --update-golden or update_golden.
    ci.yml no longer contains zsh -n update-list.sh.
    ci.yml still contains pythonhashseed matrix.
    PYTHONPATH=src ./venv/bin/pytest tests/conftest.py --collect-only exits 0 (conftest is valid).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full suite gate — pytest + ruff + mypy</name>
  <files></files>
  <read_first>
    No file reads needed — this is a validation-only task.
    Expected: zero test failures, zero skips from the deleted parity suite,
    zero ruff errors, zero mypy errors.
  </read_first>
  <action>
    Run the full quality gate in sequence:
      1. PYTHONPATH=src ./venv/bin/ruff check src tests
      2. PYTHONPATH=src ./venv/bin/mypy --strict src/maccat
      3. PYTHONPATH=src ./venv/bin/pytest -x -q

    If any command fails:
    - ruff: fix reported issues (likely unused imports in files that previously
      imported from tests.golden — but coupling intel confirmed no such imports
      exist outside the deleted files)
    - mypy: fix reported type errors in modified files
    - pytest: diagnose; if a test imports update_golden fixture (which no longer
      exists), that test file needs its fixture parameter removed — but per
      coupling intel, only test_golden_parity.py (now deleted) used it

    Do NOT proceed to commit if any gate fails.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/maccat &&
      PYTHONPATH=src ./venv/bin/ruff check src tests &&
      PYTHONPATH=src ./venv/bin/mypy --strict src/maccat &&
      PYTHONPATH=src ./venv/bin/pytest -x -q
    </automated>
  </verify>
  <done>
    All three gates exit 0.
    pytest output contains no "SKIPPED" lines mentioning parity, zsh, or golden.
    pytest output shows the same test count as before minus the parity tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → shell | Removing the zsh -n step eliminates execution of zsh against repo files in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-02 | Tampering | git rm staged deletions | accept | All deletions are git-tracked; git history retains update-list.sh |
| T-23-03 | Denial of Service | CI matrix removal | accept | PYTHONHASHSEED matrix kept intact; no CI coverage lost |
</threat_model>

<verification>
After all tasks complete:
- test ! -f update-list.sh
- test ! -f tests/test_golden_parity.py
- test ! -f tests/test_update_list_integrity.py
- test ! -d tests/golden
- grep -c "zsh -n" .github/workflows/ci.yml == 0
- grep -c "update_golden" tests/conftest.py == 0
- grep -c "pythonhashseed" .github/workflows/ci.yml >= 1
- PYTHONPATH=src ./venv/bin/pytest -x -q exits 0
- PYTHONPATH=src ./venv/bin/ruff check src tests exits 0
- PYTHONPATH=src ./venv/bin/mypy --strict src/maccat exits 0
</verification>

<success_criteria>
1. ZSH-01: update-list.sh absent from working tree.
2. ZSH-02: parity test files and golden scaffold absent; CI zsh -n step removed.
3. Full pytest suite passes with zero parity-related skips.
4. ruff and mypy --strict clean.
5. conftest.py no longer references --update-golden or update_golden.
6. CI PYTHONHASHSEED matrix and core test/lint/typecheck gates intact.
</success_criteria>

<output>
Create .planning/phases/23-retire-zsh-reference/23-02-SUMMARY.md when done.
</output>
