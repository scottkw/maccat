---
phase: 24-catalog-format-fix-parser-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/collectors/mas.py
  - tests/collectors/test_homebrew.py
autonomous: true
requirements:
  - MAS-01

must_haves:
  truths:
    - "A catalog generated after this change includes the App Store numeric ID in every mas entry"
    - "mas entries have exactly one set of parentheses around the version — no double parens"
    - "Multi-word app names (e.g. 'Final Cut Pro') are captured intact, not truncated at the second word"
    - "All existing mas collector tests pass with updated assertions"
  artifacts:
    - path: "src/maccat/collectors/mas.py"
      provides: "Rewritten _parse_mas_output that calls emit_item(name, version, id_)"
      contains: "emit_item"
    - path: "tests/collectors/test_homebrew.py"
      provides: "Updated TestMasCollector assertions and replacement two-field test"
      contains: "Safari (15.0) [1234567890]"
  key_links:
    - from: "src/maccat/collectors/mas.py::_parse_mas_output"
      to: "src/maccat/catalog/format.py::emit_item"
      via: "deferred import inside method body"
      pattern: "from maccat.catalog.format import emit_item"
---

<objective>
Rewrite `MasCollector._parse_mas_output` to extract all three columns from `mas list`
output (numeric id, multi-word name, paren-wrapped version) and route each entry through
`emit_item(name, version, id_)`. Update the three affected TestMasCollector test
assertions to reflect the new `AppName (version) [id]` format.

Purpose: MAS-01 — the App Store numeric ID must be preserved in the catalog so the
Phase 25 emitter can generate `mas install <id>` lines. The current `parts[1] parts[2]`
approach drops the ID and breaks multi-word names.

Output: Modified `mas.py` (rewritten `_parse_mas_output`) and updated test assertions
in `tests/collectors/test_homebrew.py`. No other files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/PROJECT.md
@/Users/ken/dev/maccat/.planning/ROADMAP.md
@/Users/ken/dev/maccat/.planning/phases/24-catalog-format-fix-parser-foundation/24-CONTEXT.md
@/Users/ken/dev/maccat/.planning/phases/24-catalog-format-fix-parser-foundation/24-RESEARCH.md
@/Users/ken/dev/maccat/.planning/phases/24-catalog-format-fix-parser-foundation/24-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/maccat/catalog/format.py (DO NOT MODIFY):
  emit_item(name: str, version: str, id_: str) -> str | None
  Degradation rules:
    name + version + id  ->  "name (version) [id]"
    name + version       ->  "name (version)"
    name + id            ->  "name [id]"
    name only            ->  "name"
    id only (no name)    ->  "id"           (id promoted)
    id + version (promo) ->  "id (version)" (id promoted)
    all empty            ->  None

From src/maccat/collectors/base.py:
  @dataclass Section(title: str, items: list[str], raw: bool = False)
  @dataclass CollectorResult(sections: list[Section])

Current _parse_mas_output behavior (lines 27-45 of mas.py) — to be REPLACED:
  - Awk equivalence: parts[1] parts[2] — drops numeric ID, breaks multi-word names
  - 2-field line emits trailing space (awk parity) — this behavior is REMOVED

New _parse_mas_output contract:
  - id_ = parts[0]  (numeric App Store ID)
  - If len(parts) >= 3 and parts[-1].startswith("(") and parts[-1].endswith(")"):
      version = parts[-1][1:-1]   # strip single parens — avoids ((version))
      name = " ".join(parts[1:-1])
  - Else (2-field line, no version):
      version = ""
      name = " ".join(parts[1:])
  - Route: item = emit_item(name, version, id_)
  - Import: deferred inside method body — from maccat.catalog.format import emit_item

Real mas list format (ASSUMED — from CONTEXT.md + docstring):
  "424389933  Final Cut Pro (10.7.1)"
  "409183694  Keynote (14.0)"
  "1295203466 Microsoft Remote Desktop (10.9.8)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _parse_mas_output in mas.py</name>
  <files>src/maccat/collectors/mas.py</files>

  <read_first>
    - src/maccat/collectors/mas.py — full file; understand current _parse_mas_output (lines 27-45) and collect() body (lines 47-79) which must remain unchanged
    - src/maccat/catalog/format.py — emit_item signature and all six degradation shapes (lines 16-43); this is the function being routed to
    - .planning/phases/24-catalog-format-fix-parser-foundation/24-PATTERNS.md — "src/maccat/collectors/mas.py" section for the exact new method body and deferred-import pattern
    - .planning/phases/24-catalog-format-fix-parser-foundation/24-RESEARCH.md — "New _parse_mas_output" code example and Pitfall 1 (double-paren) + Pitfall 2 (single-word edge case)
  </read_first>

  <behavior>
    - Test: "424389933  Final Cut Pro (10.7.1)" -> emit_item("Final Cut Pro", "10.7.1", "424389933") -> "Final Cut Pro (10.7.1) [424389933]"
    - Test: "409183694  Keynote (14.0)" -> emit_item("Keynote", "14.0", "409183694") -> "Keynote (14.0) [409183694]"
    - Test: "1295203466 Microsoft Remote Desktop (10.9.8)" -> emit_item("Microsoft Remote Desktop", "10.9.8", "1295203466") -> "Microsoft Remote Desktop (10.9.8) [1295203466]"
    - Test: "123  OnlyTwo" (2-field, no version) -> emit_item("OnlyTwo", "", "123") -> "OnlyTwo [123]"
    - Test: blank line or single-field line -> skipped (continue)
    - Test: version parens are stripped once: "(15.0)" -> version="15.0", NOT "(15.0)"
  </behavior>

  <action>
  Replace the body of _parse_mas_output (lines 27-45) with the three-column algorithm.
  All other content in mas.py — module docstring, imports, TITLE constant, class docstring,
  available(), collect() — must remain byte-identical to the current file.

  New _parse_mas_output:
  - Docstring: describe the real mas list format (id, multi-word name, paren-version), the
    three-column extraction, the de-paren guard, and the FMT-01 routing. Remove the awk
    equivalence docstring from the old method.
  - Add deferred import as the first statement of the method body:
    "from maccat.catalog.format import emit_item" — this is the established pattern in this
    codebase for avoiding circular imports in collectors (per PATTERNS.md shared patterns).
  - Algorithm per D-01 (MAS-01 locked decision in CONTEXT.md):
    parts = line.split()
    skip if len(parts) < 2
    id_ = parts[0]
    last = parts[-1]
    if len(parts) >= 3 and last.startswith("(") and last.endswith(")"):
        version = last[1:-1]               # strip single parens only — use [1:-1], NOT .strip("()")
        name = " ".join(parts[1:-1])       # middle fields joined
    else:
        version = ""                       # 2-field line: no version
        name = " ".join(parts[1:])
    item = emit_item(name, version, id_)
    if item is not None: lines.append(item)
  - Return lines.
  - Update the class docstring to remove the "Awk equivalence" paragraph and replace it with
    a note that each entry now routes through emit_item(name, version, id_) preserving the
    App Store numeric ID (MAS-01).
  - Do NOT change the module-level docstring reference to "update-list.sh:2249" — that
    line reference stays (the collect() behavior is structurally unchanged).
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/pytest tests/collectors/test_homebrew.py::TestMasCollector -x -q 2>&1 | tail -20</automated>
  </verify>

  <done>
    - _parse_mas_output body implements the three-column algorithm with deferred emit_item import
    - "from maccat.catalog.format import emit_item" appears inside _parse_mas_output
    - No "parts[1] parts[2]" pattern remains in the method
    - No trailing-space awk parity logic remains
    - collect() body is unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update TestMasCollector assertions in test_homebrew.py</name>
  <files>tests/collectors/test_homebrew.py</files>

  <read_first>
    - tests/collectors/test_homebrew.py — full TestMasCollector class (lines 115-187); identify the three tests that require assertion changes and the one test to replace
    - .planning/phases/24-catalog-format-fix-parser-foundation/24-PATTERNS.md — "tests/collectors/test_homebrew.py" section with exact new assertions and replacement test body
    - .planning/phases/24-catalog-format-fix-parser-foundation/24-RESEARCH.md — Pitfall 3 (test assertion format) and Pitfall 4 (obsolete trailing-space test)
  </read_first>

  <behavior>
    - Test: test_mas_collect_parses_output asserts section.items == ["Safari (15.0) [1234567890]", "Xcode (14.0) [9876543210]"]
    - Test: test_mas_two_field_line_degrades_to_name_id (renamed from test_mas_two_field_line_emits_trailing_space) asserts _parse_mas_output("123  OnlyTwo\n456  Safari (15.0)") == ["OnlyTwo [123]", "Safari (15.0) [456]"]
    - Test: test_mas_absent_returns_two_line_fallback — no change (fallback items unchanged)
    - Test: test_mas_nonzero_exit_returns_error_message — no change (error message unchanged)
    - Test: test_mas_section_title — no change
    - Test: test_mas_single_field_line_skipped — still valid (1-field line is still skipped); assertion result == [] unchanged
  </behavior>

  <action>
  Make exactly three targeted changes to tests/collectors/test_homebrew.py:

  1. test_mas_collect_parses_output (line 116): update the mock stdout to include numeric IDs
     (it already has "1234567890  Safari (15.0)\n9876543210  Xcode (14.0)"), update the
     assertion on line 129 from:
       ["Safari (15.0)", "Xcode (14.0)"]
     to:
       ["Safari (15.0) [1234567890]", "Xcode (14.0) [9876543210]"]
     Update the docstring to: "mas available — output parsed, id preserved in [id] bracket, raw=True."

  2. test_mas_two_field_line_emits_trailing_space (lines 167-180): REPLACE entirely with
     test_mas_two_field_line_degrades_to_name_id. New docstring: "2-field line ('123  OnlyTwo')
     — no version; emits 'OnlyTwo [123]' via emit_item." New body: call
     mas._parse_mas_output("123  OnlyTwo\n456  Safari (15.0)") and assert result ==
     ["OnlyTwo [123]", "Safari (15.0) [456]"]. Remove all references to trailing space,
     awk parity, and WR-02 from this test.

  3. No other tests in the file require changes. Do not touch the TestHomebrewCollector or
     TestSetappCollector classes. Do not change any imports.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/pytest tests/collectors/test_homebrew.py -x -q 2>&1 | tail -20</automated>
  </verify>

  <done>
    - test_mas_collect_parses_output asserts ["Safari (15.0) [1234567890]", "Xcode (14.0) [9876543210]"]
    - test_mas_two_field_line_emits_trailing_space no longer exists in the file
    - test_mas_two_field_line_degrades_to_name_id exists and asserts ["OnlyTwo [123]", "Safari (15.0) [456]"]
    - All other tests in test_homebrew.py pass unchanged
    - ./venv/bin/pytest tests/ -x -q exits 0 (full suite still green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mas CLI stdout -> _parse_mas_output | Output of an optional external CLI tool parsed into strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib only |
| T-24-01-01 | Tampering | mas CLI stdout | accept | mas is a trusted macOS tool (Homebrew-installed); output is name/version/id strings with no shell execution path in this plan. Shell safety (shlex.quote) is Phase 25 scope per planning notes. |
| T-24-01-02 | Information Disclosure | AppStore IDs in catalog | accept | The catalog is a local plaintext file on the user's own machine; IDs are public App Store identifiers, not secrets. |
</threat_model>

<verification>
Full suite must pass after both tasks:

```
cd /Users/ken/dev/maccat
./venv/bin/pytest tests/ -x -q
./venv/bin/ruff check src/maccat/collectors/mas.py tests/collectors/test_homebrew.py
./venv/bin/mypy src/maccat/collectors/mas.py
```

Grep assertions (no fenced code needed — verify via automated commands above):
- `grep "emit_item" src/maccat/collectors/mas.py` — must show the deferred import inside _parse_mas_output
- `grep "trailing_space" tests/collectors/test_homebrew.py` — must return nothing (old test removed)
- `grep "Safari (15.0) \[1234567890\]" tests/collectors/test_homebrew.py` — must match
</verification>

<success_criteria>
1. `./venv/bin/pytest tests/ -x -q` exits 0 — full suite green, no regressions
2. A mas list line `"424389933  Final Cut Pro (10.7.1)"` produces `"Final Cut Pro (10.7.1) [424389933]"` — correct name, single-wrapped version, bracket ID
3. A mas list line `"123  OnlyTwo"` (no version) produces `"OnlyTwo [123]"` — ID preserved, no trailing space
4. `ruff check` and `mypy src/maccat/collectors/mas.py` both exit clean
5. `grep "parts\[1\] parts\[2\]" src/maccat/collectors/mas.py` returns nothing (old awk parity code gone)
</success_criteria>

<output>
Create `.planning/phases/24-catalog-format-fix-parser-foundation/24-01-SUMMARY.md` when done.
</output>
