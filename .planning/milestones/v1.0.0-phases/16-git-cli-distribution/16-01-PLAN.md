---
phase: 16-git-cli-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/gitops.py
  - src/maccat/identity.py
  - tests/test_gitops.py
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "git_pull warns-and-continues when the repo has no remote; never raises"
    - "git_commit_and_push stages with 'git add -A -- {computer}/' (trailing slash + -- required)"
    - "git_commit_and_push skips commit when nothing is staged (no-changes guard)"
    - "git_commit_and_push creates exactly one commit per run with message 'Added [{computer}] catalog at {timestamp}'"
    - "git_commit_rename stages old-folder deletes, new-folder adds, and TSV map, then commits with 'Rename computer: ...' message"
    - "rename_machine calls gitops.git_commit_rename when auto_commit=True"
  artifacts:
    - path: src/maccat/gitops.py
      provides: "git_pull, git_commit_and_push, git_commit_rename functions"
      exports: [git_pull, git_commit_and_push, git_commit_rename]
    - path: tests/test_gitops.py
      provides: "git operations unit tests using disposable git_repo fixture"
      contains: "test_git_pull"
  key_links:
    - from: src/maccat/identity.py
      to: src/maccat/gitops.py
      via: "local import inside rename_machine body at line 625"
      pattern: "from maccat import gitops"
---

<objective>
Implement the git integration layer (gitops.py) that mirrors the zsh git_pull and git_commit_and_push functions exactly, wire the rename_machine git-commit stub in identity.py, and test all three git functions against a disposable git_repo fixture.

Purpose: All subprocess git operations for the catalog tool live in one module. Leading-dash folder safety (-- pathspec) and warn-and-continue behavior mirror zsh:2327 and zsh:2374 exactly.

Output: src/maccat/gitops.py, identity.py:625 stub filled, tests/test_gitops.py
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/dev/mac-software-list/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/PROJECT.md
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/STATE.md

<!-- Phase artifacts -->
@/Users/ken/dev/mac-software-list/.planning/phases/16-git-cli-distribution/16-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/16-git-cli-distribution/16-PATTERNS.md

<!-- Reused modules -->
@/Users/ken/dev/mac-software-list/src/maccat/identity.py
@/Users/ken/dev/mac-software-list/tests/conftest.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/maccat/collectors/homebrew.py (subprocess + shutil.which pattern):
  shutil.which("brew") is not None       # availability check pattern
  subprocess.run(["brew", ...], capture_output=True, text=True, shell=False)

From src/maccat/config.py (git subprocess pattern already established):
  subprocess.run(["git", "rev-parse", "--show-toplevel"], cwd=path, capture_output=True, text=True)

From src/maccat/identity.py:444 — rename_machine signature:
  def rename_machine(catalog_repo: Path, *, auto_commit: bool = False) -> None

From src/maccat/identity.py:625 — existing stub (the single line to replace):
  # Phase 16: if auto_commit: git_commit_rename(catalog_repo, old_name, new_name)

From tests/conftest.py:22-40 — git_repo fixture (reuse, do NOT redefine):
  @pytest.fixture()
  def git_repo(tmp_path: Path) -> Path:
      subprocess.run(["git", "init", str(tmp_path)], capture_output=True, check=True)
      subprocess.run(["git", "config", "user.email", "test@test.com"], cwd=tmp_path, capture_output=True)
      subprocess.run(["git", "config", "user.name", "Test"], cwd=tmp_path, capture_output=True)
      return tmp_path

Zsh parity references:
  update-list.sh:2327-2354  — git_pull behavior spec
  update-list.sh:2374-2431  — git_commit_and_push behavior spec
  update-list.sh:867-910    — rename git block (git_commit_rename spec)
  update-list.sh:2397       — "git add -A -- '${TARGET_LOCATION}/'" (trailing slash, -- mandatory)
  update-list.sh:2346       — bare "git pull" (no --rebase, no flags)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement gitops.py (git_pull, git_commit_and_push, git_commit_rename)</name>
  <files>src/maccat/gitops.py</files>
  <behavior>
    - git_pull on a non-git directory: prints WARNING containing "Not a git repository", returns without raising
    - git_pull on a git repo with no remote: prints WARNING containing "Failed to pull", returns without raising
    - git_commit_and_push with nothing staged: prints "No changes to commit", no commit created
    - git_commit_and_push after writing a file to computer/: creates exactly one commit, message matches "Added [personal] catalog at 20260614120000"
    - git_commit_and_push stages using ["git", "add", "-A", "--", "personal/"] (the -- and trailing slash are MANDATORY)
    - git_commit_and_push stages machine-labels.tsv without check=True (file may not exist)
    - git_commit_and_push push failure on a repo with no remote: prints WARNING containing "Failed to push", does not raise
    - git_commit_rename stages both old/ and new/ plus machine-labels.tsv; commit message is "Rename computer: 'old' -> 'new'"
    - All three functions: shutil.which("git") missing → print WARNING, return (never raise)
    - All subprocess calls: shell=False (the default), list-form args, cwd=catalog_repo
  </behavior>
  <action>
    Create src/maccat/gitops.py implementing three public functions: git_pull, git_commit_and_push, git_commit_rename.

    Module header: from __future__ import annotations; import shutil, subprocess; from pathlib import Path.

    git_pull(catalog_repo: Path) -> None — mirrors zsh:2327-2354:
    - Print blank line + 78-dash separator + "Git: Pulling latest changes from remote..." + separator (per RESEARCH.md exact zsh output block)
    - shutil.which("git") guard: WARNING + return
    - subprocess.run(["git", "rev-parse", "--git-dir"], cwd=catalog_repo, capture_output=True): non-zero → WARNING "Not a git repository..." + return
    - subprocess.run(["git", "pull"], cwd=catalog_repo, capture_output=True, text=True): success → "  Successfully pulled latest changes."; failure → WARNING block "Failed to pull from remote repository. / Continuing with local state..."
    - Bare "git pull" ONLY — no --rebase, no strategy flags (zsh:2346)

    git_commit_and_push(catalog_repo: Path, computer: str, timestamp: str) -> None — mirrors zsh:2374-2431:
    - Print blank line + 78-dash separator + "Git: Committing and pushing changes..." + separator
    - shutil.which("git") guard + rev-parse guard (same as git_pull)
    - Stage: subprocess.run(["git", "add", "-A", "--", f"{computer}/"], cwd=catalog_repo) — no capture_output here so errors surface; trailing slash in pathspec is mandatory (zsh:2397)
    - Stage TSV: subprocess.run(["git", "add", "--", "machine-labels.tsv"], cwd=catalog_repo, capture_output=True) — no check=True (file may not exist in fresh repo; zsh: 2>/dev/null || true)
    - No-changes guard: subprocess.run(["git", "diff", "--cached", "--quiet"], cwd=catalog_repo): returncode==0 → print "  No changes to commit." + return
    - Commit message: f"Added [{computer}] catalog at {timestamp}" (zsh:2410)
    - subprocess.run(["git", "commit", "-m", commit_msg], cwd=catalog_repo, capture_output=True, text=True): non-zero → WARNING "Failed to create commit." + return; success → print f"  Committed: {commit_msg}"
    - subprocess.run(["git", "push"], cwd=catalog_repo, capture_output=True, text=True): success → "  Successfully pushed to remote."; failure → WARNING block with manual push hint

    git_commit_rename(catalog_repo: Path, old_name: str, new_name: str) -> None — mirrors zsh:867-910:
    - shutil.which("git") guard + rev-parse guard
    - Stage old folder: subprocess.run(["git", "add", "-A", "--", f"{old_name}/"], cwd=catalog_repo, capture_output=True) — per zsh:886
    - Stage new folder: subprocess.run(["git", "add", "-A", "--", f"{new_name}/"], cwd=catalog_repo, capture_output=True) — per zsh:887
    - Stage TSV: subprocess.run(["git", "add", "--", "machine-labels.tsv"], cwd=catalog_repo, capture_output=True) — per zsh:888
    - No-changes guard: same diff --cached --quiet pattern
    - Commit message: f"Rename computer: '{old_name}' -> '{new_name}'" (zsh:893)
    - subprocess.run(["git", "commit", "-m", commit_msg], cwd=catalog_repo, capture_output=True, text=True)
    - Push with warn-and-continue

    Do NOT use shell=True anywhere. All args as list. No check=True on any git call (all warn-and-continue).

    Run: PYTHONPATH=src ./venv/bin/mypy --strict src/maccat/gitops.py
    Run: PYTHONPATH=src ./venv/bin/ruff check src/maccat/gitops.py
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/pytest tests/test_gitops.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/maccat/gitops.py exists with git_pull, git_commit_and_push, git_commit_rename exported
    - All three functions use shell=False list-form subprocess calls with cwd=catalog_repo
    - git add commands all include "--" before the pathspec
    - mypy --strict clean; ruff clean
    - All test_gitops.py tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire identity.py:625 rename_machine git-commit stub + tests/test_gitops.py</name>
  <files>src/maccat/identity.py, tests/test_gitops.py</files>
  <action>
    Edit src/maccat/identity.py line 625. Replace the one-line Phase 16 stub comment with:

      if auto_commit:
          from maccat import gitops
          gitops.git_commit_rename(catalog_repo, old_name, new_name)

    The import is local/deferred inside the function body (deferred-import pattern per __main__.py and get_registry()). The variables catalog_repo, old_name, new_name are already in scope at line 625 within rename_machine(). Do NOT add a top-level import of gitops to identity.py.

    Write tests/test_gitops.py covering:
    - TestGitPull class:
      - test_not_a_git_repo_warns: git_pull on a plain tmp_path (not git init'd); assert "WARNING" in capsys.readouterr().out
      - test_no_remote_warns_or_continues: git_pull on git_repo fixture (no remote configured); assert run completes without raising; output contains "WARNING" or "Successfully" (pull to a no-remote repo returns nonzero)
    - TestGitCommitAndPush class:
      - test_no_changes_returns_cleanly: git_commit_and_push on a git_repo with no staged changes; assert "No changes to commit" in output
      - test_commit_message_format: create personal/ subdirectory + write a file to it in git_repo; call git_commit_and_push(git_repo, "personal", "20260614120000"); assert log message contains "Added [personal] catalog at 20260614120000" (check via subprocess git log in test)
      - test_leading_dash_safety: computer name "personal" stages correctly via ["git", "add", "-A", "--", "personal/"]; this test verifies no subprocess.CalledProcessError is raised
      - test_push_failure_warns: git_repo has no remote; after staging+committing file, call git_commit_and_push; assert WARNING about push in output, no exception raised
    - TestGitCommitRename class:
      - test_rename_commit_message: set up old_name/ and new_name/ dirs in git_repo with files; stage them; call git_commit_rename; check git log message is "Rename computer: 'old' -> 'new'"
      - test_not_a_git_repo_warns: plain tmp_path; call git_commit_rename; assert WARNING in output
    - TestRenameIdentityIntegration class:
      - test_auto_commit_false_no_git_calls: monkeypatch gitops.git_commit_rename to track calls; rename_machine with auto_commit=False; assert gitops.git_commit_rename NOT called
      - test_auto_commit_true_calls_git: same setup but auto_commit=True; assert gitops.git_commit_rename IS called (monkeypatched to no-op)

    All tests use git_repo or tmp_path fixtures from conftest.py. Do NOT redefine git_repo. Import pattern from test_config.py: from __future__ import annotations; from pathlib import Path; import subprocess; import pytest.

    Run: PYTHONPATH=src ./venv/bin/pytest tests/test_gitops.py -x -q
    Run: PYTHONPATH=src ./venv/bin/mypy --strict src/maccat/identity.py
    Run: PYTHONPATH=src ./venv/bin/ruff check tests/test_gitops.py src/maccat/identity.py
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/pytest tests/test_gitops.py -x -q 2>&1 | tail -5 && PYTHONPATH=src ./venv/bin/mypy --strict src/maccat/identity.py 2>&1 | tail -3</automated>
  </verify>
  <done>
    - identity.py:625 stub replaced with the 3-line deferred-import gitops call
    - rename_machine calls gitops.git_commit_rename when auto_commit=True
    - tests/test_gitops.py exists with TestGitPull, TestGitCommitAndPush, TestGitCommitRename, TestRenameIdentityIntegration classes
    - All gitops tests pass; full suite still passes; mypy --strict and ruff clean on both files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → git subprocess | Computer folder name and timestamp from validated identity layer cross into git command arguments |
| git subprocess → catalog repo filesystem | git operates on files at catalog_repo path; repo is user-configured, never inferred from __file__ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | git add pathspec construction | mitigate | All git add calls use ["git", "add", ..., "--", f"{computer}/"] list form with -- separator; leading-dash folder names cannot be misparse as git options; shell=False throughout |
| T-16-02 | Spoofing | git commit message format | accept | Commit message interpolates computer (already validated by validate_computer_name in identity.py) and timestamp (14-digit datetime string from datetime.now()); no external input untrusted at this point |
| T-16-03 | Denial of Service | git push failure | accept | warn-and-continue; local commit preserved; user can recover manually; no silent data loss |
| T-16-04 | Information Disclosure | catalog content via git diff/log | accept | catalog is the intended artifact; git history is intentional; no secrets in catalog (CAT-05 enforced in Phase 15) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase; runtime remains zero-dependency stdlib-only (RESEARCH.md Package Legitimacy Audit confirms) |
</threat_model>

<verification>
PYTHONPATH=src ./venv/bin/pytest tests/test_gitops.py -v 2>&1 | tail -20
PYTHONPATH=src ./venv/bin/mypy --strict src/maccat/gitops.py src/maccat/identity.py
PYTHONPATH=src ./venv/bin/ruff check src/maccat/gitops.py src/maccat/identity.py tests/test_gitops.py
PYTHONPATH=src ./venv/bin/pytest -x -q 2>&1 | tail -5  # full suite regression check
</verification>

<success_criteria>
- git_pull, git_commit_and_push, git_commit_rename exist in src/maccat/gitops.py with shell=False list-form subprocess calls
- All git add commands include "--" before pathspec (e.g. ["git", "add", "-A", "--", f"{computer}/"])
- rename_machine in identity.py calls gitops.git_commit_rename when auto_commit=True, via deferred local import
- All test_gitops.py tests pass; full pytest suite still passes
- mypy --strict and ruff clean on all touched files
</success_criteria>

<output>
Create .planning/phases/16-git-cli-distribution/16-01-SUMMARY.md when done
</output>
