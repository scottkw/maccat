---
phase: 15-collectors
plan: 07
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/maccat/collectors/vscode.py
  - src/maccat/collectors/cursor.py
  - tests/collectors/test_vscode.py
  - tests/collectors/test_cursor.py
autonomous: true
requirements: [CAT-01, CAT-06]

must_haves:
  truths:
    - "VSCodeCollector returns 1 section 'VS Code Extensions'; CLI path preferred, extensions.json fallback"
    - "CursorCollector returns 1 section 'Cursor Extensions'; identical logic via shared _collect_editor_extensions helper"
    - "Extension ID/version split uses rsplit('@', 1) — mirrors zsh 'line%@*' / 'line##*@' (last @ only)"
    - "Display names resolved via resolve_vsc_ext_name Phase 13 helper using relativeLocation"
    - "If relativeLocation absent for an extension: display_name = ext_id (no resolve_vsc_ext_name call)"
    - "CLI absent or returns empty → extensions.json fallback with stdout NOTE message"
    - "Neither CLI nor extensions.json present → flush_section([]) producing (none found)"
  artifacts:
    - path: "src/maccat/collectors/vscode.py"
      provides: "VSCodeCollector + _collect_editor_extensions shared helper"
      exports: ["VSCodeCollector", "_collect_editor_extensions"]
    - path: "src/maccat/collectors/cursor.py"
      provides: "CursorCollector — delegates entirely to vscode._collect_editor_extensions"
      exports: ["CursorCollector"]
    - path: "tests/collectors/test_vscode.py"
      provides: "Unit tests: CLI path, extensions.json fallback, rsplit('@',1) split, NLS resolve"
      contains: "test_cli_path_resolves_display_name"
    - path: "tests/collectors/test_cursor.py"
      provides: "Unit tests: mirrors test_vscode.py with ~/.cursor/extensions paths"
      contains: "test_cursor_cli_path_collects_extensions"
  key_links:
    - from: "VSCodeCollector.collect()"
      to: "_collect_editor_extensions(~/.vscode/extensions, 'code', 'VS Code Extensions')"
      via: "shared module-level helper in vscode.py"
      pattern: "_collect_editor_extensions"
    - from: "CursorCollector.collect()"
      to: "_collect_editor_extensions(~/.cursor/extensions, 'cursor', 'Cursor Extensions')"
      via: "import from maccat.collectors.vscode"
      pattern: "from maccat\\.collectors\\.vscode import _collect_editor_extensions"
    - from: "CLI line split"
      to: "line.rsplit('@', 1)"
      via: "Pitfall D — last @ only, mirrors zsh ${line%@*} / ${line##*@}"
      pattern: "rsplit.*@.*1"
---

<objective>
Implement VSCodeCollector and CursorCollector — two identical-logic collectors differentiated only
by paths and CLI name. Both use a shared _collect_editor_extensions module-level helper in vscode.py,
and depend on the Phase 13 resolve_vsc_ext_name helper for NLS display name resolution.

Purpose: Establishes the two-path CLI/file-fallback pattern with NLS name resolution. CursorCollector
is a pure thin wrapper over the shared helper — one implementation, two section titles.
Output: vscode.py, cursor.py, test_vscode.py, test_cursor.py.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-collectors/15-RESEARCH.md
@.planning/phases/15-collectors/15-PATTERNS.md
@.planning/phases/15-collectors/15-01-SUMMARY.md
@src/maccat/catalog/format.py
@src/maccat/helpers/vsc_name.py

<interfaces>
From src/maccat/collectors/base.py (Plan 01):
  class Section:
    title: str
    items: list[str]
    raw: bool = False  # False — VS Code/Cursor go through flush_section

  class CollectorResult:
    sections: list[Section]
    warnings: list[str]

  class Collector:
    def collect(self) -> CollectorResult: ...
    def available(self) -> bool: ...
    def degraded_result(self, title: str) -> CollectorResult: ...

From src/maccat/catalog/format.py:
  emit_item(name: str, version: str, id_: str) -> str  # → "  display_name (version) [id]"

From src/maccat/helpers/vsc_name.py:
  resolve_vsc_ext_name(pkg_json: Path, ext_id: str) -> str
    Reads package.json displayName/%nls_key% and resolves NLS placeholders.
    Falls back to ext_id if package.json absent or parse fails. Never raises.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VSCodeCollector, _collect_editor_extensions helper, and CursorCollector</name>
  <files>src/maccat/collectors/vscode.py, src/maccat/collectors/cursor.py</files>

  <read_first>
    - .planning/phases/15-collectors/15-PATTERNS.md — "src/maccat/collectors/vscode.py" section (lines ~643–754): full _collect_editor_extensions helper with Path A (CLI) and Path B (extensions.json fallback)
    - .planning/phases/15-collectors/15-PATTERNS.md — "src/maccat/collectors/cursor.py" section (lines ~757–777): thin CursorCollector wrapper importing _collect_editor_extensions from vscode
    - .planning/phases/15-collectors/15-RESEARCH.md — "Collector 14: VS Code Extensions" (lines ~687–735): rsplit("@",1) rationale; relativeLocation path construction; NOTE message to stdout NOT catalog
    - .planning/phases/15-collectors/15-RESEARCH.md — "Collector 15: Cursor Extensions" (lines ~737–761): exact substitution list from VS Code
    - .planning/phases/15-collectors/15-RESEARCH.md — "Pitfall D: VS Code relativeLocation path construction": if relativeLocation absent, use ext_id directly as display_name
    - update-list.sh lines 1387–1583: zsh parity reference for both functions
    - src/maccat/helpers/vsc_name.py: confirm resolve_vsc_ext_name signature
  </read_first>

  <action>
  Create src/maccat/collectors/vscode.py:

  Module docstring: "VSCodeCollector and shared _collect_editor_extensions helper. Byte-parity with update-list.sh:1387 (collect_vscode_extensions). CursorCollector in cursor.py reuses this helper."

  Imports: from __future__ import annotations; stdlib: import json, shutil, subprocess, sys from pathlib import Path; maccat: from maccat.catalog.format import emit_item; from maccat.collectors.base import Collector, CollectorResult, Section; from maccat.helpers.vsc_name import resolve_vsc_ext_name.

  Module-level function _collect_editor_extensions(ext_dir: Path, cli_name: str, section_title: str) -> tuple[list[str], list[str]]:
    Returns (items, warnings).
    ext_json = ext_dir / "extensions.json".

    Path A — CLI (preferred):
      cli_lines = [].
      If shutil.which(cli_name):
        result = subprocess.run([cli_name, "--list-extensions", "--show-versions"], capture_output=True, text=True, shell=False).
        If returncode == 0 and result.stdout.strip(): cli_lines = result.stdout.strip().splitlines().

      If cli_lines:
        # Load extensions.json for relativeLocation → display name lookup
        ext_meta: dict[str, dict] = {} .
        If ext_json.is_file():
          Try json.loads(ext_json.read_text(encoding="utf-8")). For each entry: id_ = entry.get("identifier", {}).get("id", ""); if id_: ext_meta[id_.lower()] = entry. Except (json.JSONDecodeError, OSError): pass.
        For raw in cli_lines:
          parts = raw.rsplit("@", 1)  # mirrors zsh ${line%@*}/${line##*@} — split on LAST @
          if len(parts) != 2: continue
          id_, version = parts[0], parts[1]
          meta = ext_meta.get(id_.lower(), {})
          rel_loc = meta.get("relativeLocation", "")
          if rel_loc:
            pkg_json = ext_dir / rel_loc / "package.json"
            display_name = resolve_vsc_ext_name(pkg_json, id_)
          else:
            display_name = id_  # Pitfall D: relativeLocation absent, use id directly
          line = emit_item(display_name, version, id_); if line: items.append(line)
        Return items, [].

    Path B — extensions.json fallback:
      If not ext_json.is_file():
        print(f"  NOTE: {cli_name.capitalize()} not installed or no extensions found.", file=sys.stderr)
        Return [], [].
      If shutil.which(cli_name):  # CLI present but returned empty
        print(f"  WARNING: {cli_name} CLI returned empty list. Falling back to extensions.json.", file=sys.stderr)
      Try json.loads(ext_json.read_text(encoding="utf-8")). Except (json.JSONDecodeError, OSError): return [], [].
      For entry in entries:
        id_ = entry.get("identifier", {}).get("id", ""); if not id_: continue
        version = entry.get("version", "")
        rel_loc = entry.get("relativeLocation", "")
        pkg_json = ext_dir / rel_loc / "package.json"
        display_name = resolve_vsc_ext_name(pkg_json, id_)
        line = emit_item(display_name, version, id_); if line: items.append(line)
      Return items, [].

  class VSCodeCollector(Collector):
    TITLE = "VS Code Extensions"
    _EXT_DIR = Path.home() / ".vscode/extensions"

    def collect(self) -> CollectorResult:
      items, warnings = _collect_editor_extensions(self._EXT_DIR, "code", self.TITLE)
      Return CollectorResult(sections=[Section(title=self.TITLE, items=items)], warnings=warnings).

  __all__ = ["VSCodeCollector", "_collect_editor_extensions"]

  ---

  Create src/maccat/collectors/cursor.py:

  Module docstring: "CursorCollector — identical to VSCodeCollector with ~/.cursor/extensions and 'cursor' CLI. Byte-parity with update-list.sh:1494 (collect_cursor_extensions)."

  Imports: from __future__ import annotations; from pathlib import Path; from maccat.collectors.base import Collector, CollectorResult, Section; from maccat.collectors.vscode import _collect_editor_extensions.

  class CursorCollector(Collector):
    TITLE = "Cursor Extensions"
    _EXT_DIR = Path.home() / ".cursor/extensions"

    def collect(self) -> CollectorResult:
      items, warnings = _collect_editor_extensions(self._EXT_DIR, "cursor", self.TITLE)
      Return CollectorResult(sections=[Section(title=self.TITLE, items=items)], warnings=warnings).

  __all__ = ["CursorCollector"]
  </action>

  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && PYTHONPATH=src ./venv/bin/python -c "
from maccat.collectors.vscode import VSCodeCollector
from maccat.collectors.cursor import CursorCollector
# VSCode: verify 1 section
r = VSCodeCollector().collect()
assert len(r.sections) == 1
assert r.sections[0].title == 'VS Code Extensions'
assert r.sections[0].raw is False
# Cursor: verify 1 section
r2 = CursorCollector().collect()
assert len(r2.sections) == 1
assert r2.sections[0].title == 'Cursor Extensions'
assert r2.sections[0].raw is False
print('vscode.py + cursor.py OK')
"</automated>
  </verify>

  <done>
  - VSCodeCollector.collect() returns 1 section titled "VS Code Extensions" with raw=False
  - CursorCollector.collect() returns 1 section titled "Cursor Extensions" with raw=False
  - cursor.py imports _collect_editor_extensions from maccat.collectors.vscode (no duplicate logic)
  - CLI line split uses rsplit("@", 1) — not split("@") or split("@")[0]
  - relativeLocation absent → display_name = id_ (no resolve_vsc_ext_name call on missing path)
  - ruff check and mypy --strict pass on both modules
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for VSCodeCollector and CursorCollector</name>
  <files>tests/collectors/test_vscode.py, tests/collectors/test_cursor.py</files>

  <behavior>
  test_vscode.py:
  - test_cli_path_collects_extensions: mock code in shutil.which + subprocess.run returns "ms-python.python@2025.1.0\n"; no extensions.json (no rel_loc); items contains display_name=id and version
  - test_cli_path_resolves_display_name: CLI output "ms-python.python@2025.1.0"; extensions.json with relativeLocation; package.json in tmp_path with displayName "Python"; items contains "Python"
  - test_cli_empty_falls_back_to_extensions_json: code in PATH but subprocess returns empty stdout; extensions.json present; WARNING printed to stderr; items from extensions.json
  - test_rsplit_on_last_at: CLI line "pub.name-with@symbols@1.0.0"; id == "pub.name-with@symbols", version == "1.0.0" (last @ only)
  - test_malformed_cli_line_skipped: CLI line "no-at-separator"; line skipped (id == version case per zsh guard)
  - test_file_fallback_absent_both: code absent AND no extensions.json; NOTE to stderr; items == []
  - test_file_fallback_extensions_json_only: code absent; extensions.json present with entries; items populated
  - test_vscode_section_title: section.title == "VS Code Extensions"
  - test_vscode_raw_is_false: section.raw is False

  test_cursor.py:
  - test_cursor_cli_path_collects_extensions: mirrors test_cli_path_collects_extensions with cursor CLI + ~/.cursor/extensions
  - test_cursor_file_fallback: cursor absent; extensions.json at ~/.cursor/extensions/extensions.json; items populated
  - test_cursor_section_title: section.title == "Cursor Extensions"
  - test_cursor_raw_is_false: section.raw is False
  - test_cursor_uses_shared_helper: CursorCollector imports _collect_editor_extensions from vscode (import check)
  </behavior>

  <read_first>
    - .planning/phases/15-collectors/15-PATTERNS.md — "src/maccat/collectors/vscode.py" test note and test structure: NLS fixture creation pattern
    - tests/test_helpers.py — directory creation and JSON fixture pattern with tmp_path
    - .planning/phases/15-collectors/15-PATTERNS.md — test file header (lines ~927–955): class-per-behavior structure
  </read_first>

  <action>
  Create tests/collectors/test_vscode.py:

  Module docstring: "Tests for maccat.collectors.vscode. Behavioral spec: update-list.sh lines 1387–1476 (collect_vscode_extensions)."

  Imports: from __future__ import annotations; import json from pathlib import Path; from unittest.mock import MagicMock, patch; import pytest; import maccat.collectors.vscode as vscode_mod; from maccat.collectors.vscode import VSCodeCollector, _collect_editor_extensions.

  For CLI tests: use patch("shutil.which", return_value="/usr/bin/code") and patch("subprocess.run", return_value=mock_r) where mock_r.stdout has extension lines.

  For extensions.json tests: patch.object(VSCodeCollector, "_EXT_DIR", tmp_path / "extensions"). Create tmp_path/extensions/extensions.json with test data. For NLS resolution tests also create tmp_path/extensions/<relativeLocation>/package.json.

  NLS fixture for test_cli_path_resolves_display_name:
    extensions_dir = tmp_path / "extensions"
    (extensions_dir / "ms-python.python-2025.1.0" / "package.json").write_text(json.dumps({"displayName": "Python"}))
    extensions.json entry: {"identifier": {"id": "ms-python.python"}, "relativeLocation": "ms-python.python-2025.1.0", "version": "2025.1.0"}

  Class TestVSCodeCLIPath, TestVSCodeFileFallback, TestVSCodeDegradation following class-per-behavior.

  ---

  Create tests/collectors/test_cursor.py:

  Module docstring: "Tests for maccat.collectors.cursor. Behavioral spec: update-list.sh lines 1494–1583 (collect_cursor_extensions)."

  Imports: from __future__ import annotations; import json from pathlib import Path; from unittest.mock import MagicMock, patch; import pytest; from maccat.collectors.cursor import CursorCollector; from maccat.collectors.vscode import _collect_editor_extensions.

  For Cursor tests: patch.object(CursorCollector, "_EXT_DIR", tmp_path / "cursor_extensions"). Create fixtures in tmp_path with cursor extension layout.

  Class TestCursorCollector: behaviors listed above. Note test_cursor_uses_shared_helper verifies:
    from maccat.collectors import cursor as cursor_mod
    assert hasattr(cursor_mod, 'CursorCollector')
    import inspect; src = inspect.getsource(CursorCollector.collect)
    assert '_collect_editor_extensions' in src

  All test methods annotated -> None.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && PYTHONPATH=src ./venv/bin/pytest tests/collectors/test_vscode.py tests/collectors/test_cursor.py -x -q</automated>
  </verify>

  <done>
  - test_vscode.py: all 9 behaviors pass; test_rsplit_on_last_at verifies rsplit("@",1) correctness
  - test_cursor.py: all 5 behaviors pass; test_cursor_uses_shared_helper confirms no logic duplication
  - Full suite passes: PYTHONPATH=src ./venv/bin/pytest -x -q
  - ruff check tests/collectors/test_vscode.py tests/collectors/test_cursor.py clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code/cursor CLI stdout → subprocess | Subprocess output in list form; shell=False; output is treated as text |
| ~/.vscode/extensions/extensions.json → file parse | Local user data; no secrets; extension IDs and paths only |
| Extension package.json → resolve_vsc_ext_name | Display name resolution; Phase 13 helper handles absent file gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Elevation of Privilege | subprocess call to code/cursor CLI | mitigate | List form [cli_name, "--list-extensions", "--show-versions"]; shell=False; cli_name is a compile-time constant (never user-supplied) |
| T-15-07-02 | Information Disclosure | relativeLocation path traversal in ext_dir | accept | relativeLocation is a local relative path from extensions.json; bounded to ext_dir; resolve_vsc_ext_name handles path gracefully |
| T-15-07-SC | Tampering | No new package installs | accept | stdlib only |
</threat_model>

<verification>
- vscode.py and cursor.py import cleanly: PYTHONPATH=src python -c "from maccat.collectors.vscode import VSCodeCollector; from maccat.collectors.cursor import CursorCollector"
- VSCodeCollector().collect() returns 1 section; CursorCollector().collect() returns 1 section
- _collect_editor_extensions is importable from maccat.collectors.vscode (cursor.py uses it)
- rsplit("@", 1) used in Path A — grep confirms: grep -c 'rsplit' src/maccat/collectors/vscode.py >= 1
- ./venv/bin/pytest tests/collectors/test_vscode.py tests/collectors/test_cursor.py -x -q — all pass
- ./venv/bin/pytest -x -q — full suite passes
- ./venv/bin/ruff check src/maccat/collectors/vscode.py src/maccat/collectors/cursor.py tests/collectors/test_vscode.py tests/collectors/test_cursor.py
- ./venv/bin/mypy --strict src/maccat/collectors/vscode.py src/maccat/collectors/cursor.py
</verification>

<success_criteria>
- VSCodeCollector and CursorCollector: 1 section each; raw=False; CLI preferred with extensions.json fallback
- rsplit("@", 1) for last-@ split — not split("@") or [0]/@[1]
- relativeLocation absent → display_name = id_ (no resolve_vsc_ext_name call with bad path)
- CursorCollector is a thin wrapper — all logic in _collect_editor_extensions in vscode.py
- Tests CI-safe via subprocess mocking and tmp_path filesystem fixtures
- Existing tests + new tests all pass
</success_criteria>

<output>
Create .planning/phases/15-collectors/15-07-SUMMARY.md when done
</output>
