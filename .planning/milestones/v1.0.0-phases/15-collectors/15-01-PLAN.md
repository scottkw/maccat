---
phase: 15-collectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/collectors/__init__.py
  - src/maccat/collectors/base.py
  - tests/collectors/__init__.py
autonomous: true
requirements: [CAT-01]

must_haves:
  truths:
    - "Collector ABC is importable: from maccat.collectors.base import Collector, Section, CollectorResult"
    - "Section.raw=False is the default; Homebrew/mas/Setapp/Web will override to raw=True"
    - "REGISTRY in collectors/__init__.py lists all 12 collector classes in generate_catalog order"
    - "All 12 collector module names are imported in __init__.py; missing any causes ImportError at startup"
  artifacts:
    - path: "src/maccat/collectors/base.py"
      provides: "Collector ABC, Section dataclass (title, items, raw), CollectorResult dataclass"
      exports: ["Collector", "Section", "CollectorResult"]
    - path: "src/maccat/collectors/__init__.py"
      provides: "Ordered REGISTRY; imports all 12 collector classes"
      contains: "REGISTRY"
    - path: "tests/collectors/__init__.py"
      provides: "Empty package marker for pytest discovery"
  key_links:
    - from: "src/maccat/collectors/__init__.py"
      to: "src/maccat/collectors/base.py"
      via: "from maccat.collectors.base import Collector"
      pattern: "from maccat\\.collectors\\.base import Collector"
    - from: "REGISTRY order"
      to: "generate_catalog section order update-list.sh:2220"
      via: "HomebrewCollector, MasCollector, SetappCollector, WebAppsCollector, ClaudeCollector, CodexCollector, OpenCodeCollector, GeminiCollector, VSCodeCollector, CursorCollector, ChromeCollector, FirefoxCollector"
      pattern: "REGISTRY.*list\\[Collector\\]"
---

<objective>
Establish the Collector ABC, Section/CollectorResult dataclasses, and the REGISTRY skeleton that all downstream plans build on. Phase 16 consumes REGISTRY to write sections in the correct order; this plan defines that order.

Purpose: Creates the contracts every collector implements. Without base.py, none of the 11 collector modules can be written. Without __init__.py REGISTRY, Phase 16 has no section-order source of truth.
Output: src/maccat/collectors/base.py, src/maccat/collectors/__init__.py (with forward imports for all 12 collectors), tests/collectors/__init__.py.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-collectors/15-CONTEXT.md
@.planning/phases/15-collectors/15-RESEARCH.md
@.planning/phases/15-collectors/15-PATTERNS.md
@src/maccat/catalog/format.py
@.planning/phases/13-package-foundation-output-format/13-03-SUMMARY.md

<interfaces>
<!-- Phase 13 helpers available for import by all collectors: -->

From src/maccat/catalog/format.py:
  emit_item(name: str, version: str, id_: str) -> str
  flush_section(items: list[str]) -> list[str]   # LC_ALL=C sort -f -u; returns ["  (none found)"] when empty
  version_sort_tail(candidates: list[str]) -> str  # sort -V, returns last (highest) version string

From src/maccat/helpers/json_io.py:
  json_get(file: Path, key: str, default: str = "") -> str  # dotted-path; never raises

From src/maccat/helpers/chrome_name.py:
  chrome_ext_name(manifest_path: Path) -> str  # resolves __MSG_key__ placeholders

From src/maccat/helpers/vsc_name.py:
  resolve_vsc_ext_name(pkg_json: Path, ext_id: str) -> str  # resolves %nls_key% placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collector ABC and Section/CollectorResult dataclasses (base.py)</name>
  <files>src/maccat/collectors/base.py</files>

  <read_first>
    - .planning/phases/15-collectors/15-PATTERNS.md — "src/maccat/collectors/base.py" section (lines ~49–96): full module structure including Section.raw field rationale
    - .planning/phases/15-collectors/15-RESEARCH.md — "Architecture Patterns" section: Collector ABC code example; Section.raw=True for Homebrew/mas/Setapp/Web
    - src/maccat/catalog/format.py — module header convention (from __future__ import annotations, dataclass imports)
  </read_first>

  <action>
  Create src/maccat/collectors/__init__.py as an empty package marker (one comment line only — the full REGISTRY will be added in Task 2 once the stub exists).

  Create src/maccat/collectors/base.py with:

  Module docstring: "Collector ABC, Section, and CollectorResult — base types for all maccat source collectors."
  Header: from __future__ import annotations, then stdlib imports only (dataclasses.dataclass, dataclasses.field).

  Three exports:

  Section dataclass with fields:
    title: str
    items: list[str]   # raw emit_item() output lines — NOT yet sorted
    raw: bool = False  # if True, orchestrator writes items directly without flush_section
                       # raw=True: Homebrew, App Store, Setapp, Web-installed

  CollectorResult dataclass with fields:
    sections: list[Section]
    warnings: list[str] = field(default_factory=list)

  Collector class (no @abstractmethod — raise NotImplementedError is sufficient for mypy --strict with the existing pyproject.toml config) with:
    def collect(self) -> CollectorResult: raise NotImplementedError
    def available(self) -> bool: return True
    def degraded_result(self, title: str) -> CollectorResult:
      """Standard empty-section result. items=[] causes flush_section → '  (none found)'."""
      Return CollectorResult(sections=[Section(title=title, items=[])])

  No imports from maccat.catalog or maccat.helpers in base.py — base must have zero maccat-internal dependencies to avoid circular imports.

  The __all__ list: ["Collector", "CollectorResult", "Section"]
  </action>

  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && PYTHONPATH=src ./venv/bin/python -c "from maccat.collectors.base import Collector, Section, CollectorResult; s = Section(title='T', items=[]); assert s.raw is False; r = CollectorResult(sections=[s]); c = Collector(); assert c.available() is True; d = c.degraded_result('T'); assert d.sections[0].items == []; print('base.py OK')"</automated>
  </verify>

  <done>
  - src/maccat/collectors/base.py imports cleanly; Section/CollectorResult/Collector all constructible
  - Section(title="X", items=[]).raw is False
  - Collector().degraded_result("T").sections[0].items == []
  - mypy --strict src/maccat/collectors/base.py passes (0 errors)
  - ruff check src/maccat/collectors/base.py passes
  </done>
</task>

<task type="auto">
  <name>Task 2: REGISTRY in collectors/__init__.py + tests/collectors/__init__.py</name>
  <files>src/maccat/collectors/__init__.py, tests/collectors/__init__.py</files>

  <read_first>
    - .planning/phases/15-collectors/15-PATTERNS.md — "src/maccat/collectors/__init__.py" section (lines ~99–141): exact REGISTRY pattern and import order
    - .planning/phases/15-collectors/15-RESEARCH.md — "Section Order and REGISTRY" section: canonical 18-section order; multi-section collectors noted (Claude→3, OpenCode→3, Gemini→2)
    - .planning/phases/15-collectors/15-CONTEXT.md — "Canonical section order" list: exact order matching generate_catalog lines 2220–2313
  </read_first>

  <action>
  Create tests/collectors/__init__.py as a completely empty file (no docstring, no imports). This is the pytest subpackage marker — mirrors tests/__init__.py.

  Overwrite src/maccat/collectors/__init__.py with the full REGISTRY module:

  Module docstring: "Collector registry — ordered list matching generate_catalog section order in update-list.sh:2220."
  Header: from __future__ import annotations

  Import all 12 collector classes (alphabetical import block after the annotation):
    from maccat.collectors.base import Collector
    from maccat.collectors.chrome import ChromeCollector
    from maccat.collectors.claude import ClaudeCollector
    from maccat.collectors.codex import CodexCollector
    from maccat.collectors.cursor import CursorCollector
    from maccat.collectors.firefox import FirefoxCollector
    from maccat.collectors.gemini import GeminiCollector
    from maccat.collectors.homebrew import HomebrewCollector
    from maccat.collectors.mas import MasCollector
    from maccat.collectors.opencode import OpenCodeCollector
    from maccat.collectors.setapp import SetappCollector
    from maccat.collectors.vscode import VSCodeCollector
    from maccat.collectors.webapps import WebAppsCollector

  REGISTRY definition:
    REGISTRY: list[Collector] = [
        HomebrewCollector(),
        MasCollector(),
        SetappCollector(),
        WebAppsCollector(),
        ClaudeCollector(),    # yields 3 sections: Plugins, MCP Servers, Skills & Agents
        CodexCollector(),     # yields 1 section: MCP Servers
        OpenCodeCollector(),  # yields 3 sections: Plugins, MCP Servers, Agents
        GeminiCollector(),    # yields 2 sections: Extensions, MCP Servers
        VSCodeCollector(),
        CursorCollector(),
        ChromeCollector(),
        FirefoxCollector(),
    ]

  __all__ = ["REGISTRY", "Collector"]

  NOTE: The 12 collector modules do not exist yet — this file will raise ImportError until later plans create them. That is expected and correct; it exists now so Phase 16 can import REGISTRY once all collectors are built.

  The REGISTRY order MUST match update-list.sh generate_catalog lines 2220–2313 exactly. The expected section titles produced in REGISTRY order are:
    1. Homebrew Packages (raw)
    2. App Store Applications (raw)
    3. Setapp Applications (raw)
    4. Web-installed Applications (raw)
    5. Claude Code Plugins
    6. Claude Code MCP Servers
    7. Claude Code Skills & Agents
    8. Codex MCP Servers
    9. OpenCode Plugins
    10. OpenCode MCP Servers
    11. OpenCode Agents
    12. Gemini CLI Extensions
    13. Gemini CLI MCP Servers
    14. VS Code Extensions
    15. Cursor Extensions
    16. Google Chrome Extensions
    17. Firefox Extensions
  (Section "Installed Mac Software List" is the header written by the Phase-16 orchestrator, not a collector.)
  </action>

  <verify>
    <automated>cd /Users/ken/dev/mac-software-list && ./venv/bin/python -c "import ast, sys; tree = ast.parse(open('src/maccat/collectors/__init__.py').read()); names = [n.id for n in ast.walk(tree) if isinstance(n, ast.Name)]; print('REGISTRY in AST:', 'REGISTRY' in str(ast.dump(tree))); print('OK')"</automated>
  </verify>

  <done>
  - src/maccat/collectors/__init__.py is valid Python (parses with ast.parse)
  - REGISTRY variable defined with 12 collector instances in correct order
  - tests/collectors/__init__.py exists as empty file
  - ruff check src/maccat/collectors/__init__.py passes (ignoring ImportError — collectors not built yet)
  - The REGISTRY comment on ClaudeCollector says "yields 3 sections", CodexCollector "yields 1 section", OpenCodeCollector "yields 3 sections", GeminiCollector "yields 2 sections"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Base module → collector modules | base.py has no maccat-internal imports; circular import cannot occur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering | No new package installs this plan | accept | No npm/pip/cargo installs; all stdlib |
</threat_model>

<verification>
Full collector subpackage structure established:
- `from maccat.collectors.base import Collector, Section, CollectorResult` works
- `Section(title="T", items=[]).raw is False`
- `Collector().degraded_result("T").sections[0].items == []`
- REGISTRY defined with 12 entries in canonical section order
- `tests/collectors/__init__.py` exists (pytest discovery)
- `./venv/bin/mypy --strict src/maccat/collectors/base.py` — 0 errors
- `./venv/bin/ruff check src/maccat/collectors/base.py` — clean
</verification>

<success_criteria>
- base.py, collectors/__init__.py, and tests/collectors/__init__.py created
- Section.raw flag exists with False as default
- REGISTRY order matches generate_catalog exactly (17 sections from 12 collectors)
- mypy --strict and ruff pass on base.py
- Existing 194 tests still pass: PYTHONPATH=src ./venv/bin/pytest -x -q
</success_criteria>

<output>
Create .planning/phases/15-collectors/15-01-SUMMARY.md when done
</output>
