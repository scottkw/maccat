---
phase: 17-parity-safety-tests
plan: "02"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/test_golden_parity.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "pytest tests/test_golden_parity.py lists 17 parametrized cases, one per golden file"
    - "a section parity failure names the failing section in the test ID (e.g. test_section_parity[claude-code-plugins])"
    - "running with PYTHONHASHSEED=0 and PYTHONHASHSEED=42 both produce green"
    - "the web-installed-applications test is annotated as format-only (zsh path hardcoded)"
    - "a plain pytest run never writes to any .golden.txt file"
    - "if a golden file is absent the test XFAILs loudly, not silently passes"
  artifacts:
    - path: "tests/test_golden_parity.py"
      provides: "Parametrized parity suite over all 17 golden files (TEST-01/TEST-02)"
      exports: ["test_section_parity"]
      min_lines: 80
  key_links:
    - from: "tests/test_golden_parity.py"
      to: "tests/golden/*.golden.txt"
      via: "sorted(GOLDEN_DIR.glob('*.golden.txt')) → parametrize ids"
      pattern: "glob.*golden.txt"
    - from: "tests/test_golden_parity.py"
      to: "tests/golden/normalize.py"
      via: "from tests.golden.normalize import normalize_catalog_body"
      pattern: "normalize_catalog_body"
    - from: "tests/test_golden_parity.py"
      to: "src/maccat/collectors/*"
      via: "patch.object / patch() on module-level path constants"
      pattern: "patch.object|unittest.mock.patch"
---

<objective>
Write the parametrized golden-output parity test suite (TEST-01/TEST-02). Each of the 17
cases patches the Python collector's path constants to the same synthetic fake_home tree
used during golden generation, runs the collector, normalizes the output, and asserts byte
equality against the committed .golden.txt file.

Purpose: Prove that the Python implementation is byte-identical to the zsh reference for
every catalog section after volatile-field normalization. A failure names the exact failing
section immediately in the pytest output.

Output: tests/test_golden_parity.py with 17 parametrized cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-parity-safety-tests/17-CONTEXT.md
@.planning/phases/17-parity-safety-tests/17-RESEARCH.md
@.planning/phases/17-parity-safety-tests/17-PATTERNS.md
@.planning/phases/17-parity-safety-tests/17-01-SUMMARY.md

<interfaces>
<!-- Key types and patterns the executor needs. All path constants confirmed in 17-01. -->

From tests/golden/normalize.py (created in 17-01):
```python
def normalize_catalog_body(text: str) -> str: ...   # two-regex volatile-field stripper
def extract_section_body(catalog_text: str, section_title: str) -> str | None: ...
SEPARATOR_LINE: str  # "-" * 36
```

From tests/conftest.py (update_golden fixture added in 17-01):
```python
@pytest.fixture() def update_golden(request: pytest.FixtureRequest) -> bool:
    return request.config.getoption("--update-golden")
```

Collector path constants (all confirmed from source in planning context):
```python
# Module-level — patch with patch("maccat.collectors.<mod>._CONST", new_val):
maccat.collectors.claude._PLUGINS_PATH, _CLAUDE_JSON, _SKILLS_DIR, _AGENTS_DIR
maccat.collectors.codex._TOML_PATH
maccat.collectors.opencode._CONFIG_PATH, _AGENTS_DIR
maccat.collectors.gemini._EXT_DIR, _MCP_PATH
maccat.collectors.chrome._BASE
maccat.collectors.firefox._FF_DIR

# Class attributes — patch with patch.object(CollectorClass, "_ATTR", new_val):
VSCodeCollector._EXT_DIR
CursorCollector._EXT_DIR
SetappCollector.BASE       (also module-level BASE at maccat.collectors.setapp)
WebAppsCollector.BASE      (also module-level BASE at maccat.collectors.webapps)
```

Golden file slug mapping (title → slug → filename):
```
"Homebrew Packages"           → homebrew-packages
"App Store Applications"      → app-store-applications
"Setapp Applications"         → setapp-applications
"Web-installed Applications"  → web-installed-applications
"Claude Code Plugins"         → claude-code-plugins
"Claude Code MCP Servers"     → claude-code-mcp-servers
"Claude Code Skills & Agents" → claude-code-skills-agents
"Codex MCP Servers"           → codex-mcp-servers
"OpenCode Plugins"            → opencode-plugins
"OpenCode MCP Servers"        → opencode-mcp-servers
"OpenCode Agents"             → opencode-agents
"Gemini CLI Extensions"       → gemini-extensions
"Gemini CLI MCP Servers"      → gemini-mcp-servers
"VS Code Extensions"          → vscode-extensions
"Cursor Extensions"           → cursor-extensions
"Google Chrome Extensions"    → google-chrome-extensions
"Firefox Extensions"          → firefox-extensions
```

Section.raw flag (from base.py):
```python
# raw=True sections: Homebrew, App Store, Setapp, Web-installed
# raw=False sections: all others (flush_section called by orchestrator)
# For the parity test, each collector's collect() returns Section objects;
# the section body comes from the items list joined into text.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write parametrized parity suite test_golden_parity.py</name>
  <files>tests/test_golden_parity.py</files>
  <behavior>
    - test_section_parity[homebrew-packages]: Python HomebrewCollector().collect() with no
      patches → items list joined → normalize → matches homebrew-packages.golden.txt
    - test_section_parity[claude-code-plugins]: ClaudeCollector with _PLUGINS_PATH patched
      to fake_home → section items joined → normalize → matches claude-code-plugins.golden.txt
    - test_section_parity[web-installed-applications]: WebAppsCollector with BASE patched to
      fake_applications → items joined → normalize → matches golden; test has webapps caveat
      comment that zsh comparison is [ASSUMED] (hardcoded /Applications)
    - test_section_parity[google-chrome-extensions]: ChromeCollector with _BASE patched to
      fake_home/Library/Application Support/Google/Chrome → items → normalize → matches golden
    - When update_golden fixture is True: write normalized output to golden file and pytest.skip
    - When update_golden fixture is False (normal run): read golden file and assert ==; on
      mismatch the assertion message names the section stem
    - If golden file does not exist: pytest.fail with message naming the missing file and
      instructing to run with --update-golden
  </behavior>
  <action>
    Create tests/test_golden_parity.py. Module docstring: "Golden-output parity suite
    (TEST-01, TEST-02). Parametrized over all 17 *.golden.txt files in tests/golden/."
    `from __future__ import annotations`, imports: pathlib.Path, unittest.mock.patch,
    unittest.mock.patch as mp, pytest. Import each collector class. Import normalize from
    tests.golden.normalize.

    Module-level constants:
      GOLDEN_DIR = Path(__file__).parent / "golden"
      FAKE_HOME = GOLDEN_DIR / "fixtures" / "fake_home"
      FAKE_APPS = GOLDEN_DIR / "fixtures" / "fake_applications"
      GOLDEN_FILES = sorted(GOLDEN_DIR.glob("*.golden.txt"))

    Parametrize test_section_parity over GOLDEN_FILES with ids=[f.stem for f in GOLDEN_FILES].
    Signature: test_section_parity(golden_file: Path, update_golden: bool, tmp_path: Path) -> None.

    The test body must:
    1. Derive section_stem = golden_file.stem
    2. Call a helper build_section_body(section_stem, fake_home, fake_apps) -> str that
       dispatches to the correct collector with patched constants and returns the joined
       section body text (items joined with "\n", with empty-section "(none found)" matching
       the collector's degraded_result output).
    3. Normalize the result with normalize_catalog_body().
    4. If update_golden: write to golden_file and pytest.skip(f"Golden updated: {golden_file.name}").
    5. Else if not golden_file.exists(): pytest.fail(f"Missing golden {golden_file.name} — run pytest --update-golden").
    6. Else: expected = golden_file.read_text("utf-8"); assert normalized == expected, f"Section '{section_stem}' parity failed.\nRun with --update-golden to refresh."

    The build_section_body helper dispatches by section_stem using a match/dict lookup.
    For each section, it must patch the correct constants to fake_home subtree paths and
    call the collector. The items from collect() are joined per the catalog format. For
    raw=True sections (homebrew, app-store, setapp, web-installed): the collector.collect()
    returns a Section where items is a list of raw lines; join with "\n".
    For raw=False sections: same — join items with "\n".

    Key webapps annotation: in build_section_body for "web-installed-applications", add comment:
    "# web-installed-applications: zsh hardcodes /Applications — Python synthetic only.
    # Zsh parity is [ASSUMED] per 17-RESEARCH.md §Assumptions A1."
    Patch WebAppsCollector.BASE (and module-level maccat.collectors.webapps.BASE) to FAKE_APPS.

    For ClaudeCollector: three sections (plugins, mcp-servers, skills-agents) from one
    collector.collect() call that returns CollectorResult. Dispatch each stem to the right
    section from the result.

    Per-collector patching (use contextlib.ExitStack for multi-constant patches):
    - claude: patch _PLUGINS_PATH, _CLAUDE_JSON, _SKILLS_DIR, _AGENTS_DIR all to fake_home subtrees
    - codex: patch _TOML_PATH to fake_home/.codex/config.toml
    - opencode: patch _CONFIG_PATH and _AGENTS_DIR to fake_home/.config/opencode/ subtrees
    - gemini: patch _EXT_DIR and _MCP_PATH to fake_home/.gemini/ subtrees
    - vscode: patch.object VSCodeCollector._EXT_DIR to fake_home/.vscode/extensions
    - cursor: patch.object CursorCollector._EXT_DIR to fake_home/.cursor/extensions
    - chrome: patch _BASE to fake_home "Library/Application Support/Google/Chrome"
    - firefox: patch _FF_DIR to fake_home "Library/Application Support/Firefox"
    - homebrew/mas: no patches (relies on shutil.which returning None → fallback text)
    - setapp: patch.object SetappCollector.BASE and maccat.collectors.setapp.BASE to a
      nonexistent path (so available() returns False and the static fallback is written)

    Security: no untrusted input — all patches use fixed Path objects from FAKE_HOME/FAKE_APPS.
    generate.py is never imported in this file (parity test reads pre-committed goldens).
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list && PYTHONHASHSEED=0 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -x -q && PYTHONHASHSEED=42 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -x -q
    </automated>
  </verify>
  <done>
    tests/test_golden_parity.py exists with 17 parametrized cases.
    PYTHONHASHSEED=0 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -x -q: all 17 pass.
    PYTHONHASHSEED=42 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -x -q: all 17 pass.
    Failure IDs name the section (e.g. test_section_parity[claude-code-plugins]).
    No .golden.txt file is written during a plain pytest run.
    Full suite PYTHONPATH=src ./venv/bin/pytest -x -q still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → collector code | Patched path constants point at committed fake_home fixtures |
| test code → golden files | Read-only on normal run; write-only under --update-golden guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | golden file write on normal run | mitigate | Every golden write gated behind `if update_golden:` — normal run never reaches write path |
| T-17-02-02 | Information Disclosure | real HOME leaking into collector under test | mitigate | All path constants patched to FAKE_HOME before collector.collect() call; real HOME never read |
| T-17-02-03 | Tampering | test assert silently passes when golden absent | mitigate | pytest.fail() on missing golden (not a vacuous skip) — missing golden = hard failure |
| T-17-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; stdlib + pytest only |
</threat_model>

<verification>
PYTHONHASHSEED=0 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
PYTHONHASHSEED=42 PYTHONPATH=src ./venv/bin/pytest tests/test_golden_parity.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
# Verify no golden file has been modified:
git diff --name-only -- tests/golden/  # should be empty after a plain pytest run
PYTHONPATH=src ./venv/bin/pytest -x -q  # full suite still green
</verification>

<success_criteria>
1. tests/test_golden_parity.py exists with exactly 17 parametrized test cases
2. All 17 cases pass under PYTHONHASHSEED=0
3. All 17 cases pass under PYTHONHASHSEED=42
4. Failure test IDs name the section stem (ids=[f.stem for f in GOLDEN_FILES])
5. A plain pytest run writes 0 bytes to any .golden.txt file
6. web-installed-applications case has explicit zsh-hardcoded-path annotation/comment
7. Missing golden → pytest.fail, not silent pass or skip
8. Full suite (400+ existing + 17 new) green
</success_criteria>

<output>
Create `.planning/phases/17-parity-safety-tests/17-02-SUMMARY.md` when done
</output>
