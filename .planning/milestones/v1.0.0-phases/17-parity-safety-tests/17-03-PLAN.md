---
phase: 17-parity-safety-tests
plan: "03"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/test_safety_invariants.py
  - tests/test_update_list_integrity.py
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "pytest -m safety_invariant runs exactly 3 tests and all pass"
    - "test_prune_skips_unparseable_filename: file with name 'old-notes.txt' survives prune with archive_days=0"
    - "test_retain_keeps_all_tied_newest: two files for different hosts both survive retain"
    - "test_rename_hard_refuses_clobber: SystemExit raised when destination folder exists"
    - "test_update_list_passes_zsh_syntax_check: zsh -n update-list.sh exits 0"
    - "CI green on macos-latest for PYTHONHASHSEED 0, 42, and random (criterion 4: =random is set in CI)"
    - "CI workflow runs zsh -n update-list.sh as a named step"
  artifacts:
    - path: "tests/test_safety_invariants.py"
      provides: "Three explicit destructive-op invariant tests with pytest.mark.safety_invariant (TEST-03)"
      exports: ["test_prune_skips_unparseable_filename", "test_retain_keeps_all_tied_newest", "test_rename_hard_refuses_clobber"]
    - path: "tests/test_update_list_integrity.py"
      provides: "zsh -n syntax check test for update-list.sh (TEST-04)"
      exports: ["test_update_list_passes_zsh_syntax_check"]
    - path: "pyproject.toml"
      provides: "safety_invariant marker registered so -m filtering works without warning"
      contains: "safety_invariant"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI on macos-latest with PYTHONHASHSEED matrix [0, 42, "random"] and zsh -n step"
      contains: "pythonhashseed"
  key_links:
    - from: "tests/test_safety_invariants.py"
      to: "src/maccat/retention.py"
      via: "from maccat.retention import prune_old_archives, retain_newest_per_host"
      pattern: "prune_old_archives|retain_newest_per_host"
    - from: "tests/test_safety_invariants.py"
      to: "src/maccat/identity.py"
      via: "from maccat.identity import rename_machine"
      pattern: "rename_machine"
    - from: ".github/workflows/ci.yml"
      to: "update-list.sh"
      via: "zsh -n update-list.sh step"
      pattern: "zsh -n update-list.sh"
---

<objective>
Consolidate the three destructive-op safety invariants into an explicit tagged suite, add the
TEST-04 zsh integrity test, register the safety_invariant pytest marker, and create the
GitHub Actions CI workflow with PYTHONHASHSEED matrix [0, 42, "random"] and a zsh -n check.

Purpose: Make the three highest-risk behaviors (unparseable-skip, tied-newest, refuse-clobber)
explicitly named and filterable at -m safety_invariant. Enforce TEST-04 in both pytest and CI.
Guarantee hash-seed independence in CI via the PYTHONHASHSEED matrix.

Output: tests/test_safety_invariants.py, tests/test_update_list_integrity.py, pyproject.toml
marker registration, .github/workflows/ci.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-parity-safety-tests/17-CONTEXT.md
@.planning/phases/17-parity-safety-tests/17-RESEARCH.md
@.planning/phases/17-parity-safety-tests/17-PATTERNS.md
@.planning/phases/17-parity-safety-tests/17-01-SUMMARY.md

<interfaces>
<!-- Key signatures the executor needs — verified from source files in planning context. -->

From src/maccat/retention.py:
```python
def prune_old_archives(archive_dir: Path, archive_days: int) -> None: ...
    # Skips any file whose name does not parse as a catalog filename
    # cutoff_yyyymmdd patched via unittest.mock.patch("maccat.retention.cutoff_yyyymmdd", ...)

def retain_newest_per_host(computer_dir: Path) -> None: ...
    # Two-pass algorithm: pass 1 builds {machine: max_ts}, pass 2 moves older files to archive/
    # Files with unparseable names are skipped
    # Tied-newest: both kept (neither moved)
```

From src/maccat/identity.py:
```python
def rename_machine(catalog_repo: Path) -> None: ...
    # Raises SystemExit when destination folder already exists (refuse-clobber)
    # Interactive: reads from stdin (must monkeypatch sys.stdin.isatty + builtins.input)
```

From src/maccat/naming.py:
```python
def make_catalog_filename(machine: str, timestamp: str) -> str: ...
    # Returns "mac-software-list-[{machine}]-{timestamp}.txt"
```

Existing test patterns from test_retention.py and test_identity.py (DO NOT DUPLICATE — extract):
```python
# test_retention.py lines 206–214 (prune skips unparseable):
def _touch_catalog(directory, machine, timestamp): ...
# prune test: archive / "old-notes.txt" survives with archive_days=0

# test_retention.py lines 59–91 (tied-newest):
# Two files for different hosts with same timestamp — both kept in main

# test_identity.py lines 265–286 (refuse-clobber):
# old_dir/NewName both mkdir; input side_effect=["1", "NewName"]; pytest.raises(SystemExit)
```

From pyproject.toml [tool.pytest.ini_options] (current):
```toml
testpaths = ["tests"]
# No markers key yet — must add:
markers = ["safety_invariant: explicit destructive-op safety invariants (TEST-03)"]
```

Existing test_retention.py key lines for extraction:
- _touch_catalog helper: lines 24–28
- test_tied_newest_two_hosts_tied: lines 76–97 (uses TWO different host names, same ts)
- test_unparseable_filename_never_deleted: lines 206–214 (cutoff patch + old-notes.txt survives)
Existing test_identity.py key lines:
- test_refuse_clobber_exits_nonzero: lines 265–286
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test_safety_invariants.py and test_update_list_integrity.py, register marker</name>
  <files>
    tests/test_safety_invariants.py
    tests/test_update_list_integrity.py
    pyproject.toml
  </files>
  <action>
    Create tests/test_safety_invariants.py. Module docstring: "Explicit safety-invariant suite
    (TEST-03). Co-locates the three destructive-op invariants. No new logic — extracted from
    test_retention.py and test_identity.py. Invariants: (a) prune_old_archives NEVER deletes
    files with unparseable timestamps; (b) retain_newest_per_host keeps ALL tied-newest files;
    (c) rename_machine HARD refuses to clobber an existing folder."
    Imports: `from __future__ import annotations`, pathlib.Path, unittest.mock.patch, pytest,
    maccat.naming.make_catalog_filename, maccat.retention.prune_old_archives,
    maccat.retention.retain_newest_per_host, maccat.identity.rename_machine.

    pytestmark = pytest.mark.safety_invariant at module level.

    _touch_catalog(directory: Path, machine: str, timestamp: str) -> Path helper
    (same pattern as test_retention.py lines 24–28 — creates the file and returns its Path).

    test_prune_skips_unparseable_filename(tmp_path: Path) -> None:
    Docstring: "INVARIANT (a): prune_old_archives NEVER deletes files with unparseable names.
    Source: test_retention.py::TestPruneOldArchives::test_unparseable_filename_never_deleted"
    Create archive = tmp_path / "archive"; archive.mkdir().
    Create weird = archive / "old-notes.txt"; weird.write_text("important notes", "utf-8").
    Patch "maccat.retention.cutoff_yyyymmdd" return_value="20260601".
    Call prune_old_archives(archive, archive_days=0).
    Assert weird.exists(), "unparseable .txt in archive/ must never be deleted".

    test_retain_keeps_all_tied_newest(tmp_path: Path) -> None:
    Docstring: "INVARIANT (b): retain_newest_per_host keeps ALL files with the max timestamp.
    Uses two different host names with identical timestamps to cover the two-pass independence.
    Source: test_retention.py::TestRetainNewestPerHost::test_tied_newest_two_hosts_tied"
    ts = "20260614120000"
    f_alpha = _touch_catalog(tmp_path, "alpha", ts)
    f_beta  = _touch_catalog(tmp_path, "beta",  ts)
    retain_newest_per_host(tmp_path)
    Assert f_alpha.exists(), "alpha tied-newest must be kept in main folder".
    Assert f_beta.exists(), "beta tied-newest must be kept in main folder".
    archive_dir = tmp_path / "archive"
    Assert not archive_dir.exists() or list(archive_dir.iterdir()) == [], "archive must be empty".

    test_rename_hard_refuses_clobber(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
    Docstring: "INVARIANT (c): rename_machine raises SystemExit when destination folder exists.
    Source: test_identity.py::TestRenameMachine::test_refuse_clobber_exits_nonzero"
    import sys.
    old_dir = tmp_path / "OldName"; old_dir.mkdir().
    (old_dir / make_catalog_filename("OldName", "20260614120000")).write_text("x", "utf-8").
    new_dir = tmp_path / "NewName"; new_dir.mkdir().
    monkeypatch.setattr(sys.stdin, "isatty", lambda: True).
    with patch("builtins.input", side_effect=["1", "NewName"]):
        with pytest.raises(SystemExit):
            rename_machine(tmp_path)
    assert old_dir.is_dir(), "old folder untouched after refused rename".
    assert new_dir.is_dir(), "destination folder untouched after refused rename".

    Create tests/test_update_list_integrity.py. Module docstring: "TEST-04: update-list.sh
    integrity check. Verifies update-list.sh passes zsh -n (syntax check) at all times.
    Acts as a tripwire — if update-list.sh is accidentally modified during Python development,
    CI fails here before any parity tests run."
    `from __future__ import annotations`, subprocess, pathlib.Path.
    REPO_ROOT = Path(__file__).parent.parent.

    test_update_list_passes_zsh_syntax_check() -> None:
    Docstring: "TEST-04: update-list.sh must pass zsh -n at milestone end."
    result = subprocess.run(["zsh", "-n", str(REPO_ROOT / "update-list.sh")],
                            capture_output=True, text=True).
    assert result.returncode == 0, f"update-list.sh failed zsh -n:\n{result.stderr}".

    Update pyproject.toml: Add to [tool.pytest.ini_options]:
    markers = ["safety_invariant: explicit destructive-op safety invariants (TEST-03)"]
    Read the existing pyproject.toml first to find the exact [tool.pytest.ini_options] section,
    then append the markers key. Do not disturb other settings (testpaths, etc.).

    Security: test_update_list_integrity.py runs subprocess.run with a fixed list (no shell=True,
    no user-interpolated input). update-list.sh path comes from Path(__file__).parent.parent —
    not from any external input.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list && PYTHONPATH=src ./venv/bin/pytest tests/test_safety_invariants.py tests/test_update_list_integrity.py -v && PYTHONPATH=src ./venv/bin/pytest -m safety_invariant -v 2>&1 | grep -E "PASSED|FAILED|test_"
    </automated>
  </verify>
  <done>
    tests/test_safety_invariants.py has exactly 3 tests, all marked @pytest.mark.safety_invariant.
    PYTHONPATH=src ./venv/bin/pytest -m safety_invariant: 3 passed, 0 failed.
    test_prune_skips_unparseable_filename: passes (weird file survives).
    test_retain_keeps_all_tied_newest: passes (len == 2 in main, archive empty).
    test_rename_hard_refuses_clobber: passes (SystemExit, both dirs intact).
    tests/test_update_list_integrity.py has 1 test; zsh -n update-list.sh exits 0.
    pyproject.toml has markers = ["safety_invariant: ..."] under [tool.pytest.ini_options].
    Full suite PYTHONPATH=src ./venv/bin/pytest -x -q green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/ci.yml with PYTHONHASHSEED matrix and zsh -n step</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create .github/ and .github/workflows/ directories. Create .github/workflows/ci.yml.

    The workflow must match exactly the configuration from RESEARCH.md Q7 with the following
    required fields:

    name: CI
    on: push (branches: [main]) and pull_request.
    jobs.test.runs-on: macos-latest (required — zsh is macOS built-in; golden capture needs zsh).
    jobs.test.strategy.matrix.pythonhashseed: [0, 42, "random"].
    jobs.test.env.PYTHONHASHSEED: ${{ matrix.pythonhashseed }}.
    (The literal "random" entry satisfies ROADMAP success criterion 4 "PYTHONHASHSEED=random is set in CI" — PYTHONHASHSEED accepts "random" as a valid value, exercising a per-run randomized seed; the fixed 0/42 entries add reproducible diagnostics. This is the resolution of plan-checker warning #2.)

    Steps in order:
    1. actions/checkout@v4
    2. actions/setup-python@v5 with python-version "3.11"
    3. "Install dev dependencies" — python -m venv venv && ./venv/bin/pip install -e ".[dev]"
    4. "Lint (ruff)" — ./venv/bin/ruff check src tests
    5. "Type check (mypy)" — PYTHONPATH=src ./venv/bin/mypy --strict src/maccat
    6. "Run tests" — PYTHONPATH=src ./venv/bin/pytest -x -q
    7. "Check update-list.sh syntax (TEST-04)" — zsh -n update-list.sh

    Step 7 must be a named step so it appears visibly in CI output as TEST-04 enforcement.
    Steps 4, 5, 7 run unconditionally (not under a continue-on-error).

    Do NOT add a separate ubuntu runner or Windows matrix — macos-latest only (zsh dependency).
    Do NOT pin specific GitHub Actions SHA hashes — use version tags (actions/checkout@v4).
    The PYTHONHASHSEED matrix means CI runs three jobs per push: seed=0, seed=42, and seed=random.
    All must pass for the CI check to go green.

    Security: no secrets used in this workflow. No external service auth. The workflow only
    runs in response to push/PR events on the repository.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list && python3 -c "
import yaml, pathlib
ci = pathlib.Path('.github/workflows/ci.yml').read_text()
# Basic structural checks without importing yaml (not in stdlib):
assert 'macos-latest' in ci, 'must use macos-latest runner'
assert 'pythonhashseed: [0, 42]' in ci or 'pythonhashseed: [0,42]' in ci or '0' in ci and '42' in ci, 'must have seed matrix'
assert 'PYTHONHASHSEED' in ci, 'must set PYTHONHASHSEED env'
assert 'zsh -n update-list.sh' in ci, 'must have zsh -n TEST-04 step'
assert 'actions/checkout@v4' in ci, 'must use checkout@v4'
assert 'mypy --strict' in ci, 'must run mypy --strict'
assert 'ruff check' in ci, 'must run ruff'
print('ci.yml: all structural checks OK')
" 2>/dev/null || python3 -c "
ci = open('.github/workflows/ci.yml').read()
assert 'macos-latest' in ci
assert 'zsh -n update-list.sh' in ci
assert 'PYTHONHASHSEED' in ci
print('ci.yml: structural checks OK')
"
    </automated>
  </verify>
  <done>
    .github/workflows/ci.yml exists and is valid YAML.
    Runs on macos-latest only.
    PYTHONHASHSEED matrix: [0, 42].
    Step named "Check update-list.sh syntax (TEST-04)" runs zsh -n update-list.sh.
    Steps: checkout, setup-python 3.11, install deps, ruff, mypy --strict src/maccat, pytest -x -q, zsh -n.
    File committed to git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | GitHub Actions runner checks out code and runs test suite |
| test code → system zsh | subprocess.run(["zsh", "-n", ...]) with fixed path — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | CI runs against modified update-list.sh | mitigate | zsh -n step in CI (TEST-04 step 7) catches syntax regressions; test_update_list_integrity.py catches it in pytest suite too |
| T-17-03-02 | Elevation of Privilege | CI workflow triggers on untrusted PRs | accept | No secrets in workflow; read-only operations; GitHub's own runner isolation is sufficient |
| T-17-03-03 | Denial of Service | test_safety_invariants using real catalog repo | mitigate | All three invariant tests use tmp_path (pytest-managed isolated dir); never touch real catalog repo |
| T-17-03-04 | Information Disclosure | rename_machine test could read real identity files | mitigate | rename_machine called with tmp_path as catalog_repo arg — no real repo path ever passed |
| T-17-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; stdlib + pytest only |
</threat_model>

<verification>
PYTHONPATH=src ./venv/bin/pytest tests/test_safety_invariants.py tests/test_update_list_integrity.py -v
PYTHONPATH=src ./venv/bin/pytest -m safety_invariant -v  # exactly 3 tests
zsh -n update-list.sh  # must exit 0 (TEST-04)
python3 -c "ci=open('.github/workflows/ci.yml').read(); assert 'zsh -n update-list.sh' in ci and 'PYTHONHASHSEED' in ci; print('CI YAML: OK')"
PYTHONPATH=src ./venv/bin/pytest -x -q  # full suite green
</verification>

<success_criteria>
1. tests/test_safety_invariants.py: 3 tests, all pass, all tagged safety_invariant
2. test_prune_skips_unparseable_filename: unparseable file survives prune_old_archives(archive_days=0)
3. test_retain_keeps_all_tied_newest: two hosts with same ts → both survive → archive empty
4. test_rename_hard_refuses_clobber: SystemExit raised, old and new dirs both intact
5. tests/test_update_list_integrity.py: zsh -n update-list.sh exits 0
6. pyproject.toml markers key registered for safety_invariant
7. .github/workflows/ci.yml: macos-latest, PYTHONHASHSEED matrix [0, 42, "random"], zsh -n step
8. Full suite PYTHONPATH=src ./venv/bin/pytest -x -q green (400+ existing + 4 new tests)
</success_criteria>

<output>
Create `.planning/phases/17-parity-safety-tests/17-03-SUMMARY.md` when done
</output>
