---
phase: 17-parity-safety-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/golden/__init__.py
  - tests/golden/normalize.py
  - tests/golden/generate.py
  - tests/conftest.py
  - tests/golden/fixtures/fake_home/.gitkeep
  - tests/golden/homebrew-packages.golden.txt
  - tests/golden/app-store-applications.golden.txt
  - tests/golden/setapp-applications.golden.txt
  - tests/golden/web-installed-applications.golden.txt
  - tests/golden/claude-code-plugins.golden.txt
  - tests/golden/claude-code-mcp-servers.golden.txt
  - tests/golden/claude-code-skills-agents.golden.txt
  - tests/golden/codex-mcp-servers.golden.txt
  - tests/golden/opencode-plugins.golden.txt
  - tests/golden/opencode-mcp-servers.golden.txt
  - tests/golden/opencode-agents.golden.txt
  - tests/golden/gemini-extensions.golden.txt
  - tests/golden/gemini-mcp-servers.golden.txt
  - tests/golden/vscode-extensions.golden.txt
  - tests/golden/cursor-extensions.golden.txt
  - tests/golden/google-chrome-extensions.golden.txt
  - tests/golden/firefox-extensions.golden.txt
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "normalize_catalog_body('20260614120000') returns 'TIMESTAMP'"
    - "normalize_catalog_body('[computer-one]') returns '[MACHINE]'"
    - "tests/golden/ contains exactly 17 *.golden.txt files"
    - "a normal `pytest` run does NOT modify any .golden.txt file (mtime unchanged)"
    - "each golden file contains the normalized section body from the zsh reference"
    - "generate.py is never imported on a normal test run (lazy import guard)"
  artifacts:
    - path: "tests/golden/__init__.py"
      provides: "Python package marker so tests.golden.normalize is importable"
    - path: "tests/golden/normalize.py"
      provides: "normalize_catalog_body() and extract_section_body() pure functions"
      exports: ["normalize_catalog_body", "extract_section_body", "SEPARATOR_LINE"]
    - path: "tests/golden/generate.py"
      provides: "capture_zsh_section() golden-capture harness, zsh-subprocess driven"
      exports: ["capture_zsh_section", "REPO_ROOT"]
    - path: "tests/conftest.py"
      provides: "--update-golden addoption + update_golden fixture (extends existing)"
    - path: "tests/golden/fixtures/fake_home/.gitkeep"
      provides: "Committed synthetic HOME tree root — populated by generate.py at update time"
    - path: "tests/golden/claude-code-plugins.golden.txt"
      provides: "Reviewed golden for Claude Code Plugins section (normalized)"
    - path: "tests/golden/google-chrome-extensions.golden.txt"
      provides: "Reviewed golden for Google Chrome Extensions (synthetic profile tree)"
    - path: "tests/golden/firefox-extensions.golden.txt"
      provides: "Reviewed golden for Firefox Extensions (synthetic profiles.ini + extensions.json)"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/golden/generate.py"
      via: "update_golden fixture → lazy import inside test_golden_parity.py"
      pattern: "if update_golden:.*import.*generate"
    - from: "tests/golden/generate.py"
      to: "update-list.sh"
      via: "zsh -c 'source ...; collector_fn' subprocess"
      pattern: "subprocess.run.*zsh.*-c"
    - from: "tests/test_golden_parity.py"
      to: "tests/golden/normalize.py"
      via: "from tests.golden.normalize import normalize_catalog_body"
      pattern: "normalize_catalog_body"
---

<objective>
Build the golden-fixture harness foundation: normalize utility, zsh-subprocess capture
function, --update-golden addoption, synthetic fake_home fixture tree, and generate + commit
all 17 section-level golden fixtures by running each Python collector against controlled
synthetic inputs.

Purpose: The committed golden files are the byte-identical reference that the Wave 2 parity
tests assert against. They must exist and be reviewed before any parity assertions can be
written. The zsh source-guard at update-list.sh:2433 makes collector functions sourceable in
isolation — this plan exploits that to capture one section at a time.

Output: tests/golden/normalize.py, tests/golden/generate.py, extended tests/conftest.py,
tests/golden/fixtures/fake_home/ committed tree, and 17 *.golden.txt files committed as
reviewed artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-parity-safety-tests/17-CONTEXT.md
@.planning/phases/17-parity-safety-tests/17-RESEARCH.md
@.planning/phases/17-parity-safety-tests/17-PATTERNS.md

<interfaces>
<!-- Key constants and patterns the executor needs. Extracted from codebase. -->

From tests/conftest.py (extend, do NOT replace — existing fixtures must remain):
```python
# Existing fixtures: tmp_json, git_repo, catalog_repo — leave untouched
# Add after catalog_repo fixture:
def pytest_addoption(parser: pytest.Parser) -> None: ...
@pytest.fixture() def update_golden(request: pytest.FixtureRequest) -> bool: ...
```

From src/maccat/collectors/__init__.py — get_registry() section order (17 sections):
```
1. Homebrew Packages           (HomebrewCollector — raw)
2. App Store Applications      (MasCollector — raw)
3. Setapp Applications         (SetappCollector — raw, BASE = /Applications/Setapp)
4. Web-installed Applications  (WebAppsCollector — raw, BASE = /Applications)
5. Claude Code Plugins         (ClaudeCollector — _PLUGINS_PATH module-level)
6. Claude Code MCP Servers     (ClaudeCollector — _CLAUDE_JSON module-level)
7. Claude Code Skills & Agents (ClaudeCollector — _SKILLS_DIR, _AGENTS_DIR module-level)
8. Codex MCP Servers           (CodexCollector — _TOML_PATH module-level)
9. OpenCode Plugins            (OpenCodeCollector — _CONFIG_PATH, _AGENTS_DIR module-level)
10. OpenCode MCP Servers       (OpenCodeCollector — _CONFIG_PATH module-level)
11. OpenCode Agents            (OpenCodeCollector — _AGENTS_DIR module-level)
12. Gemini CLI Extensions      (GeminiCollector — _EXT_DIR module-level)
13. Gemini CLI MCP Servers     (GeminiCollector — _MCP_PATH module-level)
14. VS Code Extensions         (VSCodeCollector — _EXT_DIR class attr)
15. Cursor Extensions          (CursorCollector — _EXT_DIR class attr)
16. Google Chrome Extensions   (ChromeCollector — _BASE module-level)
17. Firefox Extensions         (FirefoxCollector — _FF_DIR module-level)
```

From src/maccat/collectors/ — patch targets for path constants:
```python
# Module-level (patch with patch() using dotted path):
maccat.collectors.claude._PLUGINS_PATH    # Path.home() / ".claude/plugins/installed_plugins.json"
maccat.collectors.claude._CLAUDE_JSON     # Path.home() / ".claude.json"
maccat.collectors.claude._SKILLS_DIR      # Path.home() / ".claude/skills"
maccat.collectors.claude._AGENTS_DIR      # Path.home() / ".claude/agents"
maccat.collectors.codex._TOML_PATH        # Path.home() / ".codex/config.toml"
maccat.collectors.opencode._CONFIG_PATH   # Path.home() / ".config/opencode/opencode.json"
maccat.collectors.opencode._AGENTS_DIR    # Path.home() / ".config/opencode/agents"
maccat.collectors.gemini._EXT_DIR         # Path.home() / ".gemini/extensions"
maccat.collectors.gemini._MCP_PATH        # Path.home() / ".gemini/config/mcp_config.json"
maccat.collectors.chrome._BASE            # Path.home() / "Library/Application Support/Google/Chrome"
maccat.collectors.firefox._FF_DIR         # Path.home() / "Library/Application Support/Firefox"

# Class attributes (patch with patch.object):
VSCodeCollector._EXT_DIR   # Path.home() / ".vscode/extensions"
CursorCollector._EXT_DIR   # Path.home() / ".cursor/extensions"
SetappCollector.BASE        # Path("/Applications/Setapp")  — module-level BASE too
WebAppsCollector.BASE       # Path("/Applications")          — module-level BASE too
```

From CatalogWriter format (catalog/writer.py):
```
Section format in output file:
  \n{title}\n
  ------------------------------------\n   (36 dashes)
  {body lines, one per \n}
```

Zsh source-guard (update-list.sh line 2433):
```zsh
[[ "$ZSH_EVAL_CONTEXT" =~ :file ]] && return 0
```
This fires when the script is sourced (not run directly), preventing the main block.
VERIFIED: `zsh -c 'source update-list.sh; collect_claude_plugins'` with HOME/OUTPUT_FILE set
produces correct section output in OUTPUT_FILE, not stdout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build normalize.py, generate.py, and extend conftest with --update-golden</name>
  <files>
    tests/golden/__init__.py
    tests/golden/normalize.py
    tests/golden/generate.py
    tests/conftest.py
  </files>
  <action>
    Create tests/golden/__init__.py as an empty file (Python package marker so that
    `from tests.golden.normalize import normalize_catalog_body` is importable in pytest).

    Create tests/golden/normalize.py with module docstring "Normalization helpers for
    golden-output parity tests (TEST-02)." and `from __future__ import annotations` and
    `import re`. Implement two pure functions with no maccat imports, no sorted() calls:

    normalize_catalog_body(text: str) -> str — applies exactly these two substitutions in order:
    `re.sub(r'\d{14}', 'TIMESTAMP', text)` then `re.sub(r'\[[^\]]+\]', '[MACHINE]', text)`.
    Docstring must note: volatile = 14-digit timestamps and [label] brackets; stable = everything else.

    SEPARATOR_LINE = "-" * 36 as a module-level constant.

    extract_section_body(catalog_text: str, section_title: str) -> str | None — splits on
    "\n" + SEPARATOR_LINE + "\n", finds the chunk whose rstrip("\n") ends with section_title
    (case-exact), returns the next chunk's text up to the first "\n\n" boundary. Returns None
    if section not found. This must handle the CatalogWriter format: \n{title}\n{separator}\n{body}.
    Validate: a 3-section synthetic string with known bodies must return correct bodies for each title.

    Create tests/golden/generate.py with module docstring warning it must NEVER be imported on a
    normal test run (only under --update-golden). Imports: `from __future__ import annotations`,
    subprocess, tempfile, pathlib.Path. Module-level constants:
      REPO_ROOT = Path(__file__).parent.parent.parent
      SCRIPT = REPO_ROOT / "update-list.sh"

    Implement capture_zsh_section(collector_fn: str, fake_home: Path) -> str. The zsh script
    body (the string passed to `zsh -c`) must set these variables IN the script body (not env=):
      OUTPUT_FILE={str(output_file)!r}
      HOME={str(fake_home)!r}
      SCRIPT_DIR={str(REPO_ROOT)!r}
      _section_lines=()
    Then source the script and call collector_fn. Use subprocess.run(["zsh", "-c", zsh_script],
    capture_output=True, text=True, timeout=30). Raise RuntimeError if returncode != 0 (include
    stderr). Return output_file.read_text(encoding="utf-8"). Cleanup output_file in finally block
    with unlink(missing_ok=True). One collector per call — never batch two in one subprocess.

    Extend tests/conftest.py (do NOT overwrite — append after the existing catalog_repo fixture).
    Add a separator comment "# --update-golden flag (TEST-01/02 golden fixture update guard)".
    Add pytest_addoption(parser: pytest.Parser) -> None with --update-golden, action="store_true",
    default=False, help explaining it only runs when intentional. Add update_golden(request:
    pytest.FixtureRequest) -> bool fixture returning request.config.getoption("--update-golden").
    Import pytest at top if not already imported (it is — check first).

    Security: the zsh script body uses !r (repr) for all path interpolations — this produces
    quoted shell strings that prevent word-splitting and glob expansion for any path with spaces
    or special characters. No untrusted input enters the script body — all paths come from
    Python Path objects constructed from repo structure.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list && PYTHONPATH=src ./venv/bin/python -c "
from tests.golden.normalize import normalize_catalog_body, extract_section_body, SEPARATOR_LINE
assert normalize_catalog_body('20260614120000') == 'TIMESTAMP'
assert normalize_catalog_body('[computer-one]') == '[MACHINE]'
assert normalize_catalog_body('hello 20260614120000 [Mac]') == 'hello TIMESTAMP [MACHINE]'
assert SEPARATOR_LINE == '-' * 36
print('normalize: OK')

# 3-section synthetic test for extract_section_body
SEP = '\n' + SEPARATOR_LINE + '\n'
catalog = '\nSection A\n' + SEPARATOR_LINE + '\nitem-a1\nitem-a2\n\nSection B\n' + SEPARATOR_LINE + '\nitem-b1\n'
body_a = extract_section_body(catalog, 'Section A')
assert body_a is not None and 'item-a1' in body_a, f'got: {body_a!r}'
print('extract_section_body: OK')
"
    </automated>
  </verify>
  <done>
    tests/golden/__init__.py exists (empty).
    tests/golden/normalize.py exports normalize_catalog_body, extract_section_body, SEPARATOR_LINE.
    normalize_catalog_body('20260614120000') == 'TIMESTAMP'.
    normalize_catalog_body('[computer-two.local]') == '[MACHINE]'.
    extract_section_body works on a 3-section synthetic catalog.
    tests/golden/generate.py exports capture_zsh_section with the verified zsh sourcing recipe.
    tests/conftest.py has --update-golden addoption and update_golden fixture appended.
    No existing conftest fixtures broken: PYTHONPATH=src ./venv/bin/pytest tests/conftest.py --collect-only passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build synthetic fake_home fixture tree and generate + commit 17 golden files</name>
  <files>
    tests/golden/fixtures/fake_home/.gitkeep
    tests/golden/homebrew-packages.golden.txt
    tests/golden/app-store-applications.golden.txt
    tests/golden/setapp-applications.golden.txt
    tests/golden/web-installed-applications.golden.txt
    tests/golden/claude-code-plugins.golden.txt
    tests/golden/claude-code-mcp-servers.golden.txt
    tests/golden/claude-code-skills-agents.golden.txt
    tests/golden/codex-mcp-servers.golden.txt
    tests/golden/opencode-plugins.golden.txt
    tests/golden/opencode-mcp-servers.golden.txt
    tests/golden/opencode-agents.golden.txt
    tests/golden/gemini-extensions.golden.txt
    tests/golden/gemini-mcp-servers.golden.txt
    tests/golden/vscode-extensions.golden.txt
    tests/golden/cursor-extensions.golden.txt
    tests/golden/google-chrome-extensions.golden.txt
    tests/golden/firefox-extensions.golden.txt
  </files>
  <action>
    This task generates all 17 golden fixture files. The approach is:
    (a) Run each Python collector against a synthetic fake_home tmp_path with patched path
        constants, collecting section items.
    (b) For HOME-based collectors (Claude, Codex, OpenCode, Gemini, VSCode, Cursor, Chrome,
        Firefox): ALSO run capture_zsh_section() from generate.py against the SAME fake_home
        to verify byte parity before writing the golden. Write the PYTHON output normalized
        as the golden (since the parity test will compare Python output against the golden).
    (c) For "not installed" fallback collectors (Homebrew, mas, Setapp): the golden is the
        static fallback text — Python collector.collect() with no patches needed (the tool
        is absent on the CI machine).

    Write a Python script (tmp, do not commit) that builds fake_home in a real directory
    tests/golden/fixtures/fake_home/ and generates each golden file. The fake_home directory
    IS committed (not a tmp_path) so that the zsh subprocess can read it deterministically
    on any run. Use minimal synthetic data — enough to exercise each code path without
    machine-specific values.

    Synthetic data to place in tests/golden/fixtures/fake_home/:

    .claude/plugins/installed_plugins.json — {"plugins": {"test-plugin@registry": [{"version": "1.0.0"}]}}
    .claude.json — {"mcpServers": {"my-server": {"type": "stdio", "command": "echo"}}} — MCP entry with stdio transport only, no secrets
    .claude/skills/ — empty directory (.gitkeep) — Skills & Agents section should show (none found)
    .claude/agents/ — empty directory (.gitkeep) — Skills & Agents section should show (none found)
    .codex/config.toml — minimal TOML with [mcpServers] section: [mcpServers] \n [mcpServers.my-codex-server] \n transport = "stdio"
    .config/opencode/opencode.json — {"mcp": {"servers": {"oc-server": {"type": "stdio"}}}, "providers": []} — no plugins or agents path needed for minimal
    .config/opencode/agents/ — empty directory (.gitkeep)
    .gemini/extensions/ — one subdirectory "my-gemini-ext/" with a SKILL.md containing "name: My Gemini Extension"
    .gemini/config/mcp_config.json — {"mcpServers": {"g-server": {"serverStartPath": "echo", "args": []}}} — minimal non-empty
    .vscode/extensions/extensions.json — [{"identifier": {"id": "vscomp.test-ext"}, "version": "1.2.3", "location": {"$mid": 1, "path": "/home/user/.vscode/extensions/vscomp.test-ext-1.2.3"}}]
    .cursor/extensions/extensions.json — [{"identifier": {"id": "cursor.test-ext"}, "version": "2.0.0", "location": {"$mid": 1, "path": "/home/user/.cursor/extensions/cursor.test-ext-2.0.0"}}]
    Library/Application Support/Google/Chrome/Default/Extensions/abcdefghijklmnop/1.0_0/manifest.json — {"name": "Test Extension", "version": "1.0", "manifest_version": 3}
    Library/Application Support/Firefox/profiles.ini — [Profile0]\nName=default\nIsRelative=1\nPath=Profiles/test.default
    Library/Application Support/Firefox/Profiles/test.default/extensions.json — {"addons": [{"defaultLocale": {"name": "Test Firefox Addon"}, "version": "3.0", "id": "test@mozilla"}]}

    For each of the 17 sections, generate the golden by:
    1. Patching the relevant module constant(s) to point at tests/golden/fixtures/fake_home/
    2. Calling collector.collect() to get the Section with items list
    3. Using flush_section(items, raw=section.raw) from maccat.catalog.format OR for raw
       sections calling the raw writer pattern — get the section body as text
    4. Calling normalize_catalog_body() on the result
    5. Writing to tests/golden/<slug>.golden.txt

    The file slug mapping (filename = section title lowercased with spaces replaced by hyphens):
      "Homebrew Packages" → homebrew-packages.golden.txt
      "App Store Applications" → app-store-applications.golden.txt
      "Setapp Applications" → setapp-applications.golden.txt
      "Web-installed Applications" → web-installed-applications.golden.txt
      "Claude Code Plugins" → claude-code-plugins.golden.txt
      "Claude Code MCP Servers" → claude-code-mcp-servers.golden.txt
      "Claude Code Skills & Agents" → claude-code-skills-agents.golden.txt
      "Codex MCP Servers" → codex-mcp-servers.golden.txt
      "OpenCode Plugins" → opencode-plugins.golden.txt
      "OpenCode MCP Servers" → opencode-mcp-servers.golden.txt
      "OpenCode Agents" → opencode-agents.golden.txt
      "Gemini CLI Extensions" → gemini-extensions.golden.txt
      "Gemini CLI MCP Servers" → gemini-mcp-servers.golden.txt
      "VS Code Extensions" → vscode-extensions.golden.txt
      "Cursor Extensions" → cursor-extensions.golden.txt
      "Google Chrome Extensions" → google-chrome-extensions.golden.txt
      "Firefox Extensions" → firefox-extensions.golden.txt

    Web-installed Applications caveat: WebAppsCollector.BASE is /Applications (hardcoded in zsh).
    For this golden, patch WebAppsCollector.BASE to tests/golden/fixtures/fake_applications/ (a
    minimal fake dir with one .app stub). Annotate the golden file with a comment at the top:
    "# web-installed-applications: zsh hardcodes /Applications — synthetic Python fixture only"
    "# zsh parity for this section is [ASSUMED] per 17-RESEARCH.md §Assumptions A1"
    Then the body follows. The test_golden_parity.py will annotate this test as format-only.

    Create tests/golden/fixtures/ directory structure and commit fake_home tree to git.
    Create tests/golden/fixtures/fake_applications/FakeWebApp.app/ (a .app bundle stub).

    After generating all 17 golden files, verify parity for HOME-based sections by also
    running capture_zsh_section() for at least 3 representative sections (claude plugins,
    gemini extensions, vscode extensions) and confirming that normalize(zsh_output) matches
    normalize(python_output) before committing. If they diverge, fix the synthetic data or
    the Python collector patch before committing the golden.

    Security: fake_home synthetic JSON/TOML contains NO real secrets — only transport types,
    synthetic names, and stub versions. MCP entries have only "type"/"transport" fields. The
    zsh subprocess never touches real HOME (HOME= override is in script body per RESEARCH.md
    Pitfall 1). The generate script is never imported on a normal pytest run (only called
    manually under --update-golden).
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list && python3 -c "
import os
from pathlib import Path
golden = Path('tests/golden')
files = sorted(golden.glob('*.golden.txt'))
print(f'Golden files: {len(files)}')
for f in files:
    print(f'  {f.name}: {len(f.read_text())} chars')
assert len(files) == 17, f'Expected 17, got {len(files)}: {[f.name for f in files]}'
print('Count: OK')

# Verify no golden contains 14-digit raw timestamps or raw [label] brackets
import re
ts_re = re.compile(r'\d{14}')
label_re = re.compile(r'\[[A-Z][a-zA-Z0-9\-\.]+\]')
for f in files:
    text = f.read_text()
    assert not ts_re.search(text), f'{f.name} still has raw timestamp'
    # [MACHINE] is fine; real machine labels should not appear
print('Normalization: OK')
"
      PYTHONPATH=src ./venv/bin/pytest -x -q 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    tests/golden/fixtures/fake_home/ directory exists with synthetic data for all 17 sections
    committed to git. tests/golden/fixtures/fake_applications/ exists with one .app stub.
    Exactly 17 *.golden.txt files exist in tests/golden/, all committed.
    Each golden file has normalized text (no raw 14-digit timestamps, no raw [label] strings
    except [MACHINE]).
    For at least 3 HOME-based sections, normalize(zsh_output) == normalize(python_output)
    verified before commit.
    Existing 400+ tests still pass: PYTHONPATH=src ./venv/bin/pytest -x -q green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → zsh subprocess | Shell script sourced in subprocess; path constants interpolated into script body |
| fake_home fixtures → collector code | Synthetic JSON/TOML consumed by collectors; format must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | capture_zsh_section() path interpolation | mitigate | All paths interpolated with !r (repr) producing quoted shell strings — no word-splitting or glob injection possible; paths come from Python Path objects only |
| T-17-02 | Information Disclosure | fake_home/.claude.json MCP fixtures | mitigate | Fixture JSON contains only type/transport fields, no env/args/command/url/tokens — CAT-05 guard already enforced by ClaudeCollector |
| T-17-03 | Tampering | golden files auto-updated on normal run | mitigate | generate.py never imported except under --update-golden guard; all golden writes gated behind if update_golden: check |
| T-17-04 | Denial of Service | zsh subprocess timeout | accept | timeout=30 on each capture_zsh_section call; 17 sections × 30s max = 510s upper bound; normal execution << 1s per section |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib + pytest only |
</threat_model>

<verification>
PYTHONPATH=src ./venv/bin/pytest -x -q  # full suite including existing 400+ tests
python3 -c "from tests.golden.normalize import normalize_catalog_body; assert normalize_catalog_body('20260614120000') == 'TIMESTAMP'"
python3 -c "from pathlib import Path; files = list(Path('tests/golden').glob('*.golden.txt')); assert len(files) == 17, files"
git diff --name-only HEAD -- tests/golden/  # confirms golden files committed
</verification>

<success_criteria>
1. tests/golden/__init__.py, normalize.py, generate.py all exist and are importable
2. normalize_catalog_body applies exactly two regexes (\d{14} → TIMESTAMP, \[[^\]]+\] → [MACHINE])
3. tests/conftest.py has --update-golden addoption and update_golden fixture
4. Exactly 17 *.golden.txt files in tests/golden/, all committed
5. tests/golden/fixtures/fake_home/ synthetic tree committed with data for all section types
6. For at least 3 HOME-based sections: normalize(zsh_output) == normalize(python_output)
7. No golden file contains raw 14-digit timestamps or raw machine-label strings
8. A plain `pytest` run does NOT trigger any golden file regeneration
9. Full test suite (400+ existing tests) still green
</success_criteria>

<output>
Create `.planning/phases/17-parity-safety-tests/17-01-SUMMARY.md` when done
</output>
