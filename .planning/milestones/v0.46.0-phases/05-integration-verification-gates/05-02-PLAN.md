---
phase: 05-integration-verification-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: true
requirements:
  - FMT-02

must_haves:
  truths:
    - "A full ./update-list.sh --personal --no-commit run exits 0"
    - "The produced catalog contains all 13 new section headers (with real entries or (none found))"
    - "The FMT-03 secret-leakage gate (scoped to new sections via awk) returns ZERO matches"
    - "Two consecutive --no-commit runs produce byte-identical new-section content (diff empty)"
    - "The gate harness removes its two test catalog files — git status shows no stray untracked catalogs after cleanup"
  artifacts:
    - path: "/tmp/gate_run1_new_sections.txt"
      provides: "New-sections region from run 1 (ephemeral, cleaned up)"
      contains: "Claude Code Plugins"
    - path: "/tmp/gate_run2_new_sections.txt"
      provides: "New-sections region from run 2 (ephemeral, cleaned up)"
      contains: "Claude Code Plugins"
  key_links:
    - from: "awk '/^Claude Code Plugins$/,0'"
      to: "FMT-03 grep -E pattern"
      via: "pipe"
      pattern: "grep -E \"(https?://|Bearer |[?&]key=|Authorization|sk-|ghp_|token)\""
    - from: "awk new-sections extraction"
      to: "diff"
      via: "two /tmp files"
      pattern: "diff /tmp/gate_run1_new_sections.txt /tmp/gate_run2_new_sections.txt"
---

<objective>
Run the complete ephemeral integration verification against the catalog produced by the wired
update-list.sh, and confirm all three gates pass before closing Phase 5.

Purpose: This is the milestone-critical verification phase. No new code is written; all work
is running the real script and inspecting its output. The gates confirm that:
  - FMT-02: all 13 new sections are present and the run exits 0 (graceful degradation)
  - FMT-03: zero secret patterns appear in the new-sections region of the real catalog
  - FMT-04: two consecutive runs produce byte-identical new-section content

Output: No committed artifacts. Two test catalog files are produced and removed. The phase
closes with all three gates passed and git status clean.
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.planning/phases/05-integration-verification-gates/05-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/05-integration-verification-gates/05-CONTEXT.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/PROJECT.md
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/phases/05-integration-verification-gates/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run FMT-02 full-run exit check and confirm all 13 section headers present</name>
  <files></files>
  <read_first>
    1. Read 05-RESEARCH.md "Flag 3: Real-Run Gate Harness" for the complete gate harness — it
       contains the exact shell commands for all three checks in sequence including the sleep 1,
       ls -t capture pattern, cleanup with rm, and git status verification.
    2. Read 05-RESEARCH.md "FMT-02: Graceful Degradation Path" for the list of 13 expected
       section headers and the degradation logic for each collector.
    3. Read 05-RESEARCH.md "Pitfall 2: awk range anchor fails" — add the non-empty guard after
       the awk extraction step to prevent a false leakage gate pass when wiring is absent.
    4. Read 05-CONTEXT.md "Gate Delivery Form (USER LOCKED)" — do NOT add a permanent --verify
       subcommand; do NOT commit the test catalog files.
  </read_first>
  <action>
    From the repo root (/Users/ken/dev/mac-software-list), execute a single real gate run using
    `--personal --no-commit`. The full harness is in 05-RESEARCH.md "Flag 3 / Complete gate
    harness". Steps:

    1. Record baseline (existing newest file in personal/ if any) for change detection.

    2. Run 1:
       ./update-list.sh --personal --no-commit
       Capture the produced filename with: RUN1_FILE=$(ls -t personal/mac-software-list-*.txt | head -1)
       Verify a new file was produced (RUN1_FILE != BEFORE_FILE).

    3. Non-empty guard on new sections:
       awk '/^Claude Code Plugins$/,0' "$RUN1_FILE" > /tmp/gate_run1_new_sections.txt
       Assert: [[ -s /tmp/gate_run1_new_sections.txt ]] or fail with "ERROR: new sections not
       found in catalog — wiring from Plan 05-01 may not be applied". A 0-byte file here
       means the wiring is missing and all subsequent gates would be meaningless.

    4. FMT-02: Check all 13 section headers present in RUN1_FILE:
       Expected headers (as written by write_section): Claude Code Plugins, Claude Code MCP
       Servers, Claude Code Skills & Agents, Codex MCP Servers, OpenCode Plugins, OpenCode MCP
       Servers, OpenCode Agents, Gemini CLI Extensions, Gemini CLI MCP Servers, VS Code
       Extensions, Cursor Extensions, Google Chrome Extensions, Firefox Extensions.
       For each, run: grep -qF "$section" "$RUN1_FILE" && echo "FOUND: $section" || echo "MISSING: $section"
       Any MISSING line fails FMT-02.

    Record results before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      # From repo root — verify run exits 0 and new file produced
      cd /Users/ken/dev/mac-software-list
      ./update-list.sh --personal --no-commit
      echo "Exit code: $?"

      # Capture file and check non-empty new sections
      RUN1_FILE=$(ls -t personal/mac-software-list-*.txt | head -1)
      echo "Run 1 file: $RUN1_FILE"
      awk '/^Claude Code Plugins$/,0' "$RUN1_FILE" > /tmp/gate_run1_new_sections.txt
      wc -l /tmp/gate_run1_new_sections.txt

      # FMT-02 section presence check
      for section in "Claude Code Plugins" "Claude Code MCP Servers" "Claude Code Skills & Agents" \
                     "Codex MCP Servers" "OpenCode Plugins" "OpenCode MCP Servers" "OpenCode Agents" \
                     "Gemini CLI Extensions" "Gemini CLI MCP Servers" "VS Code Extensions" \
                     "Cursor Extensions" "Google Chrome Extensions" "Firefox Extensions"; do
        grep -qF "$section" "$RUN1_FILE" && echo "FOUND: $section" || echo "MISSING: $section"
      done
    </automated>
  </verify>
  <acceptance_criteria>
    - ./update-list.sh --personal --no-commit exits 0
    - A new personal/mac-software-list-[hostname]-YYYYMMDDHHMMSS.txt file is produced
    - /tmp/gate_run1_new_sections.txt is non-empty (non-zero byte count confirms wiring is live)
    - All 13 section headers are found in RUN1_FILE (each with real entries or "(none found)")
    - No "MISSING:" lines in the FMT-02 check output
  </acceptance_criteria>
  <done>Full run exits 0; all 13 new section headers confirmed present in the real produced catalog; new-sections awk extraction is non-empty.</done>
</task>

<task type="auto">
  <name>Task 2: Run FMT-03 secret-leakage gate and FMT-04 determinism gate, then clean up</name>
  <files></files>
  <read_first>
    1. Read 05-RESEARCH.md "Flag 2: Leakage Gate — Exact Range Extraction + Pattern" — the
       complete gate command (awk pipe grep -E with 7 patterns) and the reasoning for scoping
       to new sections only (Homebrew formula names like libnghttp2 would false-positive on a
       whole-file grep).
    2. Read 05-RESEARCH.md "Flag 3: Complete gate harness" steps 4–end — second run, sleep 1,
       FMT-04 new-sections diff, best-effort full-file diff, and the rm cleanup.
    3. Read 05-RESEARCH.md "Pitfall 1: Leaving test catalog files committed" — always use
       --no-commit; rm both files; verify git status is clean.
    4. Read 05-RESEARCH.md "Pitfall 3: Second run produces same filename" — sleep 1 between
       runs is required.
  </read_first>
  <action>
    This task uses the RUN1_FILE and /tmp/gate_run1_new_sections.txt produced in Task 1.
    The complete harness is in 05-RESEARCH.md "Flag 3". Proceed as follows:

    FMT-03 LEAKAGE GATE (on run 1 new sections):
    Run: awk '/^Claude Code Plugins$/,0' "$RUN1_FILE" | grep -E "(https?://|Bearer |[?&]key=|Authorization|sk-|ghp_|token)"
    If grep produces ANY output lines → FAIL (secret pattern found in new sections).
    If grep exits non-zero (no matches) → PASS.
    Report: "FMT-03 PASS: zero secret hits" or "FMT-03 FAIL: [lines found]".

    FMT-04 DETERMINISM GATE — run 2:
    sleep 1  (ensures distinct YYYYMMDDHHMMSS timestamp in filename)
    ./update-list.sh --personal --no-commit
    RUN2_FILE=$(ls -t personal/mac-software-list-*.txt | head -1)
    Verify RUN2_FILE != RUN1_FILE (different timestamp in filename).
    awk '/^Claude Code Plugins$/,0' "$RUN2_FILE" > /tmp/gate_run2_new_sections.txt

    FIRM gate (new-sections diff):
    diff /tmp/gate_run1_new_sections.txt /tmp/gate_run2_new_sections.txt
    Empty output = PASS. Non-empty = FAIL.

    BEST-EFFORT full-file diff (informational — failure here does NOT fail the phase):
    diff "$RUN1_FILE" "$RUN2_FILE"
    If non-empty: identify which legacy section differs and report as informational.

    CLEANUP (mandatory — per USER LOCKED gate delivery form):
    rm "$RUN1_FILE" "$RUN2_FILE"
    rm -f /tmp/gate_run1_new_sections.txt /tmp/gate_run2_new_sections.txt
    git -C /Users/ken/dev/mac-software-list status
    Verify: no new staged or untracked personal/mac-software-list-*.txt files appear.

    If FMT-03 FAILS: report the exact matching lines and stop — do not proceed to commit.
    If FMT-04 FAILS (new-sections diff non-empty): report the diff and stop.
  </action>
  <verify>
    <automated>
      cd /Users/ken/dev/mac-software-list

      # FMT-03: leakage gate on run 1 new sections
      echo "=== FMT-03 LEAKAGE GATE ==="
      awk '/^Claude Code Plugins$/,0' "$RUN1_FILE" \
        | grep -E "(https?://|Bearer |[?&]key=|Authorization|sk-|ghp_|token)" \
        && echo "FMT-03 FAIL: secret pattern found" \
        || echo "FMT-03 PASS: zero secret hits"

      # FMT-04: second run + determinism check
      echo "=== FMT-04 DETERMINISM GATE ==="
      sleep 1
      ./update-list.sh --personal --no-commit
      RUN2_FILE=$(ls -t personal/mac-software-list-*.txt | head -1)
      awk '/^Claude Code Plugins$/,0' "$RUN2_FILE" > /tmp/gate_run2_new_sections.txt
      diff /tmp/gate_run1_new_sections.txt /tmp/gate_run2_new_sections.txt \
        && echo "FMT-04 PASS: new sections byte-identical" \
        || echo "FMT-04 FAIL: new sections diff non-empty"

      # Best-effort full-file diff
      echo "=== FULL-FILE DIFF (informational) ==="
      diff "$RUN1_FILE" "$RUN2_FILE" && echo "Full-file: EMPTY (bonus)" \
        || echo "Full-file: non-empty (see above — informational only)"

      # Cleanup
      echo "=== CLEANUP ==="
      rm "$RUN1_FILE" "$RUN2_FILE"
      rm -f /tmp/gate_run1_new_sections.txt /tmp/gate_run2_new_sections.txt
      git status
    </automated>
  </verify>
  <acceptance_criteria>
    - FMT-03: awk new-sections extraction piped through grep -E returns ZERO matching lines
      (grep exits non-zero; echo confirms "FMT-03 PASS: zero secret hits")
    - FMT-04: diff of the two awk-extracted new-sections files is empty (exit 0; "FMT-04 PASS")
    - RUN2_FILE has a different timestamp-based filename from RUN1_FILE (sleep 1 guarantee)
    - Both test catalog files (RUN1_FILE, RUN2_FILE) are removed after gates complete
    - Both /tmp gate temp files are removed after gates complete
    - git status after cleanup shows no new staged or untracked personal/mac-software-list-*.txt
      files (repo returns to its pre-harness state)
    - If full-file diff is non-empty: it shows only legacy section differences (informational);
      the phase does NOT fail on legacy-section volatility per the USER LOCKED determinism gate scope
  </acceptance_criteria>
  <done>FMT-03 leakage gate PASSED (zero secret hits in new sections); FMT-04 determinism gate PASSED (new-sections diff empty); test catalog files removed; git status is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| awk new-section scope | Scoped range extraction isolates new tooling sections from pre-existing Homebrew/App Store content — prevents false positives from formula names containing http substrings |
| /tmp gate files | Ephemeral; cleaned up in Task 2; never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | catalog → git push (integrated catalog) | mitigate | FMT-03 gate THIS PLAN: `awk '/^Claude Code Plugins$/,0'` scopes to new sections; `grep -E "(https?://|Bearer |[?&]key=|Authorization|sk-|ghp_|token)"` must return ZERO hits; any hit FAILS the phase before any catalog is committed; collectors emit name+transport only by construction |
| T-05-05 | Information Disclosure | /tmp gate files | accept | Files contain only catalog content (same as the .txt output); cleaned up immediately; no external access path |
| T-05-06 | Tampering | false-pass via empty awk output | mitigate | Non-empty guard: `[[ -s /tmp/gate_run1_new_sections.txt ]]` checked in Task 1 before any gate runs; 0-byte file aborts with explicit error message |
| T-05-07 | Tampering | false-pass via same-filename collision | mitigate | `sleep 1` between runs; RUN2_FILE != RUN1_FILE equality check in harness; diff of a file against itself would trivially pass — the check prevents this |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; pure shell execution only |
</threat_model>

<verification>
All three gates passed when this plan is complete:
- FMT-02: full run exits 0; all 13 section headers present in real catalog output
- FMT-03: zero secret patterns in new-sections region of real catalog
- FMT-04: two consecutive --no-commit runs produce byte-identical new-section content
- Repository is clean: no stray test catalog files committed or untracked after cleanup

Phase 5 is complete when both plans succeed.
</verification>

<success_criteria>
- ./update-list.sh --personal --no-commit exits 0
- All 13 new section headers found in produced catalog (FMT-02)
- awk-scoped grep -E returns ZERO secret-pattern matches (FMT-03 PASS)
- diff of new-sections from two consecutive runs is empty (FMT-04 PASS)
- git status is clean after test catalog cleanup (no committed or untracked test files)
</success_criteria>

<output>
Create `.planning/phases/05-integration-verification-gates/05-02-SUMMARY.md` when done.
</output>
