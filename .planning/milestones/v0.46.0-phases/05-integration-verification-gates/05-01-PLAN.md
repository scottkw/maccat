---
phase: 05-integration-verification-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - FMT-02

must_haves:
  truths:
    - "A single ./update-list.sh run produces all 13 new tooling sections after the Web-installed Applications block"
    - "The existing Homebrew, App Store, Setapp, and Web-installed sections are byte-unchanged"
    - "The archive flow and git flow are untouched — the only change is 13 added function calls"
    - "zsh -n update-list.sh exits 0 (no syntax errors)"
  artifacts:
    - path: "update-list.sh"
      provides: "generate_catalog with 13 wired collector calls in locked order"
      contains: "collect_claude_plugins"
  key_links:
    - from: "generate_catalog() body"
      to: "all 13 collect_* functions"
      via: "direct function calls after line 1463"
      pattern: "collect_claude_plugins"
---

<objective>
Wire the 13 collector functions built in Phases 2–4 into generate_catalog so a single
./update-list.sh run produces the full software + tooling catalog.

Purpose: This is the single committed code change for Phase 5. Every collector is already
defined and individually verified; this plan adds exactly 13 function calls inside
generate_catalog, in the locked order from CONTEXT.md, after the existing Web-installed
Applications block and before the closing brace of the function.

Output: update-list.sh with the 13 wiring calls inserted; all existing sections, archive
flow, and git flow untouched.
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.planning/phases/05-integration-verification-gates/05-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/05-integration-verification-gates/05-CONTEXT.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/PROJECT.md
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/STATE.md
@/Users/ken/dev/mac-software-list/update-list.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the 13-call wiring block into generate_catalog</name>
  <files>update-list.sh</files>
  <read_first>
    1. Read 05-RESEARCH.md "Flag 4: Wiring Placement + Order + Untouched-Flow Verification" and
       "Code Examples / Wiring Block" sections — these contain the EXACT block to insert, the
       exact insertion point (after the `sort >> "$OUTPUT_FILE"` pipeline, before the closing `}`
       of generate_catalog), and confirmation that archive/git flows are untouched.
    2. Read 05-CONTEXT.md "Collector Wiring Order & Placement (USER LOCKED)" — the locked order
       is authoritative; do not deviate.
    3. Read update-list.sh lines 1455–1464 to confirm the exact insertion point: the Web-installed
       Applications block ends with the `find ... | sort >> "$OUTPUT_FILE"` pipeline on line 1462–1463,
       and the closing `}` of generate_catalog is on line 1464.
  </read_first>
  <action>
    Edit update-list.sh to insert the wiring block between line 1463 and line 1464 (between the
    `sort >> "$OUTPUT_FILE"` line and the closing `}` of generate_catalog).

    The exact block to insert is in 05-RESEARCH.md "Code Examples / Wiring Block". It contains:
    - A comment section "# AI CLI Extensions & Plugins" with an echo progress line, then the 9
      AI CLI calls in this order: collect_claude_plugins, collect_claude_mcp,
      collect_claude_skills_agents, collect_codex_mcp, collect_opencode_plugins,
      collect_opencode_mcp, collect_opencode_agents, collect_gemini_extensions, collect_gemini_mcp
    - A comment section "# Editor Extensions" with an echo progress line, then:
      collect_vscode_extensions, collect_cursor_extensions
    - A comment section "# Browser Extensions" with an echo progress line, then:
      collect_chrome_extensions, collect_firefox_extensions

    Indentation: all lines are indented with 4 spaces to match the existing generate_catalog body.

    CRITICAL constraints (per D-01 in CONTEXT.md — USER LOCKED):
    - Do NOT modify any lines above the insertion point inside generate_catalog (Homebrew,
      App Store, Setapp, Web-installed blocks — lines 1407–1463).
    - Do NOT modify archive_old_catalogs, git_pull, or git_commit_and_push.
    - Do NOT add any inline section logic — each collector already calls write_section and
      flush_section internally; the wiring adds only function calls.
    - Do NOT change the insertion point. The calls go AFTER the Web-installed
      `sort >> "$OUTPUT_FILE"` and BEFORE the `}` on what was line 1464.

    After the edit, confirm the structure with a targeted read of the modified generate_catalog
    closing area to verify the 13 calls appear in order before the `}`.
  </action>
  <verify>
    <automated>
      # 1. Syntax check
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh && echo "SYNTAX OK"

      # 2. All 13 calls present in locked order (sequential occurrence in file)
      grep -n "collect_claude_plugins\|collect_claude_mcp\|collect_claude_skills_agents\|collect_codex_mcp\|collect_opencode_plugins\|collect_opencode_mcp\|collect_opencode_agents\|collect_gemini_extensions\|collect_gemini_mcp\|collect_vscode_extensions\|collect_cursor_extensions\|collect_chrome_extensions\|collect_firefox_extensions" \
        /Users/ken/dev/mac-software-list/update-list.sh \
        | grep -v "^[0-9]*:#\|^[0-9]*:.*function\|^[0-9]*:.*collect_" \
        | head -40

      # 3. Wiring calls are inside generate_catalog (appear after line 1399, before git_pull at ~1466)
      awk '/^generate_catalog\(\)/,/^git_pull\(\)/' \
        /Users/ken/dev/mac-software-list/update-list.sh \
        | grep -E "^\s+collect_"

      # 4. Existing sections untouched — verify git diff shows ONLY the 13 added call lines
      git -C /Users/ken/dev/mac-software-list diff update-list.sh | head -60
    </automated>
  </verify>
  <acceptance_criteria>
    - zsh -n update-list.sh exits 0
    - git diff update-list.sh shows ONLY the inserted wiring block (comment headers + 13 function
      calls + 3 echo lines) — no modifications to lines above the insertion point inside
      generate_catalog, no modifications to archive_old_catalogs, git_pull, or git_commit_and_push
    - The 13 calls appear inside generate_catalog in this exact order:
      collect_claude_plugins, collect_claude_mcp, collect_claude_skills_agents,
      collect_codex_mcp, collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents,
      collect_gemini_extensions, collect_gemini_mcp, collect_vscode_extensions,
      collect_cursor_extensions, collect_chrome_extensions, collect_firefox_extensions
    - `awk '/^generate_catalog/,/^git_pull/' update-list.sh | grep -c "collect_"` returns 13
      (one call per collector, no duplicates, no definition lines)
  </acceptance_criteria>
  <done>update-list.sh passes zsh -n; git diff shows exactly the 13-call wiring block added inside generate_catalog after the Web-installed sort line; all existing sections, archive flow, and git flow are byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → local filesystem | generate_catalog reads ~/.claude/, ~/.cursor/, ~/.vscode/, ~/.config/opencode/, Chrome/Firefox profiles — all local, no network |
| catalog file → git remote | The produced .txt file is committed and pushed; secret leakage here is the primary risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | catalog → git push | mitigate | FMT-03 gate in Plan 05-02 scopes grep to new sections via `awk '/^Claude Code Plugins$/,0'`; pattern catches https?://, Bearer, key=, Authorization, sk-, ghp_, token; collectors emit name+transport only by construction (verified Phases 3–4); gate FAILS phase on any hit |
| T-05-02 | Tampering | generate_catalog wiring block | mitigate | wiring adds only function calls (no new reads, no new env access); git diff reviewed in acceptance criteria; all 13 collector definitions are immutable (Phases 2–4 artifacts) |
| T-05-03 | Spoofing | none in scope | accept | pure local Zsh script; no auth surface; no new network calls added by this plan |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package manager installs in this plan; pure Zsh edit only |
</threat_model>

<verification>
After this plan:
- zsh -n update-list.sh exits 0
- git diff shows ONLY the insertion of the 13-call wiring block
- generate_catalog body contains all 13 calls in locked order after the Web-installed block
- Existing sections and flows are untouched (confirmed by git diff scope)

Full integration verification (real run + gates) is performed in Plan 05-02.
</verification>

<success_criteria>
- zsh syntax check passes
- Exactly 13 collector calls wired inside generate_catalog, in locked order, after the Web-installed Applications block, before the closing brace
- git diff on update-list.sh shows no changes outside the wiring block
</success_criteria>

<output>
Create `.planning/phases/05-integration-verification-gates/05-01-SUMMARY.md` when done.
</output>
