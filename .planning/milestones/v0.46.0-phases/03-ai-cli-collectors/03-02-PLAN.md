---
phase: 03-ai-cli-collectors
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - CDX-01
  - OC-01
  - OC-02
  - OC-03

must_haves:
  truths:
    - "Codex MCP Servers section writes (none found) because codex mcp list --json returns []"
    - "OpenCode Plugins section lists 'superpowers' (the one installed plugin)"
    - "OpenCode MCP Servers section writes (none found) because .mcp is null in opencode.json"
    - "OpenCode Agents section lists ~33 agent names from ~/.config/opencode/agents/*.md"
    - "All four collectors are defined in update-list.sh but NOT called from generate_catalog"
  artifacts:
    - path: "update-list.sh"
      provides: "collect_codex_mcp function (CLI-first, TOML fallback)"
      contains: "collect_codex_mcp"
    - path: "update-list.sh"
      provides: "collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents functions"
      contains: "collect_opencode_plugins"
  key_links:
    - from: "collect_codex_mcp"
      to: "codex mcp list --json or ~/.codex/config.toml"
      via: "CLI preferred; TOML grep fallback reads section headers only"
      pattern: "codex_mcp"
    - from: "collect_opencode_plugins"
      to: "~/.config/opencode/opencode.json .plugin array"
      via: "jq .plugin[]? — name = ${entry%%@*}"
      pattern: "opencode_plugins"
    - from: "collect_opencode_agents"
      to: "~/.config/opencode/agents/*.md"
      via: "null_glob glob + grep frontmatter name:"
      pattern: "opencode_agents"
---

<objective>
Insert four Zsh collector functions for Codex and OpenCode into update-list.sh:
`collect_codex_mcp` (CDX-01), `collect_opencode_plugins` (OC-01),
`collect_opencode_mcp` (OC-02), `collect_opencode_agents` (OC-03).

Purpose: Capture Codex and OpenCode tooling state. Codex has MCP-only coverage (no
plugin system in v0.46.0). OpenCode has three sections: plugins (bare names from the
.plugin string array), MCP servers (currently none — .mcp is null), and agents
(enumerated from the agents/ directory, same pattern as Claude agents).

Output: Four new functions appended after `collect_claude_skills_agents` (last
function inserted in Plan 03-01) and before `generate_catalog`. Functions defined
only — NOT called from generate_catalog (Phase 5 wires them).
</objective>

<execution_context>
@/Users/ken/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/phases/03-ai-cli-collectors/03-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/03-ai-cli-collectors/03-01-SUMMARY.md
@/Users/ken/dev/mac-software-list/update-list.sh

<interfaces>
<!-- Phase 1 helpers (unchanged): write_section, emit_item, flush_section, json_get -->
<!-- Phase 3 Plan 01 output: collect_claude_plugins, collect_claude_mcp, collect_claude_skills_agents now in update-list.sh -->

emit_item "$name" "$version" "$id"
  — MCP: emit_item "$name" "" "$transport" → "name [transport]"
  — Plugins (no version, no id): emit_item "$name" "" "" → bare "name"
  — Agents (no version, no id): emit_item "$name" "" "" → bare "name"

flush_section
  — Writes "(none found)" when _section_lines buffer is empty.

Key facts from research:
- codex mcp list --json returns [] on this machine (exit 0). When non-empty, each
  object has .name and .type fields (A1 assumption — verify if a server is added).
- TOML fallback: grep '^\[mcp_servers\.' + sed 's/^\[mcp_servers\.\(.*\)\]$/\1/' | tr -d '"'
  Extracts section-header names only; defaults transport to "stdio" (safe).
- opencode.json .plugin is ["superpowers@git+https://..."] — name = ${entry%%@*}
- opencode.json .mcp is null — section writes "(none found)" via flush_section
- ~/.config/opencode/agents/*.md — 33 files with name: frontmatter
- FMT-03 applies to collect_codex_mcp and collect_opencode_mcp:
  emit_item "$name" "" "$transport" only; never read env/command/args/url/headers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert collect_codex_mcp (CDX-01)</name>
  <files>update-list.sh</files>
  <read_first>
    - 03-RESEARCH.md "Pattern: Codex MCP Collector (CDX-01)" section (full code pattern)
    - 03-RESEARCH.md "Flag 2: Codex MCP — CLI vs TOML Fallback" section
    - 03-RESEARCH.md "Flag 5: Definitive FMT-03 Field Map — Codex MCP" table
    - 03-RESEARCH.md "Codex TOML Name Extraction" code example
  </read_first>
  <action>
Insert `collect_codex_mcp` immediately after the closing `}` of `collect_claude_skills_agents`.
Follow the exact pattern in 03-RESEARCH.md "Pattern: Codex MCP Collector (CDX-01)".

Key implementation requirements (per CDX-01 and FMT-03 per D-01):
- codex_config: `$HOME/.codex/config.toml`
- write_section "Codex MCP Servers" then _section_lines=() (defensive reset)
- Primary path (preferred): `if command -v codex &>/dev/null; then`
  - `cli_out=$(codex mcp list --json 2>/dev/null)`
  - If non-empty and not "[]": parse with jq: `jq -r '.[] | .name + "\t" + (.type // "stdio")'`
    IFS=$'\t' read -r name transport; clamp transport; emit_item "$name" "" "$transport"
  - After jq parse: flush_section + return (CLI path complete)
  - If "[]" or empty: fall through to TOML fallback (no entries to emit → TOML also empty → flush_section writes "(none found)")
- TOML fallback path (when CLI unavailable or returns no servers):
  - `if [[ -f "$codex_config" ]]; then`
  - `grep '^\[mcp_servers\.' "$codex_config" 2>/dev/null | sed 's/^\[mcp_servers\.\(.*\)\]$/\1/' | tr -d '"'`
  - while IFS= read -r name loop; emit_item "$name" "" "stdio" (transport defaults to stdio per TOML fallback)
  - KEY RULE (FMT-03): ONLY read section header names from TOML. Never read command/env/args/url values from TOML lines.
- flush_section at end (handles both empty and populated cases)
- Transport clamping after parse: `case "$transport" in stdio|http|sse) : ;; *) transport="stdio" ;; esac`
- All variables local; `[[ ]]`; `command -v`; `2>/dev/null`
  </action>
  <verify>
    <automated>zsh -n /Users/ken/dev/mac-software-list/update-list.sh && grep -c "^collect_codex_mcp()" /Users/ken/dev/mac-software-list/update-list.sh</automated>
  </verify>
  <done>
    - `zsh -n` exits 0
    - `grep -c "^collect_codex_mcp()" update-list.sh` returns 1
    - Function body references "codex mcp list --json" and config.toml
    - No `.env`, `.command`, `.args`, `.url`, `.headers` reads in the Codex TOML section of the function
    - generate_catalog does NOT contain a call to collect_codex_mcp
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents (OC-01, OC-02, OC-03)</name>
  <files>update-list.sh</files>
  <read_first>
    - 03-RESEARCH.md "Pattern: OpenCode Plugins Collector (OC-01)" section (full code pattern)
    - 03-RESEARCH.md "Flag 3: OpenCode Plugin and Agent Shapes" section
    - 03-RESEARCH.md "Pitfall 3: OpenCode .plugin array contains full git URLs with @"
    - 03-RESEARCH.md "Flag 5: Definitive FMT-03 Field Map — OpenCode MCP" table
  </read_first>
  <action>
Insert three functions back-to-back after `collect_codex_mcp`: `collect_opencode_plugins`,
`collect_opencode_mcp`, `collect_opencode_agents`. Follow the exact patterns in 03-RESEARCH.md.

**collect_opencode_plugins (OC-01):**
- oc_config: `$HOME/.config/opencode/opencode.json`
- write_section "OpenCode Plugins" then _section_lines=()
- If file absent: flush_section + return
- jq path: `jq -r '.plugin[]?' "$oc_config"` (the `?` handles null field silently)
  while IFS= read -r entry; name="${entry%%@*}"; emit_item "$name" "" ""
  (No version available for OpenCode plugins — bare name only per OC-01)
- plutil fallback: index loop from 0 via `plutil -extract "plugin.${idx}" raw -o -`; strip @source with ${entry%%@*}
- flush_section at end
- Do NOT emit the @source URL — only the bare name before the first @

**collect_opencode_mcp (OC-02 + FMT-03):**
- oc_config: same `$HOME/.config/opencode/opencode.json`
- write_section "OpenCode MCP Servers" then _section_lines=()
- If file absent: flush_section + return
- Check if .mcp is non-null: `jq -r '.mcp // empty' "$oc_config"` — if empty output → flush_section (writes "(none found)") + return
- If populated (future case — .mcp is null on this machine): extract name + type pairs using same FMT-03 pattern as Claude MCP: `jq -r '.mcp | to_entries[] | .key + "\t" + (.value.type // "stdio")'`; emit_item "$name" "" "$transport"; transport clamped
- plutil fallback for .mcp null check: `plutil -extract "mcp" raw` — if it errors or returns empty → flush_section
- flush_section at end

**collect_opencode_agents (OC-03):**
- agents_dir: `$HOME/.config/opencode/agents`
- write_section "OpenCode Agents" then _section_lines=()
- If dir absent: flush_section + return
- setopt local_options null_glob
- `for f in "$agents_dir"/*.md;` — `[[ -e "$f" ]] || continue`
  - `name=$(grep '^name:' "$f" | head -1 | sed 's/^name:[[:space:]]*//' | tr -d '"')`
  - Fallback: `name=$(basename "$f" .md)`
  - emit_item "$name" "" ""; reset name=""
- flush_section at end

All variables local; `[[ ]]`; `2>/dev/null`; null_glob guard on agent loop.
  </action>
  <verify>
    <automated>zsh -n /Users/ken/dev/mac-software-list/update-list.sh && grep -c "^collect_opencode_plugins()\|^collect_opencode_mcp()\|^collect_opencode_agents()" /Users/ken/dev/mac-software-list/update-list.sh</automated>
  </verify>
  <done>
    - `zsh -n` exits 0
    - `grep -c "^collect_opencode_plugins()"` returns 1, same for mcp and agents
    - collect_opencode_plugins uses `${entry%%@*}` (Pitfall 3 guard)
    - collect_opencode_mcp never reads .command/.env/.args/.url/.headers
    - collect_opencode_agents has `setopt local_options null_glob` + `[[ -e "$f" ]] || continue`
    - generate_catalog does NOT contain calls to any collect_opencode_* function
    - All four plan-02 functions present: collect_codex_mcp, collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codex mcp list --json → collector | CLI output is trusted local process; JSON array contains name/type plus secret-bearing fields that the collector must never read |
| ~/.codex/config.toml → collector | TOML section headers are the only safe extraction target; value lines must never be read |
| ~/.config/opencode/opencode.json → collector | .plugin array contains git URLs (not secrets); .mcp is currently null; when populated, same FMT-03 rules as Claude |
| ~/.config/opencode/agents/ → collector | Local .md files; name: frontmatter only; no credential fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | collect_codex_mcp — TOML fallback | mitigate | grep reads ONLY section headers `^\[mcp_servers\.`; sed extracts only the name portion; value lines (command, env, args) are never touched; TOML line iteration stops at section header level |
| T-03-05 | Information Disclosure | collect_codex_mcp — CLI JSON output | mitigate | jq reads only `.name` and `.type` from CLI array objects; transport label clamped to stdio/http/sse whitelist; `.command`, `.env`, `.args`, `.url`, `.headers` never read |
| T-03-06 | Information Disclosure | collect_opencode_mcp — populated .mcp future case | mitigate | Same FMT-03 jq pattern as Claude: `to_entries[] | .key + "\t" + (.value.type // "stdio")`; only key and type extracted; current null state means (none found) is the output today |
| T-03-07 | Information Disclosure | collect_opencode_plugins — git URL in @source suffix | accept | Only the name before first `@` is emitted via `${entry%%@*}`; the git+https:// source URL is never emitted; even if it contained a token in query string it would be stripped |
| T-03-SC | Tampering | npm/pip/cargo package installs | accept | Phase 3 installs no packages; not applicable |
</threat_model>

<verification>
After both tasks complete:

```zsh
# 1. Syntax check
zsh -n update-list.sh

# 2. All four functions present
grep "^collect_codex_mcp()\|^collect_opencode_plugins()\|^collect_opencode_mcp()\|^collect_opencode_agents()" update-list.sh

# 3. Not wired into generate_catalog
grep "collect_codex\|collect_opencode" update-list.sh | grep -v "^collect_\|^#"

# 4. FMT-03: no secret field reads in codex/opencode MCP collectors
grep -n "\.env\b\|\.command\b\|\.args\b\|\.url\b\|\.headers\b" update-list.sh | grep -v "^[0-9]*:#"

# 5. Live Codex MCP (expects (none found))
(source update-list.sh 2>/dev/null; OUTPUT_FILE=/tmp/p3-codex.txt; collect_codex_mcp) && cat /tmp/p3-codex.txt

# 6. Live OpenCode plugins (expects superpowers)
(source update-list.sh 2>/dev/null; OUTPUT_FILE=/tmp/p3-oc.txt; collect_opencode_plugins) && cat /tmp/p3-oc.txt

# 7. Live OpenCode agents (~33 entries)
(source update-list.sh 2>/dev/null; OUTPUT_FILE=/tmp/p3-oca.txt; collect_opencode_agents) && wc -l /tmp/p3-oca.txt
```
</verification>

<success_criteria>
- `zsh -n update-list.sh` exits 0
- collect_codex_mcp, collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents all defined
- Codex MCP Servers section writes "(none found)" (0 servers on this machine)
- OpenCode Plugins section writes "superpowers" (1 plugin)
- OpenCode MCP Servers section writes "(none found)" (.mcp is null)
- OpenCode Agents section lists ~33 agent names
- No call to any new collector appears inside generate_catalog
</success_criteria>

<output>
Create `.planning/phases/03-ai-cli-collectors/03-02-SUMMARY.md` when done
</output>
