---
phase: 03-ai-cli-collectors
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - GEM-01
  - GEM-02

must_haves:
  truths:
    - "Gemini CLI Extensions section lists 'conductor (0.4.1)' from ~/.gemini/extensions/conductor/"
    - "Gemini CLI MCP Servers section writes (none found) because mcp_config.json is 0 bytes"
    - "Both Gemini collectors are defined in update-list.sh but NOT called from generate_catalog"
    - "Gemini extension collector uses json_get for name+version and null_glob for dir iteration"
    - "Gemini MCP collector uses -s guard before JSON parsing to handle the empty file"
  artifacts:
    - path: "update-list.sh"
      provides: "collect_gemini_extensions function"
      contains: "collect_gemini_extensions"
    - path: "update-list.sh"
      provides: "collect_gemini_mcp function with -s empty-file guard"
      contains: "collect_gemini_mcp"
  key_links:
    - from: "collect_gemini_extensions"
      to: "~/.gemini/extensions/*/gemini-extension.json"
      via: "null_glob glob + json_get name + json_get version"
      pattern: "gemini_extensions"
    - from: "collect_gemini_mcp"
      to: "~/.gemini/config/mcp_config.json"
      via: "[[ -s ]] nonzero-size guard before jq parse"
      pattern: "gemini_mcp"
---

<objective>
Insert two Zsh collector functions for Gemini CLI into update-list.sh:
`collect_gemini_extensions` (GEM-01) and `collect_gemini_mcp` (GEM-02).

Purpose: Capture Gemini's tooling state. The extensions collector enumerates
`~/.gemini/extensions/` subdirectories and reads each `gemini-extension.json`
manifest for name+version. The MCP collector checks `~/.gemini/config/mcp_config.json`
with a nonzero-size guard (`-s`) because the file exists but is 0 bytes on this machine —
a plain `-f` check would yield a parse error from jq.

Output: Two new functions appended after `collect_opencode_agents` and before
`generate_catalog`. Functions defined only — NOT called from generate_catalog.
</objective>

<execution_context>
@/Users/ken/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/phases/03-ai-cli-collectors/03-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/03-ai-cli-collectors/03-02-SUMMARY.md
@/Users/ken/dev/mac-software-list/update-list.sh

<interfaces>
<!-- Phase 1 helpers: json_get, emit_item, flush_section, write_section -->

json_get "$file" "$key"
  — Returns scalar via jq or plutil fallback; returns "" on miss.
  — Use for reading "name" and "version" from gemini-extension.json.

emit_item "$name" "$version" "$id"
  — Gemini extensions: emit_item "$name" "$version" "" → "name (version)"
    (id empty because dir name == name; suppress identity bracket)
  — Gemini MCP: emit_item "$name" "" "$transport" → "name [transport]"

Key research facts (03-RESEARCH.md Flag 4):
- ext_base: $HOME/.gemini/extensions
- Each ext subdir contains gemini-extension.json with "name" and "version" fields.
- conductor manifest verified: {"name":"conductor","version":"0.4.1",...}
- extension-enablement.json is informational only — list ALL installed (all subdirs
  with gemini-extension.json), not just enabled ones.
- mcp_config: $HOME/.gemini/config/mcp_config.json
  File EXISTS but is 0 bytes — [[ -s "$mcp_config" ]] is the correct guard.
  [[ -f ]] returns true for a 0-byte file → jq parse error without -s guard.
- When mcp_config.json is populated: .mcpServers object, same shape as Claude.
  Extract: jq -r '.mcpServers | to_entries[] | .key + "\t" + (.value.type // "stdio")'
  FMT-03: ONLY read key and .value.type; never .value.env/.command/.args/.url/.headers
- Pitfall 4 (03-RESEARCH.md): mcp_config.json is 0 bytes — use [[ -s ]] guard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert collect_gemini_extensions (GEM-01)</name>
  <files>update-list.sh</files>
  <read_first>
    - 03-RESEARCH.md "Pattern: Gemini Extensions Collector (GEM-01)" section (full code pattern)
    - 03-RESEARCH.md "Flag 4: Gemini Extensions Manifest — GEM-01" section
    - 03-RESEARCH.md "Pitfall 5: Skills directory includes symlinks" (same null_glob pattern applies)
    - 03-RESEARCH.md "Gemini Extension Name + Version" code example
  </read_first>
  <action>
Insert `collect_gemini_extensions` immediately after the closing `}` of `collect_opencode_agents`.
Follow the exact pattern in 03-RESEARCH.md "Pattern: Gemini Extensions Collector (GEM-01)".

Key implementation requirements (per GEM-01):
- ext_base: `$HOME/.gemini/extensions`
- write_section "Gemini CLI Extensions" then _section_lines=() (defensive reset)
- If `$ext_base` dir absent: flush_section + return
- setopt local_options null_glob
- `for ext_dir in "$ext_base"/*/;` — `[[ -e "$ext_dir" ]] || continue`
  - manifest="${ext_dir}gemini-extension.json"
  - `[[ -f "$manifest" ]] || continue` (skip dirs without manifest)
  - `name=$(json_get "$manifest" "name")`
  - `version=$(json_get "$manifest" "version")`
  - `[[ -z "$name" ]] && name=$(basename "$ext_dir")`
  - emit_item "$name" "$version" ""  — id is empty (dir name == name → avoid redundant [conductor] bracket)
  - Reset name=""; version=""
- flush_section at end
- All variables local; `2>/dev/null` for any direct commands
- Expected output on this machine: "conductor (0.4.1)"
  </action>
  <verify>
    <automated>zsh -n /Users/ken/dev/mac-software-list/update-list.sh && grep -c "^collect_gemini_extensions()" /Users/ken/dev/mac-software-list/update-list.sh</automated>
  </verify>
  <done>
    - `zsh -n` exits 0
    - `grep -c "^collect_gemini_extensions()" update-list.sh` returns 1
    - Function body contains "gemini/extensions" and "gemini-extension.json"
    - `setopt local_options null_glob` present in function
    - `[[ -f "$manifest" ]] || continue` guard present
    - generate_catalog does NOT contain a call to collect_gemini_extensions
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert collect_gemini_mcp (GEM-02)</name>
  <files>update-list.sh</files>
  <read_first>
    - 03-RESEARCH.md "Flag 4: Gemini MCP Configuration Location — GEM-02" section
    - 03-RESEARCH.md "Pitfall 4: Gemini mcp_config.json is 0 bytes" (critical — must use [[ -s ]])
    - 03-RESEARCH.md "Flag 5: Definitive FMT-03 Field Map — Gemini MCP" table
    - 03-RESEARCH.md "Definitive Transport Inference Logic" table
  </read_first>
  <action>
Insert `collect_gemini_mcp` immediately after the closing `}` of `collect_gemini_extensions`.
This function has one critical implementation note: mcp_config.json EXISTS but is 0 bytes —
a `-f` guard is insufficient; `[[ -s "$mcp_config" ]]` (nonzero-size) is MANDATORY.

Key implementation requirements (per GEM-02 and FMT-03 per D-01):
- mcp_config: `$HOME/.gemini/config/mcp_config.json`
- write_section "Gemini CLI MCP Servers" then _section_lines=() (defensive reset)
- Guard: `if [[ ! -s "$mcp_config" ]]; then flush_section; return; fi`
  (covers: file absent, file exists but 0 bytes — BOTH cases → "(none found)")
- If file exists and has content (nonzero size):
  - jq path (FMT-03): `jq -r '.mcpServers | to_entries[] | .key + "\t" + (.value.type // "stdio")' "$mcp_config"`
    IFS=$'\t' read -r name transport; clamp transport; emit_item "$name" "" "$transport"
  - plutil fallback: enumerate `mcpServers` keys via `plutil -extract "mcpServers" raw -o -`;
    per key: `plutil -extract "mcpServers.${name}.type" raw -o -`
    (Same pattern as collect_claude_mcp plutil path — both are JSON objects with same shape)
  - FMT-03: ONLY read .key (server name) and .value.type; NEVER .value.env/.command/.args/.url/.headers
  - Transport clamping: `case "$transport" in stdio|http|sse) : ;; *) transport="stdio" ;; esac`
- flush_section at end
- All variables local; `[[ ]]`; `2>/dev/null`
  </action>
  <verify>
    <automated>zsh -n /Users/ken/dev/mac-software-list/update-list.sh && grep -c "^collect_gemini_mcp()" /Users/ken/dev/mac-software-list/update-list.sh</automated>
  </verify>
  <done>
    - `zsh -n` exits 0
    - `grep -c "^collect_gemini_mcp()" update-list.sh` returns 1
    - Function body uses `[[ -s "$mcp_config" ]]` or `[[ ! -s ... ]]` (nonzero-size guard — NOT bare `-f`)
    - Function body does NOT contain `[[ -f "$mcp_config" ]]` as the gating check (must be -s)
    - No `.env`, `.command`, `.args`, `.url`, `.headers` reads in the function
    - generate_catalog does NOT contain a call to collect_gemini_mcp
    - All nine Phase 3 collector functions now present: collect_claude_plugins, collect_claude_mcp, collect_claude_skills_agents, collect_codex_mcp, collect_opencode_plugins, collect_opencode_mcp, collect_opencode_agents, collect_gemini_extensions, collect_gemini_mcp
    - `zsh -n update-list.sh` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.gemini/extensions/*/gemini-extension.json → collector | Local manifest files; name+version fields only; no credential fields confirmed in conductor manifest |
| ~/.gemini/config/mcp_config.json → collector | Currently empty (0 bytes); when populated, will contain MCP server configs with potential secret fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | collect_gemini_mcp — mcp_config.json when populated | mitigate | Same FMT-03 jq pattern as Claude MCP: reads ONLY .key and .value.type; transport clamped to stdio/http/sse whitelist; never reads .value.env/.command/.args/.url/.headers |
| T-03-09 | Denial of Service | jq parse error on 0-byte mcp_config.json | mitigate | [[ -s "$mcp_config" ]] guard skips file when absent or zero-size; no jq invocation attempted on invalid file; flush_section writes "(none found)" cleanly |
| T-03-10 | Information Disclosure | gemini-extension.json malicious "name" or "version" field | accept | json_get returns the scalar string value; these are plain-text strings appended to a catalog file; no execution, no eval; worst case is a strange string in the output |
| T-03-SC | Tampering | npm/pip/cargo package installs | accept | Phase 3 installs no packages; not applicable |
</threat_model>

<verification>
After both tasks complete:

```zsh
# 1. Syntax check
zsh -n update-list.sh

# 2. Both Gemini functions present
grep "^collect_gemini_extensions()\|^collect_gemini_mcp()" update-list.sh

# 3. All nine Phase 3 collectors present
grep "^collect_claude_plugins()\|^collect_claude_mcp()\|^collect_claude_skills_agents()\|^collect_codex_mcp()\|^collect_opencode_plugins()\|^collect_opencode_mcp()\|^collect_opencode_agents()\|^collect_gemini_extensions()\|^collect_gemini_mcp()" update-list.sh | wc -l
# Expected: 9

# 4. MCP -s guard present in gemini_mcp
grep -A 10 "^collect_gemini_mcp()" update-list.sh | grep "\-s "
# Must contain [[ -s

# 5. Not wired into generate_catalog
grep "collect_gemini\|collect_claude\|collect_codex\|collect_opencode" update-list.sh | grep -v "^collect_\|^#"

# 6. Live Gemini extensions (expects conductor 0.4.1)
(source update-list.sh 2>/dev/null; OUTPUT_FILE=/tmp/p3-gem-ext.txt; collect_gemini_extensions) && cat /tmp/p3-gem-ext.txt

# 7. Live Gemini MCP (expects (none found))
(source update-list.sh 2>/dev/null; OUTPUT_FILE=/tmp/p3-gem-mcp.txt; collect_gemini_mcp) && cat /tmp/p3-gem-mcp.txt
```
</verification>

<success_criteria>
- `zsh -n update-list.sh` exits 0
- All 9 Phase 3 collectors defined in update-list.sh
- Gemini CLI Extensions section produces "conductor (0.4.1)"
- Gemini CLI MCP Servers section produces "(none found)"
- collect_gemini_mcp uses [[ -s ]] not [[ -f ]] as the file guard
- No collector is wired into generate_catalog
</success_criteria>

<output>
Create `.planning/phases/03-ai-cli-collectors/03-03-SUMMARY.md` when done
</output>
