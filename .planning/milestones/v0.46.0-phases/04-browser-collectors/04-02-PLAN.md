---
phase: 04-browser-collectors
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - FF-01
must_haves:
  truths:
    - "collect_firefox_extensions is defined in update-list.sh and produces a 'Firefox Extensions' section"
    - "All 6 app-profile addons from the default-release profile appear in name (version) [id] format"
    - "app-builtin and app-builtin-addons are excluded (12 of 18 total addons on this machine)"
    - "Firefox profiles are discovered via profiles.ini Path= entries (relative paths)"
    - "jq primary path uses tab-delimited IFS=$'\\t' read to handle spaces in addon names"
    - "plutil fallback iterates addons by index and filters location == app-profile"
    - "Firefox collector is NOT called from generate_catalog"
  artifacts:
    - path: "update-list.sh"
      provides: "collect_firefox_extensions function"
      contains: "collect_firefox_extensions"
  key_links:
    - from: "collect_firefox_extensions"
      to: "profiles.ini"
      via: "grep '^Path=' profiles.ini | sed 's/^Path=//'"
      pattern: "grep.*Path="
    - from: "collect_firefox_extensions"
      to: "emit_item → flush_section"
      via: "emit_item \"$name\" \"$version\" \"$id\" then flush_section after outer loop"
      pattern: "emit_item.*flush_section"
---

<objective>
Insert collect_firefox_extensions into update-list.sh after collect_chrome_extensions (added by Plan 01, Wave 1) and before generate_catalog.

Purpose: Satisfies FF-01 — catalogs Firefox extensions across all profiles, filtering to app-profile addons only (includes user themes, excludes built-in/system add-ons), with jq primary path and plutil index-loop fallback, merged and deduped across profiles via flush_section.

Output: update-list.sh gains collect_firefox_extensions; script passes zsh -n; function is defined but NOT called from generate_catalog (Phase 5 wires it).
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/STATE.md

<interfaces>
<!-- Key helpers already in update-list.sh (Phase 1). Executor uses these directly. -->

From update-list.sh (Phase 1 helpers):
```zsh
# emit_item <name> <version> <id>
# Appends "name (version) [id]" to _section_lines[]. Degrades gracefully on missing fields.
emit_item() { ... }

# flush_section
# LC_ALL=C sort -f -u on _section_lines[], writes to OUTPUT_FILE, resets buffer.
# Emits "(none found)" when buffer is empty.
flush_section() { ... }

# write_section <title>
write_section() { ... }
```

Insertion point: after the closing brace of collect_chrome_extensions (added by Plan 01), before the generate_catalog comment block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert collect_firefox_extensions into update-list.sh</name>
  <files>update-list.sh</files>
  <read_first>
    - update-list.sh: find closing brace of collect_chrome_extensions (added in Plan 01) — this is the insertion point
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: "collect_firefox_extensions — complete function" code block (Flag 3 profiles.ini parse, jq primary + plutil fallback, location filter) — SOURCE OF TRUTH for the implementation
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: Pitfall 4 (IFS=$'\t' for space-containing names), Pitfall 5 (defaultLocale capital L), Pitfall 6 (flush_section outside the profile loop), Pitfall 7 (_section_lines dirty state guard)
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: "Section Writing Flow" — degradation paths
  </read_first>
  <action>
Insert collect_firefox_extensions immediately after the closing brace of collect_chrome_extensions (from Plan 01), before the generate_catalog comment block. Preceded by a section comment block matching the Phase 3 comment style.

The function must implement all of the following (sourced from 04-RESEARCH.md "complete function" block):

1. Local vars declared: ff_dir, profiles_ini, rel_path, ext_json, name, version, id, idx, loc.
   ff_dir MUST be set to "$HOME/Library/Application Support/Firefox" (quoted — contains a space).
   profiles_ini="${ff_dir}/profiles.ini".

2. Call write_section "Firefox Extensions" then reset _section_lines=() (defensive reset per Phase 1 contract / Pitfall 7).

3. Guard: if [[ ! -f "$profiles_ini" ]], echo "  NOTE: Firefox not installed." and call flush_section then return.

4. Outer loop: while IFS= read -r rel_path over the output of grep '^Path=' "$profiles_ini" 2>/dev/null | sed 's/^Path=//' (process substitution). Guard [[ -z "$rel_path" ]] && continue. Build ext_json="${ff_dir}/${rel_path}/extensions.json". Guard [[ -f "$ext_json" ]] || continue.

5. Inside the outer loop, per profile — jq primary path (if command -v jq &>/dev/null):
   - while IFS=$'\t' read -r name version id over process substitution:
     jq -r '.addons[] | select(.location == "app-profile") | "\(.defaultLocale.name // .id)\t\(.version // "")\t\(.id)"' "$ext_json" 2>/dev/null
   - Guard [[ -z "$id" ]] && continue. Guard [[ -z "$name" ]] && name="$id". Call emit_item "$name" "$version" "$id".
   - CRITICAL: field is defaultLocale (capital L) not defaultlocale. Tab-separated to handle spaces in names (Pitfall 4). Do NOT use for..in over $(jq ...) — splits on spaces.

6. plutil fallback path (else branch — when jq absent):
   - idx=0, while true loop: loc=$(plutil -extract "addons.${idx}.location" raw -o - "$ext_json" 2>/dev/null) || break
   - If [[ "$loc" == "app-profile" ]]: extract name via plutil -extract "addons.${idx}.defaultLocale.name" raw, version via "addons.${idx}.version", id via "addons.${idx}.id" (each || name/version/id=""). Guard [[ -z "$id" ]] && { ((idx++)); continue; }. Guard [[ -z "$name" ]] && name="$id". Call emit_item "$name" "$version" "$id".
   - ((idx++)) at bottom of loop regardless of location filter result.

7. After the entire outer while loop (all profiles processed), call flush_section ONCE (NOT inside the profile loop — Pitfall 6). Cross-profile dedup happens here via LC_ALL=C sort -f -u.

Critical quoting requirement: every variable expansion derived from $ff_dir or $profiles_ini MUST be double-quoted. "Application Support" contains a space.

Do NOT wire the function into generate_catalog. Do NOT add a call site anywhere.
  </action>
  <verify>
    <automated>
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh
      grep -c '^collect_firefox_extensions()' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c "IFS=\$'\\\\t'" /Users/ken/dev/mac-software-list/update-list.sh
      grep -c 'defaultLocale\.name' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c "location.*app-profile\|app-profile.*location" /Users/ken/dev/mac-software-list/update-list.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - zsh -n update-list.sh exits 0 (no syntax errors)
    - grep -c '^collect_firefox_extensions()' returns 1 (function defined exactly once)
    - IFS=$'\t' read pattern present in collect_firefox_extensions (tab-delimited jq read)
    - defaultLocale.name (capital L) appears in the jq expression (not defaultlocale)
    - select(.location == "app-profile") appears in the jq expression
    - plutil fallback with index-loop (while true / addons.${idx}.location) present
    - flush_section is called once, after the outer while read loop (not inside it)
    - _section_lines=() reset present at top of function
    - generate_catalog does NOT contain a call to collect_firefox_extensions
    - Both collect_chrome_extensions and collect_firefox_extensions are defined in the file
  </acceptance_criteria>
  <done>collect_firefox_extensions is defined in update-list.sh with: profiles.ini Path= parsing, app-profile location filter, jq tab-delimited primary path, plutil index-loop fallback, emit_item/flush_section output pipeline (flush after outer loop), graceful degradation when Firefox not installed. zsh -n passes. NOT called from generate_catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → script | extensions.json and profiles.ini under user-controlled Firefox profile directories are read; addon name/version/id fields emitted to catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | extensions.json path traversal | mitigate | profiles.ini Path= values are relative paths; full path constructed as "${ff_dir}/${rel_path}/extensions.json" guarded by `[[ -f "$ext_json" ]] || continue` before any read; 2>/dev/null suppresses errors |
| T-04-03 | Information Disclosure | catalog output | accept | Addon names/versions/IDs are public metadata (same as Firefox Add-ons site); no credentials involved; FMT-03 does not apply |
| T-04-04 | Denial of Service | malformed extensions.json | accept | jq returns empty output on parse error (2>/dev/null); plutil returns non-zero and the loop breaks; no unbounded processing on corrupt input |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 4 installs no packages |
</threat_model>

<verification>
After plan execution:
- zsh -n update-list.sh exits 0
- collect_firefox_extensions defined exactly once in update-list.sh
- Both collect_chrome_extensions and collect_firefox_extensions present in file
- jq path uses IFS=$'\t' and select(.location == "app-profile") with .defaultLocale.name (capital L)
- plutil fallback iterates by index with break on non-zero exit
- flush_section called once, after outer profile loop
- generate_catalog does NOT contain collect_firefox_extensions
</verification>

<success_criteria>
- collect_firefox_extensions is defined in update-list.sh, positioned after collect_chrome_extensions and before generate_catalog
- The function correctly implements: profiles.ini Path= iteration, extensions.json presence guard, jq tab-delimited addon extraction with app-profile filter, plutil index-loop fallback, emit_item/flush_section output pipeline with flush after outer loop, graceful degradation when Firefox not installed
- zsh -n update-list.sh exits 0
- The function is NOT called from generate_catalog
</success_criteria>

<output>
Create `.planning/phases/04-browser-collectors/04-02-SUMMARY.md` when done
</output>
