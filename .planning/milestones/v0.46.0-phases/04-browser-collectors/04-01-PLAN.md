---
phase: 04-browser-collectors
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - update-list.sh
autonomous: true
requirements:
  - CHR-01
must_haves:
  truths:
    - "collect_chrome_extensions is defined in update-list.sh and produces a 'Google Chrome Extensions' section"
    - "All 7 user-installed Chrome extensions appear in name (version) [id] format"
    - "The Chrome Web Store Payments component (nmmhkkegccagdldgiimedpiccmgmieda) does NOT appear in output"
    - "No raw __MSG_ string appears in the Chrome section"
    - "Chrome version selection uses sort -V (not lexical sort)"
    - "All Application Support paths with spaces are double-quoted"
    - "Chrome collector is NOT called from generate_catalog"
  artifacts:
    - path: "update-list.sh"
      provides: "collect_chrome_extensions function"
      contains: "collect_chrome_extensions"
  key_links:
    - from: "collect_chrome_extensions"
      to: "chrome_ext_name (Phase 1 helper)"
      via: "name=$(chrome_ext_name \"$manifest\")"
      pattern: "chrome_ext_name"
    - from: "collect_chrome_extensions"
      to: "emit_item → flush_section"
      via: "emit_item \"$name\" \"$version\" \"$ext_id\""
      pattern: "emit_item.*flush_section"
---

<objective>
Insert collect_chrome_extensions into update-list.sh after collect_gemini_mcp (~line 1239) and before generate_catalog.

Purpose: Satisfies CHR-01 — catalogs Google Chrome extensions across all profiles with human-readable names resolved via the Phase 1 chrome_ext_name helper, components excluded, and deterministic sorted output.

Output: update-list.sh gains collect_chrome_extensions; script passes zsh -n; function is defined but NOT called from generate_catalog (Phase 5 wires it).
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/STATE.md

<interfaces>
<!-- Key helpers already in update-list.sh (Phase 1). Executor uses these directly. -->

From update-list.sh (lines ~259–476):
```zsh
# chrome_ext_name <manifest_path>
# Resolves __MSG_<key>__ via _locales/<default_locale>/messages.json (case-insensitive).
# Falls back to the 32-char extension ID (grandparent dir of manifest) if resolution fails.
# Never returns blank. Never returns a raw __MSG_ string.
chrome_ext_name() { ... }

# json_get <file> <key>
# Reads scalar field from JSON. jq primary, plutil fallback, returns "" if absent.
json_get() { ... }

# emit_item <name> <version> <id>
# Appends "name (version) [id]" to _section_lines[]. Degrades gracefully on missing fields.
emit_item() { ... }

# flush_section
# Sorts _section_lines[] via LC_ALL=C sort -f -u, writes to OUTPUT_FILE, resets buffer.
# Emits "(none found)" when _section_lines is empty.
flush_section() { ... }

# write_section <title>
# Writes section separator + title to OUTPUT_FILE.
write_section() { ... }
```

Insertion point: after line 1238 (closing brace of collect_gemini_mcp), before line 1240 (comment block for generate_catalog).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert collect_chrome_extensions into update-list.sh</name>
  <files>update-list.sh</files>
  <read_first>
    - update-list.sh: lines 1195–1252 (collect_gemini_mcp through generate_catalog start) — find the exact insertion point after line 1238
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: "collect_chrome_extensions — complete function" code block (Flag 1 denylist, Flag 2 version selection, Flag 5 null-glob pattern) — this is the SOURCE OF TRUTH for the implementation
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: "Section Writing Flow" — the exact section-open / write_section / _section_lines=() / flush_section pattern
  </read_first>
  <action>
Insert collect_chrome_extensions immediately after the closing brace of collect_gemini_mcp (currently line 1238), preceded by a section comment block matching the existing Phase 3 comment style.

The function must implement all of the following (sourced from 04-RESEARCH.md "complete function" block):

1. Local vars declared: chrome_base, ext_dir, ext_id, ver_dir, manifest, name, version, profile_dir.
   chrome_base MUST be set to "$HOME/Library/Application Support/Google/Chrome" (quoted — contains a space).

2. Call write_section "Google Chrome Extensions" then reset _section_lines=() (defensive reset per Phase 1 contract).

3. Guard: if [[ ! -d "$chrome_base" ]], echo "  NOTE: Google Chrome not installed." and call flush_section then return.

4. setopt local_options null_glob — set ONCE at top of the loop block, covers all glob expansions in this function invocation.

5. Outer loop: iterate "$chrome_base/Default" and "$chrome_base"/Profile\ */ (backslash-space quotes the glob pattern). For each, guard with [[ -d "${profile_dir}/Extensions" ]] || continue.

6. Inner loop: iterate "${profile_dir}/Extensions"/*/ (glob). Guard with [[ -e "$ext_dir" ]] || continue (null-glob guard per established pattern).

7. ext_id=$(basename "$ext_dir"). Skip if ext_id == "Temp".

8. Skip component IDs via case statement — all 10 IDs from 04-RESEARCH.md Flag 1 denylist (use the exact case..esac from that section; no associative array needed). The 10 IDs are listed verbatim in 04-RESEARCH.md.

9. Version selection: ver_dir=$(ls -1 "$ext_dir" 2>/dev/null | sort -V | tail -1). Guard [[ -z "$ver_dir" ]] && continue. Build manifest="${ext_dir}${ver_dir}/manifest.json". Guard [[ -f "$manifest" ]] || continue.

10. name=$(chrome_ext_name "$manifest") and version=$(json_get "$manifest" "version"). Call emit_item "$name" "$version" "$ext_id".

11. After both loops complete, call flush_section once (NOT inside the profile loop — cross-profile dedup happens at flush time per 04-RESEARCH.md Flag 4 / Pitfall 6).

Critical quoting requirement (from 04-RESEARCH.md Pitfall 2): every variable expansion that contains or is derived from $chrome_base or $profile_dir MUST be double-quoted. "Application Support" contains a space; unquoted expansions split on it.

Do NOT wire the function into generate_catalog. Do NOT add a call site anywhere.
  </action>
  <verify>
    <automated>
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh
      grep -c '^collect_chrome_extensions()' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c 'sort -V' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c 'nmmhkkegccagdldgiimedpiccmgmieda' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c 'generate_catalog.*collect_chrome\|collect_chrome.*generate_catalog' /Users/ken/dev/mac-software-list/update-list.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - zsh -n update-list.sh exits 0 (no syntax errors)
    - grep -c '^collect_chrome_extensions()' returns 1 (function defined exactly once)
    - grep -c 'sort -V' returns >= 1 (version selection present)
    - grep -c 'nmmhkkegccagdldgiimedpiccmgmieda' returns >= 1 (denylist present)
    - All 10 denylist IDs appear in the case statement (verify by counting IDs in the case block)
    - The string 'setopt local_options null_glob' appears inside collect_chrome_extensions
    - generate_catalog does NOT contain a call to collect_chrome_extensions
    - The function appears between collect_gemini_mcp and generate_catalog in the file
  </acceptance_criteria>
  <done>collect_chrome_extensions is defined in update-list.sh with: sort -V version selection, 10-ID denylist, null_glob guard, chrome_ext_name name resolution, emit_item/flush_section output, and is NOT called from generate_catalog. zsh -n passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → script | Manifest files and _locales JSON under user-controlled Chrome profile directories are read and their content (name, version) emitted to the catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | manifest.json / _locales JSON path | mitigate | Guard every manifest path with `[[ -f "$manifest" ]] || continue`; `chrome_ext_name` and `json_get` use `2>/dev/null` to suppress read errors; no shell eval of file contents |
| T-04-02 | Information Disclosure | catalog output | accept | Extension names/versions/IDs are public metadata (same data on Chrome Web Store); no credentials present; FMT-03 does not apply to browser extensions |
| T-04-03 | Denial of Service | malformed/infinite manifest | accept | `json_get` returns empty string on missing/malformed fields; `chrome_ext_name` falls back to ID; no unbounded loops on file content |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 4 installs no packages; no package manager invocations |
</threat_model>

<verification>
After plan execution:
- zsh -n update-list.sh exits 0
- collect_chrome_extensions defined exactly once in update-list.sh
- All 10 component IDs present in the denylist case statement
- sort -V appears in the function (version selection)
- setopt local_options null_glob appears in the function
- chrome_ext_name called inside the function
- flush_section called exactly once, after the outer profile loop
- generate_catalog does NOT contain collect_chrome_extensions
</verification>

<success_criteria>
- collect_chrome_extensions is defined in update-list.sh, positioned after collect_gemini_mcp and before generate_catalog
- The function correctly implements: profile enumeration (Default + Profile */), extension dir iteration with null_glob guard, sort -V version selection, 10-ID component denylist (case statement), chrome_ext_name name resolution, emit_item/flush_section output pipeline, graceful degradation when Chrome not installed
- zsh -n update-list.sh exits 0
- The function is NOT called from generate_catalog
</success_criteria>

<output>
Create `.planning/phases/04-browser-collectors/04-01-SUMMARY.md` when done
</output>
