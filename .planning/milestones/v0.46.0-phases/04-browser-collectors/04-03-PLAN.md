---
phase: 04-browser-collectors
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: true
requirements:
  - CHR-01
  - FF-01
must_haves:
  truths:
    - "Chrome collector emits exactly 7 user extension lines on this machine"
    - "Bitwarden Password Manager (2026.5.1) [nngceckbapebfimnlniiiahkandclblb] appears in Chrome output"
    - "nmmhkkegccagdldgiimedpiccmgmieda (Chrome Web Store Payments) does NOT appear in Chrome output"
    - "No raw __MSG_ string appears in Chrome output"
    - "Firefox collector emits exactly 6 app-profile addon lines on this machine"
    - "Vue.js devtools (7.7.7) [{5caff8cc-3d2e-4110-a88a-003cc85b3858}] appears in Firefox output"
    - "Two consecutive runs of each collector produce byte-identical output (determinism)"
    - "Script syntax passes zsh -n after both collectors are inserted"
  artifacts:
    - path: "/tmp/chrome-test-run1.txt"
      provides: "First Chrome collector run output (ephemeral)"
    - path: "/tmp/chrome-test-run2.txt"
      provides: "Second Chrome collector run output (ephemeral)"
    - path: "/tmp/firefox-test-run1.txt"
      provides: "First Firefox collector run output (ephemeral)"
    - path: "/tmp/firefox-test-run2.txt"
      provides: "Second Firefox collector run output (ephemeral)"
  key_links:
    - from: "self-test harness"
      to: "collect_chrome_extensions + collect_firefox_extensions"
      via: "source update-list.sh; collect_chrome_extensions; collect_firefox_extensions"
      pattern: "source.*update-list.sh"
---

<objective>
Run an ephemeral self-test of both browser collectors against this machine's real Chrome and Firefox data. Verify the live-verified fixtures (7 Chrome extensions, 6 Firefox addons), component exclusion, no raw __MSG_ strings, and byte-identical determinism across two consecutive runs. No modifications to the committed catalog flow.

Purpose: Provides automated evidence that CHR-01 and FF-01 are met by the implementations from Plans 01 and 02 before Phase 5 wires them into generate_catalog.

Output: All assertions pass, printed to stdout. Ephemeral test files in /tmp (auto-cleaned). No changes to update-list.sh or any catalog file.
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/ROADMAP.md
@/Users/ken/dev/mac-software-list/.planning/phases/04-browser-collectors/04-01-SUMMARY.md
@/Users/ken/dev/mac-software-list/.planning/phases/04-browser-collectors/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-test — source collectors, run against real data, assert live-verified fixtures</name>
  <files></files>
  <read_first>
    - update-list.sh: confirm collect_chrome_extensions and collect_firefox_extensions are present (grep for function definitions)
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: "Verified live output" section — the exact expected fixture lines for Chrome (7 extensions) and Firefox (6 addons); use these as assertion strings
    - .planning/phases/04-browser-collectors/04-RESEARCH.md: Flag 5 "Determinism confirmation" — how to structure the two-run byte-identical check
  </read_first>
  <action>
Run the self-test inline as a Zsh script (no new file committed). The test:

1. Verify function presence (prerequisite check):
   - grep -c '^collect_chrome_extensions()' update-list.sh — assert == 1
   - grep -c '^collect_firefox_extensions()' update-list.sh — assert == 1
   - If either is 0, print FAIL and stop (Plans 01/02 must run first).

2. Build a minimal Zsh harness that sources the script's helpers without running the main block:
   - Set required globals that the helpers depend on: OUTPUT_FILE=/tmp/browser-test-output.txt, _section_lines=()
   - Source only the function definitions by extracting them with sed or by sourcing update-list.sh with a guarded main block
   - Preferred approach: write a small inline test script to /tmp/browser-collector-test.zsh that:
     a. Sets OUTPUT_FILE=/tmp/browser-test-run1.txt; _section_lines=()
     b. Sources update-list.sh (the main block is guarded by the script's own [[ "$0" == ... ]] check, but if not guarded, define the necessary stubs first — check the script's main block structure)
     c. Calls collect_chrome_extensions (writes "Google Chrome Extensions" section to OUTPUT_FILE)
     d. Calls collect_firefox_extensions (writes "Firefox Extensions" section to OUTPUT_FILE)
     e. Saves /tmp/browser-test-run1.txt
     f. Repeats with OUTPUT_FILE=/tmp/browser-test-run2.txt to get the second run

   NOTE: If sourcing update-list.sh triggers the main block (which calls get_target_location interactively), the approach is:
   - Extract only the function bodies needed: use sed to extract lines from the first helper function through the end of collect_firefox_extensions, then source that extracted block
   - This avoids triggering interactive prompts

3. Run assertions:
   a. Chrome line count: grep -c 'Google Chrome Extensions' /tmp/browser-test-run1.txt is >=1 (section header present)
   b. Chrome extension count: count non-header, non-empty lines in the Chrome section — assert exactly 7
   c. Bitwarden fixture: grep 'Bitwarden Password Manager (2026.5.1) \[nngceckbapebfimnlniiiahkandclblb\]' /tmp/browser-test-run1.txt — assert found (exit 0)
   d. Component exclusion: grep 'nmmhkkegccagdldgiimedpiccmgmieda' /tmp/browser-test-run1.txt — assert NOT found (exit 1)
   e. No raw __MSG_: grep '__MSG_' /tmp/browser-test-run1.txt — assert NOT found
   f. Firefox section present: grep 'Firefox Extensions' /tmp/browser-test-run1.txt — assert found
   g. Firefox addon count: count non-header, non-empty lines in the Firefox section — assert exactly 6
   h. Vue.js devtools fixture: grep 'Vue.js devtools (7.7.7) \[{5caff8cc-3d2e-4110-a88a-003cc85b3858}\]' /tmp/browser-test-run1.txt — assert found
   i. Determinism: diff /tmp/browser-test-run1.txt /tmp/browser-test-run2.txt — assert empty diff (exit 0)

4. Print PASS/FAIL for each assertion with the assertion name. If all pass, print "ALL ASSERTIONS PASSED".

5. Cleanup: rm -f /tmp/browser-test-run1.txt /tmp/browser-test-run2.txt /tmp/browser-test-output.txt /tmp/browser-collector-test.zsh

The self-test MUST NOT write to any catalog file in personal/ or office/. MUST NOT call git. MUST NOT call generate_catalog. All output goes to /tmp paths only.
  </action>
  <verify>
    <automated>
      zsh -n /Users/ken/dev/mac-software-list/update-list.sh
      grep -c '^collect_chrome_extensions()' /Users/ken/dev/mac-software-list/update-list.sh
      grep -c '^collect_firefox_extensions()' /Users/ken/dev/mac-software-list/update-list.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - zsh -n update-list.sh exits 0
    - Self-test assertion: Chrome section contains exactly 7 extension lines
    - Self-test assertion: "Bitwarden Password Manager (2026.5.1) [nngceckbapebfimnlniiiahkandclblb]" found in Chrome output
    - Self-test assertion: "nmmhkkegccagdldgiimedpiccmgmieda" does NOT appear in Chrome output
    - Self-test assertion: No "__MSG_" string appears anywhere in either section's output
    - Self-test assertion: Firefox section contains exactly 6 addon lines
    - Self-test assertion: "Vue.js devtools (7.7.7) [{5caff8cc-3d2e-4110-a88a-003cc85b3858}]" found in Firefox output
    - Self-test assertion: diff of run1 vs run2 is empty (determinism)
    - ALL ASSERTIONS PASSED printed by the harness
    - No catalog files in personal/ or office/ modified
    - All /tmp test files cleaned up
  </acceptance_criteria>
  <done>All self-test assertions pass for both collectors. Chrome: 7 extensions, Bitwarden present, Web Store component excluded, no raw __MSG_. Firefox: 6 app-profile addons, Vue.js devtools present. Determinism confirmed (diff empty). /tmp files cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → /tmp | Test output written to /tmp only; no catalog flow involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Self-test /tmp files | accept | /tmp files are ephemeral and cleaned up after each run; no sensitive data (extension names are public metadata) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Self-test installs nothing |
</threat_model>

<verification>
After plan execution:
- zsh -n update-list.sh exits 0
- All 9 self-test assertions pass and print PASS
- "ALL ASSERTIONS PASSED" printed
- No personal/ or office/ catalog files modified
- /tmp test files removed
</verification>

<success_criteria>
Phase 4 success criteria from ROADMAP.md are met:
1. "Google Chrome Extensions" section produced with 7 user extensions in name (version) [id] format; __MSG_ resolved; component excluded
2. "Firefox Extensions" section produced with 6 app-profile addons; built-in/system add-ons excluded
3. Missing-browser degradation: both collectors degrade to "(none found)" note when browser absent
4. Determinism: two consecutive runs produce byte-identical output (diff empty)
</success_criteria>

<output>
Create `.planning/phases/04-browser-collectors/04-03-SUMMARY.md` when done
</output>
