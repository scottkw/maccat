---
phase: 02-editor-collectors
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: true
requirements:
  - VSC-01
  - CUR-01
must_haves:
  truths:
    - "Running collect_vscode_extensions against real ~/.vscode/extensions/extensions.json produces >=1 line matching 'name (version) [id]' format"
    - "Running collect_cursor_extensions against real ~/.cursor/extensions/extensions.json produces >=1 line matching 'name (version) [id]' format"
    - "A %key% NLS placeholder extension resolves to its real displayName string (no raw %...% in output)"
    - "An extension with a missing package.json falls back to the extension ID as name; run continues"
    - "Two consecutive collector runs on unchanged extensions produce byte-identical output (determinism)"
    - "zsh -n update-list.sh exits 0 after all insertions"
  artifacts:
    - path: "/tmp/gsd-phase2-test.zsh"
      provides: "ephemeral self-test script (not committed)"
      contains: "assert_eq"
  key_links:
    - from: "self-test"
      to: "update-list.sh resolve_vsc_ext_name"
      via: "source update-list.sh in test harness with stub OUTPUT_FILE"
      pattern: "source.*update-list.sh|resolve_vsc_ext_name"
---

<objective>
Run an ephemeral self-test in /tmp that exercises both collectors against real extension data on this machine AND synthetic fixtures for edge cases (%nls% placeholder, missing package.json). The test script is never committed.

Purpose: Confirm that the Phase 2 implementations (02-01-PLAN) satisfy all four Phase 2 success criteria before planning is declared complete. Mirrors the Phase 1 self-test approach (01-01-PLAN Task 2).
Output: All assertions pass; results printed to stdout. No files committed.
</objective>

<execution_context>
@/Users/ken/dev/mac-software-list/.planning/phases/02-editor-collectors/02-02-PLAN.md
</execution_context>

<context>
@/Users/ken/dev/mac-software-list/.planning/phases/02-editor-collectors/02-RESEARCH.md
@/Users/ken/dev/mac-software-list/.planning/phases/01-shared-helpers-foundation/01-01-SUMMARY.md
@/Users/ken/dev/mac-software-list/update-list.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run ephemeral self-test for Phase 2 collectors</name>
  <files>/tmp/gsd-phase2-test.zsh</files>

  <read_first>
    1. Read 02-RESEARCH.md "End-to-end output (verified on this machine)" section — the expected sorted VS Code output (22 extensions) is listed there. The test can spot-check a known entry like "Auto Rename Tag (0.1.10) [formulahendry.auto-rename-tag]".
    2. Read 02-RESEARCH.md "Full verification results (5 NLS extensions)" table — use one of the five verified NLS extensions for the synthetic fixture test (e.g. visualstudioexptteam.intellicode-api-usage-examples with %extension.title% → "IntelliCode API Usage Examples").
    3. Read 01-01-SUMMARY.md to recall the harness pattern: source helpers from update-list.sh with stub OUTPUT_FILE, then call assert_eq.
    4. Read update-list.sh lines 469-476 (flush_section) to understand _section_lines global — the test harness must set OUTPUT_FILE to a /tmp path before sourcing.
  </read_first>

  <action>
  Write /tmp/gsd-phase2-test.zsh as a self-contained Zsh test harness. Do NOT use setopt err_exit (learned from Phase 1 deviation — it causes early abort on subshell non-zero exit). Use explicit if/then assertion blocks.

  **Harness setup:**
  - Set OUTPUT_FILE to /tmp/gsd-phase2-test-output.txt (writable; cleared between test groups)
  - Set _section_lines=() (global array required by emit_item/flush_section)
  - Source update-list.sh functions by extracting them via awk (line-number range of the helper+collector block), writing to /tmp/gsd-phase2-helpers.zsh, and sourcing that file. This avoids executing the script's main block.
    - Alternatively: source update-list.sh with a guard that prevents the main block from running. The main block starts at "# == MAIN SCRIPT EXECUTION ==" (line ~673). Extract lines 259 through that comment using awk NR range, source the extracted file.
  - PASS/FAIL counter: pass_count=0, fail_count=0

  **assert_eq helper (same as Phase 1 pattern):**
    assert_eq() {
      local label="$1" expected="$2" actual="$3"
      if [[ "$actual" == "$expected" ]]; then
        echo "  PASS: $label"
        ((pass_count++))
      else
        echo "  FAIL: $label"
        echo "    expected: '$expected'"
        echo "    actual:   '$actual'"
        ((fail_count++))
      fi
    }

  **Test group A — resolve_vsc_ext_name: plain displayName**
    - Create /tmp/gsd-p2-plain/package.json with {"displayName": "Error Lens", "version": "3.28.0"}
    - result=$(resolve_vsc_ext_name "/tmp/gsd-p2-plain/package.json" "usernamehw.errorlens")
    - assert_eq "A1: plain displayName" "Error Lens" "$result"

  **Test group B — resolve_vsc_ext_name: missing displayName falls back to ID**
    - Create /tmp/gsd-p2-nodisplay/package.json with {"version": "1.0.0"} (no displayName key)
    - result=$(resolve_vsc_ext_name "/tmp/gsd-p2-nodisplay/package.json" "publisher.ext-id")
    - assert_eq "B1: missing displayName → ID fallback" "publisher.ext-id" "$result"

  **Test group C — resolve_vsc_ext_name: %key% placeholder with flat NLS key**
    - Create /tmp/gsd-p2-nls/ directory
    - /tmp/gsd-p2-nls/package.json: {"displayName": "%displayName%", "version": "0.2.9"}
    - /tmp/gsd-p2-nls/package.nls.json: {"displayName": "HTML Preview"}
    - result=$(resolve_vsc_ext_name "/tmp/gsd-p2-nls/package.json" "george-alisson.html-preview-vscode")
    - assert_eq "C1: %displayName% NLS resolution" "HTML Preview" "$result"

  **Test group D — resolve_vsc_ext_name: dotted NLS key (e.g. extension.title)**
    - Create /tmp/gsd-p2-dotkey/ directory
    - /tmp/gsd-p2-dotkey/package.json: {"displayName": "%extension.title%", "version": "0.2.9"}
    - /tmp/gsd-p2-dotkey/package.nls.json: {"extension.title": "IntelliCode API Usage Examples"}
    - result=$(resolve_vsc_ext_name "/tmp/gsd-p2-dotkey/package.json" "visualstudioexptteam.intellicode-api-usage-examples")
    - assert_eq "D1: dotted NLS key %extension.title%" "IntelliCode API Usage Examples" "$result"

  **Test group E — resolve_vsc_ext_name: %key% with missing nls file → ID fallback**
    - Create /tmp/gsd-p2-nonls/ directory
    - /tmp/gsd-p2-nonls/package.json: {"displayName": "%displayName%"}
    - (no package.nls.json created)
    - result=$(resolve_vsc_ext_name "/tmp/gsd-p2-nonls/package.json" "fallback.id")
    - assert_eq "E1: missing nls file → ID fallback" "fallback.id" "$result"

  **Test group F — collect_vscode_extensions: real data from ~/.vscode/extensions/extensions.json**
    - Reset: OUTPUT_FILE=/tmp/gsd-p2-vsc-out.txt; _section_lines=(); > "$OUTPUT_FILE"
    - Call: collect_vscode_extensions
    - Read the output file; check it contains the "VS Code Extensions" section header
    - Count lines matching the pattern "^.+ (.+) \[.+\]$" (name (version) [id] format):
      line_count=$(grep -cE '^\S.*\(.+\) \[.+\]$' "$OUTPUT_FILE" 2>/dev/null || echo 0)
    - assert_eq "F1: VS Code section header present" "1" "$(grep -c 'VS Code Extensions' "$OUTPUT_FILE")"
    - assert that line_count >= 1:
      if [[ "$line_count" -ge 1 ]]; then
        echo "  PASS: F2: VS Code collector emits >=1 name (version) [id] lines (got $line_count)"
        ((pass_count++))
      else
        echo "  FAIL: F2: VS Code collector emits >=1 name (version) [id] lines (got $line_count)"
        ((fail_count++))
      fi
    - Spot-check a known extension from 02-RESEARCH.md verified output:
      known="Auto Rename Tag (0.1.10) [formulahendry.auto-rename-tag]"
      assert_eq "F3: known VS Code entry present" "1" "$(grep -Fc "$known" "$OUTPUT_FILE")"
    - Check that raw %...% strings are NOT in the output:
      raw_pct=$(grep -cE '%[A-Za-z]' "$OUTPUT_FILE" 2>/dev/null || echo 0)
      assert_eq "F4: no raw %key% placeholders in VS Code output" "0" "$raw_pct"

  **Test group G — collect_cursor_extensions: real data from ~/.cursor/extensions/extensions.json**
    - Reset: OUTPUT_FILE=/tmp/gsd-p2-cur-out.txt; _section_lines=(); > "$OUTPUT_FILE"
    - Call: collect_cursor_extensions
    - assert_eq "G1: Cursor section header present" "1" "$(grep -c 'Cursor Extensions' "$OUTPUT_FILE")"
    - line_count=$(grep -cE '^\S.*\(.+\) \[.+\]$' "$OUTPUT_FILE" 2>/dev/null || echo 0)
    - assert >=1 lines (same pattern as F2) — "G2: Cursor collector emits >=1 name (version) [id] lines"
    - Check that raw %...% strings are NOT in the output: "G3: no raw %key% placeholders in Cursor output"

  **Test group H — missing package.json falls back to ID, run continues**
    - Reset: OUTPUT_FILE=/tmp/gsd-p2-missing-pkg.txt; _section_lines=(); > "$OUTPUT_FILE"
    - Create a minimal synthetic extensions.json at /tmp/gsd-p2-fake-ext/:
      /tmp/gsd-p2-fake-ext/extensions.json:
      [{"identifier":{"id":"test.ext-nopkg"},"version":"1.2.3","relativeLocation":"test.ext-nopkg-1.2.3"}]
      (no test.ext-nopkg-1.2.3/package.json directory)
    - Call resolve_vsc_ext_name on a nonexistent package.json:
      result=$(resolve_vsc_ext_name "/tmp/gsd-p2-fake-ext/test.ext-nopkg-1.2.3/package.json" "test.ext-nopkg")
    - assert_eq "H1: missing pkg.json → ID fallback" "test.ext-nopkg" "$result"

  **Test group I — determinism: two consecutive real runs produce identical output**
    - Reset: OUTPUT_FILE=/tmp/gsd-p2-det1.txt; _section_lines=(); > "$OUTPUT_FILE"; collect_vscode_extensions
    - Reset: OUTPUT_FILE=/tmp/gsd-p2-det2.txt; _section_lines=(); > "$OUTPUT_FILE"; collect_vscode_extensions
    - diff_result=$(diff /tmp/gsd-p2-det1.txt /tmp/gsd-p2-det2.txt)
    - assert_eq "I1: VS Code two-run determinism (empty diff)" "" "$diff_result"
    - Same for Cursor (det3 vs det4): "I2: Cursor two-run determinism (empty diff)"

  **Final summary:**
    echo ""
    echo "Results: $pass_count PASS, $fail_count FAIL"
    [[ $fail_count -eq 0 ]] && echo "ALL TESTS PASSED" || { echo "FAILURES DETECTED"; exit 1; }

  **Cleanup:** rm -rf /tmp/gsd-p2-plain /tmp/gsd-p2-nodisplay /tmp/gsd-p2-nls /tmp/gsd-p2-dotkey /tmp/gsd-p2-nonls /tmp/gsd-p2-fake-ext /tmp/gsd-p2-*.txt /tmp/gsd-phase2-helpers.zsh /tmp/gsd-phase2-test.zsh (at end of script, after summary).

  After writing the file, run it:
    zsh /tmp/gsd-phase2-test.zsh
  </action>

  <verify>
    <automated>zsh /tmp/gsd-phase2-test.zsh 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Script runs to completion without aborting (no exit 1 mid-run from setopt err_exit)
    - A1: resolve_vsc_ext_name returns the plain displayName string unchanged
    - B1: resolve_vsc_ext_name returns ext_id when displayName is absent from package.json
    - C1: %displayName% placeholder resolves to the NLS value (e.g. "HTML Preview") via .[$k] jq lookup
    - D1: %extension.title% (dotted flat NLS key) resolves to "IntelliCode API Usage Examples" — confirms .[$k] not getpath is used
    - E1: Missing package.nls.json causes ID fallback, not abort
    - F1: VS Code section header "VS Code Extensions" appears in output
    - F2: VS Code collector emits >=1 lines matching "name (version) [id]" format against real ~/.vscode/extensions/extensions.json
    - F3: A known verified entry (e.g. "Auto Rename Tag (0.1.10) [formulahendry.auto-rename-tag]") appears in the output
    - F4: No raw %key% strings in VS Code output
    - G1-G3: Same three assertions for Cursor collector against real ~/.cursor/extensions/extensions.json
    - H1: Missing package.json falls back to extension ID as the display name; collect run continues
    - I1: VS Code two consecutive runs produce empty diff
    - I2: Cursor two consecutive runs produce empty diff
    - Final: "ALL TESTS PASSED" printed; script exits 0
  </acceptance_criteria>

  <done>All self-test assertions pass (0 FAIL); output confirms real data round-trip, NLS resolution, fallback cases, and determinism. Script exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → resolve_vsc_ext_name | Synthetic JSON files in /tmp used as inputs; no network, no real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02V-01 | Information Disclosure | Self-test OUTPUT_FILE written to /tmp | accept | Test output contains only extension metadata (display names, versions, IDs). /tmp is local, ephemeral. File is cleaned up at end of test. |
| T-02V-SC | Tampering | npm/pip/cargo installs | accept | Self-test installs nothing. Pure Zsh, no package manager invocations. |
</threat_model>

<verification>
  zsh /tmp/gsd-phase2-test.zsh exits 0 and prints "ALL TESTS PASSED"
  Individual assertions confirmed in output (no FAIL lines)
</verification>

<success_criteria>
Plan 02 complete when:
- [ ] Self-test script runs to completion (no early exit)
- [ ] All assertions pass: A1, B1, C1, D1, E1, F1, F2, F3, F4, G1, G2, G3, H1, I1, I2
- [ ] "ALL TESTS PASSED" printed; exit code 0
- [ ] Ephemeral files cleaned up (no /tmp/gsd-p2-* files remaining)
- [ ] No committed files (test script is throwaway)
</success_criteria>

<output>
Create `.planning/phases/02-editor-collectors/02-02-SUMMARY.md` when done.
</output>
