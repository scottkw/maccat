---
phase: 23-retire-zsh-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_helpers.py
autonomous: true
requirements:
  - ZSH-03
must_haves:
  truths:
    - "TestChromeExtName in test_helpers.py includes a method that passes a messages.json
       whose top-level is a JSON array (not an object) and asserts chrome_ext_name
       degrades to ext_id without raising"
    - "TestResolveVscExtName in test_helpers.py includes a method that passes a
       package.nls.json whose top-level is a JSON array (not an object) and asserts
       resolve_vsc_ext_name degrades to ext_id without raising"
    - "Both new methods are named test_msg_non_dict_messages_json_degrades_to_ext_id
       and test_nls_non_dict_top_level_degrades_to_ext_id respectively and appear in
       grep output"
    - "Full pytest run exits 0 with no new failures"
  artifacts:
    - path: "tests/test_helpers.py"
      provides: "Two new test methods, one per class"
      contains:
        - "test_msg_non_dict_messages_json_degrades_to_ext_id"
        - "test_nls_non_dict_top_level_degrades_to_ext_id"
  key_links:
    - from: "tests/test_helpers.py (TestChromeExtName)"
      to: "src/maccat/helpers/chrome_name.py:52-53"
      via: "isinstance(messages, dict) guard — branch not previously exercised"
      pattern: "isinstance.*dict.*return ext_id"
    - from: "tests/test_helpers.py (TestResolveVscExtName)"
      to: "src/maccat/helpers/vsc_name.py:53-54"
      via: "isinstance(nls, dict) guard — branch not previously exercised"
      pattern: "isinstance.*dict.*return ext_id"
---

<objective>
Backfill the two genuine branch gaps in tests/test_helpers.py before the parity
suite is deleted.

Purpose: test_helpers.py ALREADY covers chrome_name and vsc_name comprehensively
(TestChromeExtName — 10 methods; TestResolveVscExtName — 8 methods). The __MSG__
and %key% NLS resolution paths are directly tested. Two narrow branches remain
untested: the isinstance(messages, dict) guard in chrome_name.py (line 52-53) and
the isinstance(nls, dict) guard in vsc_name.py (line 53-54). Both guards handle
valid-JSON-but-wrong-shape top-level values (e.g. a JSON array). Adding one test
method per class closes these gaps without redundancy — per ZSH-03 "audit + fill
gaps; do NOT manufacture redundant cases."

NOTE: The third candidate case (vsc_name NLS value = integer scalar) was evaluated
and omitted. The existing test_nls_v2_object_value_degrades_to_ext_id already
exercises the isinstance(resolved, str) guard. An integer value hits the identical
code branch — not a distinct uncovered path.

Output: tests/test_helpers.py with two new test methods appended to their
respective existing classes.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-retire-zsh-reference/23-CONTEXT.md

<interfaces>
From src/maccat/helpers/chrome_name.py (lines 42-53) — the untested guard:

  try:
      messages = json.loads(messages_file.read_text(encoding="utf-8"))
  except (json.JSONDecodeError, OSError, UnicodeDecodeError):
      return ext_id

  if not isinstance(messages, dict):   # <-- this branch has no test
      return ext_id

From src/maccat/helpers/vsc_name.py (lines 43-54) — the untested guard:

  try:
      nls = json.loads(nls_file.read_text(encoding="utf-8"))
  except (json.JSONDecodeError, OSError, UnicodeDecodeError):
      return ext_id

  if not isinstance(nls, dict):         # <-- this branch has no test
      return ext_id

Existing test class helpers in tests/test_helpers.py:

  TestChromeExtName._make_ext(tmp_path, ext_id, version, manifest, locales)
    -> Path to manifest.json; locales arg creates _locales/<locale>/messages.json

  TestResolveVscExtName uses tmp_path directly; creates package.json and
  package.nls.json as siblings in tmp_path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add two branch-gap test methods to tests/test_helpers.py</name>
  <files>tests/test_helpers.py</files>
  <read_first>
    tests/test_helpers.py — read fully. Understand _make_ext helper in
    TestChromeExtName and the tmp_path fixture usage in TestResolveVscExtName.
    Append new methods at the END of each class, after the last existing method.

    src/maccat/helpers/chrome_name.py — confirm the isinstance(messages, dict)
    guard is at lines 52-53 before writing the test.

    src/maccat/helpers/vsc_name.py — confirm the isinstance(nls, dict) guard is
    at lines 53-54 before writing the test.
  </read_first>
  <action>
    Append one method to TestChromeExtName and one method to TestResolveVscExtName
    in tests/test_helpers.py. Do NOT create new files; do NOT alter any existing
    test methods.

    Method 1 — append to TestChromeExtName (after test_non_string_message_degrades_to_ext_id):

    Name: test_msg_non_dict_messages_json_degrades_to_ext_id

    Use _make_ext to build an extension with manifest name "__MSG_extName__" and
    the default locale "en". Do NOT use the locales kwarg. Instead, manually create
    the _locales/en/ directory inside the versioned manifest dir (manifest_path.parent)
    and write messages.json with content that is a valid JSON array, e.g. [1, 2, 3].
    Assert that chrome_ext_name(manifest_path) == ext_id (the ext_id string used
    when calling _make_ext). The return must not raise.

    Method 2 — append to TestResolveVscExtName (after test_nls_v2_object_value_degrades_to_ext_id):

    Name: test_nls_non_dict_top_level_degrades_to_ext_id

    Use tmp_path to write package.json with displayName = "%appTitle%" and
    package.nls.json with content that is a valid JSON array, e.g. ["a", "b"].
    Assert that resolve_vsc_ext_name(pkg_json, "ext-id") == "ext-id". The return
    must not raise.

    Both methods use the same import pattern as their sibling methods (local import
    inside the test method body). Both use only stdlib (json, pathlib); no new deps.
    Both follow the existing two-space indentation and type annotation style of the
    surrounding class.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/maccat && PYTHONPATH=src ./venv/bin/pytest tests/test_helpers.py -v -q 2>&1 | tail -20</automated>
    <automated>grep -n "test_msg_non_dict_messages_json_degrades_to_ext_id\|test_nls_non_dict_top_level_degrades_to_ext_id" /Users/ken/dev/maccat/tests/test_helpers.py</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/ruff check tests/test_helpers.py && ./venv/bin/python -m mypy --strict src/maccat 2>&1 | tail -5</automated>
  </verify>
  <done>
    Both new method names appear in grep output.
    pytest tests/test_helpers.py exits 0 with all methods passing (no existing
    test regressions; both new methods pass).
    ruff check exits 0.
    mypy --strict src/maccat exits 0 (test file changes do not affect src typing).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test helpers -> fs | All paths constructed from tmp_path — no real HOME reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-01 | Information Disclosure | test_helpers.py | accept | Test-only code; all paths use pytest tmp_path; no production surface |
</threat_model>

<verification>
After task completion:
- grep confirms both new method names exist in tests/test_helpers.py
- PYTHONPATH=src ./venv/bin/pytest tests/test_helpers.py -q exits 0
- ./venv/bin/ruff check tests/test_helpers.py exits 0
- ./venv/bin/python -m mypy --strict src/maccat exits 0
</verification>

<success_criteria>
1. tests/test_helpers.py gains exactly two new test methods — one in TestChromeExtName,
   one in TestResolveVscExtName — targeting the isinstance(..., dict) guards.
2. No new test files are created (tests/helpers/ is untouched).
3. All 29 test methods in tests/test_helpers.py pass (27 existing + 2 new).
4. ruff and mypy --strict remain clean.
</success_criteria>

<output>
Create .planning/phases/23-retire-zsh-reference/23-01-SUMMARY.md when done.
</output>
