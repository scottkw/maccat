---
phase: "30-markdown-emitter-md-plumbing"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/naming.py
  - src/maccat/retention.py
  - src/maccat/identity.py
  - tests/test_naming.py
  - tests/test_safety_invariants.py
autonomous: true
requirements:
  - MD-01
  - FILE-01

must_haves:
  truths:
    - "make_catalog_filename() returns a string ending in '.md' not '.txt'"
    - "parse_catalog_filename() returns None for a '.txt' filename and a valid result for '.md'"
    - "retain_newest_per_host() operates on '*.md' files — a stray '.txt' in the folder is left untouched"
    - "prune_old_archives() operates on '*.md' files — a stray '.txt' in the archive is left untouched"
    - "discover_computer_folders() recognizes a folder with '.md' catalogs as a valid computer folder"
    - "rename_machine() rewrites '.md' filenames when renaming — not '.txt' filenames"
    - "test_naming.py assertions use '.md' literals; test_safety_invariants.py line 60 uses '.md' literal"
  artifacts:
    - path: "src/maccat/naming.py"
      provides: "make_catalog_filename returning .md; _FILENAME_RE matching .md"
      contains: "\\.md$"
    - path: "src/maccat/retention.py"
      provides: "retain_newest_per_host and prune_old_archives glob .md files"
      contains: "mac-software-list-*.md"
    - path: "src/maccat/identity.py"
      provides: "discover_computer_folders and rename_machine glob .md files"
      contains: "mac-software-list-*.md"
  key_links:
    - from: "src/maccat/retention.py"
      to: "src/maccat/naming.py"
      via: "parse_catalog_filename used to validate glob matches"
      pattern: "parse_catalog_filename"
    - from: "src/maccat/identity.py"
      to: "src/maccat/naming.py"
      via: "make_catalog_filename called in rename_machine rename loop"
      pattern: "make_catalog_filename"
---

<objective>
Move the filename extension and all glob patterns from .txt to .md (FILE-01, MD-01).

Purpose: naming.py is the single source of truth for the filename convention.
retention.py and identity.py each have hardcoded glob strings that must match it.
After this plan, the entire retention/discovery/rename pipeline operates on .md files.
Tests are updated so the test suite reflects the new convention.

Output:
- src/maccat/naming.py: _FILENAME_RE and make_catalog_filename use .md
- src/maccat/retention.py: three glob sites changed (lines 64, 75, 118)
- src/maccat/identity.py: two glob sites changed (lines 158, 549)
- tests/test_naming.py: all .txt literal assertions updated to .md
- tests/test_safety_invariants.py: line 60 literal filename updated to .md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-markdown-emitter-md-plumbing/30-CONTEXT.md
@.planning/phases/30-markdown-emitter-md-plumbing/30-RESEARCH.md
@.planning/phases/30-markdown-emitter-md-plumbing/30-PATTERNS.md

<interfaces>
<!-- Key change sites extracted from codebase. -->

naming.py (change sites):
```python
# Line 18-20 — regex:
_FILENAME_RE = re.compile(
    r"^mac-software-list-\[(?P<machine>[^\[\]]+)\]-(?P<ts>\d{14})\.txt$"
)
# Line 71 — format string:
return f"mac-software-list-[{machine}]-{timestamp}.txt"
```

retention.py (change sites):
```python
# Line 64: for f in target_dir.glob("mac-software-list-*.txt"):
# Line 75: for f in target_dir.glob("mac-software-list-*.txt"):
# Line 118: for f in archive_dir.glob("mac-software-list-*.txt"):
```

identity.py (change sites):
```python
# Line 158: if any(d.glob("mac-software-list-*.txt")):
# Line 549: for file_path in rewrite_dir.glob("mac-software-list-*.txt"):
```

test_safety_invariants.py (change site):
```python
# Line 60:
weird = archive / "mac-software-list-[alpha]-2026.txt"
# Must become .md so the malformed-filename test fires against the .md glob
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update naming.py, retention.py, identity.py glob sites</name>
  <files>src/maccat/naming.py, src/maccat/retention.py, src/maccat/identity.py</files>
  <read_first>
    - src/maccat/naming.py — full file; note _FILENAME_RE at lines 18-20, make_catalog_filename at line 71, module docstring at line 3 and function docstring at line 69 (update .txt references there too)
    - src/maccat/retention.py lines 40-125 — the three glob sites at lines 64, 75, 118; also docstrings at lines 41-42 and 96-97 that reference *.txt
    - src/maccat/identity.py lines 135-165 and 540-560 — the two glob sites at lines 158 and 549; also line 144 docstring referencing mac-software-list-*.txt
    - .planning/phases/30-markdown-emitter-md-plumbing/30-RESEARCH.md Code Examples section (naming.py diff, retention.py diff, identity.py diff)
    - .planning/phases/30-markdown-emitter-md-plumbing/30-PATTERNS.md naming.py, retention.py, identity.py sections
  </read_first>
  <action>In naming.py: change \.txt$ to \.md$ in _FILENAME_RE regex (line 19); change .txt to .md in the f-string on line 71; update the module-level docstring (line 3) and make_catalog_filename docstring (line 69) where they mention .txt or the filename example. In retention.py: change all three occurrences of "mac-software-list-*.txt" to "mac-software-list-*.md" (lines 64, 75, 118); update docstring references at lines 41-42 and 96-97 from *.txt to *.md. In identity.py: change both occurrences of "mac-software-list-*.txt" to "mac-software-list-*.md" (lines 158 and 549); update line 144 docstring reference from *.txt to *.md. Do not change any logic, only the extension strings and docstrings. Stray legacy .txt files will fall outside the .md glob — they are not deleted or moved, simply invisible to retention. This is the correct behavior per FILE-01 (replaced, not duplicated).</action>
  <verify>
    <automated>cd /Users/ken/dev/maccat && grep -rn "\.txt" src/maccat/naming.py src/maccat/retention.py src/maccat/identity.py | grep -v "^.*#" | grep -v "docstring\|legacy\|old\|txt.*is\|the .txt\|stray\|\.txt.*becomes\|\.txt.*catalog" | grep "mac-software-list.*\.txt\|_FILENAME_RE.*txt\|\.txt\"\|\.txt'" ; echo "exit=$?"</automated>
  </verify>
  <done>make_catalog_filename("x","20260614120000") returns "mac-software-list-[x]-20260614120000.md"; _FILENAME_RE does not match .txt filenames; all three retention globs use *.md; both identity globs use *.md; ruff + mypy --strict clean</done>
</task>

<task type="auto">
  <name>Task 2: Update test_naming.py and test_safety_invariants.py assertions</name>
  <files>tests/test_naming.py, tests/test_safety_invariants.py</files>
  <read_first>
    - tests/test_naming.py — full file; all lines containing .txt (lines 20, 25, 30, 35, 41, 59, 63, 67, 80, 86, 93, 97, 108-109, 115, 125, 132, 136); note line 93 test_missing_txt_extension_returns_none tests that a filename without extension returns None — after the change .txt is the "wrong extension" test case
    - tests/test_safety_invariants.py lines 40-70 — line 60 literal filename and the surrounding comment explaining the invariant being tested (glob-matches but timestamp-fails)
    - .planning/phases/30-markdown-emitter-md-plumbing/30-PATTERNS.md test_naming.py section and test_safety_invariants.py section
    - .planning/phases/30-markdown-emitter-md-plumbing/30-RESEARCH.md Pitfall 4, Pitfall 5, Pitfall 6 (conftest auto-inherits; test_naming.py and test_safety_invariants.py need manual updates)
  </read_first>
  <action>In tests/test_naming.py: replace every occurrence of the literal string ".txt" in filename arguments to parse_catalog_filename with ".md" — specifically on lines 20, 25, 30, 35, 41, 59, 63, 67, 80, 86, 93, 97, 108 (two occurrences: argument and expected string), 115, 125, 132, 136. On line 93 (test_missing_txt_extension_returns_none or test_wrong_extension_returns_none): the test currently passes a filename without any extension and expects None — that test stays logically valid; additionally check line 93 area for a test_wrong_extension_returns_none that may test ".txt" as the wrong extension — if it passes "...20260614120000.txt" and expects None (because .txt is not .md), that logic is already correct after naming.py update so it just needs the comment updated. Update line 97 test that passes a filename ending in ".txt" (wrong extension) — this now correctly returns None since .md is expected; update assertion description only if needed. For the assert result == "mac-software-list-[personal]-20260614120000.txt" assertion (line 109): change to .md. In tests/test_safety_invariants.py: on line 60 change "mac-software-list-[alpha]-2026.txt" to "mac-software-list-[alpha]-2026.md" so the file matches the *.md glob but fails parse_catalog_filename due to 4-digit timestamp — preserving the invariant that unparseable-but-glob-matching filenames are skipped gracefully. Update the surrounding comment (lines 44-51) to reference *.md instead of *.txt.</action>
  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/test_naming.py tests/test_safety_invariants.py tests/test_retention.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>All tests in test_naming.py pass with .md assertions; test_safety_invariants.py passes with the .md literal filename at line 60; test_retention.py passes (auto-inherits .md via make_catalog_filename); no .txt literal remains in test_naming.py filename arguments or expected values; ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem glob → retention/identity logic | Glob pattern change is the only surface; no user-controlled input reaches glob strings |
| legacy .txt catalogs on disk | Left untouched after extension change — .md glob is narrower than .txt+.md combined |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | retention.py glob (FILE-01) | mitigate | Replacing .txt with .md (not duplicating) ensures legacy .txt files are not touched by the retention sweep — they are invisible to the new glob. A stray .txt cannot be accidentally archived or pruned. |
| T-30-06 | Denial of Service | rename_machine identity.py line 549 | accept | rename_machine already handles missing files gracefully (iterating an empty glob is a no-op). No new failure mode introduced by the glob change. |
| T-30-07 | Information Disclosure | git commit message references filename | accept | The filename passed to gitops.git_commit_and_push comes from make_catalog_filename (now .md) — no credential surface in filename. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — stdlib only. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
All tests green:
- cd /Users/ken/dev/maccat && ./venv/bin/python -m pytest tests/test_naming.py tests/test_retention.py tests/test_safety_invariants.py tests/test_identity.py -x -q
- cd /Users/ken/dev/maccat && ./venv/bin/python -m ruff check src/maccat/naming.py src/maccat/retention.py src/maccat/identity.py
- cd /Users/ken/dev/maccat && ./venv/bin/python -m mypy --strict src/maccat/naming.py src/maccat/retention.py src/maccat/identity.py

Glob sanity check (no stray .txt in production glob strings):
- grep -n "mac-software-list-\*\.txt" src/maccat/naming.py src/maccat/retention.py src/maccat/identity.py
(should return no matches)
</verification>

<success_criteria>
- make_catalog_filename("x","20260614120000") == "mac-software-list-[x]-20260614120000.md"
- parse_catalog_filename("mac-software-list-[x]-20260614120000.txt") is None
- parse_catalog_filename("mac-software-list-[x]-20260614120000.md") is not None
- All three retention.py glob strings are "mac-software-list-*.md"
- Both identity.py glob strings are "mac-software-list-*.md"
- test_naming.py, test_retention.py, test_safety_invariants.py all pass
- ruff + mypy --strict clean on all three modified source files
</success_criteria>

<output>
Create .planning/phases/30-markdown-emitter-md-plumbing/30-02-SUMMARY.md when done
</output>
