---
context: phase
phase: 30-markdown-emitter-md-plumbing
task: 0
total_tasks: 2
status: in_progress
last_updated: 2026-06-18T19:31:43.887Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. The constraint below was discovered through an actual failure this session.
> Acknowledge it explicitly before proceeding.

- [ ] CONSTRAINT: Worktree editable-install pollution — after a worktree executor is merged and its worktree removed, `import maccat` fails on main (`ModuleNotFoundError`) and the WHOLE pytest run collection-errors. — Structural mitigation: run `./venv/bin/pip install -e .` on main IMMEDIATELY after any worktree merge and BEFORE the post-merge test gate.

**Do not proceed to a post-merge test run until that box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree editable-install pollution | A worktree executor's `pip install -e .` repoints the venv editable link to the worktree path; after the worktree is removed, every test errors at collection with `No module named 'maccat'`. Manifested this session as 28 collection errors after the wave-1 merge. | blocking | Re-run `./venv/bin/pip install -e .` on main before running `./venv/bin/pytest tests/` after any worktree merge. |
| Incomplete `.txt`→`.md` site inventory | Phase 30 RESEARCH claimed a complete file-change inventory but missed `reinstall/picker.py`'s catalog glob, which broke reinstall catalog discovery after the rename. | advisory | When migrating a file-extension/glob convention, grep the WHOLE `src/` tree for the old token (`grep -rn '\.txt' src/`) — don't trust a curated inventory. |

<current_state>
Autonomous milestone run (`/gsd-autonomous`) of **v3.0.0 Markdown Catalog Format** (3 phases: 30, 31, 32). Paused mid-**Phase 30**, mid-**wave 2**.

Phase 30 wave 1 is fully merged into `main` (HEAD `9e90e96`):
- **30-01** — `src/maccat/catalog/markdown.py` (`render_markdown_catalog` + helpers) and `CatalogWriter.write_raw`; 52 unit tests.
- **30-02** — `.txt`→`.md` in `naming.py` (`_FILENAME_RE` + `make_catalog_filename`), `retention.py` (3 globs), `identity.py` (2 globs), and test updates.
- **picker fix** (`fix(30)`, commit `9e90e96`) — `reinstall/picker.py` catalog glob `.txt`→`.md` + its `TestFindNewestCatalog` fixtures (a FILE-01 site RESEARCH missed).

Wave 2 (**30-03**) was dispatched as a worktree executor but **killed mid-investigation when the user paused — it made zero commits and its worktree was discarded.** Nothing to recover; re-dispatch from scratch.

Test state on `main` right now: green EXCEPT 3 transitional `test_cli.py` failures (`TestNoCommit::test_no_commit_catalog_file_written`, `TestGenerateThenSweep::test_just_written_catalog_not_archived`, `TestGenerateThenSweep::test_timestamp_captured_after_git_pull`). These fail because `make_catalog_filename` now returns `.md` while those assertions still glob `.txt` — they are exactly the assertions 30-03 fixes.
</current_state>

<completed_work>

Completed (merged to main):
- Phase 30 discuss (30-CONTEXT.md), research (30-RESEARCH.md), pattern map (30-PATTERNS.md), 3 plans, plan-check (passed, 0 blockers).
- 30-01: markdown emitter + write_raw — Done
- 30-02: .txt→.md filename/glob plumbing — Done
- picker.py glob fix (gap discovered by post-merge gate) — Done
</completed_work>

<remaining_work>

- **30-03 (wave 2, not started):** Replace the `cli.py` generate loop's per-section `write_section`/`write_lines`/`flush_section` calls with a single `render_markdown_catalog(...)` + `writer.write_raw(...)` call; fix the 5 `test_cli.py` glob assertions from `.txt` to `.md`.
- **Finish Phase 30:** post-merge gate (pip install -e . FIRST → full suite green) → `gsd-sdk query roadmap.update-plan-progress 30 30-01|30-02|30-03 complete` → `gsd-verifier` for phase 30.
- **Phase 31:** Markdown-Only Reinstall Parser (RIN-01, RIN-02) — discuss → plan → execute.
- **Phase 32:** Convert Command (CONV-01/02/03) — discuss → plan → execute.
- **Milestone lifecycle:** audit-milestone → complete-milestone v3.0.0 → cleanup.
</remaining_work>

<decisions_made>

- Markdown format locked in 30-CONTEXT.md: `---` YAML frontmatter (snake_case `computer`/`hostname`/`generated`/`maccat_version`, `generated` double-quoted ISO-8601), title stays `# Installed Mac Software List`, 3-col `Name | Version | ID` tables (empty cells, `\|` escaping), raw Homebrew/mas rendered in the SAME table via a duplicated `ITEM_RE`, `(none found)` line for empties, `LC_ALL=C sort -f -u` preserved, byte-deterministic modulo the timestamp.
- Fixed the reinstall picker glob in Phase 30 (not deferred to 31) because it's a FILE-01 plumbing site; Phase 31 still owns the parser + `.txt`-refusal in the parse step.
- `__version__` NOT bumped in Phase 30 (stays 2.1.0); deferred to a release step after Phase 32.
</decisions_made>

<blockers>
- None outstanding. (The editable-install pollution above is a procedure constraint, not a blocker.)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state for `/gsd:resume-work`.
2. `.planning/phases/30-markdown-emitter-md-plumbing/30-03-PLAN.md` — the next plan to execute.
3. `.planning/phases/30-markdown-emitter-md-plumbing/30-01-SUMMARY.md` + `30-02-SUMMARY.md` — what 30-03 builds on (`render_markdown_catalog` signature, `write_raw`, `.md` filename).
4. `.planning/PROJECT.md` — source of truth (the root + repo `CLAUDE.md` describe a STALE Zsh script; the real tool is the Python `maccat` package under `src/maccat/`).

## Infrastructure State
- Branch `main` at `9e90e96`; no agent worktrees remain (`git worktree list` shows only the primary).
- `.planning/STATE.md` had an uncommitted `state.begin-phase` write — committed with this handoff.
- venv editable install is currently correct on main (re-installed this session after the wave-1 merge).

<context>
This was an autonomous full-milestone run. The flow per phase is smart-discuss → (research → pattern-map → plan → plan-check) → execute-phase (wave-based, worktree-isolated parallel executors) → post-merge gate → verify. Phase 30 is one 30-03 plan + a verify away from done. Resume by re-dispatching 30-03 exactly as the other executors were dispatched (isolation=worktree, sonnet), merging, and running the post-merge gate — then continue autonomously through Phases 31 and 32 and the milestone lifecycle.
</context>

<next_action>
Start with: re-dispatch wave 2 plan **30-03** as a `gsd-executor` worktree agent (isolation=worktree, model sonnet) based at the current HEAD. On completion: merge the branch into main, run `./venv/bin/pip install -e .`, then `./venv/bin/pytest tests/` (expect fully green). Then `roadmap.update-plan-progress` for 30-01/30-02/30-03, and run `gsd-verifier` for phase 30.
</next_action>
