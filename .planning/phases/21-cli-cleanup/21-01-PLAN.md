---
phase: 21-cli-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/identity.py
  - src/maccat/cli.py
  - src/maccat/gitops.py
  - src/maccat/retention.py
  - src/maccat/catalog/writer.py
  - src/maccat/naming.py
autonomous: true
requirements:
  - CLI-03
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "resolve_computer_selection accepts only computer: str | None and returns str | None"
    - "--personal, --office, --machine are absent from _build_parser()"
    - "config-subcommand guard references only rename and computer"
    - "--rename x selecting-flag guard references only computer"
    - "resolve_computer_selection call site in cli.py passes only computer=args.computer"
    - "No 'personal'/'office'/'machine' parameter names remain in src/maccat/ as flag identifiers"
  artifacts:
    - path: "src/maccat/identity.py"
      provides: "Simplified resolve_computer_selection(*, computer: str | None) -> str | None"
      contains: "def resolve_computer_selection"
    - path: "src/maccat/cli.py"
      provides: "Parser with only --computer in the selecting group; updated guards and call site"
  key_links:
    - from: "src/maccat/cli.py"
      to: "src/maccat/identity.py"
      via: "resolve_computer_selection(computer=args.computer)"
      pattern: "resolve_computer_selection\\(computer=args\\.computer\\)"
---

<objective>
Remove --personal, --office, and --machine from the argparse parser and from
resolve_computer_selection. Collapse the four-way flag resolver to a single
--computer validator. Update both guards and the call site in cli.py. Scrub
docstring examples in the four ancillary files.

Purpose: Phase 21 core surgery — eliminate dead flag paths so the source is
the ground truth before tests are updated in Plan 02.

Output:
- identity.py: resolve_computer_selection has signature (*, computer: str | None) -> str | None
- cli.py: parser has no --personal/--office/--machine; guards mention only --computer; call site passes computer=args.computer
- gitops.py, retention.py, catalog/writer.py, naming.py: docstring examples reworded
</objective>

<execution_context>
@/Users/ken/dev/maccat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/dev/maccat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/ROADMAP.md
@/Users/ken/dev/maccat/.planning/STATE.md
@/Users/ken/dev/maccat/.planning/phases/21-cli-cleanup/21-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Simplify resolve_computer_selection in identity.py</name>
  <files>src/maccat/identity.py</files>

  <read_first>
  src/maccat/identity.py — read the full resolve_computer_selection function
  (lines ~81–131) including its docstring, the count logic, the mutual-exclusion
  SystemExit, and each branch.
  </read_first>

  <action>
  Rewrite resolve_computer_selection to the simplified signature per D-02
  (CONTEXT.md "resolve_computer_selection" decision):

  New signature: def resolve_computer_selection(*, computer: str | None) -> str | None

  Body:
  - If computer is falsy (None or ""), return None (interactive fallback).
  - Otherwise call validate_computer_name(computer) and return computer.
  - Remove: the personal, office, machine parameters; the count = sum([...]) block;
    the count > 1 SystemExit; all branches for personal, office, machine.

  Update the docstring to reflect the new semantics: one flag (--computer),
  validate-and-return or None for interactive fallback. Remove the "more than one
  set" / "exactly one set" / "zero set" rules. Remove the zsh analog reference to
  lines 199–268 (the mutual-exclusion block no longer mirrors those zsh lines).

  Keep: validate_computer_name call, the bool(computer) / "" falsy guard, return type
  str | None, the NOTE about --rename x selecting-flag belonging to cli.py.

  mypy --strict must remain clean: ensure no Optional widening issues.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -c "from maccat.identity import resolve_computer_selection; import inspect; sig = inspect.signature(resolve_computer_selection); params = list(sig.parameters); assert params == ['computer'], f'unexpected params: {params}'; print('signature OK')"</automated>
  </verify>

  <done>
  resolve_computer_selection(*, computer: str | None) -> str | None with no personal/
  office/machine params; passes the import+signature check above; ruff check and
  mypy --strict on identity.py report zero errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update cli.py — parser, guards, call site, module docstring</name>
  <files>src/maccat/cli.py</files>

  <read_first>
  src/maccat/cli.py — read the full file (all 313 lines). Key locations:
  - Module docstring flag list (lines 28–43): lists --personal, --office, --machine
  - _build_parser mutually exclusive group (lines 65–92): four add_argument calls
  - Config guard (line 188): any([args.rename, args.personal, args.office, args.computer, args.machine])
  - Rename guard (line 212): any([args.personal, args.office, args.computer, args.machine])
  - resolve_computer_selection call (lines 240–245): passes personal=, office=, machine=
  </read_first>

  <action>
  Make four targeted edits per D-01 and D-02 from CONTEXT.md:

  1. Module docstring (lines ~28–43): Remove the three flag lines
     "--personal Use 'personal' folder.", "--office Use 'office' folder.", and
     "--machine Use named folder (kept as separate dest=...)" from the Flags section.
     Leave --computer and all other flag lines unchanged.

  2. _build_parser (lines ~65–92): Remove the three group.add_argument blocks for
     --personal (lines ~67–72), --office (lines ~73–77), and --machine (lines ~88–93).
     Keep the group definition (parser.add_mutually_exclusive_group()) and the
     --computer block. The mutually exclusive group now has a single member;
     that is acceptable (see CONTEXT.md Claude's Discretion: keep or remove group,
     Claude's call). Since a single-member group still allows --computer to be parsed
     correctly and the code is simpler with the group present, keep it.

  3. Config subcommand guard (line ~188): Replace
       any([args.rename, args.personal, args.office, args.computer, args.machine])
     with
       any([args.rename, args.computer])
     Update the error message string to reference only "--rename and --computer"
     (drop "--personal, --office, and --machine" from the text).

  4. Rename guard (line ~212): Replace
       args.rename and any([args.personal, args.office, args.computer, args.machine])
     with
       args.rename and bool(args.computer)
     Update the error message string to reference only "--computer"
     (drop "--personal, --office, and --machine" from the text).

  5. resolve_computer_selection call site (lines ~240–245): Replace the four-argument
     call with:
       computer_pre = resolve_computer_selection(computer=args.computer)
     Remove the personal=args.personal, office=args.office, machine=args.machine lines.

  After edits: args.personal, args.office, args.machine must not appear anywhere in
  cli.py (they no longer exist as attributes on the Namespace).
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -c "from maccat.cli import _build_parser; p = _build_parser(); args = p.parse_args(['--computer', 'Laptop']); assert args.computer == 'Laptop'; args2 = p.parse_args([]); assert args2.computer is None; print('parser OK')"</automated>
  </verify>

  <done>
  _build_parser() has no --personal/--office/--machine arguments; --computer still
  works; the config guard and rename guard no longer reference personal/office/machine
  attributes; the call site is resolve_computer_selection(computer=args.computer);
  ruff check and mypy --strict on cli.py report zero errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scrub docstring examples in gitops.py, retention.py, writer.py, naming.py</name>
  <files>
    src/maccat/gitops.py
    src/maccat/retention.py
    src/maccat/catalog/writer.py
    src/maccat/naming.py
  </files>

  <read_first>
  Read only the affected lines before editing — do not re-read files already in context:
  - gitops.py line ~102: Args section for git_commit_and_push — "personal", "office"
  - retention.py line ~56: Args section for prune_old_archives — "personal"
  - catalog/writer.py line ~25: Usage example in CatalogWriter docstring — "personal/catalog-2026.txt"
  - naming.py line ~69: Returns example — "mac-software-list-[personal]-..."
  </read_first>

  <action>
  Replace the "personal"/"office" example strings in these four docstrings/comments
  with a generic folder name per D-01 (CONTEXT.md: "reword to a generic example folder
  name — these are comments/docstrings only, no logic"):

  - gitops.py line ~102: Change 'e.g. "personal", "office"' to 'e.g. "MyMac", "WorkLaptop"'
  - retention.py line ~56: Change 'e.g. catalog_repo / "personal"' to 'e.g. catalog_repo / "MyMac"'
  - catalog/writer.py line ~25: Change 'CatalogWriter(Path("personal/catalog-2026.txt"))' to
    'CatalogWriter(Path("MyMac/catalog-2026.txt"))'
  - naming.py line ~69: Change the Returns example from
    "'mac-software-list-[personal]-20260614120000.txt'" to
    "'mac-software-list-[MyMac]-20260614120000.txt'"

  These are documentation-only changes. Do NOT alter any logic, return values, or
  test fixtures.
  </action>

  <verify>
    <automated>cd /Users/ken/dev/maccat && grep -rn '"personal"\|"office"' src/maccat/gitops.py src/maccat/retention.py src/maccat/catalog/writer.py src/maccat/naming.py | grep -v "test_" | wc -l | xargs -I{} test {} -eq 0 && echo "no personal/office strings in target docstrings" || echo "FAIL: personal/office still present"</automated>
  </verify>

  <done>
  The four files contain no "personal"/"office" string literals in their docstrings;
  ruff check reports zero errors on the changed files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → argparse | User-controlled flag values cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | resolve_computer_selection | accept | Pure refactor — no new trust surface; validate_computer_name call preserved |
| T-21-02 | Elevation of Privilege | _build_parser removing group | accept | Removing flags reduces attack surface; no new privilege paths |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `cd /Users/ken/dev/maccat && ./venv/bin/python -m ruff check src/maccat/identity.py src/maccat/cli.py src/maccat/gitops.py src/maccat/retention.py src/maccat/catalog/writer.py src/maccat/naming.py` — exits 0
2. `cd /Users/ken/dev/maccat && ./venv/bin/python -m mypy --strict src/maccat/identity.py src/maccat/cli.py` — exits 0
3. `cd /Users/ken/dev/maccat && grep -rn 'args\.personal\|args\.office\|args\.machine' src/maccat/cli.py` — returns no output
4. `cd /Users/ken/dev/maccat && grep -rn 'personal=\|office=\|machine=' src/maccat/identity.py` — returns no output (no old param names)
</verification>

<success_criteria>
- resolve_computer_selection signature is (*, computer: str | None) -> str | None — no other params
- _build_parser contains no --personal, --office, or --machine add_argument calls
- Config guard: any([args.rename, args.computer]) — no personal/office/machine attributes
- Rename guard: args.rename and bool(args.computer) — no personal/office/machine attributes
- Call site: resolve_computer_selection(computer=args.computer) — single keyword arg
- ruff check and mypy --strict pass on all modified files
- The four docstring files contain no "personal"/"office" string literals
</success_criteria>

<output>
Create `.planning/phases/21-cli-cleanup/21-01-SUMMARY.md` when done.
</output>
