---
phase: 18-public-repo-migration-genericized-fresh-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /tmp/maccat-staging  # staging tree built outside this repo (see action notes)
autonomous: true
requirements: [MIG-02, GEN-01, GEN-02, GEN-03, GEN-04]
user_setup: []

must_haves:
  truths:
    - "A staging directory exists containing only the genericized maccat tree (code, tests, build tooling, docs, zsh reference, .planning, CLAUDE.md) plus new LICENSE/.gitignore/example config"
    - "The staging tree contains zero personal catalog .txt files, zero personal hostnames, no dist/maccat.pyz, no venv/, and none of the three stray root test scripts"
    - "Every text file in the staging tree (README, .planning/, docs/, CLAUDE.md included) has been scrubbed of real personal hostnames/machine labels; a tree-wide grep for the personal-identifier tokens returns zero matches (excluding tests/golden fixtures)"
    - "The staging README documents install-from-Releases, catalog-dir config precedence, and usage with neutral placeholders only"
    - "An open-source MIT LICENSE file and an example config.toml are present in the staging tree"
  artifacts:
    - path: "<staging>/src/maccat/__main__.py"
      provides: "Migrated maccat package entry point"
    - path: "<staging>/LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "<staging>/.gitignore"
      provides: "Ignore rules for artifacts + personal catalog data"
      contains: "venv/"
    - path: "<staging>/config.example.toml"
      provides: "Template config showing catalog-dir configuration"
      contains: "catalog_dir"
    - path: "<staging>/README.md"
      provides: "Genericized, install-from-Releases README"
      contains: "Releases"
  key_links:
    - from: "<staging>/README.md"
      to: "config.example.toml"
      via: "documented config precedence (--catalog-dir > MACCAT_CATALOG_DIR > ~/.config/maccat/config.toml)"
      pattern: "MACCAT_CATALOG_DIR"
    - from: "scrub task (Task 4)"
      to: "entire staging tree"
      via: "deterministic token replacement across all text files"
      pattern: "computer-one|computer-two.local|Example Computer"
---

<objective>
Build a genericized staging tree (outside this repo's working tree) that will become
the public `maccat` repo's first commit. Copy ONLY the locked include-list paths, exclude
all personal data and build artifacts, add a new MIT LICENSE, a `.gitignore`, and an
example `config.toml`, rewrite the README as a general-purpose install-from-Releases tool,
and SCRUB real personal identifiers (hostnames / machine labels) from EVERY text file in
the tree (`.planning/`, `docs/`, `CLAUDE.md`, README — not just README).

Purpose: Genericization (GEN-01/02/03/04) and content migration (MIG-02) must happen on a
clean copy BEFORE any git history exists, so no personal data is ever committed to the new
repo. The tree-wide scrub closes the gap where `.planning/` (~37 files) and a `docs/superpowers`
design spec carry real personal hostnames (`computer-one.local`, `computer-two.local`,
`Example Computer`). This plan produces the prepared tree; Plan 02 inits git, runs the privacy
gate, and pushes.
Output: A `/tmp/maccat-staging.*` directory ready for `git init`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md

<interfaces>
<!-- maccat config resolution the README/example config must document accurately. -->
<!-- Precedence (verified in src/maccat config layer): --catalog-dir flag > MACCAT_CATALOG_DIR env > ~/.config/maccat/config.toml > error -->
<!-- CLI entry point: project.scripts maccat = "maccat.__main__:main"; package = src/maccat -->
<!-- pyproject.toml: name="maccat", version="1.0.0", requires-python>=3.11, license={text="MIT"}, zero runtime deps -->
<!-- pyproject has NO authors field → MIT copyright line uses "Ken Scott" per locked decision -->
<!-- Real personal-identifier tokens present in the source tree (enumerated by scanning tracked files outside personal/office):
       computer-one.local, computer-one,
       computer-two.local, computer-two,
       computer-two.local,
       "Example Computer", "Example Computer", "Example Computer"
     These appear in .planning/ (~37 files), docs/superpowers/specs/2026-06-14-computer-folder-model-design.md,
     README.md, CLAUDE.md, and one stray root script (already excluded). tests/golden/* are CLEAN (synthetic placeholders) — confirmed by scan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the include-list into a clean staging tree (exclude all personal data + artifacts)</name>
  <files>/tmp/maccat-staging (created)</files>
  <read_first>
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (locked include/exclude list)
    - ./pyproject.toml (confirm package layout to copy)
    - ./.gitignore (existing ignore patterns to carry forward; note the tracked .vscode test fixture exception under tests/golden/)
  </read_first>
  <action>
    Create a fresh empty staging dir: STAGING=$(mktemp -d /tmp/maccat-staging.XXXXXX); echo "$STAGING". Do NOT use the repo's working tree as the destination and do NOT delete anything from this repo.
    Copy ONLY the locked INCLUDE paths from the repo root into $STAGING, preserving structure: src/maccat/, tests/, pyproject.toml, scripts/ (entire dir — build-pyz.sh), docs/, update-list.sh, .planning/, README.md, CLAUDE.md, .python-version, and the .github/ dir (existing ci.yml — kept; Phase 19 extends it). Use `git archive HEAD | tar -x -C "$STAGING"` to export tracked files cleanly, which inherently excludes untracked venv/ and dist artifacts and respects .gitignore-tracked state — then prune anything in the EXCLUDE list that git tracks.
    After export, hard-delete from $STAGING any of the EXCLUDE items that may have been tracked: rm -rf "$STAGING/dist" "$STAGING/venv" "$STAGING/personal" "$STAGING/office"; rm -f "$STAGING/machine-labels.tsv" "$STAGING/test-parse-arguments-11-02.sh" "$STAGING/test-rename-back-12-02.sh" "$STAGING/test-rename-front-12-01.sh"; find "$STAGING" -name '*.pyz' -delete; find "$STAGING" -name '.DS_Store' -delete. KEEP CLAUDE.md in $STAGING — it is useful contributor/project guidance and will be genericized by the scrub in Task 4 (do NOT delete it).
    Remove any catalog .txt files anywhere in the tree EXCEPT legitimate test fixtures/golden data under tests/ (those are required by the suite). Scan: find "$STAGING" -name 'mac-software-list-*.txt' -not -path "*/tests/*" -delete. Leave tests/golden fixtures intact.
    Record the chosen $STAGING path in the SUMMARY so Plan 02 can reuse it.
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); test -f "$S/pyproject.toml" && test -d "$S/src/maccat" && test -d "$S/tests" && test -f "$S/update-list.sh" && test -d "$S/.planning" && test -f "$S/CLAUDE.md" && test ! -d "$S/dist" && test ! -d "$S/venv" && test ! -d "$S/personal" && test ! -d "$S/office" && test ! -f "$S/machine-labels.tsv" && test ! -f "$S/test-parse-arguments-11-02.sh" && test ! -f "$S/test-rename-back-12-02.sh" && test ! -f "$S/test-rename-front-12-01.sh" && [ "$(find "$S" -name '*.pyz' | wc -l | tr -d ' ')" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The staging dir contains src/maccat/, tests/, pyproject.toml, scripts/build-pyz.sh, docs/, update-list.sh, .planning/, CLAUDE.md, .github/ci.yml
    - `ls "$S/dist" 2>/dev/null` and `ls "$S/venv" 2>/dev/null` and `ls "$S/personal" 2>/dev/null` and `ls "$S/office" 2>/dev/null` all exit non-zero
    - `ls "$S/machine-labels.tsv" "$S/test-parse-arguments-11-02.sh" "$S/test-rename-back-12-02.sh" "$S/test-rename-front-12-01.sh" 2>/dev/null` exits non-zero (all three stray root scripts absent)
    - `find "$S" -name '*.pyz'` prints nothing
    - `find "$S" -name 'mac-software-list-*.txt' -not -path '*/tests/*'` prints nothing
  </acceptance_criteria>
  <done>Staging tree holds exactly the include-list (plus required test fixtures and CLAUDE.md); zero excluded items present.</done>
</task>

<task type="auto">
  <name>Task 2: Add LICENSE, .gitignore, and example config to the staging tree</name>
  <files>/tmp/maccat-staging.*/LICENSE, /tmp/maccat-staging.*/.gitignore, /tmp/maccat-staging.*/config.example.toml</files>
  <read_first>
    - ./.gitignore (carry forward existing rules, including the tracked tests/golden/.vscode fixture exception)
    - ./pyproject.toml (confirm no authors field → copyright holder is "Ken Scott")
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (gitignore + config requirements)
  </read_first>
  <action>
    In the staging dir $S:
    Write LICENSE containing the standard MIT License text with copyright line "Copyright (c) 2026 Ken Scott" (pyproject has no authors field; use "Ken Scott" per locked decision). Use the canonical MIT text (the full permission + warranty-disclaimer paragraphs).
    Write/overwrite .gitignore covering at minimum: venv/, .venv/, dist/, build/, __pycache__/, *.py[cod], *.pyc, *.egg-info/, *.pyz, .pytest_cache/, .mypy_cache/, .ruff_cache/, .DS_Store, .vscode/, .idea/ — PLUS personal catalog patterns so personal data can never be committed later: personal/, office/, machine-labels.tsv, mac-software-list-*.txt, *-[*].txt. PRESERVE the existing negation exceptions for the test fixture: `!tests/golden/fixtures/fake_home/.vscode/` and `!tests/golden/fixtures/fake_home/.vscode/**` (otherwise the broad .vscode/ rule excludes a committed golden fixture).
    Write config.example.toml: a template showing the user how to point maccat at their own catalog repo via the config file, using the real config key the app reads (catalog_dir). Use a neutral placeholder path (e.g. catalog_dir = "/path/to/your/catalog-repo") with a commented note that --catalog-dir flag and MACCAT_CATALOG_DIR env override it. No personal hostnames/paths.
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); grep -q "MIT License" "$S/LICENSE" && grep -q "Ken Scott" "$S/LICENSE" && grep -q "venv/" "$S/.gitignore" && grep -q "office/" "$S/.gitignore" && grep -q "tests/golden/fixtures/fake_home/.vscode/" "$S/.gitignore" && grep -q "catalog_dir" "$S/config.example.toml" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MIT License" "$S/LICENSE"` is >= 1 and `grep -c "Ken Scott" "$S/LICENSE"` is >= 1
    - `.gitignore` contains `venv/`, `dist/`, `*.pyz`, `office/`, `personal/`, `machine-labels.tsv`, and `mac-software-list-*.txt`
    - `.gitignore` retains the `!tests/golden/fixtures/fake_home/.vscode/` negation line
    - `config.example.toml` contains the `catalog_dir` key and contains no personal hostname (grep -v '^#' "$S/config.example.toml" | grep -c -i "Kens-\|'Example.Computer\|computer-two.local'|MacBook" prints 0)
  </acceptance_criteria>
  <done>LICENSE (MIT), .gitignore (artifacts + personal patterns + fixture exception), and config.example.toml all present and correct in staging.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the staging README as a genericized install-from-Releases tool</name>
  <files>/tmp/maccat-staging.*/README.md</files>
  <read_first>
    - ./README.md (existing zsh-framed README — source of feature/usage content to genericize)
    - ./pyproject.toml (name=maccat, CLI entry, requires-python>=3.11)
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (README/genericization requirements)
  </read_first>
  <action>
    Overwrite $S/README.md as a general-purpose tool README for `maccat`. It MUST cover:
    (1) What maccat catalogs (apps, Homebrew, mas, Setapp, web apps; AI-CLI tooling for Claude Code/Codex/OpenCode/Gemini; VS Code/Cursor extensions; Chrome/Firefox extensions) — reuse the feature descriptions from the existing README, keeping the Privacy note about MCP entries capturing name+transport only.
    (2) Install: download the latest `maccat.pyz` from the GitHub Releases page (github.com/scottkw/maccat/releases) and run it with python (e.g. `python3 maccat.pyz` or `./maccat.pyz`); requires Python >= 3.11; macOS only.
    (3) Configuration: document catalog-dir resolution precedence exactly — `--catalog-dir` flag > `MACCAT_CATALOG_DIR` env var > `~/.config/maccat/config.toml` (catalog_dir key) > error. Reference config.example.toml as a template.
    (4) Usage: basic run, how computer-folders/machine labels work, retention/archive behavior — using NEUTRAL placeholders only (e.g. `<your-computer>`, `home`/`work`) instead of the personal `personal`/`office` folders, and NO personal hostnames (remove all `Kens-...`, `computer-two.local`, `Example Computer` examples — replace with generic placeholders like `My Laptop`, `<machine-label>`).
    Remove the old zsh-only `./update-list.sh --personal` framing as the primary install/usage path (the zsh script is kept only as a parity reference; it can be mentioned as such, but install/usage docs are for the maccat .pyz).
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); grep -q -i "Releases" "$S/README.md" && grep -q "MACCAT_CATALOG_DIR" "$S/README.md" && grep -q "config.example.toml\|config.toml" "$S/README.md" && [ "$(grep -v '^>' "$S/README.md" | grep -c -i 'Example.Computer\|computer-two.local'|'Example.Computer\|computer-two.local'|'Example.Computer\|computer-two.local')" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README contains "Releases", "MACCAT_CATALOG_DIR", and references a config template (config.toml/config.example.toml)
    - README documents the full precedence chain `--catalog-dir` > `MACCAT_CATALOG_DIR` > `~/.config/maccat/config.toml`
    - `grep -i "Kens-\|'Example.Computer\|computer-two.local'|Example Computer\|MacBook-Air.local"` over README prints zero matches (no personal hostnames/labels)
    - README documents install-from-Releases of the `.pyz`, not `./update-list.sh --personal` as the primary path
  </acceptance_criteria>
  <done>README reads as a generic maccat tool: install-from-Releases, config precedence, neutral usage placeholders, zero personal identifiers.</done>
</task>

<task type="auto">
  <name>Task 4: Scrub real personal identifiers from the ENTIRE staging tree (.planning, docs, CLAUDE.md, all text files)</name>
  <files>/tmp/maccat-staging.* (in-place edits across all text files, tests/golden/* excluded)</files>
  <read_first>
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (zero-personal-data requirement, GEN-03)
    - the `<interfaces>` block above (enumerated personal-identifier token list)
  </read_first>
  <action>
    Resolve $S (the staging dir from Task 1). This task scrubs REAL personal identifiers from the WHOLE tree — README was handled in Task 3, but `.planning/` (~37 files), `docs/superpowers/specs/2026-06-14-computer-folder-model-design.md`, and `CLAUDE.md` still carry real hostnames/machine labels. Do NOT touch this repo's working tree — operate only inside $S.

    STEP A — Enumerate completely (do not trust a hardcoded list): first scan the staging tree to discover EVERY real personal token present, so the replacement set is complete. Run:
      `git -C "$S" 2>/dev/null grep -ohE "computer-one[A-Za-z.-]*|computer-two[A-Za-z.-]*|[A-Za-z-]*computer-two.local|Ken.?s MacBook( Air)?|Example Computer" -- . ':(exclude)tests/golden/*'` (or, since $S is not yet a git repo, use `grep -rohE` over $S excluding tests/golden/). Sort -u the results. The known set (confirmed against the source tree) is: `computer-one.local`, `computer-one`, `computer-two.local`, `computer-two`, `computer-two.local`, `Example Computer`, `Example Computer`, `Example Computer`. If the scan reveals additional tokens, add them to the replacement set — do NOT silently skip them.

    STEP B — Apply deterministic replacements across all text files in $S, EXCLUDING tests/golden/* (deliberate parity fixtures — leave byte-for-byte intact) and the binary/`.git` paths. Apply MOST-SPECIFIC tokens first so substrings don't get half-replaced (e.g. replace `computer-one.local` before `computer-one`). Mapping (neutral placeholders):
      - `computer-one.local` → `computer-one.local`
      - `computer-one`       → `computer-one`
      - `computer-two.local`          → `computer-two.local`
      - `computer-two`                → `computer-two`
      - `computer-two.local`                → `computer-two.local`   (these are the same author's two machines; map the second host family to computer-two)
      - `Example Computer`               → `Example Computer`
      - `Example Computer`                → `Example Computer`
      - `Example Computer`                   → `Example Computer`
    Implement via a loop over the discovered token→replacement pairs using a tool that does literal (not regex) replacement to avoid surprises (e.g. iterate files from `grep -rlF <token>` and rewrite with an in-place substitution; on macOS use `sed -i ''` with the token escaped, or perl `-pi -e 's/\Qtoken\E/repl/g'`). Skip $S/tests/golden and any path under .git. Operate on text files only (skip binaries).
    Note: the bare format-string `mac-software-list-[` legitimately appears in code/tests/docs and is NOT personal data — do NOT scrub it. Only the enumerated hostname/machine-label tokens are scrubbed.

    STEP C — Self-verify: after scrubbing, a tree-wide grep for the personal tokens (excluding tests/golden/*) MUST return zero files. Record in the SUMMARY: the full enumerated token list, the count of files modified, and the post-scrub zero-match confirmation.
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); HITS=$(grep -rlE 'computer-one|computer-two|computer-two.local|Ken.?s MacBook' "$S" --exclude-dir=.git --exclude-dir=golden 2>/dev/null | grep -v '/tests/golden/' | wc -l | tr -d ' '); [ "$HITS" = "0" ] && echo PASS || { echo "FAIL: $HITS files still contain personal identifiers"; grep -rlE 'computer-one|computer-two|computer-two.local|Ken.?s MacBook' "$S" --exclude-dir=.git | grep -v '/tests/golden/'; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - A tree-wide scan `grep -rlE 'computer-one|computer-two|computer-two.local|Ken.?s MacBook' "$S" --exclude-dir=.git` returns ZERO files OUTSIDE tests/golden/ (i.e. .planning/, docs/, CLAUDE.md, README all clean)
    - tests/golden/* fixtures are byte-for-byte unchanged by this task (they contain no real hostnames — confirmed — and must remain intact as parity fixtures)
    - The SUMMARY records the enumerated token list, files-modified count, and the zero-match confirmation
    - The bare `mac-software-list-[` format string is still present where it legitimately occurs (code/tests/docs) — it was NOT scrubbed
  </acceptance_criteria>
  <done>Every text file in the staging tree (README, .planning/, docs/, CLAUDE.md) is free of real personal hostnames/machine labels; tests/golden fixtures untouched; tree-wide grep confirms zero matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo (personal data) → staging tree | Personal catalogs/hostnames must NOT cross into the staging copy that becomes a public repo |
| author's machine → public GitHub | (Plan 02) staging tree is what gets published; this plan must guarantee it is clean before that |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | personal catalog .txt / hostnames leaking into staging tree | mitigate | `git archive HEAD` export + explicit rm of EXCLUDE list + `find -name 'mac-software-list-*.txt'` sweep (Task 1); Plan 02 re-scans before any push |
| T-18-02 | Information Disclosure | machine-labels.tsv (hostname→label map) copied | mitigate | explicit `rm -f machine-labels.tsv` in Task 1; added to .gitignore in Task 2 |
| T-18-03 | Information Disclosure | personal hostnames embedded in README/config examples | mitigate | README rewrite (Task 3) + grep gate for personal identifiers; config uses neutral placeholders |
| T-18-04 | Tampering | committed build artifact dist/maccat.pyz shipped as if canonical | mitigate | `find -name '*.pyz' -delete` (Task 1) + `*.pyz` in .gitignore (Task 2); CI/Releases produce it in Phase 19 |
| T-18-08 | Information Disclosure | real personal hostnames/machine labels embedded in .planning/ (~37 files), docs/superpowers spec, and CLAUDE.md leaking into the public tree | mitigate | Task 4 tree-wide deterministic scrub (enumerate-then-replace) across all text files except tests/golden/*; self-verifying zero-match grep; Plan 02 gate re-confirms |
</threat_model>

<verification>
- Staging dir exists with the full include-list, required test fixtures, and CLAUDE.md.
- Zero excluded items (dist, venv, personal, office, machine-labels.tsv, all three stray test scripts, *.pyz, personal catalog .txt) present in staging.
- LICENSE (MIT), .gitignore (artifacts + personal patterns + fixture exception), config.example.toml present.
- README is genericized: install-from-Releases, config precedence, no personal identifiers.
- ENTIRE staging tree (README, .planning/, docs/, CLAUDE.md) scrubbed: tree-wide grep for personal tokens returns zero matches (tests/golden/* excluded and untouched).
- This repo's working tree is unmodified (only reads + an external staging dir written).
</verification>

<success_criteria>
A `/tmp/maccat-staging.*` directory holds the prepared, genericized maccat tree containing only
the locked include-list (plus CLAUDE.md and required test fixtures) and new
LICENSE/.gitignore/config.example.toml/rewritten README, with zero personal data or build
artifacts and every text file scrubbed of real personal hostnames/machine labels, ready for a
fresh `git init` in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/18-public-repo-migration-genericized-fresh-history/18-01-SUMMARY.md` when done.
Record the exact staging dir path, the enumerated personal-token list, and the files-modified scrub count so Plan 02 can reuse it.
</output>
</content>
</invoke>
