---
phase: 18-public-repo-migration-genericized-fresh-history
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - /tmp/maccat-staging  # git init + commit happen inside the staging tree from Plan 01
autonomous: false
requirements: [MIG-01, MIG-03]
user_setup: []

must_haves:
  truths:
    - "The staging tree is a fresh git repo with a single initial commit (no inherited history from this repo)"
    - "Both the working tree AND the entire git log of the new repo contain zero REAL personal data (hostnames, machine labels) and zero catalog data files"
    - "A human confirms the privacy-gate scan results (files scanned, zero matches, staging path) before the irreversible public push"
    - "A new public GitHub repo scottkw/maccat exists with default branch main and the staging tree pushed as its only history"
  artifacts:
    - path: "<staging>/.git"
      provides: "Fresh git history with exactly one initial commit"
    - path: "github.com/scottkw/maccat"
      provides: "Public GitHub repo hosting the migrated maccat"
  key_links:
    - from: "<staging> initial commit"
      to: "github.com/scottkw/maccat main"
      via: "git push to gh-created public repo"
      pattern: "maccat"
---

<objective>
Turn the genericized staging tree from Plan 01 into the public `maccat` repo with a FRESH git
history: `git init`, one initial commit, then a privacy gate that scans BOTH the working tree and
the entire git log for REAL personal data. A human checkpoint surfaces the gate results before the
irreversible publication. Only after the gate passes AND the human confirms, create the public
GitHub repo via `gh` and push.

Purpose: MIG-03 (fresh history, zero personal data anywhere in tree OR log) is a hard privacy gate;
MIG-01 (public repo created via gh). The gate detects REAL personal identifiers only (the bare
`mac-software-list-[` format string is NOT personal data and legitimately appears in code/tests/docs),
runs BEFORE the first public push, and a human confirms before anything goes public — publishing a
public repo is irreversible.
Output: github.com/scottkw/maccat, public, default branch main, single-commit fresh history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md
@.planning/phases/18-public-repo-migration-genericized-fresh-history/18-01-SUMMARY.md

<interfaces>
<!-- gh is authenticated as scottkw with repo + workflow scopes (verified). -->
<!-- Locked repo identity: gh repo create scottkw/maccat --public ; default branch main. -->
<!-- THIS repo's origin is a PRIVATE a private Git host remote (a private catalog remote) — MUST NOT be touched. The new repo is on GitHub and is independent. -->
<!-- REAL personal-identifier tokens to scan for (enumerated from this repo's tracked content; same list scrubbed in Plan 01 Task 4):
       computer-one, computer-two, attlocal\.net, "Example Computer" (covers "Example Computer" / "Example Computer").
     After Plan 01's scrub these must be ABSENT — this gate confirms it.
     NOTE: do NOT match the bare format string `mac-software-list-[` — it is NOT personal data and appears legitimately in
     src/maccat/naming.py, tests/test_naming.py, tests/test_safety_invariants.py, README, update-list.sh, and .planning/ prose. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh git init + single initial commit in the staging tree</name>
  <files>/tmp/maccat-staging.*/.git</files>
  <read_first>
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-01-SUMMARY.md (exact staging dir path)
    - /tmp/maccat-staging.*/.gitignore (confirm ignore rules are in place before staging files)
  </read_first>
  <action>
    Resolve the staging path from the Plan 01 SUMMARY (S=...). Confirm it is NOT this repo's path and NOT a subdir of it.
    Run inside $S only (use `git -C "$S" ...`, never cd into this repo): `git -C "$S" init -b main` (fresh history, default branch main). Do NOT add any remote yet, and NEVER reference this repo's a private Git host origin.
    Stage everything honoring .gitignore: `git -C "$S" add -A`. Then sanity-check that no excluded patterns slipped into the index: `git -C "$S" ls-files | grep -E 'maccat\.pyz$|^venv/|^dist/|^personal/|^office/|machine-labels\.tsv'` MUST print nothing (if it does, fix .gitignore / unstage and stop).
    Create exactly one initial commit: `git -C "$S" commit -m "Initial commit: maccat — macOS software & tooling cataloger"`. Author identity may use the local git config; the commit message and content must contain no personal hostnames.
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); [ "$(git -C "$S" rev-list --count HEAD)" = "1" ] && [ "$(git -C "$S" rev-parse --abbrev-ref HEAD)" = "main" ] && [ -z "$(git -C "$S" ls-files | grep -E 'maccat\.pyz$|^venv/|^dist/|^personal/|^office/|machine-labels\.tsv')" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "$S" rev-list --count HEAD` prints exactly `1`
    - `git -C "$S" rev-parse --abbrev-ref HEAD` prints `main`
    - `git -C "$S" ls-files | grep -cE '^(venv|dist|personal|office)/|maccat\.pyz$|machine-labels\.tsv'` prints `0`
    - `git -C "$S" remote -v` prints nothing (no remote added yet)
  </acceptance_criteria>
  <done>Staging tree is a fresh single-commit git repo on branch main with no excluded files tracked and no remote.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Privacy gate — scan working tree AND full git log for REAL personal data (BLOCKING, pre-push)</name>
  <files>/tmp/maccat-staging.* (read-only scan)</files>
  <read_first>
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-01-SUMMARY.md (staging path + enumerated personal-token list from the scrub)
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (privacy gate is a hard requirement; list of personal identifiers)
  </read_first>
  <action>
    Resolve $S. Run a scan for REAL personal data only across three surfaces. Exclude legitimate test fixtures under tests/golden/ from the working-tree content check (golden fixtures contain synthetic placeholders, confirmed free of real hostnames in Plan 01).
    Define the REAL-personal-identifier regex (NO bare format string): `RX='computer-one|computer-two|attlocal\.net|Ken.?s MacBook'`. (If Plan 01's SUMMARY enumerated additional real tokens, append them to RX.) Do NOT match `mac-software-list-\[` — that format string is not personal data and appears legitimately in code/tests/docs; matching it would cause a guaranteed false-positive halt.
    (a) Working tree — tracked content: `git -C "$S" grep -nE "$RX" -- . ':(exclude)tests/golden/*'` MUST find nothing. (After Plan 01 Task 4's scrub this is expected clean; the gate confirms.)
    (b) Full git history — every commit, every blob: `git -C "$S" log --all -p | grep -nE "$RX"` MUST find nothing (this is the MIG-03 log gate — confirms real personal data is absent from the entire history, not just the tip; fresh single-commit history ⇒ should be trivially clean, but verify).
    (c) Actual catalog data files present in the tree: `find "$S" -name 'mac-software-list-*.txt' -not -path '*/tests/*'` MUST print nothing (catalog snapshots must never be in the public repo).
    If ANY surface returns a match, this is a HIGH-severity privacy failure: STOP, do NOT proceed to the checkpoint or repo creation/push, report the matching file/line, and treat as a blocking gate (the staging tree must be re-scrubbed in Plan 01 territory and re-committed before continuing). Per project RULE 0, halt on detection.
    Capture for the checkpoint: the count of files scanned (`git -C "$S" ls-files | wc -l`), the three per-surface match counts (all zero), and the staging path $S.
  </action>
  <verify>
    <automated>S=$(ls -d /tmp/maccat-staging.* 2>/dev/null | tail -1); RX='computer-one|computer-two|attlocal\.net|Ken.?s MacBook'; WT=$(git -C "$S" grep -nE "$RX" -- . ':(exclude)tests/golden/*' 2>/dev/null | wc -l | tr -d ' '); LOG=$(git -C "$S" log --all -p 2>/dev/null | grep -nE "$RX" | wc -l | tr -d ' '); STRAY=$(find "$S" -name 'mac-software-list-*.txt' -not -path '*/tests/*' | wc -l | tr -d ' '); [ "$WT" = "0" ] && [ "$LOG" = "0" ] && [ "$STRAY" = "0" ] && echo PASS || { echo "FAIL wt=$WT log=$LOG stray=$STRAY"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "$S" grep -nE "$RX" -- . ':(exclude)tests/golden/*'` returns zero lines (working tree free of real personal data)
    - `git -C "$S" log --all -p | grep -cE "$RX"` prints `0` (entire history clean — the MIG-03 gate)
    - `find "$S" -name 'mac-software-list-*.txt' -not -path '*/tests/*'` prints nothing (no catalog data files)
    - The gate does NOT match the bare `mac-software-list-[` format string (no false-positive halt)
    - On any non-zero result the executor halts before the checkpoint (no repo creation, no push)
  </acceptance_criteria>
  <done>Working tree, complete git log, and tree filenames are proven free of REAL personal data (hostnames, machine labels, catalog files) with no false positives. Gate passed → ready for human confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Human confirms privacy-gate results before the irreversible public push</name>
  <files>/tmp/maccat-staging.* (no edits — review only)</files>
  <what-built>
    A fresh single-commit staging git repo (Task 1) that passed the blocking privacy gate (Task 2):
    working tree + full git log + tree filenames are all confirmed free of real personal hostnames,
    machine labels, and catalog data files. The NEXT step (`gh repo create scottkw/maccat --public --push`)
    is IRREVERSIBLE — it publishes this content to a public GitHub repo. This checkpoint is the one
    acceptable pause before publication.
  </what-built>
  <action>
    PAUSE for human verification. Do NOT run Task 4 until the human types "approved".
    Surface the privacy-gate summary captured in Task 2: the staging path $S, the files-scanned count, and the three per-surface match counts (working-tree content, full git log, stray catalog files — all must be 0). Present it plainly so the human can confirm before the irreversible publication.
  </action>
  <how-to-verify>
    Review the privacy-gate summary the executor surfaces:
    1. Staging path $S that will be published.
    2. Files scanned count (`git -C "$S" ls-files | wc -l`).
    3. Per-surface match counts — all THREE must read 0: working-tree content scan, full `git log --all -p` scan, stray catalog-file find.
    4. (Optional spot-check) Eyeball `git -C "$S" ls-files | sort` to confirm the file list is the genericized include-set (src/maccat, tests, docs, .planning, CLAUDE.md, LICENSE, .gitignore, config.example.toml, README, update-list.sh, .github/ci.yml) with no personal/office/catalog files.
    Repo identity is LOCKED per CONTEXT and will be: owner `scottkw`, name `maccat`, visibility `public`, default branch `main` — do not change it.
  </how-to-verify>
  <verify>
    <human-check>Human reviewed the privacy-gate summary (files scanned + three zero-match counts + staging path) and typed "approved" to authorize the irreversible public push.</human-check>
  </verify>
  <resume-signal>Type "approved" to authorize the irreversible public push, or describe what must be fixed first (halts publication).</resume-signal>
  <done>Human has confirmed the privacy-gate results and explicitly authorized publication; or has halted with required fixes.</done>
</task>

<task type="auto">
  <name>Task 4: Create the public GitHub repo and push the fresh history</name>
  <files>/tmp/maccat-staging.* (remote + push), github.com/scottkw/maccat (created)</files>
  <read_first>
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-01-SUMMARY.md (staging path)
    - .planning/phases/18-public-repo-migration-genericized-fresh-history/18-CONTEXT.md (locked repo identity: scottkw/maccat, public, main)
  </read_first>
  <action>
    PRECONDITION: Task 2's gate MUST have passed AND the Task 3 human checkpoint MUST have been approved. Do not run this task otherwise.
    Confirm gh identity: `gh auth status` shows account scottkw. Confirm the target name is free: `gh repo view scottkw/maccat` should fail (not found) before creation; if it already exists, STOP and report rather than overwriting.
    Resolve $S. Create the public repo AND push the staging tree's main in one step using gh's source/push flags so only the prepared fresh history is published:
    `gh repo create scottkw/maccat --public --source "$S" --remote origin --push --description "Catalog every piece of software on your Mac — apps, extensions, plugins, MCP servers."`
    (`--source "$S"` sets the local staging dir as the source, `--remote origin` adds the GitHub origin to the STAGING repo only — this never touches this repo's private remote origin — and `--push` pushes the single-commit main.)
    After creation, verify visibility, default branch, and that the pushed history is the single fresh commit.
  </action>
  <verify>
    <automated>[ "$(gh repo view scottkw/maccat --json visibility -q .visibility)" = "PUBLIC" ] && [ "$(gh repo view scottkw/maccat --json defaultBranchRef -q .defaultBranchRef.name)" = "main" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view scottkw/maccat --json visibility -q .visibility` prints `PUBLIC`
    - `gh repo view scottkw/maccat --json defaultBranchRef -q .defaultBranchRef.name` prints `main`
    - `git -C "$S" rev-list --count origin/main` prints `1` (only the fresh initial commit was pushed)
    - `git -C "$S" remote get-url origin` points to github.com/scottkw/maccat (NOT the a private Git host host); this repo's own `.git` was never modified
    - `gh api repos/scottkw/maccat/contents/LICENSE -q .name` prints `LICENSE` (genericized tree is what got published)
  </acceptance_criteria>
  <done>Public repo scottkw/maccat exists on GitHub with default branch main and the single-commit fresh, genericized history pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging tree → public GitHub | The push publishes content irreversibly; nothing personal may cross |
| staging git origin ↔ this repo's private remote origin | Must stay fully independent; this repo's remote/tree must never be touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Information Disclosure | REAL personal data in working tree OR git log published to a PUBLIC repo | mitigate | Task 2 blocking three-surface gate (`git grep` + `git log --all -p` + catalog-file find) matching only real personal tokens (no bare format-string false positive), runs BEFORE any repo creation/push; halts on any match (RULE 0) |
| T-18-06 | Tampering | accidentally pushing to / overwriting this repo's private remote origin | mitigate | All git ops use `git -C "$S"` against the isolated staging dir; gh `--remote origin` adds origin only to staging; verify `remote get-url origin` is the GitHub host |
| T-18-07 | Elevation/Exposure | creating the repo private-then-public, or wrong owner | mitigate | Single `gh repo create scottkw/maccat --public` (public from creation); verify visibility=PUBLIC and owner via gh api post-create |
| T-18-09 | Information Disclosure | irreversible publication proceeding on an unreviewed tree | mitigate | Task 3 blocking human checkpoint surfaces the privacy-gate summary (files scanned, three zero-match counts, staging path) BEFORE `gh repo create --push`; publication requires explicit "approved" |
| T-18-SC | Tampering | n/a — no package installs in this phase | accept | This phase copies/commits existing tracked code; no npm/pip/cargo installs occur |
</threat_model>

<verification>
- Fresh history: `git -C "$S" rev-list --count HEAD` == 1, branch main, no excluded files tracked.
- Privacy gate (blocking): working tree AND full `git log --all -p` contain zero REAL personal identifiers (hostnames/machine labels); no catalog .txt outside tests/; the bare `mac-software-list-[` format string is NOT matched (no false positive).
- Human checkpoint: gate summary (files scanned, three zero-match counts, staging path) confirmed before the irreversible push.
- Public repo: `gh repo view scottkw/maccat` visibility PUBLIC, default branch main, single-commit origin/main, LICENSE present via gh api.
- This repo untouched: its `.git`/origin (a private Git host) unchanged; only the external staging dir got a remote.
</verification>

<success_criteria>
A public GitHub repo `scottkw/maccat` exists (default branch main) whose entire fresh git history —
working tree and full log — is verifiably free of REAL personal data, built solely from the
genericized staging tree, published only after a human confirmed the privacy-gate results, with this
repo's private remote and working tree left untouched.
</success_criteria>

<output>
Create `.planning/phases/18-public-repo-migration-genericized-fresh-history/18-02-SUMMARY.md` when done.
Record the public repo URL and the privacy-gate results (files scanned + working-tree + log + stray-catalog scan counts).
</output>
</content>
