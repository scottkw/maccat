---
phase: 32-convert-command
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - src/maccat/cli.py
  - tests/test_convert.py
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-03

must_haves:
  truths:
    - "maccat convert --from PATH.txt writes PATH.md, removes PATH.txt, and exits 0"
    - "maccat convert --from PATH.txt --no-commit writes PATH.md and removes PATH.txt without touching git"
    - "maccat convert --from PATH.txt when PATH.md already exists exits non-zero with 'ERROR' in the message"
    - "maccat convert --from MISSING.txt exits non-zero with 'ERROR'"
    - "maccat convert --from badly-named.txt exits non-zero with 'ERROR'"
    - "The produced .md is parseable by parse_markdown_catalog (full-chain round-trip passes)"
    - "The 'Installed Mac Software List' H1 appears exactly once in the .md output (no spurious ## heading)"
    - "pytest tests/test_convert.py exits 0 with all tests passing"
    - "Existing test suites (test_cli.py, test_gitops.py) remain green"
  artifacts:
    - path: "tests/test_convert.py"
      provides: "Unit + integration tests: file ops, error paths, no-commit, round-trip, git staging"
      exports: []
    - path: "src/maccat/cli.py"
      provides: "convert subparser registration in _build_parser + dispatch at step 4b"
      contains: "subcommand == \"convert\""
  key_links:
    - from: "src/maccat/cli.py::_build_parser"
      to: "convert subparser with --from (required=True) and --no-commit (default=argparse.SUPPRESS)"
      via: "subparsers.add_parser('convert', ...)"
      pattern: "subparsers.add_parser.*convert"
    - from: "src/maccat/cli.py::run"
      to: "src/maccat/convert.py::run_convert"
      via: "deferred import at step 4b; returns immediately after run_convert"
      pattern: "args.subcommand == .convert"
    - from: "tests/test_convert.py"
      to: "src/maccat/convert.py::run_convert"
      via: "direct call with argparse.Namespace args + monkeypatched gitops"
      pattern: "run_convert"
---

<objective>
Wire the `convert` subcommand into `cli.py` (subparser registration + step-4b dispatch) and
write the full test suite in `tests/test_convert.py`.

Purpose: Plan 01 built the engine; this plan makes it reachable via `maccat convert --from PATH`
and proves correctness through the full test pyramid (unit error paths, file-op happy path,
--no-commit, git staging, and the full-chain round-trip that converts a .txt fixture then
parse_markdown_catalog's the output).

Output:
  - src/maccat/cli.py — convert subparser in _build_parser + dispatch block at step 4b
  - tests/test_convert.py — test classes covering all acceptance criteria
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/phases/32-convert-command/32-CONTEXT.md
@/Users/ken/dev/maccat/.planning/phases/32-convert-command/32-RESEARCH.md
@/Users/ken/dev/maccat/.planning/phases/32-convert-command/32-PATTERNS.md
@/Users/ken/dev/maccat/.planning/phases/32-convert-command/32-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 output and existing codebase. No codebase exploration needed. -->

From src/maccat/convert.py (Plan 01 output):
  run_convert(args: argparse.Namespace) -> None
    args.from_path: str   # path to the .txt file
    args.no_commit: bool  # if True, skip git entirely

From src/maccat/gitops.py (Plan 01 output):
  git_commit_convert(catalog_repo: Path, md_path: Path, txt_path: Path) -> None
  # warn-and-continue; no return value

From src/maccat/cli.py (existing — insertion points):
  _build_parser() -> argparse.ArgumentParser
    # subparsers = parser.add_subparsers(dest="subcommand")  # line 97
    # reinstall_parser block ends at line 143 — insert convert_parser after this
    # _build_parser docstring "Subcommands:" section — add "convert" line
  run() -> None
    # step 4b at lines 250-255 — insert convert dispatch immediately after reinstall --from dispatch
    # Pattern: if args.subcommand == "convert": from maccat.convert import run_convert; run_convert(args); return

From src/maccat/reinstall/parser.py (for round-trip test):
  parse_markdown_catalog(path: Path) -> ParsedCatalog   # raises ValueError for .txt input

From tests/conftest.py:
  git_repo(tmp_path) -> Path   # git init'd tmp_path with test user.email/user.name configured

argparse.SUPPRESS behaviour (critical — WR-03 from cli.py lines 117-143):
  --no-commit on the convert subparser MUST use default=argparse.SUPPRESS, NOT default=False.
  A plain default=False unconditionally clobbers a top-level --no-commit True.

Fixture .txt content for tests:
  "Installed Mac Software List\n"
  "------------------------------------\n"
  "\n"
  "Homebrew Packages\n"
  "------------------------------------\n"
  "wget (1.21.3)\n"
  "git (2.44.0)\n"
  "\n"
  "App Store Applications\n"
  "------------------------------------\n"
  "Final Cut Pro (10.7.1) [424389933]\n"
  "\n"
  "Setapp Applications\n"
  "------------------------------------\n"
  "  (none found)\n"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire convert subcommand into cli.py</name>
  <files>src/maccat/cli.py</files>
  <read_first>
    - src/maccat/cli.py lines 96-143 (subparsers setup + reinstall_parser block — insertion point for convert_parser)
    - src/maccat/cli.py lines 148-170 (run() docstring — update Orchestration Order comment to mention convert)
    - src/maccat/cli.py lines 246-255 (step 4b reinstall --from dispatch — sibling insertion point)
    - .planning/phases/32-convert-command/32-PATTERNS.md (cli.py subparser registration pattern + dispatch pattern)
  </read_first>
  <action>
    Two insertions in cli.py. Do not change anything outside the described insertion points.

    INSERTION 1 — in _build_parser(), after the reinstall_parser block (after line 143):
      Add convert_parser block:
        convert_parser = subparsers.add_parser("convert", help="Convert a legacy .txt catalog to .md format")
        convert_parser.add_argument("--from", metavar="PATH", dest="from_path", required=True,
                                    help="Legacy .txt catalog file to convert")
        convert_parser.add_argument("--no-commit", action="store_true", dest="no_commit",
                                    default=argparse.SUPPRESS,
                                    help="Perform file operations without git commit")
      --from must be required=True (no valid behaviour for bare `maccat convert`).
      --no-commit must use default=argparse.SUPPRESS (WR-03: prevents clobbering top-level --no-commit).

    INSERTION 2 — in run(), immediately after the reinstall --from dispatch block (after line 255):
        if args.subcommand == "convert":
            from maccat.convert import run_convert
            run_convert(args)
            return
      This goes BEFORE step 4c (resolve_catalog_repo) — convert is repo-agnostic like reinstall --from.
      Deferred import per PKG-03.

    ALSO update the _build_parser docstring "Subcommands:" list to add:
      "  convert          Convert a legacy .txt catalog to .md format."

    Do NOT add --rename guard for convert (convert has no interaction with the rename workflow).
    Do NOT call resolve_catalog_repo before the convert dispatch.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m mypy --strict src/maccat/cli.py</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m ruff check src/maccat/cli.py</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m maccat convert --help</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/pytest tests/test_cli.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `./venv/bin/python -m maccat convert --help` exits 0 and prints usage mentioning --from and --no-commit
    - `./venv/bin/python -m maccat convert` (no --from) exits non-zero (argparse error: --from is required)
    - mypy --strict passes on cli.py with zero errors
    - ruff check passes on cli.py with zero errors
    - tests/test_cli.py still passes in full (no regressions from the addition)
    - `grep -c "argparse.SUPPRESS" src/maccat/cli.py` returns at least 2 (reinstall + convert)
    - `grep -c "subcommand == .convert" src/maccat/cli.py` returns 1
    - The convert dispatch appears BEFORE the resolve_catalog_repo call in run()
  </acceptance_criteria>
  <done>cli.py registers the convert subparser with --from (required) and --no-commit (SUPPRESS default); dispatch at step 4b routes to run_convert; mypy --strict and ruff pass; test_cli.py still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_convert.py — full test suite</name>
  <files>tests/test_convert.py</files>
  <read_first>
    - tests/reinstall/test_reinstall_cli.py (sys.argv + monkeypatch pattern; fixture structure; MagicMock for git)
    - tests/reinstall/test_picker_and_reinstall_cli.py (argparse.Namespace direct call pattern; SystemExit assertion; chmod skip-root pattern)
    - tests/conftest.py (git_repo fixture; tmp_path usage)
    - .planning/phases/32-convert-command/32-PATTERNS.md (test patterns for convert: fixture .txt, round-trip test, git staging test)
  </read_first>
  <behavior>
    - Test 1 (happy path via cli): `maccat convert --from .../mac-software-list-[TestMac]-20260101120000.txt` writes .md, removes .txt, exits 0
    - Test 2 (--no-commit): run_convert with no_commit=True writes .md, removes .txt, does NOT call git_commit_convert
    - Test 3 (missing file): run_convert exits non-zero when --from path does not exist
    - Test 4 (bad filename): run_convert exits non-zero when filename doesn't match _TXT_FILENAME_RE
    - Test 5 (target .md exists / no-clobber): run_convert exits non-zero when .md already exists; neither file is touched
    - Test 6 (unreadable file): run_convert exits non-zero when file exists but os.R_OK fails; skipif root
    - Test 7 (git staging): git_commit_convert is called with correct (catalog_repo, md_path, txt_path) when no_commit=False
    - Test 8 (round-trip full-chain): convert a .txt fixture, then parse_markdown_catalog the resulting .md — must not raise; parsed sections match expected titles; 'Installed Mac Software List' section NOT present as a ParsedSection (header skipped); the .md text contains exactly one '# Installed Mac Software List' line (no ## duplicate)
    - Test 9 (degraded/empty section): convert a .txt with a section containing only the NONE_FOUND_SENTINEL — the resulting .md section renders as '(none found)'
  </behavior>
  <action>
    Create tests/test_convert.py. Use pytest classes to group related tests.

    Module-level fixture constant:
      _MINIMAL_TXT_CATALOG — the fixture .txt content shown in the context interfaces block above.

    Helper function (module-level, not a fixture):
      def _make_convert_args(from_path: str | None = None, no_commit: bool = False) -> argparse.Namespace:
          return argparse.Namespace(from_path=from_path, no_commit=no_commit)

    Fixture (class or module level):
      @pytest.fixture() def fixture_txt_catalog(tmp_path: Path) -> Path:
          catalog = tmp_path / "mac-software-list-[TestMac]-20260101120000.txt"
          catalog.write_text(_MINIMAL_TXT_CATALOG, encoding="utf-8")
          return catalog

    Tests use deferred imports inside test bodies where needed (PKG-03 compatible).
    Patch git_commit_convert as `monkeypatch.setattr("maccat.gitops.git_commit_convert", MagicMock())`
    for tests that don't want real git ops.

    For the cli integration test (Test 1), use monkeypatch.setattr(sys, "argv", [...]) then call
    `from maccat.cli import run; run()`.

    For the git staging test (Test 7), place the .txt inside a git_repo subdirectory to make
    relative_to() succeed:
      computer_dir = git_repo / "TestMac"; computer_dir.mkdir()
      txt_path = computer_dir / "mac-software-list-[TestMac]-20260101120000.txt"
      Then verify git_commit_convert mock was called once with
      (txt_path.parent.parent, txt_path.with_suffix(".md"), txt_path).

    For the round-trip test (Test 8), do NOT patch git_commit_convert — use no_commit=True.
    After run_convert succeeds, call parse_markdown_catalog(md_path). Assert:
      - md_path.exists() is True
      - Not fixture_txt_catalog.exists() (original removed)
      - parsed result has sections with titles "Homebrew Packages", "App Store Applications",
        "Setapp Applications" (and NOT "Installed Mac Software List")
      - md_path.read_text().count("# Installed Mac Software List") == 1 (H1 appears once)
      - md_path.read_text() does NOT contain "## Installed Mac Software List"

    For Test 5 (no-clobber): create the .md BEFORE calling run_convert; assert the .txt is
    still present and .md is unchanged after the SystemExit.

    Standard patterns from reinstall tests:
      - pytest.raises(SystemExit) with `assert exc.value.code != 0` for all error cases
      - @pytest.mark.skipif(os.geteuid() == 0, reason="root bypasses file permission checks")
        for Test 6
      - Use finally: os.chmod(f, 0o644) in Test 6 to restore permissions

    All tests must be self-contained (no real filesystem outside tmp_path; no real git remote).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/pytest tests/test_convert.py -v</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m mypy --strict tests/test_convert.py</automated>
    <automated>cd /Users/ken/dev/maccat && ./venv/bin/python -m ruff check tests/test_convert.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_convert.py -v` exits 0 with all 9 test cases passing (or more if sub-cases are split)
    - mypy --strict passes on tests/test_convert.py with zero errors
    - ruff check passes on tests/test_convert.py with zero errors
    - The round-trip test (Test 8) calls parse_markdown_catalog on the convert output without raising
    - The round-trip test asserts md_path.read_text().count("# Installed Mac Software List") == 1 (exactly one H1, no spurious ##)
    - The no-clobber test (Test 5) asserts that the original .txt is still present after the SystemExit
    - The git staging test (Test 7) asserts git_commit_convert mock was called exactly once with the correct three arguments
    - The --no-commit test (Test 2) asserts git_commit_convert mock was NOT called
    - Full suite still green: `./venv/bin/pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>test_convert.py covers all 9 behaviours; pytest -v exits 0; mypy --strict and ruff pass; full test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argparse → cli.py | --from PATH enters via argparse; validated in run_convert before use |
| test harness → convert pipeline | Tests use tmp_path isolation; no real filesystem or git remote touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Tampering | cli.py: convert dispatch position | mitigate | Dispatch placed BEFORE resolve_catalog_repo (consistent with reinstall --from); no repo resolution attempted for a repo-agnostic command |
| T-32-08 | Spoofing | --no-commit SUPPRESS default | mitigate | default=argparse.SUPPRESS on the convert subparser prevents clobbering a top-level --no-commit True (Pitfall 2 in RESEARCH.md) |
| T-32-09 | Denial of Service | required=True on --from | accept | argparse rejects bare `maccat convert` with usage error; no silent no-op possible |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib-only |
</threat_model>

<verification>
After both tasks complete, run the full suite:
  1. `./venv/bin/pytest tests/test_convert.py -v` — all 9+ convert tests pass
  2. `./venv/bin/pytest tests/ -x -q` — full suite green (no regressions in test_cli.py, test_gitops.py, reinstall/)
  3. `./venv/bin/python -m maccat convert --help` — prints usage with --from and --no-commit
  4. `./venv/bin/python -m maccat convert` (no args) — exits non-zero (argparse: --from required)
  5. `./venv/bin/python -m mypy --strict src/maccat/cli.py tests/test_convert.py` — zero errors
  6. `./venv/bin/python -m ruff check src/maccat/cli.py tests/test_convert.py` — zero errors
  7. Spot-check round-trip: create a minimal .txt in /tmp, run `maccat convert --from ... --no-commit`,
     verify .md exists and .txt is gone.
</verification>

<success_criteria>
- `maccat convert --from PATH.txt` is reachable via the CLI and delegates to run_convert
- All 9 test behaviours covered and passing in test_convert.py
- Full test suite green (no regressions)
- mypy --strict + ruff clean on cli.py and tests/test_convert.py
- Round-trip test proves .txt → convert → .md → parse_markdown_catalog is lossless
- Spurious ## heading invariant asserted in tests
- No-clobber invariant asserted in tests
- Atomicity invariant (txt not deleted until md written) asserted in tests
</success_criteria>

<output>
Create `/Users/ken/dev/maccat/.planning/phases/32-convert-command/32-02-SUMMARY.md` when done.
</output>
