---
phase: 28-chromium-refactor-edge-brave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maccat/collectors/chromium.py
  - src/maccat/collectors/chrome.py
  - tests/collectors/test_chrome.py
autonomous: true
requirements:
  - BRW-01
  - BRW-02

must_haves:
  truths:
    - "ChromiumBaseCollector exists in chromium.py with all shared profile-scan logic"
    - "ChromeCollector is a thin subclass overriding _base, _title, _denylist only"
    - "Chrome catalog output is byte-identical to the pre-refactor output"
    - "COMPONENT_DENYLIST is defined in chromium.py and re-exported from chrome.py"
    - "test_chrome.py passes with the new patch.object(ChromeCollector, '_base', new=...) targets"
  artifacts:
    - path: "src/maccat/collectors/chromium.py"
      provides: "ChromiumBaseCollector base class, COMPONENT_DENYLIST"
      exports: ["ChromiumBaseCollector", "COMPONENT_DENYLIST"]
    - path: "src/maccat/collectors/chrome.py"
      provides: "ChromeCollector thin subclass; COMPONENT_DENYLIST re-export"
      exports: ["ChromeCollector", "COMPONENT_DENYLIST"]
  key_links:
    - from: "src/maccat/collectors/chrome.py"
      to: "src/maccat/collectors/chromium.py"
      via: "from maccat.collectors.chromium import COMPONENT_DENYLIST, ChromiumBaseCollector"
      pattern: "from maccat.collectors.chromium import"
    - from: "tests/collectors/test_chrome.py"
      to: "ChromeCollector._base"
      via: "patch.object(ChromeCollector, '_base', new=...)"
      pattern: "patch.object(ChromeCollector"
---

<objective>
Extract ChromiumBaseCollector from chrome.py into a new chromium.py base class.
Chrome becomes a thin subclass (3 class-attribute overrides). Chrome output remains byte-identical.
test_chrome.py patch targets migrate from the module-level constant to the class attribute.

Purpose: Establishes the shared base that Plan 02 uses to add Edge and Brave as thin subclasses.
The Chrome byte-parity gate must pass before adding Edge or Brave.

Output: chromium.py (base class + COMPONENT_DENYLIST), chrome.py (thin subclass + re-export),
test_chrome.py (updated patch targets, all tests green).
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ken/dev/maccat/.planning/PROJECT.md
@/Users/ken/dev/maccat/.planning/ROADMAP.md
@/Users/ken/dev/maccat/.planning/phases/28-chromium-refactor-edge-brave/28-CONTEXT.md
@/Users/ken/dev/maccat/.planning/phases/28-chromium-refactor-edge-brave/28-PATTERNS.md
@/Users/ken/dev/maccat/src/maccat/collectors/chrome.py
@/Users/ken/dev/maccat/tests/collectors/test_chrome.py

<interfaces>
<!-- Key types and contracts from the codebase the executor needs. -->

From src/maccat/collectors/base.py (do not re-read; use these):
  class Collector(ABC):
      def collect(self) -> CollectorResult: ...
  @dataclass
  class Section:
      title: str
      items: list[str]
      raw: bool = False
  @dataclass
  class CollectorResult:
      sections: list[Section]

From src/maccat/catalog/format.py (imports the executor will need):
  from maccat.catalog.format import emit_item, version_sort_tail

From src/maccat/helpers/chrome_name.py:
  from maccat.helpers.chrome_name import chrome_ext_name

From src/maccat/helpers/json_io.py:
  from maccat.helpers.json_io import json_get

Existing chrome.py COMPONENT_DENYLIST (10 IDs — copy verbatim, do not alter):
  "nmmhkkegccagdldgiimedpiccmgmieda"
  "ghbmnnjooekpmoecnnnilnnbdlolhkhi"
  "aapocclcgogkmnckokdopfmhonfmgoek"  # note: differs from PATTERNS.md draft (use actual file)
  "blpcfgokakmgnkcojhhkbfbldkacnbeo"
  "felcaaldnbdncclmgdcncolpebgiejap"
  "aohghmighlieiainnegkcijnfilokake"
  "apdfllckaahabafndbhieahigkjlhalf"
  "pjkljhegncpnkpknbcohdijeoejaedia"
  "mhjfbmdgcfjbbpaeojofohoefgiehjai"
  "pkedcjkdefgpdelpbcmbmeomcjbeemfm"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create chromium.py with ChromiumBaseCollector and migrate COMPONENT_DENYLIST</name>
  <files>src/maccat/collectors/chromium.py</files>
  <behavior>
    - collect() on a base with no _base dir set returns items=[] and prints NOTE to stderr
    - _collect_profile() returns [] on OSError from the first iterdir() call (profile-level guard)
    - _collect_profile() skips individual ext dirs that raise OSError on iterdir(), collects the others
    - _collect_profile() skips ext_id == "Temp", ext_id starting with "_", and any ext_id in self._denylist
    - _collect_profile() calls version_sort_tail() to select the highest version dir (not max/sorted[-1])
    - A base dir containing only NativeMessagingHosts (no profile dirs with Extensions/) returns items=[] with no NOTE (presence detection via profile enumeration, not bare is_dir())
    - collect() returns Section with raw=False (flush_section dedup is handled by the orchestrator)
  </behavior>
  <action>
    Create src/maccat/collectors/chromium.py as follows.

    Line 1: `from __future__ import annotations`

    Imports: `import sys`, `from pathlib import Path`, then the four project imports:
    `from maccat.catalog.format import emit_item, version_sort_tail`,
    `from maccat.collectors.base import Collector, CollectorResult, Section`,
    `from maccat.helpers.chrome_name import chrome_ext_name`,
    `from maccat.helpers.json_io import json_get`.

    `__all__ = ["ChromiumBaseCollector", "COMPONENT_DENYLIST"]`

    COMPONENT_DENYLIST: Move the frozenset verbatim from chrome.py lines 17-30. Copy all 10 IDs
    exactly as they appear in the current chrome.py source — do NOT use the PATTERNS.md draft
    which has one ID that differs. The authoritative source is the live chrome.py file.

    Class ChromiumBaseCollector(Collector):
      Docstring: "Shared Chromium extension scan, parameterized by browser name and base path.
      Subclasses override _base, _title, and _denylist only."
      Class attributes (annotated):
        _base: Path = Path()
        _title: str = ""
        _denylist: frozenset[str] = frozenset()

    _collect_profile(self, extensions_dir: Path) -> list[str]:
      Move the body verbatim from chrome.py lines 50-82. The single change:
        Replace `ext_id in COMPONENT_DENYLIST` with `ext_id in self._denylist`.
      Keep both OSError guards (extensions_dir.iterdir() and ext_dir.iterdir()).
      Keep the Temp/"_"-prefix guards. Keep version_sort_tail, chrome_ext_name, json_get, emit_item calls.

    collect(self) -> CollectorResult:
      Move the body verbatim from chrome.py lines 84-103. Changes:
        Replace `_BASE` → `self._base` (all 4 occurrences).
        Replace `_TITLE` → `self._title` (2 occurrences).
        Replace `COMPONENT_DENYLIST` in the denylist check inside _collect_profile (already done above).
      The NOTE message: `print(f"  NOTE: {self._title.replace(' Extensions', '')} not installed.", file=sys.stderr)`
      or more simply mirror the chrome.py NOTE exactly by using a derived browser name.
      Simplest approach: use `self._title` in the NOTE so the message names the section title
      (e.g. "NOTE: Google Chrome Extensions not installed.") — acceptable since Chrome currently
      says "Google Chrome not installed." If exact parity with the current chrome.py NOTE message
      is needed, add a `_browser_name: str = ""` class attribute and use it in the NOTE.
      Decision: add `_browser_name: str = ""` as a fourth class attribute and set it in each subclass
      for a precise NOTE message (matches CONTEXT.md "graceful degradation" spirit). Set ChromeCollector._browser_name = "Google Chrome".

    Presence detection (per CONTEXT.md and PATTERNS.md): The `not self._base.is_dir()` check fires
    the NOTE and returns early ONLY when the base dir does not exist. When base dir exists but has no
    Extensions dirs (NativeMessagingHosts-only), the profile loop naturally yields no ext_root hits,
    returning items=[]. Do NOT add an extra is_dir() guard after the loop start.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/python -m pytest tests/collectors/test_chrome.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/maccat/collectors/chromium.py exists
    - `from maccat.collectors.chromium import ChromiumBaseCollector, COMPONENT_DENYLIST` succeeds
    - COMPONENT_DENYLIST in chromium.py is a frozenset of exactly 10 IDs
    - ChromiumBaseCollector has class attributes _base, _title, _denylist, _browser_name
    - ruff and mypy --strict pass on the file (run after Task 2 since chrome.py must import chromium.py)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor chrome.py to thin subclass + re-export; migrate test_chrome.py patch targets</name>
  <files>src/maccat/collectors/chrome.py, tests/collectors/test_chrome.py</files>
  <behavior>
    - `from maccat.collectors.chrome import COMPONENT_DENYLIST, ChromeCollector` succeeds (backward compat)
    - ChromeCollector._base == Path.home() / "Library/Application Support/Google/Chrome"
    - ChromeCollector._title == "Google Chrome Extensions"
    - ChromeCollector._denylist == COMPONENT_DENYLIST
    - ChromeCollector._browser_name == "Google Chrome"
    - All 13 test_chrome.py test cases pass with patch.object(ChromeCollector, "_base", new=...) targets
    - test_chrome_not_installed prints "NOTE" and "Google Chrome" to stderr (byte-parity with prior behavior)
    - ruff + mypy --strict clean on both files
  </behavior>
  <action>
    PART A — Rewrite chrome.py as a thin subclass (~18 lines):

    Line 1: `from __future__ import annotations`
    Imports: `from pathlib import Path`, then:
    `from maccat.collectors.chromium import COMPONENT_DENYLIST, ChromiumBaseCollector`
    `__all__ = ["ChromeCollector", "COMPONENT_DENYLIST"]`  # re-export — mandatory for backward compat
    Module constant: `_BASE = Path.home() / "Library/Application Support/Google/Chrome"`
    (Keep _BASE as a module-level constant for documentation/readability, even though tests now patch
    the class attribute. The _BASE constant is not read at runtime after the refactor; ChromeCollector._base
    reads from the class attribute set at definition time.)
    Module constant: `_TITLE = "Google Chrome Extensions"`  # keep for test_section_titles.py access

    class ChromeCollector(ChromiumBaseCollector):
      Docstring: "Thin subclass — Chrome-specific paths and denylist only."
      _base = _BASE
      _title = _TITLE
      _denylist = COMPONENT_DENYLIST
      _browser_name = "Google Chrome"

    That is the entire chrome.py after refactor.

    PART B — Update test_chrome.py patch targets:

    Every `patch.object(chrome_mod, "_BASE", base)` call must become
    `patch.object(ChromeCollector, "_base", new=base)`.

    Locations to update (check current test_chrome.py for exact line numbers):
      TestChromeCollect.test_collects_default_profile — patch call
      TestChromeCollect.test_collects_multiple_profiles — patch call
      TestChromeCollect.test_chrome_section_title — patch call
      TestChromeCollect.test_chrome_raw_is_false — patch call
      TestChromeExclusions.test_skips_component_extension — patch call
      TestChromeExclusions.test_skips_temp_directory — patch call
      TestChromeExclusions.test_skips_underscore_directory — patch call
      TestChromeExclusions.test_version_sort_tail_used — patch call
      TestChromeDegradation.test_chrome_not_installed — patch call
    (test_profile_iterdir_oserror_degrades and test_ext_dir_iterdir_oserror_skips_that_ext
    call _collect_profile directly on a ChromeCollector() instance — no patch needed there.)

    After the change, each test that checked module_mod._BASE becomes class-attr patch.
    The keyword arg must be `new=base` (not positional) — `patch.object(ChromeCollector, "_base", new=base)`.

    Do NOT alter any assertion logic, import lines, or test structure. Only the patch target changes.

    Import line 14 is `from maccat.collectors.chrome import COMPONENT_DENYLIST, ChromeCollector` —
    this stays unchanged because chrome.py re-exports COMPONENT_DENYLIST.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv/bin/python -m pytest tests/collectors/test_chrome.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All test_chrome.py tests pass (green, no errors, no skips)
    - `PYTHONPATH=src ./venv/bin/ruff check src/maccat/collectors/chromium.py src/maccat/collectors/chrome.py` exits 0
    - `MYPYPATH=src ./venv/bin/mypy --strict src/maccat/collectors/chromium.py src/maccat/collectors/chrome.py` exits 0
    - chrome.py __all__ includes "COMPONENT_DENYLIST" (re-export present)
    - No `patch.object(chrome_mod, "_BASE"` pattern remains in test_chrome.py
    - Chrome section output is byte-identical: the section title "Google Chrome Extensions", items format, raw=False, all unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python | Reads extension manifest.json files from the user's local browser profile dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28A-01 | Information Disclosure | chromium.py reads manifest.json | accept | Same trust level as existing Chrome collector; files are in the user's own profile dir; no credentials, no network |
| T-28A-02 | Tampering | Malformed manifest.json content | accept | json_get returns empty string on missing/invalid fields; emit_item skips empty entries; already handled by the existing Chrome collector |
| T-28A-SC | Tampering | No new package installs in this plan | accept | stdlib-only refactor; no pip install commands |
</threat_model>

<verification>
Full regression check after both tasks:

```
PYTHONPATH=src ./venv/bin/python -m pytest tests/collectors/test_chrome.py -v --tb=short
MYPYPATH=src ./venv/bin/mypy --strict src/maccat/collectors/chromium.py src/maccat/collectors/chrome.py
PYTHONPATH=src ./venv/bin/ruff check src/maccat/collectors/chromium.py src/maccat/collectors/chrome.py tests/collectors/test_chrome.py
```

Chrome byte-parity gate: every test in test_chrome.py must pass, including title assertion
("Google Chrome Extensions"), raw=False assertion, and the not-installed NOTE/stderr test.
</verification>

<success_criteria>
- chromium.py exists; `ChromiumBaseCollector` and `COMPONENT_DENYLIST` importable from it
- chrome.py is ~18 lines; imports from chromium.py; `__all__` re-exports COMPONENT_DENYLIST
- `from maccat.collectors.chrome import COMPONENT_DENYLIST, ChromeCollector` still works (backward compat)
- All 13 test_chrome.py tests pass with the new `patch.object(ChromeCollector, "_base", new=...)` patch form
- Zero `patch.object(chrome_mod, "_BASE"` occurrences remain in test_chrome.py
- ruff and mypy --strict exit 0 on all three files
</success_criteria>

<output>
Create `.planning/phases/28-chromium-refactor-edge-brave/28-01-SUMMARY.md` when done
</output>
